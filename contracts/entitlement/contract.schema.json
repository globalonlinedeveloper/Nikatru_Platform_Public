{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nikatru.com/contracts/entitlement/contract.schema.json",
  "title": "NIKATRU entitlement contract",
  "description": "The vocabulary three runtimes have to agree about: which money world a row belongs to, and why access was taken away. Authored in contracts/entitlement/contract.js; this file grades the machine-readable copy that Dart code generation reads. A schema is what makes the JSON copy checkable by something other than a reader.",
  "type": "object",
  "additionalProperties": false,
  "required": ["moneyEnvironments", "revocationReasons", "revenuecatEventReasons"],
  "properties": {
    "$schema": { "type": "string" },
    "moneyEnvironments": {
      "description": "The two money worlds. [5]M-12 is that they never mix; the environment comes from the Worker's configuration, never from a notification payload.",
      "type": "array",
      "minItems": 2,
      "uniqueItems": true,
      "items": { "enum": ["live", "sandbox"] }
    },
    "revocationReasons": {
      "description": "The revocation-lifecycle reason set. Must equal the rows seeded by services/platform/migrations/0004_money_rail.sql section E.",
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["reason", "restores"],
        "properties": {
          "reason": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_]*$",
            "description": "The stored value. snake_case, because it is written into a database column and read back by three runtimes."
          },
          "restores": {
            "type": "boolean",
            "description": "Whether this reason GIVES ACCESS BACK. Exactly one member is true today (chargeback_reversed). A copy of this set that drops the field, or flips it, locks out a customer who raised a dispute in error and lost it — and nothing else in this rail restores access to a row that was taken away."
          }
        }
      }
    },
    "revenuecatEventReasons": {
      "description": "Which RevenueCat webhook event means which revocation reason. `reason: null` records an event that is deliberately NOT a revocation, which is a different fact from an event nobody has mapped — the client, the Worker and any extension all have to share that distinction. Every non-null reason must be a member of revocationReasons, and every runtime copy of this map must be equal: tooling/ci/assert-entitlement-contract.mjs limb 6 holds both.",
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["event", "reason", "dateDerived", "notAGrant", "why"],
        "properties": {
          "event": {
            "type": "string",
            "pattern": "^[A-Z][A-Z0-9_]*$",
            "description": "The RevenueCat webhook event type, VERBATIM as the vendor spells it. A normalised copy would be a second vocabulary nobody could compare against the vendor's own reference."
          },
          "reason": {
            "type": ["string", "null"],
            "pattern": "^[a-z][a-z0-9_]*$",
            "description": "The revocation reason this event means, or null when the event is not a revocation at all. An event this table does not name revokes nothing — the conservative direction for a table whose error mode is locking out a paying customer."
          },
          "dateDerived": {
            "type": "boolean",
            "description": "TRUE when the event's ACCESS outcome is decided by the paid-through date carried on the event, not by the event name — so a consumer that acts on the name alone is wrong for one of the shapes the name covers. CANCELLATION is the case that forces the field: the same event name is sent for cancel-at-period-end (access continues) and for a REFUND (expiration_at_ms in the past, access ends now, and the honest reason is refund_approved). The Dart and JSON copies carry this field for exactly that reason: prose in a why: string does not reach the generated Dart table, and a Dart consumer reading only (event, reason) would revoke a paying customer. services/platform/src/lib/mor/revenuecat.ts reads these events as paid-through rulings (until 2026-09-16 the retired legacy route services/subscriptiontracker-api/src/routes/webhooks.ts called them grace-class)."
          },
          "notAGrant": {
            "type": "boolean",
            "description": "TRUE when the vendor describes the event as NO access change although it carries no reason and is not date-derived, which is the shape the ruling otherwise reads as a grant (2026-09-24, F912). REQUIRED and never defaulted, because it fails closed: services/platform's revenueCatAccessRulingForRow goes on to 'grant' only on an explicit false, so a row that omitted it would be a grant nobody decided, and tooling/ci/assert-entitlement-contract.mjs limb 6b refuses such a row by name. True is allowed only with reason null and dateDerived false. services/platform/src/lib/mor/revenuecat.ts acknowledges a true row BEFORE routing step A ([ADR 085] A, append 2026-09-24): it writes nothing, so routing it could change no entitlement."
          },
          "why": {
            "type": "string",
            "minLength": 30,
            "description": "The sentence that forces the answer. A mapping with no ground cannot be reviewed and cannot be re-decided when the vendor renames an event."
          }
        }
      }
    }
  },
  "$comment": "There is deliberately no minItems on revocationReasons beyond 1 and no enumeration of the member names here. A schema that restated the set would be a fifth copy of it, which is the failure this directory exists to remove. Equality with the SQL seed and with contract.js is a GUARD's job (tooling/ci/assert-entitlement-contract.mjs), not a schema's."
}
