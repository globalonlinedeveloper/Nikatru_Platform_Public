name: nikatru_workspace
description: >-
  Nikatru Cross Platform Apps — monorepo workspace root (Dart pub workspaces +
  Melos 8). Members: the shared packages under packages/ AND the Subly app
  (apps/subscriptiontracker/). P0.8 promoted the spine packages + Subly to workspace members
  so a single `melos run gate` analyzes and tests the whole tree in one
  resolution.
publish_to: none

environment:
  sdk: ^3.9.0

# Workspace members — explicit paths (Dart pub workspaces / Melos 8: no globs).
# Pure-Dart and Flutter packages share ONE resolution; the workspace_gate CI
# lane resolves with `flutter pub get` because the Flutter members require the
# Flutter tool (plain `dart pub get` cannot resolve `sdk: flutter` deps).
# tooling/ci/assert-version-consistency.mjs reads this list: each member's
# `environment:` floors are compared with tooling/versions.json.
workspace:
  - packages/analysis
  - packages/core
  - packages/api_client
  - packages/auth_supabase
  - packages/billing_revenuecat
  - packages/chassis_screens
  - packages/design_system
  - packages/external_links
  - packages/notifications
  - packages/platform_storage
  - packages/purchases
  - packages/telemetry
  - apps/subscriptiontracker

# ─────────────────────────────────────────────────────────────────────────────
# 🔴 EXACT, NOT A CARET RANGE. No guard read this line until 2026-08-17, when
# mutating the declaration named ci.yml and nothing else.
#
# 📏 THE VERSION IS DECLARED ONCE, as `melos` in tooling/versions.json, and this
# comment carries no version number on purpose: until 2026-09-25 it quoted the
# 2026-08-17 pin eight times while the line below had moved on
# (O-PUBSPEC-FLOORS-UNTIED-TO-THE-PIN). No script rewrites a comment, so a number
# written here goes stale at the next bump; tooling/ci/assert-version-consistency.mjs
# names any melos version in this file's comments that is not the pin.
#
# 🗂️ THE PLACES IT IS WRITTEN ARE NOT LISTED HERE EITHER. They are REQUIRED_YIELD
# in tooling/ci/assert-version-consistency.mjs: each is an entry the guard
# compares with tooling/versions.json, and an entry that stops matching is
# COVERAGE LOST. pubspec.lock is the one place outside it: the resolver writes
# it, and `flutter pub get --enforce-lockfile` holds it to this line.
#
# WHY THE PIN AND THE ACTIVATE ARE NOT REDUNDANT. CI activates a GLOBAL melos;
# `melos run gate` — §14's definition of green — runs the one resolved HERE. A
# caret lets those be different runners with no diff in the repo, which is the
# same shape as the `^4.0.0` that floated the brick's stamped service onto a
# wrangler whose miniflare had been unpublished (tooling/versions.json
# $wrangler_comment).
#
# Measured before pinning rather than assumed (2026-08-17, on that day's pin):
# with the version exact, `flutter pub get --enforce-lockfile` exited 0 and
# pubspec.lock was BYTE-IDENTICAL — the lock already resolved that version, so
# the caret was buying range it never used. The guard compares this literal
# whole, so a range operator coming back is unequal to the pin and fails.
# ─────────────────────────────────────────────────────────────────────────────
dev_dependencies:
  melos: 8.7.0

# ─────────────────────────────────────────────────────────────────────────────
# THE ONE OVERRIDE, AND WHY IT IS NOT A SHRUG
# ─────────────────────────────────────────────────────────────────────────────
# A pub WORKSPACE is a single resolution, so every member's dev tooling has to
# agree with every other member's. Re-read 2026-09-25 from the local pub cache,
# not from pub.dev: the melos pin of that day (8.7.0) requires
# `cli_util ">=0.5.0 <0.7.0"`, and flutter_launcher_icons 0.14.4 — the version
# pubspec.lock resolves — requires `cli_util ^0.4.1`, which stops below 0.5.0.
# The ranges still do not meet, so the override still earns its place; the lock
# resolves cli_util 0.5.1 under it. Whether a flutter_launcher_icons newer than
# 0.14.4 accepts 0.5.0 was NOT re-read that day. The first measurement
# (2026-08-04, against that day's melos) had pub enumerate every version back to
# 0.9.0: they split between `cli_util ^0.3.5` and `>=0.4.0 <0.5.0`, while `msix
# 3.18.0`'s `image ^4.0.0` rules out the older half. So the icon generator and
# the workspace runner cannot coexist without this line, and the icon generator
# is what stands between this factory and shipping Flutter's logo again.
#
# It is safe HERE and would not be in general, because of how narrowly
# flutter_launcher_icons uses cli_util. VERIFIED BY RUNNING IT rather than by
# reading a changelog: under this override the generator produced all 29 native
# icons for apps/subscriptiontracker, and — the run that actually matters — a freshly stamped
# app #2 went from every icon byte-identical to Flutter's stock to none of them,
# in one command (tooling/ci/assert-launcher-icons.mjs red, then green).
#
# The alternative was `dart pub global activate flutter_launcher_icons`, which
# [pipeline F-8] rejects for exactly the right reason: a globally activated tool
# resolves whatever is newest that day, and nothing in the repo would record
# which version drew the icons that shipped. An override is pinned by
# pubspec.lock and visible in a diff; a global activation is neither.
#
# ⚠️ REVISIT WHEN flutter_launcher_icons WIDENS TO cli_util ^0.5.0, and delete
# this block then — an override nobody needs is an override nobody re-examines.
dependency_overrides:
  cli_util: ^0.5.0

# Melos 7+/8 configuration lives here (melos.yaml no longer exists).
melos:
  scripts:
    analyze:
      run: melos exec --fail-fast -- dart analyze
      description: >-
        Analyze every workspace member. `dart analyze` resolves Flutter types
        from the shared package config; infos stay non-fatal, matching the
        per-lane `flutter analyze --no-fatal-infos` posture.
    test:
      run: melos exec --fail-fast --dir-exists=test --no-flutter -- dart test && melos exec --fail-fast --dir-exists=test --flutter -- flutter test
      description: >-
        Pure-Dart members (core, api_client) run under `dart test`; Flutter
        members (design_system, telemetry, subscriptiontracker) run under `flutter test`.
    gate:
      run: melos run analyze && melos run test
      description: >-
        The unified quality gate — analyze + test across every member. §14's
        definition of green: `melos bootstrap && melos run gate` passes.
