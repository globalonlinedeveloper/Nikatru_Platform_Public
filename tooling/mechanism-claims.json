{
  "_what": "O-UNGRADED-MECHANISM-CLAIMS. Every sentence in this repository that describes how a mechanism works in the shapes below is a CANDIDATE. Each candidate is judged here (`claims`) or counted in `backlog`. Read by tooling/ci/assert-mechanism-claims.mjs, and by nothing else.",
  "_rule": "A NEW candidate arrives with a `claims` entry, or the build is red. `proven`: `test` + `case` name a test that fails when the sentence goes false, and `proves` says how. `demoted`: `reason` says why the sentence asserts nothing a test could grade. An entry covers the candidates that start on a line inside its `anchor`, which must appear exactly once in `file`. `backlog` is the count of unjudged candidates per file, measured when the guard landed. It may only shrink: judge a site, or reword it, and lower the row in the same change. `node tooling/ci/assert-mechanism-claims.mjs --measure` prints the rows the tree implies.",
  "_shapes": "A single space in `pattern` stands for whitespace and comment leaders, so a sentence that wraps across comment lines still matches. Case-insensitive. The shapes only FIND candidates; whether a sentence is true is a reader's judgement, recorded below (trap grep-02).",
  "shapes": [
    {
      "id": "no-second-list",
      "pattern": "no (?:second|separate|exemption|hardcoded|other|manual) (?:list|copy)",
      "why": "The seed sentence (verify-supabase-templates.mjs) said there was no second list, over a loop that walked one."
    },
    {
      "id": "nothing-to-update",
      "pattern": "nothing (?:here )?(?:needs (?:to )?|to )(?:be )?(?:updat\\w*|go stale)",
      "why": "Says a mechanism maintains itself. False the day somebody adds the list it says is not needed."
    },
    {
      "id": "cannot-drift",
      "pattern": "can(?:not|'t) drift",
      "why": "Says two things are held together by construction. True only while the construction under it is what the sentence says."
    },
    {
      "id": "sync-automatically",
      "pattern": "stays? in sync automatically",
      "why": "The same promise as cannot-drift, in other words."
    },
    {
      "id": "derived-from-record",
      "pattern": "derived from the record",
      "why": "The seed defect's own wording, next to code that derived nothing."
    },
    {
      "id": "derived-not-listed",
      "pattern": "derived,? not (?:listed|written|typed)",
      "why": "Says a set is computed from its source. A hardcoded set under the same sentence reads identically."
    },
    {
      "id": "no-copy-to",
      "pattern": "no copy to (?:drift|forget)",
      "why": "The same promise as no-second-list."
    }
  ],
  "claims": [
    {
      "file": "tooling/ops/verify-supabase-templates.mjs",
      "anchor": "there is no second list here to forget to update.",
      "status": "proven",
      "test": "tooling/ci/test/mail-transport-claims.test.mjs",
      "case": "A2 — a hardening field the OLD hardcoded array did not contain now drifts: exit 1",
      "proves": "The seed instance. The case drifts a recorded field the old hardcoded COMPARE array did not name; it exits 1 only if the comparison set is read from the record. Red against the pre-#779 code, green now."
    },
    {
      "file": "tooling/ci/test/mail-transport-claims.test.mjs",
      "anchor": "there is no second list here to forget to update.\" That",
      "status": "proven",
      "test": "tooling/ci/test/mail-transport-claims.test.mjs",
      "case": "A2 — a hardening field the OLD hardcoded array did not contain now drifts: exit 1",
      "proves": "Quotes the seed sentence in the header of the cases that grade it; A2 is the red control."
    },
    {
      "file": "docs/platform/supabase/README.md",
      "anchor": "It is derived from the record now",
      "status": "proven",
      "test": "tooling/ci/test/mail-transport-claims.test.mjs",
      "case": "A2 — a hardening field the OLD hardcoded array did not contain now drifts: exit 1",
      "proves": "Restates the seed fix for a human reader; the same case goes red if the comparison stops being read from the record."
    },
    {
      "file": "tooling/ci/assert-mail-transport-claims.mjs",
      "anchor": "THERE IS NO EXEMPTION\n// LIST HERE AT ALL — nothing to add a file to, nothing to go stale",
      "status": "proven",
      "test": "tooling/ci/test/mail-transport-claims.test.mjs",
      "case": "an uncorrected claim is reported at EVERY path an exemption list would name",
      "proves": "Plants an uncorrected claim at every tracked path that mentions the superseded transport today (derived from the real register and git ls-files), plus the guard, its suite, the live checker and the ADR, and requires each to be reported. Measured 2026-09-16: adding docs/platform/supabase/README.md to the register skip turns it red. The one skip the guard has (its own register) is stated at its `continue` and left out of the case."
    },
    {
      "file": "tooling/ci/assert-workflow-hardening.mjs",
      "anchor": "NOT AN EXEMPTION — there is no exemption list here, and there must not",
      "status": "proven",
      "test": "tooling/ci/test/guards.test.mjs",
      "case": "REFUSES (exit 2) on a job it cannot classify, rather than demanding the impossible",
      "proves": "A job with no runs-on (a reusable-workflow call) must make the limb refuse with exit 2. A skip list that let it through would exit 0 and fail the case."
    },
    {
      "file": "tooling/ci/test/guards.test.mjs",
      "anchor": "There is NO exemption list here; the guard says it cannot tell.",
      "status": "proven",
      "test": "tooling/ci/test/guards.test.mjs",
      "case": "REFUSES (exit 2) on a job it cannot classify, rather than demanding the impossible",
      "proves": "The sentence is the comment inside the case that grades it."
    },
    {
      "file": "tooling/ci/assert-d1-sql-inventory.mjs",
      "anchor": "all derived from the file; there is no exemption list.",
      "status": "proven",
      "test": "tooling/ci/test/d1-sql-inventory.test.mjs",
      "case": "dropping the identifier regex from the shared erasure route is red",
      "proves": "R3 on the REAL tree: the file that interpolates identifiers loses its evidence and R3 must name it. A list excusing that file would keep it green. The sibling case 'widening subscriptions.ts's closed column union to `string` is red' does the same for the other interpolating file."
    },
    {
      "file": "tooling/ci/assert-entitlement-contract.mjs",
      "anchor": "There is deliberately NO exemption list: a list here",
      "status": "proven",
      "test": "tooling/ci/test/entitlement-contract.test.mjs",
      "case": "FAILS on a THIRD Worker restating ACTIVE_TYPES / INACTIVE_TYPES / GRACE_TYPES",
      "proves": "A transcription at a path no code names must fail limb 7b; its sibling 'PASSES when that third runtime IMPORTS the contract' shows the import is what clears it. WEBHOOK_REL is the declared required member, named in the same header, not an exemption."
    },
    {
      "file": "tooling/ci/assert-play-declarations.mjs",
      "anchor": "THE ROUTES ARE DERIVED, NOT LISTED.",
      "status": "proven",
      "test": "tooling/ci/test/play-declarations.test.mjs",
      "case": "the route set comes from the INVENTORY: re-pointed at a missing file, it fails on THAT path",
      "proves": "Re-points the inventory's erasure route at a missing file while account.ts stays. Only a route set read from the inventory sees it. Measured 2026-09-16: hardcoding account.ts turns this case and its sibling red."
    },
    {
      "file": "tooling/ci/assert-play-declarations.mjs",
      "anchor": "is no second list of routes to drift from the first.",
      "status": "proven",
      "test": "tooling/ci/test/play-declarations.test.mjs",
      "case": "…and re-pointed at a file that exists, the OLD route may be deleted and it passes",
      "proves": "Deletes account.ts after the inventory stops naming it. A second list of routes would still demand account.ts and go red."
    },
    {
      "file": "tooling/ci/test/play-declarations.test.mjs",
      "anchor": "that no second list of routes exists",
      "status": "proven",
      "test": "tooling/ci/test/play-declarations.test.mjs",
      "case": "the route set comes from the INVENTORY: re-pointed at a missing file, it fails on THAT path",
      "proves": "The header of the two cases that grade the claim."
    },
    {
      "file": "tooling/ci/assert-play-declarations.mjs",
      "anchor": "stops being swept here in the same edit, and there is no second list to",
      "status": "proven",
      "test": "tooling/ci/test/play-declarations.test.mjs",
      "case": "the needle list comes from the DECLARATION: a tell invented in this case is swept at once",
      "proves": "A package name that exists only in the fixture declaration must be swept in the brick. Measured 2026-09-16: a hardcoded ['geolocator'] needle list turns it red. The calibration case '🔬 THE CALIBRATION CASE — a tell of a claim answering TRUE is NOT a needle' covers the removal direction."
    },
    {
      "file": "tooling/ci/text-reductions.mjs",
      "anchor": "no second copy rather than asking a reader to believe it.",
      "status": "proven",
      "test": "tooling/ci/test/text-reductions.test.mjs",
      "case": "exactly one file under tooling DECLARES the mask, and it is this module",
      "proves": "Reads every tracked tooling .mjs and requires exactly one declaration of codeMask / NON_CODE. A second copy fails it."
    },
    {
      "file": "tooling/ci/assert-guard-coverage.mjs",
      "anchor": "its own test asks the tree that no second copy exists",
      "status": "proven",
      "test": "tooling/ci/test/text-reductions.test.mjs",
      "case": "exactly one file under tooling DECLARES the mask, and it is this module",
      "proves": "The same claim about codeMask as text-reductions.mjs, restated in the NOT_A_SCANNER reason; the same case grades it."
    },
    {
      "file": "tooling/ci/assert-guard-coverage.mjs",
      "anchor": "\"there is no exemption list here on purpose",
      "status": "demoted",
      "reason": "Quotes a note this file has since retired. The sentences after it record that the check became FOUND ⊆ REACHED instead. It is history, and asserts nothing about the code as it stands."
    },
    {
      "file": "tooling/ci/d1-sql-inventory.mjs",
      "anchor": "pinned by [R4] so the prose and this constant cannot drift.",
      "status": "proven",
      "test": "tooling/ci/test/d1-sql-inventory.test.mjs",
      "case": "changing the claim in one file is red",
      "proves": "Edits the cause sentence in one real file; R4 must name it."
    },
    {
      "file": "tooling/ci/test/d1-sql-inventory.test.mjs",
      "anchor": "R4 — the cause sentence cannot drift from the constant",
      "status": "proven",
      "test": "tooling/ci/test/d1-sql-inventory.test.mjs",
      "case": "changing the claim in one file is red",
      "proves": "The describe title of the cases that grade it."
    },
    {
      "file": "tooling/ops/register.json",
      "anchor": "the INSTALLED cert is the only source that cannot drift.",
      "status": "demoted",
      "reason": "Quoted as THE PRIOR TEXT of expiring.cert.origin-ca's `response`, kept as history on 2026-09-16 when the row was corrected by a live Cloudflare API read. The live `response` before it no longer makes the claim; the quote asserts nothing about the row as it stands. ⏱ 2026-09-18: the row itself is now RETIRED (certificate revoked, O-ORIGIN-CA-KEEP-OR-REVOKE); the sentence survives verbatim inside _retiredRows.rows[].row, as history."
    },
    {
      "file": "tooling/ops/register.json",
      "anchor": "The INSTALLED certificate's own notAfter is the only source that cannot drift;",
      "status": "demoted",
      "reason": "Quoted as THE PRIOR TEXT of expiring.cert.origin-ca's `expiryKnownAt`, kept as history on 2026-09-16. The live text before it names the Cloudflare API read and says an installed copy matches only if openssl agrees; the quote asserts nothing about the row as it stands. ⏱ 2026-09-18: the row itself is now RETIRED (certificate revoked, O-ORIGIN-CA-KEEP-OR-REVOKE); the sentence survives verbatim inside _retiredRows.rows[].row, as history."
    },
    {
      "file": "tooling/ci/appimage-signing.mjs",
      "anchor": "against the register's declared names, so the two cannot drift silently.",
      "status": "proven",
      "test": "tooling/ci/test/appimage-signing.test.mjs",
      "case": "🔴 COVERAGE LOST when the register stops declaring the key transport name",
      "proves": "The fixture register declares only SOMETHING_ELSE_B64; the run must exit 2 naming B64_ENV. Delete the cross-check and the constant drifts from the register silently, and the case goes red. Mutation-verified 2026-09-18: removing the `!declared.includes(B64_ENV)` refusal turns it red."
    },
    {
      "file": "tooling/ci/apple-signing.mjs",
      "anchor": "cannot drift silently. Renaming a secret in the register without",
      "status": "proven",
      "test": "tooling/ci/test/apple-signing.test.mjs",
      "case": "a name declared and unknown here comes out as `extra`",
      "proves": "registerDrift is the comparison the sentence names; a register name the role map does not know must come back as `extra`, which the main path turns into COVERAGE LOST. Its sibling 'a name expected here and dropped there comes out as `absent`' covers the other direction."
    },
    {
      "file": "tooling/ci/assert-a11y-coverage.mjs",
      "anchor": "is DERIVED from the domain minus the sweeps, so it cannot drift from",
      "status": "proven",
      "test": "tooling/ci/test/a11y-coverage.test.mjs",
      "case": "an unswept surface PRINTS and does not fail the build — a NEW sheet nobody has swept",
      "proves": "Adds a sheet nobody listed anywhere and requires it to appear in the printed unswept list. A hand-kept deferral list would not name it."
    },
    {
      "file": "tooling/ci/assert-adapter-capabilities.mjs",
      "anchor": "The DOMAIN is derived, not typed: a descriptor that declares a",
      "status": "proven",
      "test": "tooling/ci/test/adapter-capabilities.test.mjs",
      "case": "FAILS when a canSchedule descriptor declares no scheduleContract",
      "proves": "A descriptor that declares canSchedule with no scheduleContract must fail: the obligation follows from the declaration, not from a typed list of adapters."
    },
    {
      "file": "tooling/ci/assert-app-address-shape.mjs",
      "anchor": "address and the identity cannot drift apart.",
      "status": "proven",
      "test": "tooling/ci/test/app-address-shape.test.mjs",
      "case": "the apex host with the wrong path",
      "proves": "A row whose url path is not \"/\" + its slug must exit 1 with \"but its slug is\". Drop limb 2 and the address and the identity can differ, and the case goes red."
    },
    {
      "file": "tooling/ci/assert-app-naming.mjs",
      "anchor": "written once, HERE, and read from the schema — so the two cannot drift.",
      "status": "proven",
      "test": "tooling/ci/test/app-naming.test.mjs",
      "case": "the cap VANISHING from the schema is COVERAGE LOST, not \"no cap to check\"",
      "proves": "Deletes maxLength from the fixture schema and requires exit 2. A cap typed into the guard would keep checking and exit 0. Mutation-verified 2026-09-18: a hardcoded cap of 30 turns it red."
    },
    {
      "file": "tooling/ci/assert-apple-privacy-manifest.mjs",
      "anchor": "`unresolved` blocks rather than being restated here, so the two cannot drift:",
      "status": "proven",
      "test": "tooling/ci/test/apple-privacy-manifest.test.mjs",
      "case": "it PRINTS what it cannot see on every run, in the audit's own words",
      "proves": "Added 2026-09-18: the case plants a cannotSee item that exists only in the fixture audit and requires it to print. A guard carrying its own copy of the text would print the real sentences and never the planted one. Mutation-verified 2026-09-18: capping the printed items at the three the real audit holds turns it red."
    },
    {
      "file": "tooling/ci/assert-captcha-gated-call-sites.mjs",
      "anchor": "note the gated set is DERIVED, not written:",
      "status": "proven",
      "test": "tooling/ci/test/captcha-gated-call-sites.test.mjs",
      "case": "an interface with no captchaToken parameter is COVERAGE LOST, not a clean tree",
      "proves": "Strips captchaToken from the interface and requires exit 2. A written method list would still hold four names and run clean."
    },
    {
      "file": "tooling/ci/assert-ceiling-budget.mjs",
      "anchor": "THE DOMAIN IS DERIVED, NOT TYPED. A hand-written list of",
      "status": "proven",
      "test": "tooling/ci/test/ceiling-budget.test.mjs",
      "case": "THE DOMAIN IS DERIVED: a ceiling a configCeilings check COUNTS needs no location",
      "proves": "The same ceiling row turns from green to red when only the configCeilings check that counts it is removed. A typed list would give the same answer both times. Mutation-verified 2026-09-18."
    },
    {
      "file": "tooling/ci/assert-channel-claims.mjs",
      "anchor": "it binds to that one so the two cannot drift.",
      "status": "proven",
      "test": "tooling/ci/test/channel-claims.test.mjs",
      "case": "tracks the sibling UPWARD — raising MIN_SITES raises this floor too",
      "proves": "Raises MIN_SITES in the fixture sibling only; this guard must then demand 3. A local floor would not move and the case goes red. Mutation-verified 2026-09-18."
    },
    {
      "file": "tooling/ci/assert-channel-register.mjs",
      "anchor": "tool.json's own `storeMetadata.stores` so the two cannot drift apart.",
      "status": "proven",
      "test": "tooling/ci/test/channel-register.test.mjs",
      "case": "FAILS when the tool.json does not declare the store the row names",
      "proves": "The row names a store the fixture tool.json does not declare; the run must exit 1 naming it."
    },
    {
      "file": "tooling/ci/assert-channel-register.mjs",
      "anchor": "two cannot drift. The register did repeat them, and nothing compared the copy",
      "status": "proven",
      "test": "tooling/ci/test/android-signing.test.mjs",
      "case": "the env-var names really are read from Gradle, not hard-coded here",
      "proves": "The sentence says android-signing.mjs parses its names from build.gradle.kts. The case renames every variable in the fixture Gradle file and requires the script to ask for the renamed ones. It does not cover tooling/release/submit-play.mjs, which the same sentence also names; section 9 itself is graded by 'FAILS when the BUILD FILE renames a value and the register does not' in channel-register.test.mjs."
    },
    {
      "file": "tooling/ci/assert-channel-register.mjs",
      "anchor": "SUBJECT SET, DERIVED NOT LISTED:",
      "status": "proven",
      "test": "tooling/ci/test/channel-register.test.mjs",
      "case": "FAILS a pin block declaring a sentinel and NO value field",
      "proves": "Plants a new pin block on windows-store that carries only a sentinel; it acquires the checks by declaring it and must fail. Its sibling 'ignores a signing block that declares no sentinel' covers the other side."
    },
    {
      "file": "tooling/ci/assert-chassis-ledger.mjs",
      "anchor": "THE DECLARED TOTALS ARE RECOMPUTED from the rows, so the summary cannot",
      "status": "proven",
      "test": "tooling/ci/test/chassis-ledger.test.mjs",
      "case": "totals.lines drifting from the rows FAILS",
      "proves": "Adds 100 to totals.lines alone and requires exit 1 naming it; siblings do the same for totals.files and totals.unclassified. Mutation-verified 2026-09-18: deleting the totals.lines comparison turns it red."
    },
    {
      "file": "tooling/ci/assert-clone-contract.mjs",
      "anchor": "THE COVERAGE FLOOR IS DERIVED, NOT TYPED.",
      "status": "proven",
      "test": "tooling/ci/test/clone-contract.test.mjs",
      "case": "COVERAGE LOST when a DEPLOYED Worker carries no wrangler.jsonc",
      "proves": "A new Worker directory with an entry point and no config must be COVERAGE LOST. A typed \"at least N configs\" floor would pass it. Note the rule is now \"every directory with an entry point\" (services/_shared/ has none); the sentence below the anchor still says \"every directory\"."
    },
    {
      "file": "tooling/ci/assert-discovery-surface.mjs",
      "anchor": "the generated discovery surfaces cannot drift",
      "status": "proven",
      "test": "tooling/ci/test/discovery-surface.test.mjs",
      "case": "a registry entry added and never regenerated FAILS",
      "proves": "Adds a registry entry after generation; the guard must name the missing landing page."
    },
    {
      "file": "tooling/ci/assert-erasure-reach.mjs",
      "anchor": "columns at all and is not in this guard's domain — the distinction is derived,",
      "status": "proven",
      "test": "tooling/ci/test/erasure-reach.test.mjs",
      "case": "FAILS when a user-owned table has no register row at all",
      "proves": "Removes the register row for a table that has a user_id column; the guard must find the table from the migrations and fail. A listed domain would not see it."
    },
    {
      "file": "tooling/ci/assert-listing-assets.mjs",
      "anchor": "capture geometry cannot drift from the declared contract without one",
      "status": "proven",
      "test": "tooling/ci/test/listing-assets.test.mjs",
      "case": "a recorded size that contradicts the register fails",
      "proves": "CAPTURE.json records 720x1280 against a register recommending 1080x1920; the run must exit 1 naming both."
    },
    {
      "file": "tooling/ci/assert-ops-register.mjs",
      "anchor": "Re-derives `_maxUnreadable` from the rows, so the constant cannot drift",
      "status": "proven",
      "test": "tooling/ci/test/ops-register.test.mjs",
      "case": "🔴 a ceiling ABOVE the derivation FAILS — that is the weakening direction",
      "proves": "Sets the committed constant one above deriveUnreadableCeiling and requires an error. The sibling cases pin equality and the stricter direction."
    },
    {
      "file": "tooling/ci/assert-palette-consistent.mjs",
      "anchor": "the reason tokens.css cannot drift out of the subject unnoticed.",
      "status": "proven",
      "test": "tooling/ci/test/palette-consistent.test.mjs",
      "case": "a named source that declares no :root at all is COVERAGE LOST",
      "proves": "Rewrites tokens.css so it declares no :root and requires exit 2 naming tokens.css. The sibling 'losing a NAMED source gives the named message, not the count one' covers the file leaving the subject."
    },
    {
      "file": "tooling/ci/assert-release-lane-generic.mjs",
      "anchor": "cannot drift from the workspace without this guard's own reading drifting",
      "status": "proven",
      "test": "tooling/ci/test/release-lane-generic.test.mjs",
      "case": "the emitter and the grader read ONE workspace — the ids round-trip to the paths limb A expects",
      "proves": "Every id --emit-apps prints must appear in the grader's output for the same fixture workspace. Two readers that disagreed would fail it."
    },
    {
      "file": "tooling/ci/assert-retention-coverage.mjs",
      "anchor": "to the retention ids, so the two lists cannot drift apart.",
      "status": "proven",
      "test": "tooling/ci/test/retention-coverage.test.mjs",
      "case": "a required external id with no row FAILS",
      "proves": "Removes the row for an id _requiredCoverage names; the run must exit 1 naming it."
    },
    {
      "file": "tooling/ci/build-enforcement-index.mjs",
      "anchor": "register, so the two documents cannot drift.",
      "status": "proven",
      "test": "tooling/ci/test/enforcement-index.test.mjs",
      "case": "COVERAGE LOST when tooling/dod-register.json is absent",
      "proves": "Deletes dod-register.json from the fixture and requires exit 2. A copy of the lane and human rows inside the generator would still produce an index."
    },
    {
      "file": "tooling/ci/channel-arming.mjs",
      "anchor": "cannot drift from the register by carrying a copy of it.",
      "status": "proven",
      "test": "tooling/ci/test/channel-arming.test.mjs",
      "case": "🔴 the derivation never reads the channel ID — rename the row and the answer is identical",
      "proves": "Renames every real row and requires the same verdict. A module carrying a copy of the register keyed on channel ids would change its answer."
    },
    {
      "file": "tooling/ci/assert-app-yaml.mjs",
      "anchor": "only way two readers of one contract cannot drift is for there to be one",
      "status": "demoted",
      "reason": "Design rationale for importing the renderer's `plan` instead of re-deriving it. The claim is the import statement directly below; a shared reader and an identical copy produce the same output, so no test can fail on the difference."
    },
    {
      "file": "tooling/ci/assert-copy-parity.mjs",
      "anchor": "because there was no second copy.",
      "status": "demoted",
      "reason": "A runtime failure message describing the matrix (no second live copy of the product exists to compare). It says nothing about how this guard is built, so the shape matched a false positive."
    },
    {
      "file": "tooling/ci/assert-copy-parity.mjs",
      "anchor": "so there is no second COPY —",
      "status": "demoted",
      "reason": "A runtime failure message describing the matrix (no slot besides the origin carries the product). It says nothing about how this guard is built, so the shape matched a false positive."
    },
    {
      "file": "tooling/ci/assert-discovery-surface.mjs",
      "anchor": "below cannot drift from the file it is about.",
      "status": "demoted",
      "reason": "Describes how the code is organised: one named constant or function that several call sites share. A shared definition and two copies that happen to agree behave identically, so no test can fail on the difference; the structure is checked by reading the definition, which sits directly under the sentence."
    },
    {
      "file": "tooling/ci/assert-input-contract.mjs",
      "anchor": "Named here rather than inlined so the failure text and the walk cannot drift",
      "status": "demoted",
      "reason": "Describes how the code is organised: one named constant or function that several call sites share. A shared definition and two copies that happen to agree behave identically, so no test can fail on the difference; the structure is checked by reading the definition, which sits directly under the sentence."
    },
    {
      "file": "tooling/ci/assert-store-matrix.mjs",
      "anchor": "of the message cannot drift apart between limbs.",
      "status": "demoted",
      "reason": "Describes how the code is organised: one named constant or function that several call sites share. A shared definition and two copies that happen to agree behave identically, so no test can fail on the difference; the structure is checked by reading the definition, which sits directly under the sentence."
    },
    {
      "file": "tooling/ci/assert-web-cache-policy.mjs",
      "anchor": "entry-point limb, the shipped-file limb and the class-probe limb cannot",
      "status": "demoted",
      "reason": "Describes how the code is organised: one named constant or function that several call sites share. A shared definition and two copies that happen to agree behave identically, so no test can fail on the difference; the structure is checked by reading the definition, which sits directly under the sentence."
    },
    {
      "file": "tooling/ci/assert-submission-safety.mjs",
      "anchor": "now NAMES the constant, so there is one string and prose cannot drift from",
      "status": "demoted",
      "reason": "History inside a dated incident note: a header comment once quoted a value and now names the constant instead. It is about the wording of a comment, which no test executes."
    },
    {
      "file": "tooling/ci/test/app-versioning.test.mjs",
      "anchor": "describe('assert-app-versioning — the version must be DERIVED, not typed', () => {",
      "status": "proven",
      "test": "tooling/ci/test/app-versioning.test.mjs",
      "case": "FAILS when the --emit derive step is absent",
      "proves": "The fixture workflow drops the step that derives the version from pubspec.yaml and the guard must exit 1 saying it never derives the version. A guard that accepted a typed version would pass it, and the case goes red."
    },
    {
      "file": "tooling/ci/test/app-versioning.test.mjs",
      "anchor": "// [pipeline 9]R-2's NATIVE HALF — the lane set is DERIVED, not typed.",
      "status": "proven",
      "test": "tooling/ci/test/app-versioning.test.mjs",
      "case": "a DEFERRED lane is exempt and its exemption is PRINTED, naming the row",
      "proves": "The fixture register adds an android-play lane row and the run must print it by row id with \"1 served and checked, 1 deferred and printed\". A hardcoded lane array would never name a row it was not typed with, and the case goes red."
    },
    {
      "file": "tooling/ci/test/chrome-splice.test.mjs",
      "anchor": "// Derived, not listed. A page added tomorrow is covered the moment it exists,",
      "status": "proven",
      "test": "tooling/ci/test/chrome-splice.test.mjs",
      "case": "every served .html under the root is IN the contract by default",
      "proves": "isChromePage must accept a path that exists in no list (some/page/added/tomorrow.html). A hand-kept page list would reject it, and the case goes red."
    },
    {
      "file": "tooling/ci/test/content-qa-checks.test.mjs",
      "anchor": "//   1. DERIVED, NOT LISTED. The per-check cases below are generated by iterating",
      "status": "proven",
      "test": "tooling/ci/test/content-qa-checks.test.mjs",
      "case": "COVERAGE — the fixture set equals the declared set, in both directions",
      "proves": "Compares the planted fixtures against DECLARED_CHECKS read at runtime. A sixth declared check with no fixture goes red naming itself, which is the property the sentence promises; the per-check loop below it then adds a failing case of its own."
    },
    {
      "file": "tooling/ci/test/cors-allowlist.test.mjs",
      "anchor": "// so the match cannot drift onto a longer name.",
      "status": "demoted",
      "reason": "Describes this case's own assertion regexes (quote-delimited, rx-escaped, on the next two lines). A test cannot fail because one of its own assertions became looser: a looser pattern passes more, never less."
    },
    {
      "file": "tooling/ci/test/github-matrix.test.mjs",
      "anchor": "// SIBLING IMPORTS — ONE derivation, `GUARD_SIBLINGS`, deliberately not two, so the plants cannot",
      "status": "demoted",
      "reason": "A \"one derivation, so they cannot drift\" claim about this test file's own constant. No test can tell one shared constant from two copies that happen to agree today; the single definition is visible where it is declared."
    },
    {
      "file": "tooling/ci/test/github-matrix.test.mjs",
      "anchor": "Derived ONCE here rather than twice, so the two plants cannot drift.",
      "status": "demoted",
      "reason": "A \"one derivation, so they cannot drift\" claim about this test file's own constant. No test can tell one shared constant from two copies that happen to agree today; the single definition is visible where it is declared."
    },
    {
      "file": "tooling/ci/test/guard-coverage.test.mjs",
      "anchor": "// R2 originally read FOUND ⊆ INVOKED and carried a note: \"there is no",
      "status": "demoted",
      "reason": "Quoted as R2's ORIGINAL note, as history; the text after it says the note came due and was answered by reachability derived from imports. The quote asserts nothing about the guard as it stands. The live mechanism is exercised by 'a shared module whose LAST import is deleted becomes unreached and FAILS' in the same file."
    },
    {
      "file": "tooling/ci/test/guard-sweep-invocations.test.mjs",
      "anchor": "test('the real workflows DO start guards with a node flag — the fixture, derived not typed', () => {",
      "status": "demoted",
      "reason": "Describes how this test file builds its own fixture, and the derivation sits on the lines under it. No case can fail because the harness became hand-typed: a typed copy passes on the day it is typed and only breaks on the next change to the real tree, which is the hazard the sentence warns about, not something a test here grades."
    },
    {
      "file": "tooling/ci/test/monitor-coverage.test.mjs",
      "anchor": "describe('assert-monitor-coverage — the deployed set is derived, not typed', () => {",
      "status": "proven",
      "test": "tooling/ci/test/monitor-coverage.test.mjs",
      "case": "FAILS when a Worker gains a custom domain nothing declares",
      "proves": "Adds a custom domain to the fixture wrangler.jsonc that no list names and requires exit 1 naming it. A typed hostname set would not see it. Mutation-verified 2026-09-18: restricting the Worker derivation to a typed list of three hostnames turns it red."
    },
    {
      "file": "tooling/ci/test/no-clone-tells.test.mjs",
      "anchor": "// the SAME derived name the cases above use, so they cannot drift into being",
      "status": "demoted",
      "reason": "A \"one derivation, so they cannot drift\" claim about this test file's own constant. No test can tell one shared constant from two copies that happen to agree today; the single definition is visible where it is declared."
    },
    {
      "file": "tooling/ci/test/no-hardcoded-strings.test.mjs",
      "anchor": "// the header cannot drift from the buckets underneath it.",
      "status": "proven",
      "test": "tooling/ci/test/no-hardcoded-strings.test.mjs",
      "case": "prints the three dead keys, exits 0, and states its domain",
      "proves": "The assertion right under the sentence counts the printed per-key lines and requires them to equal the number in the owner header. A header computed apart from the buckets and disagreeing with them fails it."
    },
    {
      "file": "tooling/ci/test/ops-status.test.mjs",
      "anchor": "// ⚠️ DERIVED, NOT TYPED. If these were written as `{'api.nikatru.com': …}` the",
      "status": "proven",
      "test": "tooling/ci/test/ops-status.test.mjs",
      "case": "every row of the real register is accounted for — probed or printed, none silently dropped",
      "proves": "Counts the rows of tooling/monitor-register.json live and requires probed + printed to equal it. The sentence names a host added to the register and never heard of by the tests; with a typed surface set that host breaks this equality, so it cannot be covered on paper only."
    },
    {
      "file": "tooling/ci/test/package-earned.test.mjs",
      "anchor": "describe('the list cannot drift from the tree', () => {",
      "status": "proven",
      "test": "tooling/ci/test/package-earned.test.mjs",
      "case": "FAILS on a justification for a package that no longer exists",
      "proves": "Drops packages/telemetry from the fixture tree while its justification stays, and requires exit 1 naming it. A list that did not check the tree would pass, and the case goes red."
    },
    {
      "file": "tooling/ci/test/platform-proof-fresh.test.mjs",
      "anchor": "// ⚠️ DERIVED, NOT TYPED. Writing today's two crons in here would make the",
      "status": "demoted",
      "reason": "Describes how this test file builds its own fixture, and the derivation sits on the lines under it. No case can fail because the harness became hand-typed: a typed copy passes on the day it is typed and only breaks on the next change to the real tree, which is the hazard the sentence warns about, not something a test here grades."
    },
    {
      "file": "tooling/ci/test/play-declarations.test.mjs",
      "anchor": "describe('assert-play-declarations — a settled question cannot drift back', () => {",
      "status": "proven",
      "test": "tooling/ci/test/play-declarations.test.mjs",
      "case": "FAILS when an answer recorded as settled is re-nulled — the backslide",
      "proves": "Records a question as resolved, re-nulls its answer and re-lists it as unresolved; the guard must exit 1 saying the answer is STILL null. Drop the re-check and the settled question drifts back silently, and the case goes red."
    },
    {
      "file": "tooling/ci/test/release-durable.test.mjs",
      "anchor": "describe('release-manifest.mjs — the expected-format set is DERIVED, not typed', () => {",
      "status": "proven",
      "test": "tooling/ci/test/release-durable.test.mjs",
      "case": "a register whose rows are all lane-less contributes NOTHING — and with no extra, nothing survives",
      "proves": "A register whose only row has no lane must yield just the declared .apk. A typed format set returns the same formats whatever the register says, and the case goes red. Mutation-verified 2026-09-18: returning a typed seven-format set from expectedReleaseFormats turns it red. Corrected 2026-09-24 (O-RELEASE-RECORD-GUESSES-CHANNEL-FROM-EXTENSION): the declared extras are empty, so the case now requires the EMPTY set; re-verified 2026-09-24 with the same typed seven-format mutation, which turns the renamed case red."
    },
    {
      "file": "tooling/ci/test/render-payload.test.mjs",
      "anchor": "* 🔴 DERIVED, NOT LISTED — changed 2026-09-09, and the list it replaces recorded",
      "status": "demoted",
      "reason": "Describes how this test file builds its own fixture, and the derivation sits on the lines under it. No case can fail because the harness became hand-typed: a typed copy passes on the day it is typed and only breaks on the next change to the real tree, which is the hazard the sentence warns about, not something a test here grades."
    },
    {
      "file": "tooling/ci/test/sworn-store-files.test.mjs",
      "anchor": "// …and the channel README, which limb 9 resolves. Derived, not listed: the",
      "status": "demoted",
      "reason": "Describes how this test file builds its own fixture, and the derivation sits on the lines under it. No case can fail because the harness became hand-typed: a typed copy passes on the day it is typed and only breaks on the next change to the real tree, which is the hazard the sentence warns about, not something a test here grades."
    },
    {
      "file": "tooling/app-yaml/render-privacy.mjs",
      "anchor": "so it cannot drift from what the code does without a build failing.",
      "status": "proven",
      "test": "tooling/ci/test/app-yaml.test.mjs",
      "case": "MUTATION: a hand edit to the per-app notice page is caught",
      "proves": "The page is a rendering of privacy.yaml. The case hand-edits the rendered notice page and requires render-privacy --check to exit 1 naming the page, so a page that drifts from its declaration fails a build. The declaration-to-code half is graded separately by limb 4 of the same file (privacy.yaml against the sworn store declarations)."
    },
    {
      "file": "tooling/bricks/app/__brick__/apps/{{app_id}}/lib/core/app_config.dart",
      "anchor": "Composed from the two consts above so it cannot drift from either",
      "status": "proven",
      "test": "tooling/ci/test/stamp-text-fidelity.test.mjs",
      "case": "a literal release with the RIGHT id but a frozen version still fails",
      "proves": "assert-stamp-text-fidelity resolves the TelemetryConfig release through the stamped app_config.dart consts (evalDartString over `consts`). A release that is a literal rather than `$appId@$appVersion` resolves to a frozen version half and exits 1, which is this case; its sibling \"a literal release naming ANOTHER app fails\" covers the id half."
    },
    {
      "file": "tooling/bricks/app/__brick__/apps/{{app_id}}/lib/main.dart",
      "anchor": "the same object and cannot drift apart.",
      "status": "proven",
      "test": "tooling/ci/test/guards.test.mjs",
      "case": "FAILS when main.dart drops the single-instance override",
      "proves": "assert-stamp-properties (notification-tap-observed) requires `notificationServiceProvider.overrideWithValue(notifications)` in the brick main.dart. The case deletes that one override, which is what makes the tap source and the schedule target two objects, and the guard exits 1."
    },
    {
      "file": "tooling/bricks/app/__brick__/apps/{{app_id}}/lib/state/providers.dart",
      "anchor": "ONE READ for the two states that mean it, so the gate cannot drift from the",
      "status": "proven",
      "test": "tooling/bricks/app/__brick__/apps/{{app_id}}/test/chassis_properties_test.dart",
      "case": "a reset link that CANNOT be exchanged lands on the explanation",
      "proves": "Driven through the real router and the real ResetPasswordScreen with nothing navigating by hand. If the router gate stopped reading the arrival limb (holding on `recovering` only), a failed exchange would never reach the screen and this widget case goes red; the sibling \"a recovery arrival lands on the reset screen, unprompted\" covers the recovering limb. Runs in the stamped-app lane, not the node suite."
    },
    {
      "file": "tooling/bricks/app/hooks/pre_gen.dart",
      "anchor": "there is no other list, deliberately",
      "status": "demoted",
      "reason": "True by inspection today: `vocabulary` is computed from the duty-matrix rows. But pre_gen.dart runs only inside a mason stamp, and no test in tooling/ci/test or the stamp lanes adds a market to the matrix and asserts the hook accepts it, or adds a typed list and goes red. Nothing grades the sentence; judged 2026-09-18."
    },
    {
      "file": "tooling/capability-register.json",
      "anchor": "rather than stored as prose, so the register cannot drift from the",
      "status": "proven",
      "test": "tooling/ci/test/capability-register.test.mjs",
      "case": "no demand and no module is the LEGAL state, and it says so, dated",
      "proves": "The case requires the printed line `no consumer declares the habit module.` for a gate row whose id is `habit`. MUTATION-VERIFIED 2026-09-18: the print in assert-capability-register.mjs changed from `${m.id}` to a fixed word (the line no longer derived from the id) turned this case red; restored byte-exact (sha256 checked)."
    },
    {
      "file": "tooling/ceilings.json",
      "anchor": "THE DOMAIN IS DERIVED, NOT TYPED. The guard does not read a count from here.",
      "status": "proven",
      "test": "tooling/ci/test/ceiling-budget.test.mjs",
      "case": "THE DOMAIN IS DERIVED: a ceiling a configCeilings check COUNTS needs no location",
      "proves": "The same per-account ceiling row with the same absent read location is green while the d1Databases configCeilings check counts it, and becomes a failure when only that check is removed. A typed domain would not move when the check does."
    },
    {
      "file": "tooling/ceilings.json",
      "anchor": "so the printed reminder cannot drift away from the limit it is about.",
      "status": "proven",
      "test": "tooling/ci/test/ceiling-budget.test.mjs",
      "case": "the passing shape PRINTS every read location [limb 8 green]",
      "proves": "The case requires the printed reminder to carry `it must stay under 100000 reads (per-account-per-day)`, a figure the fixture holds only on the kv.readsPerDay ceiling row and never in the read location. If the guard stopped reading the limit off the ceiling row, the print loses it and the case goes red."
    },
    {
      "file": "tooling/channel-register.json",
      "anchor": "IT IS DECLARED ON ios-appstore ONLY, AND macos-appstore CARRIES NO SECOND COPY.",
      "status": "demoted",
      "reason": "A recorded design choice about the register's own contents, true today (privacy-manifest.json appears once, in storeMetadataContract.perChannel.ios-appstore.additionalFiles; read 2026-09-18). No guard refuses a second copy on macos-appstore, and the consequence it names (a second required file on disk) is a counterfactual no test states."
    },
    {
      "file": "tooling/content_pipeline/examples/lingo-phrases/gates/licence-clearance.json",
      "anchor": "so this file cannot drift into a claim nobody re-checks.",
      "status": "demoted",
      "reason": "The re-check exists (assert-content-licences.mjs, the gate-artifact block after the recipe loop), but no test mutates this artifact against the register. The only NOT CLEARED case in content-pipeline.test.mjs is satisfied by the recipe limb alone, so it stays green if the artifact comparison is deleted. Untested rather than proven; judged 2026-09-18."
    },
    {
      "file": "tooling/dod-register.json",
      "anchor": "so the row and its measurement cannot drift apart in silence.",
      "status": "demoted",
      "reason": "No guard reads `insideTheClaim` or its `measuredBy` (grep of tooling/ci and tooling/scripts, 2026-09-18), and the register says in the next sentence that check-dod-sync.mjs does not read this list. Naming the measuring file is a convention; nothing fails when the row and that file part."
    },
    {
      "file": "tooling/legal/data-inventory.json",
      "anchor": "DERIVED, NOT LISTED. The right-hand side is computed from the tree on every run:",
      "status": "proven",
      "test": "tooling/ci/test/data-inventory.test.mjs",
      "case": "a new table with no row FAILS",
      "proves": "A CREATE TABLE added to a migration with no register row fails assert-data-inventory. That is only possible if the store set is computed from the migrations; a listed set would not contain the new table."
    },
    {
      "file": "tooling/monitor-register.json",
      "anchor": "THE HOSTNAME LIST IS DERIVED, NOT TYPED.",
      "status": "proven",
      "test": "tooling/ci/test/monitor-coverage.test.mjs",
      "case": "FAILS when a Worker gains a custom domain nothing declares",
      "proves": "A custom domain added to a wrangler config, with no register row, fails assert-monitor-coverage. Only a deployed set computed from the tree can see a host nobody typed."
    },
    {
      "file": "tooling/ops/post-deploy-smoke.mjs",
      "anchor": "one declaration of that format — so this probe cannot drift from the file the",
      "status": "proven",
      "test": "tooling/ci/test/post-deploy-smoke.test.mjs",
      "case": "an EMPTY manifest is COVERAGE LOST, never a pass, and waiting cannot help",
      "proves": "An empty manifest must produce COVERAGE LOST, which only happens if the expected set comes from the manifest. MUTATION-VERIFIED 2026-09-18: judgeReleaseAssets's `parseManifest(manifestText)` replaced by a list typed in the file turned this case red; restored byte-exact (sha256 checked)."
    },
    {
      "file": "tooling/ops/register.json",
      "anchor": "so the constants in the guard and the declaration here cannot drift apart.",
      "status": "proven",
      "test": "tooling/ci/test/e2e-proof-fresh.test.mjs",
      "case": "MUTATION — the register renaming the target is COVERAGE LOST, not a silent pass",
      "proves": "assertTimerRecordDeclared re-reads duty.workflow.e2e.yml's recordQuery.timer from this register and compares it to the guard's constants. The case renames the target in a copy of the real register and requires COVERAGE LOST."
    },
    {
      "file": "tooling/ops/upload-web-sourcemaps.mjs",
      "anchor": "unlike the release name it cannot drift from what the SDK reports.",
      "status": "proven",
      "test": "tooling/ci/test/web-sourcemaps.test.mjs",
      "case": "🔴 the uploaded blob is a REAL ZIP whose root holds a manifest naming this org and release",
      "proves": "The case requires the uploaded manifest to carry, for both the script and its map, the `debug-id` read from the `//# debugId=` line in the built source. That line is the one the SDK reports from. If the uploader took the key from anywhere else, this case goes red. The SDK side of the sentence is outside this repo."
    },
    {
      "file": "tooling/platform-register.json",
      "anchor": "THE WORKER LIST ITSELF IS DERIVED, NOT LISTED:",
      "status": "proven",
      "test": "tooling/ci/test/platform-register.test.mjs",
      "case": "🔴 FAILS on a Worker that declares `main` on disk and is in NO register entry (tree → register)",
      "proves": "With `appWorkers` deleted, the Worker config declaring `main` is the only problem reported. A listed Worker set would have nothing to compare against; its sibling \"(register → tree)\" case covers the other direction."
    },
    {
      "file": "tooling/release/generate-snapcraft.mjs",
      "anchor": "freedesktop entry, so Name, Comment and Categories cannot drift between the two",
      "status": "proven",
      "test": "tooling/ci/test/snapcraft-generable.test.mjs",
      "case": "REFUSES a store category with no freedesktop equivalent rather than passing it through",
      "proves": "The refusal text `has no freedesktop equivalent recorded` exists only in deriveDesktopEntry (tooling/store/render-linux-icons.mjs). The case goes red if the snap launcher stops being built from that function, which is the sentence's claim."
    },
    {
      "file": "tooling/scripts/check-dod-sync.mjs",
      "anchor": "THE SIBLING'S NAME IS DERIVED, NOT TYPED. The line below",
      "status": "demoted",
      "reason": "True by inspection: PRIVATE_SIBLING_NAME is computed from basename(ROOT). No test exercises it. dod-sync.test.mjs builds its corpus as an in-tree `Private/`, the second candidate, so a typed sibling name would pass every case. Judged 2026-09-18."
    },
    {
      "file": "tooling/scripts/check-selection-record.mjs",
      "anchor": "THE SIBLING'S NAME IS DERIVED, NOT TYPED. The line below",
      "status": "demoted",
      "reason": "True by inspection: PRIVATE_SIBLING_NAME is computed from basename(ROOT). No test exercises it. selection-record.test.mjs builds its corpus as an in-tree `Private/`, the second candidate, so a typed sibling name would pass every case. Judged 2026-09-18."
    },
    {
      "file": "tooling/scripts/guard-sweep.mjs",
      "anchor": "workflow YAML so they cannot drift from CI",
      "status": "proven",
      "test": "tooling/ci/test/guard-sweep-invocations.test.mjs",
      "case": "🔴 arguments folded onto a `run: >` continuation line are recorded",
      "proves": "The case derives the guard invocations from the real workflows and requires `--invocations` to record the argument CI passes on the folded line. MUTATION-VERIFIED 2026-09-18: the recorded argument string forced to empty (arguments no longer taken from the YAML) turned this case red; restored byte-exact (sha256 checked)."
    },
    {
      "file": "tooling/scripts/spec-guards.mjs",
      "anchor": "What replaces them is anchor-derived and cannot drift.",
      "status": "demoted",
      "reason": "A claim about future layouts. CANDIDATE_ROOTS is built from the walked-up workspace anchor, but no test moves the repo to another depth and shows that a depth-counted root would fail. spec-guards-worktree.test.mjs grades the worktree/main-checkout resolution, a different property. Judged 2026-09-18."
    },
    {
      "file": "tooling/sites/generate-well-known.mjs",
      "anchor": "two halves of this generator cannot drift into two different definitions of",
      "status": "proven",
      "test": "tooling/ci/test/well-known-shape.test.mjs",
      "case": "GREEN CONTROL: generated files pass every limb",
      "proves": "The generator and assert-well-known-shape both import SURFACES and mobilePresence from this file. The green control requires the generator's output to pass the guard over a populated tree, so two different definitions of \"has a mobile presence\" fail it."
    },
    {
      "file": "tooling/store/render-play-graphics.mjs",
      "anchor": "graphic and the app's icon cannot drift apart:",
      "status": "demoted",
      "reason": "The re-render comparison (`render-play-graphics.mjs --check`) needs a browser and runs only in that lane. No node test re-colours the brand SVGs and asserts the listing graphic follows. listing-assets.test.mjs grades dimensions, alpha and presence, not provenance. Judged 2026-09-18."
    },
    {
      "file": "tooling/store/render-splash.mjs",
      "anchor": "declared size and this table cannot drift apart silently.",
      "status": "proven",
      "test": "tooling/ci/test/launcher-icons.test.mjs",
      "case": "FAILS when the storyboard declares a size the imageset does not have",
      "proves": "assert-launcher-icons imports this table and exits 1 when the storyboard declares LaunchImage at a size the imageset does not carry. The sibling \"FAILS when the imageset catalogue no longer names the files\" covers Contents.json."
    },
    {
      "file": "extensions/Extension/Full_Screen_Shot/background.js",
      "anchor": "in), and it is graded against the message file, so the two cannot drift.",
      "status": "proven",
      "test": "extensions/Extension/Full_Screen_Shot/test/background-sim.node.js",
      "harness": "check",
      "case": "every sentence the worker can say is word-for-word the English message file's",
      "proves": "The sim drives every error the worker can raise through the real background.js and compares each sentence it says with _locales/en/messages.json; one sentence that differs from the message file makes the check false and the sim exit non-zero. extensions.yml's sims job runs it (scripts/run-tests.mjs <tool>). Cited through the register's `harness` form (assert-mechanism-claims M6, 2026-09-19): the case is the label of a check()/expect() call in a file that binds that function. Judged 2026-09-19."
    },
    {
      "file": "extensions/Extension/Full_Screen_Shot/pages/common.js",
      "anchor": "two agree, so the copy cannot drift.",
      "status": "proven",
      "test": "extensions/Extension/Full_Screen_Shot/test/i18n-sim.node.js",
      "harness": "check",
      "case": "the shipped plural suffix table matches i18n/plurals.mjs exactly",
      "proves": "Compares the shipped copy (common.js FS_PLURAL_SUFFIX) with the build-time source (i18n/plurals.mjs SUFFIX) by value; an edit to either side alone makes the check false. extensions.yml's sims job runs it. Cited through the register's `harness` form (assert-mechanism-claims M6, 2026-09-19): the case is the label of a check()/expect() call in a file that binds that function. Judged 2026-09-19."
    },
    {
      "file": "extensions/Extension/Full_Screen_Shot/pages/db.js",
      "anchor": "two cannot drift. */",
      "status": "proven",
      "test": "extensions/Extension/Full_Screen_Shot/test/background-sim.node.js",
      "harness": "check",
      "case": "every store the database creates is on the list the wipe and the sweep walk",
      "proves": "Reads the shipped pages/db.js and compares every createObjectStore name with the STORES list the wipe and the sweep walk; a store created and not listed (or listed and not created) makes the check false. extensions.yml's sims job runs it. Cited through the register's `harness` form (assert-mechanism-claims M6, 2026-09-19): the case is the label of a check()/expect() call in a file that binds that function. Judged 2026-09-19."
    },
    {
      "file": "extensions/Extension/Full_Screen_Shot/publish/COMPLIANCE-CHECKLIST.md",
      "anchor": "not restate `version`, `description` or the permissions and therefore *cannot* drift from the",
      "status": "demoted",
      "reason": "Publishing-doc prose recording a dated run of check-version.mjs and quoting its overlay limb. The mechanism is real: publish/manifest.firefox.json is an RFC 7386 merge patch with no `version` key, and check-version.mjs limb 4 fails when an overlay states a version that disagrees with manifest.json. But no case plants a `version` in an overlay (scripts/test/selftest.node.js has no overlay expect(), and no node:test file reaches check-version), so nothing a register can name fails when the sentence goes false. Judged 2026-09-18."
    },
    {
      "file": "extensions/Extension/Full_Screen_Shot/publish/COMPLIANCE-CHECKLIST.md",
      "anchor": "that: `publish/manifest.firefox.json is an overlay — it does not restate the version, so it cannot",
      "status": "demoted",
      "reason": "Publishing-doc prose recording a dated run of check-version.mjs and quoting its overlay limb. The mechanism is real: publish/manifest.firefox.json is an RFC 7386 merge patch with no `version` key, and check-version.mjs limb 4 fails when an overlay states a version that disagrees with manifest.json. But no case plants a `version` in an overlay (scripts/test/selftest.node.js has no overlay expect(), and no node:test file reaches check-version), so nothing a register can name fails when the sentence goes false. Judged 2026-09-18."
    },
    {
      "file": "extensions/Extension/Full_Screen_Shot/publish/COMPLIANCE-CHECKLIST.md",
      "anchor": "Written down so the summary and the table cannot drift apart again",
      "status": "demoted",
      "reason": "Editorial intent in a compliance document: the CSP summary line and the table under it are two pieces of prose kept in step by the writer. No script reads either, so nothing fails when they disagree. Judged 2026-09-18."
    },
    {
      "file": "extensions/Extension/Full_Screen_Shot/publish/package.node.js",
      "anchor": "deliberately, so that a version bump cannot drift between the two engines.",
      "status": "demoted",
      "reason": "Design rationale for the overlay's shape, above the merge code. The shape (no `version` in publish/manifest.firefox.json) is what check-version.mjs limb 4 inspects, but no case plants a version in an overlay and asserts a failure (scripts/test/selftest.node.js has no overlay expect(), and it is not node:test). Judged 2026-09-18."
    },
    {
      "file": "extensions/Extension/Full_Screen_Shot/test/aihandoff-sim.node.js",
      "anchor": "cannot drift apart in a later edit to one of them.",
      "status": "demoted",
      "reason": "Describes how the record under test is built (count and leaf tally written by one expression), inside the sim that checks it. A single expression and two that agree today read the same to every check; the structure is visible in the producer. Judged 2026-09-18."
    },
    {
      "file": "extensions/Extension/Full_Screen_Shot/test/pixel-sim/history-harness.js",
      "anchor": "the extension ships cannot drift apart without a hard error.",
      "status": "demoted",
      "reason": "Describes how this harness builds its own fixture: idsInHtml() reads pages/history.html on the lines under it. No case can fail because the harness became hand-typed; a typed copy passes on the day it is typed. Judged 2026-09-18."
    },
    {
      "file": "extensions/Extension/Full_Screen_Shot/tool.json",
      "anchor": "two rows naming one target cannot drift into two builds",
      "status": "proven",
      "test": "tooling/ci/test/channel-register.test.mjs",
      "case": "FAILS when tool.json`s two declarations of which stores a target reaches disagree — the forward half is wrong",
      "proves": "assert-channel-register derives the pack-target map from BOTH declarations in tool.json (targets.chromium.stores and each storeMetadata.stores row's target) and exits 1 when they disagree, so a store row that moves to another target (a second build) is refused. Mutation-verified on the real tree 2026-09-18: storeMetadata.stores.edge.target chromium -> firefox took the guard from 0 to 1 with this case's message; restored byte-exact by sha256. The sibling REVERSE-half case covers the other direction."
    },
    {
      "file": "extensions/Extension/Full_Screen_Shot/tool.json",
      "anchor": "it does not restate the version, so it cannot drift from it.",
      "status": "demoted",
      "reason": "Publishing-doc prose recording a dated run of check-version.mjs and quoting its overlay limb. The mechanism is real: publish/manifest.firefox.json is an RFC 7386 merge patch with no `version` key, and check-version.mjs limb 4 fails when an overlay states a version that disagrees with manifest.json. But no case plants a `version` in an overlay (scripts/test/selftest.node.js has no overlay expect(), and no node:test file reaches check-version), so nothing a register can name fails when the sentence goes false. Judged 2026-09-18."
    },
    {
      "file": "extensions/core/test/coverage.node.js",
      "anchor": "the rule cannot drift away from the data it is about:",
      "status": "demoted",
      "reason": "Describes how this coverage sim builds its expectation (read from core/core.json, not typed). No case can fail because the rule became hand-typed; a typed list passes on the day it is typed and breaks only on the next core change. Judged 2026-09-18."
    },
    {
      "file": "extensions/core/test/settings.node.js",
      "anchor": "one code path, so the two cannot drift",
      "status": "proven",
      "test": "extensions/core/test/settings.node.js",
      "harness": "check",
      "case": "skInitSettings is skMigrateSettings — one code path, so the two cannot drift",
      "proves": "The sentence is the label of its own check: skInitSettings.toString() must contain skMigrateSettings, so an init that stops delegating to the migration makes it false. The file destructures check from core/test/harness.js; extensions.yml's core job runs core/test/*.node.js. Cited through the register's `harness` form (assert-mechanism-claims M6, 2026-09-19): the case is the label of a check()/expect() call in a file that binds that function. Judged 2026-09-19."
    },
    {
      "file": "extensions/docs/ARCHITECTURE.md",
      "anchor": "restate the version, so it cannot drift from it`.",
      "status": "demoted",
      "reason": "Publishing-doc prose recording a dated run of check-version.mjs and quoting its overlay limb. The mechanism is real: publish/manifest.firefox.json is an RFC 7386 merge patch with no `version` key, and check-version.mjs limb 4 fails when an overlay states a version that disagrees with manifest.json. But no case plants a `version` in an overlay (scripts/test/selftest.node.js has no overlay expect(), and no node:test file reaches check-version), so nothing a register can name fails when the sentence goes false. Judged 2026-09-18."
    },
    {
      "file": "extensions/docs/RELEASING.md",
      "anchor": "`templates/tool/publish/manifest.firefox.json` **is** a full manifest carrying",
      "status": "demoted",
      "reason": "Publishing-doc prose recording a dated run of check-version.mjs and quoting its overlay limb. The mechanism is real: publish/manifest.firefox.json is an RFC 7386 merge patch with no `version` key, and check-version.mjs limb 4 fails when an overlay states a version that disagrees with manifest.json. But no case plants a `version` in an overlay (scripts/test/selftest.node.js has no overlay expect(), and no node:test file reaches check-version), so nothing a register can name fails when the sentence goes false. Judged 2026-09-18."
    },
    {
      "file": "extensions/scripts/assert-e2e-proof-fresh.mjs",
      "anchor": "THE EXPECTED SET IS DERIVED, NOT TYPED",
      "status": "demoted",
      "reason": "True by inspection: EXPECT_DIRS is filled by walking <Category>/<Tool>/test/e2e on the lines below. scripts/test/selftest.node.js grades this gate with expect() cases (legs=0/1, stale proof), which the register cannot name, and tooling/ci/test/e2e-proof-fresh.test.mjs grades a different file (tooling/ci/assert-e2e-proof-fresh.mjs). No nameable case adds a tool directory and asserts the expected set grows. Judged 2026-09-18."
    },
    {
      "file": "extensions/scripts/changelog-section.mjs",
      "anchor": "cannot drift because it is derived from the file, not written down beside it.",
      "status": "demoted",
      "reason": "Design rationale for a floor of one. scripts/test/selftest.node.js records changelog-section.mjs as an OPEN GAP (no case), so nothing fails if the floor became a typed number. Judged 2026-09-18."
    },
    {
      "file": "extensions/scripts/check-version.mjs",
      "anchor": "'it does not restate the version, so it cannot drift from it'",
      "status": "demoted",
      "reason": "A runtime pass message, printed when the overlay has no `version` key. It describes the subject it just inspected, and the fail branch above it is the mechanism; no case plants a version in an overlay (selftest has no overlay expect(), and it is not node:test). Judged 2026-09-18."
    },
    {
      "file": "extensions/scripts/lib/toolinfo.mjs",
      "anchor": "emits, so the two sides of the comparison above cannot drift on spelling.",
      "status": "demoted",
      "reason": "Describes why RE_LOCALE_CATALOGUE is spelled to match localeMessageFiles' output. A shared definition and two copies that happen to agree behave identically today, so no test can fail on the difference; the structure is checked by reading the definition. Judged 2026-09-18."
    },
    {
      "file": "extensions/scripts/policy-check.mjs",
      "anchor": "API, so generator and gate cannot drift apart.",
      "status": "demoted",
      "reason": "Design rationale: generator and gate both read the plural categories from Intl.PluralRules at run time. Two readers of one runtime API agree by construction and no test can fail on the difference; selftest grades policy-check with expect() cases the register cannot name. Judged 2026-09-18."
    },
    {
      "file": "extensions/scripts/publish-catalog.mjs",
      "anchor": "Named once so the --check failure and the rewrite notice cannot drift apart.",
      "status": "demoted",
      "reason": "Describes code organisation: BOM_WHY is one constant used by two messages. A shared definition and two copies that happen to agree behave identically today, so no test can fail on the difference; the structure is checked by reading the definition. Judged 2026-09-18."
    },
    {
      "file": "extensions/scripts/schema/tool.schema.json",
      "anchor": "so a fourth list of store names cannot drift.",
      "status": "proven",
      "test": "extensions/scripts/test/selftest.node.js",
      "harness": "expect",
      "case": "a store set that disagrees with the schema vocabulary is caught",
      "proves": "Deletes one store from a fixture tool.json's storeMetadata and runs the real check-store-metadata.mjs, expecting exit 1 and \"two declarations of one fact\"; a gate that stopped deriving the vocabulary from the schema would pass it. extensions.yml's selftest job runs selftest.node.js. Cited through the register's `harness` form (assert-mechanism-claims M6, 2026-09-19): the case is the label of a check()/expect() call in a file that binds that function. Judged 2026-09-19."
    },
    {
      "file": "extensions/templates/tool/publish/preflight.mjs",
      "anchor": "from the real browser, so it cannot drift from the product",
      "status": "demoted",
      "reason": "A remediation hint in a template preflight, printed when publish/store/ holds no screenshot. It is a promise about a process (regenerate on every release), not a mechanism any test executes: no case captures a screenshot, changes the product and asserts the shot follows. Judged 2026-09-18."
    },
    {
      "file": "extensions/templates/tool/publish/shots.mjs",
      "anchor": "free on every release, so it cannot drift from the product.",
      "status": "demoted",
      "reason": "Header rationale for generated screenshots, in a template script that needs a real browser. It is a promise about a process (regenerate on every release), not a mechanism any test executes: no case captures a screenshot, changes the product and asserts the shot follows. Judged 2026-09-18."
    },
    {
      "file": "extensions/templates/tool/publish/shots.mjs",
      "anchor": "'so they cannot drift from the product.'",
      "status": "demoted",
      "reason": "The closing console line of the same template script. It is a promise about a process (regenerate on every release), not a mechanism any test executes: no case captures a screenshot, changes the product and asserts the shot follows. Judged 2026-09-18."
    },
    {
      "file": "apps/subscriptiontracker/integration_test/app_test.dart",
      "anchor": "both out of AppLocalizations means this assertion cannot drift from the",
      "status": "demoted",
      "reason": "Describes how this integration test builds its own expectation (from AppLocalizations, not a literal), which the lines under it show. No test grades how another test computes its expected value, and the integration lane runs against a live target, not in CI's unit suites. judged 2026-09-18."
    },
    {
      "file": "apps/subscriptiontracker/lib/core/app_config.dart",
      "anchor": "Composed from the two consts above so it cannot drift from either",
      "status": "demoted",
      "reason": "True by inspection: `telemetryRelease = '$appId@$appVersion'`. The brick's copy of this sentence is proven by stamp-text-fidelity, but that guard runs only on the probe stamp (ci.yml --vars _probe_vars.json); nothing reads this app's app_config.dart and no test in apps/subscriptiontracker/test pins telemetryRelease. (The \"NOT WIRED YET\" paragraph that sat under it was corrected 2026-09-19: main.dart passes AppConfig.telemetryRelease.) judged 2026-09-18."
    },
    {
      "file": "apps/subscriptiontracker/lib/features/add/add_subscription_sheet.dart",
      "anchor": "merely resembling it — the two cannot drift when one of them is edited.",
      "status": "demoted",
      "reason": "True by construction today (the renewal InputDecorator wears _fieldDecoration). No test compares the renewal field's decoration with the text fields'; dark_group_sheets_test.dart pins radii of BoxDecorations, not the InputDecoration they share. A hand-rolled replacement that looked the same would stay green. judged 2026-09-18."
    },
    {
      "file": "apps/subscriptiontracker/lib/features/add/add_subscription_sheet.dart",
      "anchor": "and the category dropdown all wear it, so the four cannot drift apart.",
      "status": "demoted",
      "reason": "Same judgement as the renewal-field sentence above: _fieldDecoration is shared by construction, and no widget test asserts the four fields resolve to one decoration. judged 2026-09-18."
    },
    {
      "file": "apps/subscriptiontracker/lib/features/home/home_screen.dart",
      "anchor": "that arithmetic rather than as a literal 1261 so it cannot drift from",
      "status": "demoted",
      "reason": "width_home_test.dart pins BOTH the constants (form 420, dividerWidth 1, expanded 840) and HomeScreen.asideMinBodyWidth == 1261 as separate literals. A literal 1261 in place of the arithmetic passes that test unchanged, so nothing grades the \"expressed as arithmetic\" half of the sentence. judged 2026-09-18."
    },
    {
      "file": "apps/subscriptiontracker/lib/features/monetization/manage_plan_screen.dart",
      "anchor": "navigation source as the purchase count, so the two cannot drift apart by",
      "status": "demoted",
      "reason": "assert-purchase-path.mjs §E walks the BRICK lib (BRICK_LIB) and the chassis files it delegates to; it never reads apps/subscriptiontracker/lib, so this app's router does not feed either count. The sentence is proven for packages/chassis_screens (see that entry), not for this copy. judged 2026-09-18."
    },
    {
      "file": "apps/subscriptiontracker/lib/l10n/app_en.arb",
      "anchor": "so the sentence cannot drift from the chart.",
      "status": "proven",
      "test": "apps/subscriptiontracker/test/a11y_semantics_test.dart",
      "case": "[en] the chart announces the total AND every category",
      "proves": "The expected label (expectedDonutLabel) is built from SubMath.categoryTotals over the same seeded providers, one a11yCategoryShare per category, joined. If the screen composed {breakdown} from anything but the painter's category list (a category dropped, reordered or restated), the announced sentence no longer contains the expected one and this case goes red."
    },
    {
      "file": "apps/subscriptiontracker/lib/state/providers/auth.dart",
      "anchor": "ONE READ for the two states that mean it, so the gate cannot drift from the",
      "status": "proven",
      "test": "apps/subscriptiontracker/test/password_reset_test.dart",
      "case": "an EXPIRED link ROUTES here by itself, and says which failure it was",
      "proves": "Driven through the real router with no navigation by hand. If shouldHoldForPasswordReset stopped reading the arrival limb (holding on `recovering` only), an expired link settles on /sign-in and this case goes red; its sibling \"a recovery arrival moves the app to /reset-password with NO navigation\" covers the recovering limb. Same shape as the brick entry for this sentence."
    },
    {
      "file": "apps/subscriptiontracker/lib/state/providers/config.dart",
      "anchor": "consumer instead, and because it is the same object the two names cannot",
      "status": "demoted",
      "reason": "True by construction: `const kSublyDefaultConfig = kAppDefaultConfig`. config_default_test.dart pins the alias's VALUES against the server contract, but nothing asserts identical(kSublyDefaultConfig, kAppDefaultConfig); a copied const with equal values passes. judged 2026-09-18."
    },
    {
      "file": "apps/subscriptiontracker/store/ios-appstore/README.md",
      "anchor": "file, which is why `macos-appstore/` carries no second copy",
      "status": "proven",
      "test": "tooling/ci/test/apple-privacy-manifest.test.mjs",
      "case": "limb 1 — a hand-edited macOS manifest is caught too, not just iOS",
      "proves": "assert-apple-privacy-manifest renders the macOS PrivacyInfo.xcprivacy from this ios-appstore audit and compares byte for byte; this case edits the macOS manifest alone and the guard exits non-zero, so the macOS side has no source of its own. WEAK on the literal \"no second copy\": a stray macos-appstore/privacy-manifest.json would be ignored, not caught."
    },
    {
      "file": "apps/subscriptiontracker/store/ios-appstore/privacy-manifest.json",
      "anchor": "The plugin set is DERIVED, not listed by hand.",
      "status": "proven",
      "test": "tooling/ci/test/apple-privacy-manifest.test.mjs",
      "case": "limb 4 — a NEW plugin with no inventory row: \"nobody said what required-reason APIs this uses\"",
      "proves": "The case adds a plugin to .flutter-plugins-dependencies (what the build links) without an inventory row, and the guard fails naming it. That is the equality limb the sentence says derivation buys; its sibling \"limb 4 — EQUALITY, not subset\" covers the reverse direction."
    },
    {
      "file": "apps/subscriptiontracker/test/a11y/keyboard_sweep_test.dart",
      "anchor": "cannot drift from what the app shows: re-point `/budget` at a different",
      "status": "demoted",
      "reason": "Describes this test rig's own construction (it evaluates route.builder rather than naming a screen class), visible in the code under it. No test grades how another test chooses its subject. judged 2026-09-18."
    },
    {
      "file": "apps/subscriptiontracker/test/a11y_semantics_test.dart",
      "anchor": "SAME providers the screen read, so the assertion cannot drift from the data",
      "status": "demoted",
      "reason": "Describes how this test file builds its expected labels (from the pumped container), visible in expectedDonutLabel and its siblings. A statement about a test's own construction; nothing grades it. judged 2026-09-18."
    },
    {
      "file": "apps/subscriptiontracker/test/a11y_semantics_test.dart",
      "anchor": "cannot drift from the state the row was painted from.",
      "status": "demoted",
      "reason": "Describes the expectation's source in the lines directly below (c.read(analyticsConsentProvider)). A test's own construction; nothing grades it. judged 2026-09-18."
    },
    {
      "file": "apps/subscriptiontracker/test/a11y_semantics_test.dart",
      "anchor": "expectation cannot drift from the currency the sheet formatted.",
      "status": "demoted",
      "reason": "Describes the expectation's source in the lines directly below (MoneyFormatter over l10n.localeName). A test's own construction; nothing grades it. judged 2026-09-18."
    },
    {
      "file": "apps/subscriptiontracker/test/l10n_group_home_test.dart",
      "anchor": "and the surface it opens cannot drift into two words for one action.",
      "status": "proven",
      "test": "apps/subscriptiontracker/test/l10n_group_home_test.dart",
      "case": "the shell reads its chrome from the arb",
      "proves": "The group's \"[$code] five tab labels, the demo banner and the FAB\" cases assert the FAB Tooltip message == l.addSubscriptionTitle in en and ta; giving the FAB any other key turns them red (mutation-verified 2026-09-18: message -> l10n.navHome, red in both locales). The sheet half (its heading reads addSubscriptionTitle) is pinned by dark_group_sheets_test.dart."
    },
    {
      "file": "packages/chassis_screens/lib/monetization/manage_plan_screen.dart",
      "anchor": "navigation source as the purchase count, so the two cannot drift apart by",
      "status": "proven",
      "test": "tooling/ci/test/purchase-path.test.mjs",
      "case": "🔴 FAILS when there is NO cancel path — the `0 <= 0` case that used to pass",
      "proves": "assert-purchase-path §E counts both paths with one hops() BFS over the context.go edges of the brick lib plus the chassis files it delegates to (this file among them, printed on every run). Deleting the settings -> /manage-plan edge makes the cancel count infinite and the guard exits 1; the sibling \"FAILS when there is no PURCHASE path either\" removes the /paywall edge from the same graph."
    },
    {
      "file": "packages/core/lib/src/content/pack_verifier.dart",
      "anchor": "`cryptography` package this file verifies with, so the encoding cannot drift",
      "status": "demoted",
      "reason": "The production key was generated once on the owner's machine by tool/gen_pack_keypair.dart; nothing re-runs it. assert-pack-roundtrip.mjs says so in its header: CI uses a TEST keypair and cannot go red on a seed that no longer matches pinned k1 (owner-run canary only). judged 2026-09-18."
    },
    {
      "file": "packages/core/lib/src/promo/promo_gate.dart",
      "anchor": "The state is bundled with the verdict so the two cannot drift apart.",
      "status": "proven",
      "test": "packages/core/test/promo_gate_test.dart",
      "case": "the returned state is what stops it showing twice",
      "proves": "Feeds the first decision's own .state into a second decide and expects shownTooRecently. If the decision stopped carrying the incremented state (returned the input, or left the count to the caller), the second call would say show again and this case goes red."
    },
    {
      "file": "packages/core/lib/src/storage/key_value_store.dart",
      "anchor": "and containsKey routes through here; there is no second copy of this",
      "status": "demoted",
      "reason": "storage_test.dart proves qualify() namespaces correctly, but no guard or test searches the chassis for a second `'$appId$separator'`-style concatenation; a second, correct copy elsewhere would pass everything. judged 2026-09-18."
    },
    {
      "file": "packages/design_system/lib/src/tokens/app_text.dart",
      "anchor": "cannot drift: a future edit that rebuilds light through `copyWith` would",
      "status": "proven",
      "test": "packages/design_system/test/app_text_test.dart",
      "case": "every style is the SAME OBJECT as the const it replaces",
      "proves": "Asserts `same(AppText.x)` for all six light styles. A light branch rebuilt through copyWith would compare equal but not identical, and this case goes red, which is exactly the drift the sentence rules out."
    },
    {
      "file": "packages/design_system/lib/src/tokens/app_text.dart",
      "anchor": "already makes for its `muted` token, so the two cannot drift apart.",
      "status": "proven",
      "test": "packages/design_system/test/app_text_test.dart",
      "case": "secondary styles become onSurfaceVariant",
      "proves": "Asserts the dark muted style colour equals theme.extension<AppThemeX>()!.muted as well as scheme.onSurfaceVariant. If either AppText.resolve or AppThemeX.fromScheme moved muted to another slot alone, the two stop matching and this case goes red."
    },
    {
      "file": "packages/design_system/lib/src/widgets/content_pane.dart",
      "anchor": "widget cannot drift apart either.",
      "status": "proven",
      "test": "packages/design_system/test/content_pane_test.dart",
      "case": ".form caps at AppBreakpoints.form",
      "proves": "Measures the rendered width against AppBreakpoints.form at a 1280 surface. A widget cap typed as its own number drifts from the constant and goes red (mutation-verified 2026-09-18: ContentPane.form maxWidth -> 400, Expected 420.0 Actual 400.0); the .pane and .reading siblings cover the other two."
    },
    {
      "file": "packages/design_system/lib/src/widgets/two_pane.dart",
      "anchor": "pinned in `two_pane_test.dart` so the arithmetic cannot drift",
      "status": "proven",
      "test": "packages/design_system/test/two_pane_test.dart",
      "case": "the documented table",
      "proves": "Asserts TwoPaneSplit.forWidth at 840, 1141, 1280 and 1400 against the list/detail columns of the doc table. Changing the split arithmetic without the prose (or the reverse) makes a row disagree and the case goes red. The 839 single-column row is covered by \"null below 840, a split at and above it\"."
    },
    {
      "file": "packages/design_system/lib/src/widgets/two_pane.dart",
      "anchor": "subtracted, so the drawn gap and the reserved gap cannot drift.",
      "status": "proven",
      "test": "packages/design_system/test/two_pane_test.dart",
      "case": "the columns tile the width exactly, left to right",
      "proves": "At 840 it asserts the drawn VerticalDivider width == TwoPaneSplit.dividerWidth, the divider at x=420 and the detail starting at 421 and ending at 840. A divider drawn at a width other than the one the arithmetic subtracted moves or overflows the detail and this case goes red."
    },
    {
      "file": "packages/design_system/test/app_text_test.dart",
      "anchor": "free to drift tomorrow. Identity cannot drift.",
      "status": "proven",
      "test": "packages/design_system/test/app_text_test.dart",
      "case": "every style is the SAME OBJECT as the const it replaces",
      "proves": "The sentence explains the enclosing case's own `same(...)` matchers. Those fail on a copyWith-rebuilt style that `equals` would accept, so the case itself is what goes red when light stops being the const objects."
    },
    {
      "file": "packages/design_system/test/app_text_test.dart",
      "anchor": "// so the two cannot drift apart.\n      expect(s.muted.color, scheme.onSurfaceVariant);",
      "status": "proven",
      "test": "packages/design_system/test/app_text_test.dart",
      "case": "secondary styles become onSurfaceVariant",
      "proves": "The enclosing case asserts s.muted.color == theme.extension<AppThemeX>()!.muted, so AppText and AppThemeX choosing different slots turns it red. Same proof as the app_text.dart sentence it restates."
    },
    {
      "file": "packages/platform_storage/lib/src/prefs_key_value_store.dart",
      "anchor": "[NamespacedKeyValueStore] — one implementation, no second copy",
      "status": "demoted",
      "reason": "prefs_key_value_store_test.dart proves the RAW prefs keys carry the app id, but a second, correct prefixing implementation here would pass it; nothing asserts the delegate is core's NamespacedKeyValueStore or that no other concatenation exists. judged 2026-09-18."
    },
    {
      "file": "packages/purchases/lib/src/offering.dart",
      "anchor": "a matrix of currencies, so the two cannot drift.",
      "status": "proven",
      "test": "packages/purchases/test/offering_test.dart",
      "case": "the offering and the portfolio money type CANNOT DRIFT",
      "proves": "The group asserts formattedPrice == price.plainFormat() over USD, INR, EUR, JPY, KWD and an unknown code. Changing either rendering alone turns it red (mutation-verified 2026-09-18: the unknown-code branch `'$currencyCode $major'` -> no space, red on ZZZ and KWD)."
    },
    {
      "file": "packages/purchases/test/offering_test.dart",
      "anchor": "group('the offering and the portfolio money type CANNOT DRIFT'",
      "status": "proven",
      "test": "packages/purchases/test/offering_test.dart",
      "case": "the offering and the portfolio money type CANNOT DRIFT",
      "proves": "The group title is the claim and the group is its proof: every matrix case compares the two renderings directly, so they cannot diverge while it is green. Mutation-verified with the offering.dart entry."
    },
    {
      "file": "packages/telemetry/test/telemetry_bootstrap_test.dart",
      "anchor": "It is written the same way so the two cases cannot drift",
      "status": "demoted",
      "reason": "A note on why two test cases share a shape; it asserts nothing about a mechanism a test could grade. judged 2026-09-18."
    },
    {
      "file": ".github/workflows/build-platforms.yml",
      "anchor": "# why: DERIVED, NOT LISTED. A hand-written app list here is a second copy of the",
      "status": "demoted",
      "reason": "Rationale for the `prepare` step directly below, which runs `assert-release-lane-generic.mjs --emit-apps`. Nothing grades that the matrix is DERIVED: limb A passes a hand-written matrix that happens to equal today's workspace ('a matrix over both apps is the CORRECT generic shape and passes' in tooling/ci/test/release-lane-generic.test.mjs) and catches only the rot, a second app no lane covers. The guard's own header calls this link \"a shared implementation, not an assertion\". Judged 2026-09-19."
    },
    {
      "file": ".github/workflows/deploy-web.yml",
      "anchor": "# why: DERIVED, NOT LISTED. A hand-written app list here is a second copy of the",
      "status": "demoted",
      "reason": "Rationale for the `prepare` step directly below, which runs `assert-release-lane-generic.mjs --emit-apps`. Nothing grades that the matrix is DERIVED: limb A passes a hand-written matrix that happens to equal today's workspace ('a matrix over both apps is the CORRECT generic shape and passes' in tooling/ci/test/release-lane-generic.test.mjs) and catches only the rot, a second app no lane covers; limb D reads `paths:`, `working-directory`, `run:` and `with:`, never `strategy.matrix`. The guard's own header calls this link \"a shared implementation, not an assertion\". Judged 2026-09-19."
    },
    {
      "file": ".github/workflows/e2e.yml",
      "anchor": "# why: DERIVED, NOT LISTED. A hand-written app list here is a second copy of the",
      "status": "demoted",
      "reason": "Rationale for the `prepare` step directly below, which runs `assert-release-lane-generic.mjs --emit-apps`. Nothing grades that the matrix is DERIVED: limb A passes a hand-written matrix that happens to equal today's workspace ('a matrix over both apps is the CORRECT generic shape and passes' in tooling/ci/test/release-lane-generic.test.mjs) and catches only the rot, a second app no lane covers. The guard's own header calls this link \"a shared implementation, not an assertion\". Judged 2026-09-19."
    },
    {
      "file": ".github/workflows/e2e.yml",
      "anchor": "shape that cannot drift — there is no list of exempt runs to maintain",
      "status": "demoted",
      "reason": "A design argument comparing two shapes (refuse at the ref vs an exempt-run list), not a property of the step below. No test executes the rehearsal preflight step. Judged 2026-09-19."
    },
    {
      "file": ".github/workflows/extensions-ci.yml",
      "anchor": "# DERIVED, NOT LISTED. The set is read out of the workflow files, so it",
      "status": "demoted",
      "reason": "A comment inside an inline `run:` bash block. No test executes this step (no case in tooling/ci/test or extensions/scripts/test reads it); the `find`/`grep` the sentence describes sits directly beneath it and is the mechanism, run on every CI pass. Nothing a node:test case could grade without re-implementing the step. Judged 2026-09-19."
    },
    {
      "file": ".github/workflows/extensions-ci.yml",
      "anchor": "# DERIVED, NOT LISTED — the same rule the gate inventory above follows,",
      "status": "demoted",
      "reason": "A comment inside an inline `run:` bash block. No test executes this step (no case in tooling/ci/test or extensions/scripts/test reads it); the `find`/`grep` the sentence describes sits directly beneath it and is the mechanism, run on every CI pass. Nothing a node:test case could grade without re-implementing the step. Judged 2026-09-19."
    },
    {
      "file": ".github/workflows/extensions-ci.yml",
      "anchor": "# DERIVED, NOT LISTED — the same rule the gate inventory at the top of",
      "status": "demoted",
      "reason": "A comment inside an inline `run:` bash block. No test executes this step (no case in tooling/ci/test or extensions/scripts/test reads it); the `find`/`grep` the sentence describes sits directly beneath it and is the mechanism, run on every CI pass. Nothing a node:test case could grade without re-implementing the step. Judged 2026-09-19."
    },
    {
      "file": ".github/workflows/extensions.yml",
      "anchor": "- name: Run every suite in test/e2e (derived, not listed)",
      "status": "demoted",
      "reason": "A step NAME, shown on the checks list. The derivation is the inline bash below it, which no test executes. Judged 2026-09-19."
    },
    {
      "file": "README.md",
      "anchor": "so they cannot drift. It asserts **completeness, not greenness**",
      "status": "proven",
      "test": "tooling/ci/test/guard-sweep-invocations.test.mjs",
      "case": "🔴 arguments folded onto a `run: >` continuation line are recorded",
      "proves": "Derives every folded guard invocation from the REAL workflows and requires `guard-sweep.mjs --invocations` to have recorded the argument the YAML passes. A sweep whose arguments stopped being read from the YAML (typed, or a one-line reader) records them empty and this case goes red."
    },
    {
      "file": "catalog/store-matrix.json",
      "anchor": "precisely so the two blocks cannot drift into each thinking the other declared it",
      "status": "demoted",
      "reason": "A data note in the registry. The limb it names exists (assert-store-matrix.mjs, the `infrastructure` repo with NO github.outOfMatrix line), but no case in store-matrix.test.mjs or github-matrix.test.mjs plants an infrastructure entry without its outOfMatrix line, so no named test fails if that limb goes. Coverage gap, recorded. Judged 2026-09-19."
    },
    {
      "file": "docs/ci/README.md",
      "anchor": "cannot drift apart. It **refuses the whole run (exit 2) and writes nothing**",
      "status": "proven",
      "test": "tooling/ci/test/propagate-versions.test.mjs",
      "case": "--write moves the three melos sites and the drift guard then passes",
      "proves": "Runs the propagator with --write and then the guard it claims to share a rule table with; the guard must pass. A propagator carrying its own copy of the rules that disagreed with the guard would leave a site the guard reads unwritten, and this case goes red."
    },
    {
      "file": "docs/ci/build-platforms.md",
      "anchor": "matrix iterates. One function, one file: a lane cannot drift from the",
      "status": "demoted",
      "reason": "Prose mirror of the build-platforms.yml `prepare` step, judged demoted there: nothing grades that the lane calls the emitter (a hand-written matrix equal to today's workspace passes limb A). assert-release-lane-generic.mjs names this link \"a shared implementation, not an assertion\". Judged 2026-09-19."
    },
    {
      "file": "docs/ci/deploy-web.md",
      "anchor": "GRADES this lane. One function, one file: this lane cannot drift from the",
      "status": "demoted",
      "reason": "Prose mirror of the deploy-web.yml `prepare` step, judged demoted there: nothing grades that the lane calls the emitter, and limb D does not read `strategy.matrix`. assert-release-lane-generic.mjs names this link \"a shared implementation, not an assertion\". Judged 2026-09-19."
    },
    {
      "file": "docs/ci/deploy-web.md",
      "anchor": "debug id cannot drift from what the SDK reports",
      "status": "demoted",
      "reason": "A property of GlitchTip's and the SDK's debug-id format (derived from the bundle bytes by glitchtip-cli), external to this repository. Nothing here could grade it. Judged 2026-09-19."
    },
    {
      "file": "docs/ci/extensions.md",
      "anchor": "scripts/schema/tool.schema.json so it cannot drift into a fourth list.",
      "status": "proven",
      "test": "extensions/scripts/test/selftest.node.js",
      "harness": "expect",
      "case": "a store set that disagrees with the schema vocabulary is caught",
      "proves": "The same fact as the tool.schema.json storeMetadata description, graded by the same case: a fixture whose storeMetadata disagrees with the schema's store vocabulary must make check-store-metadata.mjs exit 1. Cited through the register's `harness` form (assert-mechanism-claims M6, 2026-09-19): the case is the label of a check()/expect() call in a file that binds that function. Judged 2026-09-19."
    },
    {
      "file": "docs/ci/extensions.md",
      "anchor": "here cannot drift into a claim about a submission",
      "status": "proven",
      "test": "tooling/ci/test/deployment-record.test.mjs",
      "case": "a SUBMITTABLE store row REFUSES the origin state — it is not the easy way past naming a submission",
      "proves": "Runs record-deployment.mjs on a submittable store row with `--state pending_manual_publish` and requires the refusal before the API. If the script accepted the origin state on a row this factory can submit through, the flag could stand in for a submission and this case goes red. Its sibling 'a NON-submittable store row REFUSES `in_review`' pins the other half."
    },
    {
      "file": "docs/ci/extensions.md",
      "anchor": "Derived, not listed — the same rule as the CI matrix. A tool joins the",
      "status": "demoted",
      "reason": "Prose mirror of the extensions.yml e2e `set` step, an inline bash block no test executes. Judged 2026-09-19."
    },
    {
      "file": "docs/ci/extensions.md",
      "anchor": "### before step **Run every suite in test/e2e (derived, not listed)**",
      "status": "demoted",
      "reason": "A heading that quotes the extensions.yml step name. Judged 2026-09-19."
    },
    {
      "file": "docs/ci/extensions.md",
      "anchor": "DERIVED, NOT LISTED — the rule the discover job already follows. A",
      "status": "demoted",
      "reason": "Prose mirror of the extensions.yml e2e suite/library split, an inline bash block no test executes. Judged 2026-09-19."
    },
    {
      "file": "docs/ci/ops-watch.md",
      "anchor": "**And no second copy in `assert-platform-proof-fresh.mjs`.**",
      "status": "demoted",
      "reason": "Records a design decision NOT to add a second copy of the ops-watch rule, and why. It describes the absence of an edit, not a mechanism a test could flip. Judged 2026-09-19."
    },
    {
      "file": "services/_shared/src/entitlement-read.ts",
      "anchor": "the unparseable-date rule are not re-implemented here and cannot drift.",
      "status": "proven",
      "test": "services/platform/test/bundle-entitlements.test.ts",
      "case": "G11 — sandbox money grants no production unlock, on the bundle branch too",
      "proves": "Drives the bundle branch with grants from the other money world and with NO environment, and requires a denial, then the same world and requires a grant. A bundle branch that decided on its own copy of the rule and dropped the environment limb goes red here. WEAK: it grades the environment limb only (lifetime and grace dates are pinned by sibling cases in the same file), and a re-implementation that happened to agree would pass."
    },
    {
      "file": "services/platform/test/checkout.test.ts",
      "anchor": "describe('REQUIRED_COVERAGE — the price map and the served offerings cannot drift apart'",
      "status": "proven",
      "test": "services/platform/test/checkout.test.ts",
      "case": "every SERVED offering is either priced by the rail or DECLARED pending, and every price id has a served offering",
      "proves": "Compares PADDLE_PRICE_IDS and RAIL_PRICE_PENDING against the served offerings in BOTH directions. A price id with no served offering, or a served offering with neither a price nor a pending reason, turns it red."
    },
    {
      "file": "services/platform/test/github-dispatch.test.ts",
      "anchor": "// cannot drift from either.",
      "status": "proven",
      "test": "services/platform/test/github-dispatch.test.ts",
      "case": "🔴 a dispatched workflow whose duty rows still read `event=schedule` MUST keep its own schedule",
      "proves": "The comment sits inside this case; it imports tooling/ops/register.json with ?raw and reads the real workflows, so the case grades the committed files rather than a copy of them."
    },
    {
      "file": "services/platform/test/twinned-worker-modules.test.ts",
      "anchor": "services/_shared/src/${d.module}. Every carrier re-exports it, so there is no second copy for this ",
      "status": "demoted",
      "reason": "Text of a failure message the anti-rot limb prints when an exception row outlives its twin. It describes the finding it reports; the limb is 'no exception row has outlived what it excuses'. Judged 2026-09-19."
    },
    {
      "file": "services/platform/test/twinned-worker-modules.test.ts",
      "anchor": "file cannot drift from itself, which is strictly stronger than holding copies",
      "status": "proven",
      "test": "services/platform/test/twinned-worker-modules.test.ts",
      "case": "every carrier of a shared-home module is EXACTLY a re-export of it",
      "proves": "Requires every carrier of a module with a home under services/_shared/src to be exactly a re-export. A carrier that forks a copy back is two files, not one, and this case goes red."
    },
    {
      "file": "services/platform/test/twinned-worker-modules.test.ts",
      "anchor": "// THE SUBJECT SET IS DERIVED, NOT LISTED. Every `services/*/src/lib/*.ts`",
      "status": "proven",
      "test": "services/platform/test/twinned-worker-modules.test.ts",
      "case": "the derived subject set still finds both Workers and both twinned modules",
      "proves": "Floors the derived Worker and twin sets, and each group separately, so a rename that empties the derivation goes red instead of iterating nothing. WEAK: it proves the floor half of the sentence; a typed list that met the floors would also pass."
    },
    {
      "file": "services/subscriptiontracker-api/src/middleware/auth.ts",
      "anchor": "the two cannot drift into a weaker one an attacker reaches by making the",
      "status": "demoted",
      "reason": "Gradeable and NOT graded, recorded as a gap: no case in services/subscriptiontracker-api/test/auth.test.ts sends a wrong-issuer or wrong-audience ES256 token down the cache-fallback path. 'the fallback does NOT widen — a FOREIGN ES256 key is still 401 against the cache' pins the KEY, not the verify options, so a fallback called with weaker `opts` would stay green. Judged 2026-09-19."
    },
    {
      "file": "services/subscriptiontracker-api/test/entitlements.test.ts",
      "anchor": "// against src/types.ts's `Entitlement` in entitlements.test.ts so it cannot",
      "status": "demoted",
      "reason": "Quotes the harness comment of PR #94 as HISTORY, to say the guard it promised did not exist. The quote asserts nothing about the file as it stands. Judged 2026-09-19."
    },
    {
      "file": "sites/nikatru/functions/api/subscribe.js",
      "anchor": "// promise and the code cannot drift apart quietly.",
      "status": "proven",
      "test": "tooling/ci/test/site-integrity.test.mjs",
      "case": "FAILS the moment the copy changes underneath it",
      "proves": "Plants a Function quoting a SITE PROMISE and a page whose copy no longer carries that sentence verbatim; check-site-integrity.mjs must exit 1. If the marker stopped being checked against the deploy root, the copy could move quietly and this case goes red."
    },
    {
      "file": "sites/nikatru/subscriptiontracker/privacy.html",
      "anchor": "so it cannot drift from what the code does without a build failing.",
      "status": "proven",
      "test": "tooling/ci/test/app-yaml.test.mjs",
      "case": "MUTATION: a hand edit to the per-app notice page is caught",
      "proves": "Hand-edits this page in a copy of the real tree and requires `render-privacy.mjs --check` to exit 1 naming it. A page that stopped being compared with what privacy.yaml renders turns this red. WEAK on the last hop: declaration-vs-code is graded by other limbs (the live-sink case in the same file), not by this case."
    },
    {
      "file": "tooling/ci/assert-adapter-capabilities.mjs",
      "anchor": "Derived, not typed, by\n//    adapter-set.mjs's deriveAdapters()",
      "status": "proven",
      "test": "tooling/ci/test/adapter-set.test.mjs",
      "case": "assert-package-boundaries and assert-adapter-capabilities derive the same adapter set",
      "proves": "Both guards now import deriveAdapters() from tooling/ci/adapter-set.mjs. The case runs BOTH guards over copies of the real pubspecs plus a package whose only non-path dependency is a non-flutter `sdk:` (the rule the two old copies disagreed on) and requires the adapter list each prints to be equal. Mutation-verified 2026-09-19: restoring assert-package-boundaries.mjs's old private reader turned it red. Judged 2026-09-19."
    },
    {
      "file": "tooling/ci/assert-deploy-triggers-deploy.mjs",
      "anchor": "//      DERIVED, NOT LISTED. For every glob of the shape `X/**`, the tree at `X`",
      "status": "demoted",
      "reason": "Gradeable and NOT graded, recorded as a gap: `judgeImports` (limb 3) has no case in tooling/ci/test/assert-deploy-triggers-deploy.test.mjs; the real-tree case calls `judge` (limbs 1-2) only. Judged 2026-09-19."
    },
    {
      "file": "tooling/ci/assert-no-store-bundle-copy.mjs",
      "anchor": "// Derived, not listed: every `store/` directory under `apps/` and `extensions/`,",
      "status": "demoted",
      "reason": "Gradeable and NOT graded: every case in bundle-provenance-guards.test.mjs runs over the existing store dirs; none adds a new store/ directory and requires it read, so a typed list of today's trees passes. Judged 2026-09-19."
    },
    {
      "file": "tooling/ci/assert-ops-register.mjs",
      "anchor": "that script's own source so the two cannot drift.",
      "status": "demoted",
      "reason": "Weakly graded, recorded as a gap: ops-register.test.mjs asserts `gateCheckName(REPO_ROOT) === 'ci-gate'` on the real tree only; no case plants a different `const GATE`, so a hard-coded 'ci-gate' would pass. The comment beneath names the three-reader debt itself. Judged 2026-09-19."
    },
    {
      "file": "tooling/ci/assert-ops-register.mjs",
      "anchor": "// ── [14]O-7 · the deploy-job domain, DERIVED from the recorded claims",
      "status": "demoted",
      "reason": "Gradeable and NOT graded: the O-7 cases inject `deployJobs` into `evaluate` directly; the derivation from the workflows in main() is reached only by the real-tree run, which asserts no count. Judged 2026-09-19."
    },
    {
      "file": "tooling/ci/assert-provider-graph-acyclic.mjs",
      "anchor": "// DERIVED, not listed: every `apps/*/lib` plus the brick template's own. A",
      "status": "proven",
      "test": "tooling/ci/test/provider-graph-acyclic.test.mjs",
      "case": "(a) the #258 pair — read into a provider that watches the reader",
      "proves": "The fixture app is `apps/demo`, a name no list in the guard holds; the case requires the cycle there to be found. A typed app list would never open apps/demo and the case goes red."
    },
    {
      "file": "tooling/ci/assert-publish-gate.mjs",
      "anchor": "required artifacts DERIVED not listed; ",
      "status": "demoted",
      "reason": "A runtime pass message, printed after the checks above it; it describes the run, and the header limb is the mechanism. Judged 2026-09-19."
    },
    {
      "file": "tooling/ci/assert-release-provenance.mjs",
      "anchor": "// tooling/release/submit-play.mjs:375, so the two checks cannot drift.",
      "status": "demoted",
      "reason": "The comment itself says the receiver is a naming choice that cannot change a verdict on well-formed YAML; nothing compares the two spellings. The predicate it feeds is pinned by 'FAILS when the submit job declares no environment:'. Judged 2026-09-19."
    },
    {
      "file": "extensions/scripts/changelog-section.mjs",
      "anchor": "(RE_CHANGELOG_VERSION_HEADING), so the two readers of this file cannot drift",
      "case": "changelogTop() and changelog-section.mjs agree on every heading spelling",
      "proves": "The heading pattern is exported once from lib/toolinfo.mjs and imported here. The case feeds six heading spellings (accepted and rejected) to changelogTop() and to this script run as a CLI over a fixture tool, and requires the same verdict from both. Mutation-verified 2026-09-19: a local stricter copy here (`^## \\[`) disagreed on `##[2.0.0]` and turned it red. Judged 2026-09-19.",
      "status": "proven",
      "test": "tooling/ci/test/extensions-shared-constants.test.mjs"
    },
    {
      "file": "extensions/scripts/pack.mjs",
      "anchor": "templates/tool/publish/pack.mjs imports too, so a tool's own packager and\n   this one cannot drift on it.",
      "case": "scripts/pack.mjs and the template packager import DOS_TIME/DOS_DATE from lib/zip-time.mjs and define no copy",
      "proves": "Both packers import DOS_TIME/DOS_DATE from extensions/scripts/lib/zip-time.mjs; the case reads each file's comment-free code and fails if either stops importing from that module or defines its own. Its sibling case compares Full_Screen_Shot's CommonJS copy by value. Mutation-verified 2026-09-19: restoring the local constant in pack.mjs turned it red. Judged 2026-09-19.",
      "status": "proven",
      "test": "tooling/ci/test/extensions-shared-constants.test.mjs"
    },
    {
      "file": "extensions/scripts/publish-cws-token.mjs",
      "anchor": "preflight import it, so the preflight and both callers cannot drift apart",
      "case": "extensions.yml's CWS_* secret names equal the Chrome preflight's, which include CWS_SA_ENV",
      "proves": "publish-cws.mjs, publish-cws-keepalive.mjs and publish-arming.mjs import CWS_SA_ENV. The case requires the Chrome preflight (LANES['chrome-webstore'].secrets, imported) to name CWS_SA_ENV and requires extensions.yml's secrets.CWS_* set, read from comment-blanked workflow lines, to equal the preflight's set with each mapped to an env var of the same name. Mutation-verified 2026-09-19: renaming CWS_SA_ENV without the workflow turned it red. Judged 2026-09-19.",
      "status": "proven",
      "test": "tooling/ci/test/extensions-shared-constants.test.mjs"
    },
    {
      "file": "tooling/ci/test/modal-detection.test.mjs",
      "anchor": "DERIVED, NOT WRITTEN DOWN. This line was",
      "status": "proven",
      "test": "tooling/ci/test/modal-detection.test.mjs",
      "case": "a package whose suites use flutter_test but whose pubspec stopped saying so is caught by the oracle",
      "proves": "The expected root count is derived from the copied tree by a second reading (workspace members whose suites import package:flutter_test/), not written down. The case plants a package the guard does not derive (no flutter_test: in its pubspec) while its suite imports flutter_test, and asserts the root-count check THROWS. Measured 2026-09-19: with the equality neutered (`if (false) assert.equal`) the case goes red, exit 1; restored byte-exact, green. The sibling case \"a NEW package root is counted, not refused\" plants a declared package and stays green, which the old literal /in 9 root(s)/ could not."
    },
    {
      "file": "apps/subscriptiontracker/lib/features/insights/insights_screen.dart",
      "anchor": "a figure derived from `cats` cannot",
      "status": "proven",
      "test": "apps/subscriptiontracker/test/rounded_breakdown_surfaces_test.dart",
      "case": "INSIGHTS — the legend adds up to the number in the ring",
      "proves": "The sentence claims the donut centre and its legend cannot disagree because both are derived from the same `cats`. The case pumps the screen over six categories whose naive per-row rounding sums to 94 against a whole of 97, reads the rendered centre and the six rendered legend Texts back out of the tree, and asserts the column sums to the centre. Mutation 2026-09-20: putting the legend back on its own `money.formatBagRounded(cats[i].value)` — a second rounding decision — turns it red at 94 != 97. judged 2026-09-20."
    },
    {
      "file": "apps/subscriptiontracker/test/width_shell_fab_test.dart",
      "anchor": "first case of the group pins the copy to the source text, so the two cannot",
      "status": "proven",
      "test": "apps/subscriptiontracker/test/width_shell_fab_test.dart",
      "case": "the six rows are the store suite's own",
      "proves": "The at-rest group seeds a COPY of kIllustrative (the store suite is an integration binding, not an importable library). The case parses the kIllustrative block out of integration_test/store_screenshots_test.dart and asserts the parsed rows equal the copy, in order, so an edited, added, dropped or reordered row on either side fails here. Mutations 2026-09-22, each restored byte-exact and green again: the copy's 'Video streaming' price 15.99 -> 15.98 turns it red (at location [0][1] is '15.99' instead of '15.98'); a seventh row added to the source's kIllustrative turns it red (at location [6]). judged 2026-09-22. ⏱ 2026-09-22 (store-frame-followup): both copies now carry a fourth column, the renewal offset in days (3, 6, 11, 17, 24, 29); the pattern accepts three or four columns and still compares each row whole, so an offset edited on either side fails here too. Mutation for the writer, UNRUN in the draft: the copy's 'Video streaming' offset '3' -> '4' must turn it red at [0][3], then restore byte-exact."
    },
    {
      "file": "services/_shared/test/preflight.ts",
      "anchor": "THE SEAM, AND WHY IT CANNOT DRIFT.",
      "status": "proven",
      "test": "services/platform/test/cors.test.ts",
      "case": "every mounted route is preflight-approved for its own method, from a listed origin",
      "proves": "The sentence claims the methods checked are the Worker's mounting itself (Hono `app.routes`, sub-apps merged), not a second list kept in step. The case preflights every entry of the real app's route table with that route's own method. Mutation 2026-09-22: services/platform/src/middleware/cors.ts back to `GET, POST, DELETE, OPTIONS` (the shipped defect) turns it red with `PUT /v1/account/apple-token -> 204, allow-origin=https://nikatru.com, allow-methods=GET, POST, DELETE, OPTIONS` — a route no hand-typed list in the old test named, reached only because it is read from the mounting. Restored byte-exact, green again. judged 2026-09-22."
    },
    {
      "file": "services/platform/test/cors.test.ts",
      "anchor": "preflight allows every method a MOUNTED route answers — derived, not listed",
      "status": "proven",
      "test": "services/platform/test/cors.test.ts",
      "case": "every mounted route is preflight-approved for its own method, from a listed origin",
      "proves": "The describe title is the claim; this case, inside it, reads its endpoints from `mountedEndpoints(app.routes)` and nothing else. Mutation 2026-09-22: PUT removed from the platform Allow-Methods turns it red naming `PUT /v1/account/apple-token`, which the hand-typed ['GET', 'POST', 'DELETE', 'OPTIONS'] loop it replaced stayed green through. Restored byte-exact (sha256 d608e4eb0f0d16ec), green again. judged 2026-09-22."
    },
    {
      "file": "services/subscriptiontracker-api/test/cors.test.ts",
      "anchor": "preflight covers every method the routes actually expose — derived, not listed",
      "status": "proven",
      "test": "services/subscriptiontracker-api/test/cors.test.ts",
      "case": "every mounted route is preflight-approved for its own method, from a listed origin",
      "proves": "The describe title is the claim; this case, inside it, reads its endpoints from `mountedEndpoints(app.routes)` and nothing else. Mutation 2026-09-22: PUT removed from `allowMethods` in src/middleware/cors.ts turns it red with `PUT /v1/budget -> 204, allow-origin=https://nikatru.com, allow-methods=GET,POST,PATCH,DELETE,OPTIONS`. Restored byte-exact (sha256 01e8e91934e9424a), green again. judged 2026-09-22."
    },
    {
      "file": "tooling/ci/assert-apps-gov-in-media.mjs",
      "anchor": "and it cannot drift from them without this guard saying so.",
      "status": "proven",
      "test": "tooling/ci/test/apps-gov-in-media.test.mjs",
      "case": "NEGATIVE: a re-captured Play original makes the derived file STALE, naming the command",
      "proves": "The case re-captures one Play original after the derivation and asserts the guard names the derived file STALE with the --write command; it passes only while the guard re-derives from the committed originals. The hand-edited-output and tampered-record cases are the other two drift directions."
    },
    {
      "file": "tooling/ci/assert-store-metadata.mjs",
      "anchor": "the money answers and the age answer are DERIVED, not typed",
      "status": "proven",
      "test": "tooling/ci/test/store-metadata.test.mjs",
      "case": "FAILS \"No\" on the payment gateway when the row declares a rail that sells",
      "proves": "The paymentGateway and financial-transactions answers are read off channel-register purchaseRail.rail (ADR 094 item 5). The case flips the row to a selling rail under a typed \"No\"; it exits 1 only if the answer is derived from the row. Added 2026-09-23 with the apps-gov-in-apk-1 lane."
    },
    {
      "file": "tooling/ci/assert-auth-callbacks.mjs",
      "anchor": "off `enum AuthFlow` so the allow-list limb cannot drift from it",
      "status": "proven",
      "test": "tooling/ci/test/auth-callbacks.test.mjs",
      "case": "FAILS when enum AuthFlow gains a marker the allow list does not admit",
      "proves": "The case adds magicLink('magic-link') to enum AuthFlow in a fixture copy and requires exit 1 naming the missing `?nk_auth=magic-link` allow-list entry; it passes only while the allow-list limb reads its markers off the enum. Mutation 2026-09-23: the enum read at assert-auth-callbacks.mjs:238 replaced with a typed list of the five markers turns it red; restored byte-exact (sha256 0534ce7cefa16d55), green again. Added with the native-auth-callback lane (sign-in audit PR A)."
    }
  ],
  "backlog": {}
}
