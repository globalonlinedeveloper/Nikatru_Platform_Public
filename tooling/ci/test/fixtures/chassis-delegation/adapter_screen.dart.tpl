// The ADAPTER chassis step 4 leaves behind: same path, same route, same class
// name, and none of the body. Every property a guard used to read here is now
// one import away.
import 'package:flutter/material.dart';
import '__IMPORT__';

class __CLASS__ extends StatelessWidget {
  const __CLASS__({super.key});

  @override
  Widget build(BuildContext context) => const __BODY__();
}
