// The MUTATION: the same widget with the property removed. Nothing about the
// adapter changes, which is exactly why a guard that reads only the adapter
// cannot tell this tree from the one beside it.
import 'package:flutter/material.dart';

class __CLASS__ extends StatelessWidget {
  const __CLASS__({super.key, this.caps});

  final dynamic caps;

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
