{
  "_readme": [
    "THE DEFINITION-OF-DONE REGISTER — [pipeline N-1] Private/requirements/ (was pipeline/06-app-build.md, folded into the JSON spec 2026-08-15)",
    "",
    "The machine-readable half of the factory's Definition of Done: one row per DoD item, each naming",
    "HOW it is enforced. The prose one-pager lives at Private/requirements/definition-of-done.md and",
    "POINTS AT THIS FILE rather than restating it — the items live in one place, or they drift in two.",
    "",
    "WHY THE SPLIT, AND WHY THIS HALF IS PUBLIC.",
    "The stage doc's acceptance for N-1 asks CI to parse Private/MASTER_PLAN.md §4 and the DoD page.",
    "Both live under Private/, which is GITIGNORED — CI cannot read either, so as written the",
    "requirement was unenforceable from the only place a build can fail. Exactly the class of blocker",
    "stage 8 hit, and its resolution there was to give the machine-checkable half an in-tree home",
    "(tooling/legal/); this mirrors that reasoning. So:",
    "",
    "  · tooling/dod-register.json (HERE, public)      — what a guard can check, and does:",
    "                                                    tooling/ci/assert-app-dod.mjs.",
    "  · Private/requirements/definition-of-done.md    — the one-page prose version, private.",
    "  · tooling/scripts/check-dod-sync.mjs            — a LOCAL check, the only place with read",
    "                                                    access to BOTH trees. It asserts this",
    "                                                    register's item ids are set-equal to",
    "                                                    MASTER_PLAN §4's lettered items and to the",
    "                                                    prose page's, and that no dated cut has crept",
    "                                                    back onto the page. What CI cannot see, it",
    "                                                    PRINTS rather than silently omitting.",
    "",
    "FIELDS.",
    "  id          the MASTER_PLAN §4 letter. The set of ids here must equal the set of letters there;",
    "              adding a letter to §4 without adding a row here fails check-dod-sync.mjs.",
    "  title       short name, for messages.",
    "  enforcedBy  'guard' | 'lane' | 'human'. There is no fourth value and no blank.",
    "  check       guard  -> the filename in tooling/ci/ that enforces it. assert-app-dod.mjs resolves",
    "                        it to a `run:` step on a NON-COMMENT line, in a workflow whose `on:`",
    "                        includes push, whose job is in ci-gate's `needs:`. Presence of the file is",
    "                        NOT sufficient — a guard nothing runs cannot fail a build.",
    "              lane   -> the ci.yml job name. Same last clause: it must be in ci-gate's `needs:`.",
    "              human  -> the humanReview row id in every app's dod.json that carries the verdict.",
    "  humanHalf   optional. Names the humanReview row that carries the JUDGMENT half of an item whose",
    "              mechanical half is guarded. It must name a real required humanReview row, so an item",
    "              cannot point its human half at nothing.",
    "  why         the sentence that justifies the enforcement choice.",
    "",
    "THE CUT LIST IS PART OF THE CONTRACT, NOT AN APPENDIX. Every entry is a DoD sub-item that was",
    "deliberately priced out, with the date and the decision that cut it. check-dod-sync.mjs fails if a",
    "cut phrase reappears in the prose page's ITEMS section — a cut that quietly returns is how a",
    "one-pager becomes a four-pager again, which is what rotted the first time.",
    "",
    "`reclaimed` — A THIRD LIST, ADDED 2026-08-13, AND IT EXISTS BECAUSE A CUT IS NOT THE ONLY WAY OUT.",
    "A criterion that the tree ALREADY SATISFIES belongs in neither list: cutting it publishes a false",
    "admission, and requiring it invents debt that is already paid. Each row names the SC, its WCAG LEVEL,",
    "the file:line that satisfies it TODAY, and the cheap guard that would keep it satisfied. The guards",
    "are `⬜ NOT BUILT` and say so on every row — a named guard is not an enforced guard, and this register",
    "is the last place that should blur the two.",
    "",
    "🔴 WHY THE LEVEL FIELD IS NOT DECORATION. WCAG 2.2 §5.2.1: for Level AA conformance the page must",
    "satisfy ALL Level A and Level AA success criteria. Two of the criteria cut on 2026-08-13 (SC 3.2.6",
    "and SC 3.3.7) are LEVEL A. Cutting a Level A criterion while an app's store listing publishes",
    "'Accessible — WCAG 2.2 AA' makes that claim false by construction, and MASTER_PLAN.md §4-F's",
    "'Why a criterion in neither list is the bug' paragraph (:222 as measured 2026-08-13 — anchored by its",
    "TEXT because the line moves, which it already did once today) routes that claim through [8]K-3: every",
    "published legal claim is asserted true of the code. The level is what makes a cut checkable against",
    "the claim instead of against taste.",
    "",
    "`aaaAdopted` / `aaaNotAdopted` / `aaaAwaitingOwner` — A FOURTH, FIFTH AND SIXTH LIST, ADDED 2026-08-13.",
    "The owner's decision that day: Level AA everywhere, PLUS a NAMED SET of AAA criteria that are free or",
    "chassis-cheap. FULL AAA IS REJECTED, on W3C's own advice — WCAG 2.2 §5.2.1 Note 2, verbatim: \"It is not",
    "recommended that Level AAA conformance be required as a general policy for entire sites because it is",
    "not possible to satisfy all Level AAA success criteria for some content.\"",
    "",
    "🔴 WHY THREE LISTS AND NOT TWO, AND WHY THE THIRD IS THE IMPORTANT ONE. The whole §4-F thread exists",
    "because SEVEN criteria sat in neither the required nor the cut list and nobody noticed. Applying that",
    "same rule to the AAA set is what these lists are for: WCAG 2.2 has 31 AAA success criteria, and every",
    "one of them is now in exactly one list. 20 adopted · 10 not adopted · 1 (SC 2.2.4 Interruptions) that",
    "the owner's brief did not name at all, parked in `aaaAwaitingOwner` rather than silently sorted by an",
    "agent. Filing an unnamed criterion into a list the owner never chose is the SAME defect as the",
    "2026-08-13 over-cut, in the same direction: a classification nobody made, read later as one somebody did.",
    "",
    "🔴 THE COUNT IN THE BRIEF WAS 18 AND THE ENUMERATION IS 20 — measured by listing them, 2026-08-13.",
    "4 already-satisfied + 5 satisfied-by-absence + 10 chassis-build + 1 with-carve-out = 20. The brief's own",
    "count contradicted its own list, which is the exact shape CLAUDE.md records for the 5-of-6/4-of-6",
    "platform line. The enumeration governs; 20 is the number, and 20 + 10 + 1 = 31 closes the AAA set.",
    "",
    "⚠️ THE CLAIM DOES NOT CHANGE, AND `claimWording` BELOW IS WHY. There is no \"AA+\" and no \"partial AAA\":",
    "§5.2.1 is all-or-nothing per level. The published claim stays LEVEL AA, with this adopted list attached",
    "as §5.3.2's OPTIONAL component. Never reach for §5.4 Statement of Partial Conformance — that clause is",
    "for uncontrolled third-party content, and misusing it misstates a defined term of the standard.",
    "",
    "⬜ ADOPTED IS NOT MET. Rows carry `state` and, where it is not met, `unmetDebt: true`. Contrast and",
    "reduce-motion are MEASURED UNBUILT and stay ⬜ debt exactly as the 2026-08-13 adoption left them;",
    "[2]C-14's rung is NOT raised by any row here. A guard named in `guard` with `guardStatus` ⬜ NOT BUILT",
    "is a specification, never coverage — the same warning the `reclaimed` rows carry, for the same reason.",
    "",
    "",
    "`insideTheClaim` — A SEVENTH LIST, ADDED 2026-08-21, BECAUSE THE §4-F DEFECT HAPPENED AGAIN ONE LEVEL LOWER",
    "THAN ANYBODY WAS LOOKING. The six lists above cover the AAA set, the cuts and the reclaims. NOTHING covered a",
    "Level A or AA criterion that is simply INSIDE the published claim, has been MEASURED, and is FAILING — and",
    "SC 2.1.1 Keyboard (Level A) sat in NO list at all while its own strict superset, SC 2.1.3 Keyboard (No Exception)",
    "(Level AAA), sat in `aaaAdopted` carrying a named guard. That is backwards by the standard's own arithmetic:",
    "§5.2.1 makes 2.1.1 MANDATORY for the published AA claim and leaves 2.1.3 optional decoration on top of it.",
    "The register tracked the optional one and not the mandatory one.",
    "",
    "🔴 WHY IT IS NOT FILED IN AN EXISTING LIST. `reclaimed` is for criteria the tree ALREADY SATISFIES — this",
    "one demonstrably does not. `cuts` is for work deliberately priced out — nothing was priced out here; nobody",
    "looked. `aaaAdopted` is for criteria BEYOND the claimed level — 2.1.1 is inside it. A row that fits none of the",
    "six and is filed into one of them anyway is the same defect as the 2026-08-13 over-cut: a classification nobody",
    "made, read later as one somebody did.",
    "",
    "⚠️ A ROW HERE IS THE MOST EXPENSIVE SHAPE IN THIS FILE. A cut is an admission the owner signed; an unmet AAA",
    "adoption is optional debt nobody promised. A MEASURED LEVEL A FAILURE is neither — it is [8]K-3 exposure against",
    "a claim that is published today. Rows carry `state` and `unmetDebt` like the AAA rows, and additionally name the",
    "FILE that measured them in `measuredBy`, so the row and its measurement cannot drift apart in silence.",
    "",
    "⚠️ check-dod-sync.mjs does not read this list either, for the same reason it does not read the other three.",
    "",
    "Enforced by tooling/ci/assert-app-dod.mjs (public half) + tooling/scripts/check-dod-sync.mjs (local).",
    "NOTE: check-dod-sync.mjs does NOT read these three lists — it reads `items` and `cuts` only. That is",
    "stated rather than discovered: these rows are prose on both sides today, exactly as `reclaimed` is."
  ],
  "source": "Private/MASTER_PLAN.md §4 — 'Per-app production checklist = reusable Definition of Done', items A-H",
  "humanReviewRows": [
    "four-states",
    "depth-beyond-viewing",
    "first-value-and-paywall",
    "polish"
  ],
  "items": [
    {
      "id": "A",
      "title": "Complete screen set",
      "enforcedBy": "guard",
      "check": "assert-screen-set.mjs",
      "why": "the non-app-specific screens ship in the chassis, so the register of 25 screens plus its anchors (22 when this was written; MEASURED 2026-08-25 from `tooling/screen-register.json` -> 25 declared, 24 `present`, 1 `blocked`) is what makes 'complete' a query rather than a memory [C-13]"
    },
    {
      "id": "B",
      "title": "Four distinguishable states on every data screen",
      "enforcedBy": "human",
      "check": "four-states",
      "why": "39-CHASSIS §6 downgraded widget-per-state from CI: seven screens x fifty apps of golden-backed state widgets is unaffordable. The judgment stays; the RECORDING of it is mechanical [N-8]"
    },
    {
      "id": "C",
      "title": "Adaptive — five Material window classes",
      "enforcedBy": "guard",
      "check": "assert-stamp-properties.mjs",
      "why": "the 'ui-invariants-inherited' property pins the five classes at their exact Material edges; the tree once covered three and 600-639 got the phone layout"
    },
    {
      "id": "D",
      "title": "Dark mode, persisted",
      "enforcedBy": "guard",
      "check": "assert-stamp-properties.mjs",
      "why": "'theme-triplet-supplied' plus 'theme-mode-persisted' anchor BOTH limbs — the write and the read-back — because a choice saved and never restored looks exactly like no persistence at all"
    },
    {
      "id": "E",
      "title": "Localized — zero hardcoded strings",
      "enforcedBy": "guard",
      "check": "assert-no-hardcoded-strings.mjs",
      "why": "l10n retrofitted across fifty shipped apps is the canonical near-free-at-birth item; the chassis also ships a second locale so the seam can actually be exercised"
    },
    {
      "id": "F",
      "title": "Accessible — the mechanical WCAG 2.2 limbs",
      "enforcedBy": "guard",
      "check": "assert-stamp-properties.mjs",
      "humanHalf": "polish",
      "why": "text-scale clamp, 48px targets and non-empty icon labels are asserted on the stamped app; the manual screen-reader pass is a recorded cut (no Apple device) and stays a11y debt, not a gate. 48px is the HOUSE standard, NOT the WCAG figure — WCAG 2.2 AA's floor is SC 2.5.8 at 24x24 CSS px and 48 is Material's; relabelled in MASTER_PLAN 4-F on 2026-08-13 so nobody reads '48px, WCAG' and concludes something false. The number itself is unchanged here and in chassis_properties_test.dart (greaterThanOrEqualTo(48.0)), because loosening a shipped, negative-tested invariant to 24 buys nothing. WIDENED 2026-08-13: 4.5:1 contrast (SC 1.4.3, AA) was adopted into [2]C-14 as an OWED acceptance, together with reduce-motion (SC 2.3.3, which is LEVEL AAA and is therefore a HOUSE EXTRA, not part of the AA claim) — neither is built; disableAnimations and contrastRatio both measure 0 occurrences in source. ~~the other five WCAG criteria DoD 4-F named are the five cuts dated 2026-08-13 below~~ REVERSED SAME DAY, 2026-08-13, and the retraction is the point: FOUR of those five were measured SATISFIED by the tree that already ships (SC 2.5.7, 3.2.6, 3.3.7, 3.3.8 — see `reclaimed`), and two of them are LEVEL A, so cutting them contradicted the published 'WCAG 2.2 AA' claim under WCAG §5.2.1. One cut survives as a cut and it was narrowed: SC 2.4.11's CI GATE is cut, the CRITERION stays owed AA debt in [2]C-14"
    },
    {
      "id": "G",
      "title": "Polished — the app's own brand icon and splash",
      "enforcedBy": "guard",
      "check": "assert-stamp-brand-assets.mjs",
      "humanHalf": "polish",
      "why": "the mechanical half is that the stamp generates the app's OWN icons rather than shipping Flutter's stock ones — measured against the live SDK's assets, never pinned hashes. Motion tokens, skeletons-not-spinners and friendly empty/error states are judgment and live in the humanHalf row"
    },
    {
      "id": "H",
      "title": "Proven — the app analyzes clean and its own suite passes",
      "enforcedBy": "lane",
      "check": "app_brick",
      "why": "the lane stamps a fresh app, resolves it, analyzes it and runs its tests on every push. The per-feature effect proof is assert-app-dod.mjs's `features` block; the golden matrix, the >=80% number and E2E-per-environment are all recorded cuts below"
    }
  ],
  "cuts": [
    {
      "id": "golden-matrix",
      "what": "Alchemist golden-per-breakpoint x {compact,medium,expanded,large,XL} x {light,dark}",
      "decided": "2026-07-25",
      "by": "39-CHASSIS §4 cut 2 / §6",
      "pageMustNotContain": "Alchemist",
      "why": "every intentional design change becomes N image approvals x 50 apps"
    },
    {
      "id": "coverage-percentage",
      "what": ">=80% unit coverage on core + logic",
      "decided": "2026-07-25",
      "by": "39-CHASSIS §6",
      "pageMustNotContain": "80%",
      "why": "a percentage of any tests is gameable; N-5 demands DISCRIMINATING tests instead — a named effect anchor and a dated mutation record"
    },
    {
      "id": "manual-screen-reader",
      "what": "a manual screen-reader pass per platform",
      "decided": "2026-07-25",
      "by": "39-CHASSIS §6 / MASTER_PLAN §4-F",
      "pageMustNotContain": "screen-reader pass",
      "why": "no Apple device; logged as a11y debt rather than carried as a gate that can never go green"
    },
    {
      "id": "widget-per-state-as-ci",
      "what": "widget-per-state as a CI assertion",
      "decided": "2026-07-25",
      "by": "39-CHASSIS §6",
      "pageMustNotContain": "widget-per-state",
      "why": "downgraded to item B's human verdict; not re-mechanised here"
    },
    {
      "id": "e2e-per-environment",
      "what": "integration E2E per environment (six)",
      "decided": "2026-07-29",
      "by": "Private/requirements/ [6]N-6 — was pipeline/06-app-build.md N-6, folded into the JSON spec 2026-08-15",
      "pageMustNotContain": "per environment",
      "why": "MASTER_PLAN §4-H wins on intent, N-6 on cost: ONE web E2E is kept. The cost is stated rather than hidden — five of six platforms have no end-to-end proof under any requirement in any of the fourteen stages, and the purchase->unlock leg cannot be reversed on iOS/Android at all under store billing policy"
    },
    {
      "id": "focus-not-obscured",
      "what": "the focus-not-obscured CI GATE (SC 2.4.11) — the GATE only; the criterion stays owed",
      "level": "AA",
      "decided": "2026-08-13",
      "by": "owner 2026-08-13, on 00-COMPLETENESS-AUDIT.md:445-454; NARROWED same day on the W3C research",
      "pageMustNotContain": "focus-not-obscured",
      "cutIsTheGateNotTheCriterion": true,
      "why": "PER-SCREEN. Whether the focused control is hidden behind a sticky header, a bottom sheet or a snackbar is a property of each screen's own overlay stack, so no shared-theme value can establish it and no stamp-time probe can falsify it over a chassis with two screens. Retrofit cost is ordinary, which is the half of C-14's test it fails. ⚠️ NARROWED 2026-08-13, and the narrowing is what the other four reclaims were tested against: what is cut is the CI GATE, never the CRITERION. SC 2.4.11 is LEVEL AA, so under WCAG §5.2.1 it is inside every 'Accessible — WCAG 2.2 AA' claim an app publishes; it is carried as UNMET AA DEBT in [2]C-14 beside contrast, not as an absence. Flutter does NOT give it for free — neither focus-traversal auto-scroll nor Scaffold's keyboard-inset handling stops a SnackBar, a persistent bottom sheet or a sticky header from covering a focused control, so 'the framework handles it' is not available as a reason. ~~its four siblings were cut on this same shape~~ that generalisation was wrong: the four were measured against the tree and found SATISFIED (see `reclaimed`). This entry stays a cut because nothing in the tree satisfies it and nothing in the tree can falsify it"
    }
  ],
  "reclaimed": [
    {
      "id": "dragging-alternative",
      "sc": "2.5.7",
      "level": "AA",
      "title": "Dragging Movements",
      "state": "SATISFIED",
      "decided": "2026-08-13",
      "by": "owner 2026-08-13 — reverses the same-day cut on the W3C primary research",
      "premiseThatWasFalse": "'PER-SCREEN, not chassis: … the chassis ships none [no drag gesture]' — FACTUALLY FALSE, and false about the CHASSIS itself, not merely about the frozen legacy app",
      "evidence": "tooling/bricks/app/__brick__/apps/{{app_id}}/lib/features/firstrun/onboarding_screen.dart:110 is a PageView.builder — a horizontal drag surface, shipped by the brick, inherited by every stamped app. Mirrored in the frozen legacy app at apps/subly/lib/features/onboarding/onboarding_screen.dart:158, whose own comment at :169 says 'PageView pans on the horizontal axis'. It CONFORMS anyway, and that is why this is a reclaim rather than a defect: SC 2.5.7 asks for a single-pointer alternative that is not path-based, and the brick ships two on the same screen — the Next FilledButton at :178-181 calls _pages.nextPage(), and the Skip TextButton at :105 calls _finish(). Legacy app equivalents: Next at :379 -> _next() -> _controller.nextPage() (:73-75), Skip at :351 -> _finish()",
      "guard": "the carousel must retain a non-drag advance: for every file under __brick__ that constructs a PageView, assert the same file also constructs a control whose onPressed reaches nextPage( or the finish handler. Cheap because the domain is non-empty and stays non-empty — the guard fails the day somebody deletes the button and leaves the swipe",
      "guardStatus": "⬜ NOT BUILT — named, not enforced. Do not read this row as coverage",
      "why": "the cut priced a retrofit that does not need pricing: the alternative is already there. Leaving it cut would have published an admission of a gap the tree does not have"
    },
    {
      "id": "consistent-help",
      "sc": "3.2.6",
      "level": "A",
      "title": "Consistent Help",
      "state": "SATISFIED (vacuously — the trigger condition is absent)",
      "decided": "2026-08-13",
      "by": "owner 2026-08-13 — reverses the same-day cut on the W3C primary research",
      "premiseThatWasFalse": "'it presumes a help channel the factory does not define' — the factory does define one; what it does not do is REPEAT it, and repetition is the criterion's own trigger",
      "evidence": "SC 3.2.6 binds only where a help mechanism is repeated on multiple pages in a set — where it appears once, there is no relative order to be consistent about. Measured: the brick's help channel is AppConfig.supportEmail (tooling/bricks/app/__brick__/apps/{{app_id}}/lib/core/app_config.dart:40), surfaced on the Settings screen ONLY (settings_screen.dart:351 row, :500 mailto). Zero other stamped screens carry a help affordance, so the criterion's antecedent is unsatisfied and the criterion is satisfied. Legacy app matches the shape: helpAndSupport + AppConfig.contactUrl at apps/subly/lib/features/settings/settings_screen.dart:604,606 and the mailto at :907, Settings only",
      "w3cBasis": "WCAG 2.2 Conformance: \"if there is no content to which a success criterion applies, the success criterion is satisfied\" (W3C, WCAG 2.2)",
      "guard": "a help affordance appearing on more than one screen must keep its relative order: count the stamped screens that reference supportEmail/contactUrl; while the count is 1 the guard prints the count and passes, and the moment it exceeds 1 it asserts the affordance sits at the same relative position in each. A count printed on every run is what stops this becoming an assertion over an empty set that nobody notices went empty",
      "guardStatus": "⬜ NOT BUILT — named, not enforced. Do not read this row as coverage",
      "why": "LEVEL A. Cutting it while publishing 'Accessible — WCAG 2.2 AA' made the published claim false by construction under WCAG §5.2.1, and it was cut for a cost that does not exist — the criterion is already met"
    },
    {
      "id": "redundant-entry-autofill",
      "sc": "3.3.7",
      "level": "A",
      "title": "Redundant Entry",
      "state": "SATISFIED (vacuously — no process re-asks information, and the one re-entry is an explicit exception)",
      "decided": "2026-08-13",
      "by": "owner 2026-08-13 — reverses the same-day cut on the W3C primary research",
      "premiseThatWasFalse": "the premise ('the chassis ships no such flow') was CORRECT — the error was the conclusion drawn from it. A criterion whose trigger is absent is SATISFIED, not cut; cutting it records a gap where the standard records conformance",
      "evidence": "SC 3.3.7 binds only where a process asks for information already entered in the same process. Measured across the brick's auth flow: sign_up_screen.dart:142,149 (email, newPassword), sign_in_screen.dart:89,96 (email, password), reset_password_screen.dart:272,280 — no step re-asks an earlier step's answer. The single re-entry is reset-password's confirm field (:280 beside :272), which is WCAG's own named 'essential' exception to 3.3.7 (confirming a new password), and BOTH fields carry AutofillHints.newPassword so a password manager can populate them without the user retyping anything. Legacy app identical: apps/subly/lib/features/auth/sign_up_screen.dart:145,152 and reset_password_screen.dart:275,283",
      "guard": "no flow may re-ask information already given: within one route's widget tree, assert no two text fields declare the SAME autofillHints value unless the pair is on the register's named confirm-field allowlist (today: reset-password new/confirm). Cheap, and it fails on exactly the shape the criterion forbids",
      "guardStatus": "⬜ NOT BUILT — named, not enforced. Do not read this row as coverage",
      "why": "LEVEL A, same §5.2.1 argument as SC 3.2.6. And the autofillHints that satisfy it already ship, so the cut priced work that was already done"
    },
    {
      "id": "accessible-auth-paste-otp",
      "sc": "3.3.8",
      "level": "AA",
      "title": "Accessible Authentication (Minimum)",
      "state": "SATISFIED",
      "decided": "2026-08-13",
      "by": "owner 2026-08-13 — reverses the same-day cut on the W3C primary research",
      "premiseThatWasFalse": "'The chassis ships no OTP screen … so there is nothing to assert on today' — it inverted the polarity of the criterion. PASTING an OTP is what SATISFIES 3.3.8; BLOCKING paste is what violates it. Absence of an OTP screen is therefore conformance, not an unassertable void",
      "evidence": "Every chassis auth field is a plain TextField whose paste path is untouched — measured, source only: enableInteractiveSelection: false -> 0 occurrences, oneTimeCode -> 0, maxLength: 1 -> 0, across tooling/bricks, apps/subly/lib and packages/*/lib. Password entry is the cognitive-function test in play, and the chassis meets 3.3.8's Mechanism exception directly: AutofillHints.password / .newPassword / .email at sign_in_screen.dart:89,96 · sign_up_screen.dart:142,149 · reset_password_screen.dart:272,280, so a password manager fills the field and nothing suppresses the paste menu",
      "residualRisk": "the register's original hazard was correctly identified and is KEPT as a hazard rather than as a cut: a segmented one-box-per-digit OTP field is the common way 3.3.8 is broken, and [2]C-13's auth screens are stage-5 work. That is a FUTURE RISK, not a PRESENT FAILURE, and the difference is the whole reason this row is a reclaim",
      "guard": "no OTP field may disable paste: assert enableInteractiveSelection: false, contextMenuBuilder returning an empty widget, and maxLength: 1 all stay at ZERO occurrences in auth-feature source. The domain is empty today and the guard must PRINT that count every run, so the day an OTP screen lands the count moves and the guard starts biting instead of silently continuing to pass",
      "guardStatus": "⬜ NOT BUILT — named, not enforced. Do not read this row as coverage",
      "why": "cutting it recorded a failure the tree does not have, and would have carried that admission under the published AA claim. The thing worth watching is the future OTP screen, and a hazard is what this row records"
    }
  ],
  "insideTheClaim": [
    {
      "id": "keyboard",
      "sc": "2.1.1",
      "level": "A",
      "title": "Keyboard",
      "basis": "required by the published claim — WCAG 2.2 §5.2.1: a Level AA claim requires EVERY Level A success criterion",
      "state": "🟨 FAILING WHERE IT WAS MEASURED, FIXED WHERE IT WAS MEASURED, UNKNOWN ELSEWHERE — 2026-08-21: 3 screens of apps/subly swept, all three failed. 2026-08-25: the same 3 re-swept after the shared primitive landed, all three now reachable end to end. 14 of the app's 17 screen-bearing routes have never been swept, so this is NOT 🟩 and must not be read as one. The dated fields below are the 2026-08-21 measurement and are left exactly as written; `reMeasured2026-08-25` is the new one.",
      "unmetDebt": true,
      "🔴 whyThereWasNoRow": "the register carried SC 2.1.3 Keyboard (No Exception), Level AAA, in `aaaAdopted` with a named guard — and NOTHING for SC 2.1.1. 2.1.3 is the strict SUPERSET: it deletes 2.1.1’s path-dependent-input exception, so 2.1.3 met implies 2.1.1 met, always, and the converse never holds. Tracking only the superset let ALL of the factory’s keyboard debt read as optional beyond-AA decoration while the mandatory limb went unnamed. Same shape as the `focus-not-obscured-enhanced` row’s `consequenceForTheCutList` note — that pairing is spelled out there, and this one is spelled out here, because a superset row standing alone is how the base criterion disappears",
      "measuredBy": "apps/subly/test/keyboard_traversal_test.dart, 2026-08-21 — real Tab / Shift-Tab / Enter key events dispatched through the app’s own WidgetsApp shortcut bindings, at a 1079x2400 surface. 1079 is the body width AppScaffold hands a branch on a maximised 1440px desktop (measured 2026-08-21); 2400 is tall enough that none of the three screens scrolls, which the test ASSERTS (maxScrollExtent == 0) rather than assumes — a culled control is invisible to a sweep, so a short viewport under-reports and never says so",
      "evidence": "Controls reachable by Tab, out of controls present: LOGIN 4 of 8 · SETTINGS 9 of 27 · HOME 17 of 20. 55 controls, 30 reachable, 25 KEYBOARD-DEAD. 🔴 THE FAILURE IS REACHABILITY AND NOTHING ELSE, and the same sweep establishes that by ruling the neighbours out: every traversal orbit closes back onto its own first element (no trap, SC 2.1.2), Shift-Tab retraces it exactly (no one-way trap), consecutive focus rects run in reading order (SC 2.4.3), and Enter on login’s focused sign-in button reaches _submit and answers (activation, not merely focus). What fails is that 25 controls exist which no Tab press can land on. Every one of them is the app’s hand-rolled `Semantics(button: true)` + `GestureDetector` pair: Semantics gives a screen reader a ROLE and gives a keyboard NOTHING, because it creates no FocusNode. The nine settings controls that DO traverse are the nine built on a Material InkWell or ListTile — that screen is keyboard-operable exactly where it used Material and nowhere it did not",
      "worstSingleInstance": "login’s sign-up toggle. apps/subly/lib/features/auth/login_screen.dart:562 records it in its own comment as ‘the ONLY control that reaches registration from the screen every signed-out visitor is routed to’ (and :553 as ‘the only way to reach sign-up’) — so on the measured tree a keyboard-only user cannot create an account. Runner-up: home’s three dead controls are its app-bar actions (notifications, settings, calendar), i.e. every route OFF a screen whose rows all traverse fine",
      "🔴 consequenceForThePublishedClaim": "`claimWording.claimText` is ‘Accessible — WCAG 2.2 Level AA’, and MASTER_PLAN.md §4-F routes that through [8]K-3: every published legal claim is asserted true of the code. On the measurement above it is not true of apps/subly today. This row does NOT by itself settle the claim — 3 screens is not the whole app, and the rest are UNMEASURED, which is the honest reading and is stated so nobody rounds it in either direction: failing where measured, unknown elsewhere. 📏 NUMBER CORRECTED 2026-08-25, SUBSTANCE UNTOUCHED: this sentence read '3 screens is not 22, and the other 19 are UNMEASURED'. 22 is the CHASSIS screen register's 2026-08-06 `present` count, not apps/subly's route count, so '19 unmeasured' was arithmetic across two different sets. MEASURED 2026-08-25: `grep -c 'GoRoute(' apps/subly/lib/core/router.dart` -> 19 routes, 2 redirect-only, so 17 screen-bearing and 14 UNMEASURED — see `guardStatus` below for both denominators and their commands. The error ran in the direction that overstates the gap here (19 vs 14) and understates it in `guardStatus` (22 vs the register's 25 today); the reading — failing where measured, unknown elsewhere — is unchanged either way. Whether the listing wording moves before the controls do is the OWNER’s call; it is recorded here as owed, not taken by an agent",
      "🟢 reMeasured2026-08-25": "SAME RIG, SAME 1079x2400 SURFACE, SAME THREE SCREENS, AFTER THE FIX. Controls reachable by Tab, out of controls present: LOGIN 8 of 8 (was 4) · SETTINGS 25 of 27 (was 9) · HOME 20 of 20 (was 17). 55 controls, 53 reachable by Tab. THE FIX IS THE ONE THIS ROW'S `why` PREDICTED AND IT WAS DONE ONCE: `packages/design_system/lib/src/widgets/focusable_tap.dart` — `FocusableTap`, a `FocusableActionDetector` carrying the call site's own `Semantics` annotation unchanged, `ActivateIntent` AND `ButtonActivateIntent` bound to the same callback, and a focus ring painted as a FOREGROUND decoration so wrapping a control cannot move a pixel of the 48px tap targets `a11y_semantics_test` pins. Every hand-rolled `Semantics` + `GestureDetector` pair on the three screens was rebuilt on it: login's sign-up toggle, the three `PoweredByNikatru` legal links (which is ONE widget counted on TWO screens), settings' four currency chips, four `_Toggle` switches, five wired `_LinkRow`s and the profile card, and home's notifications button, account avatar and calendar link. `onTap == null` still drops the role AND the focus node together, so the two inert `_LinkRow`s stay unfocusable by the same condition that keeps them unannounced. 🔴 AND ONE THING IN THE 2026-08-21 `evidence` ABOVE IS MEASURED FALSE, CORRECTED HERE RATHER THAN EDITED THERE: \"every one of the eighteen it does NOT visit is a hand-rolled `_Toggle`, `_LinkRow` or currency chip\" is untrue of two of them, and so is \"that screen is keyboard-operable exactly where it used Material and nowhere it did not\". The `en` and `ta` rows are Material `RadioListTile`s inside a `RadioGroup`, which is ONE Tab stop by design and moves within itself on the arrow keys — the behaviour a radio group is supposed to have. MEASURED 2026-08-25: ArrowDown from the group's Tab stop lands focus on the `en` tile. 🟢 COVERAGE GREW LATER THE SAME DAY, AND THE SENTENCE BEFORE THIS ONE IS LEFT EXACTLY AS WRITTEN BECAUSE IT IS STILL TRUE — it was one step SHORT of the claim it was supporting, not wrong. The test that carried it re-focused its start node before every ArrowDown and so never pressed a second one, which left the `ta` tile's operability asserted NOWHERE while the prose said the group 'moves on the arrow keys'. RE-MEASURED 2026-08-25 with the walk widened to the group's whole orbit: exactly ONE of settings' 25 Tab-orbit nodes sits inside the `RadioGroup`, and three ArrowDowns from it walk `System` -> `English` -> `தமிழ்` -> `System`, so BOTH skipped tiles are now evidenced and the group is shown to CYCLE rather than merely to move once. The finding did not move; the evidence caught up with it. SC 2.1.1 asks whether a KEYBOARD can operate a control, not whether TAB can land on it, so a Tab-only sweep scores a conformant radio group as two failures. The honest 2026-08-21 total was 23 keyboard-dead, not 25 — the shape of the finding is unchanged and the number is not. Those two are still counted OUTSIDE the Tab pins (25 of 27, not 27 of 27) and their operability is asserted by its own case with its own exact count, so the correction cannot act as a loosened matcher. ⚠️ WHAT IS STILL OWED, STATED SO NOBODY ROUNDS IT UP: 14 of the 17 screen-bearing routes `apps/subly/lib/core/router.dart` declares are UNMEASURED; the focus ring `FocusableTap` introduces has NO measured contrast ratio against any of the grounds it lands on, and SC 2.4.7 Focus Visible / SC 2.4.11 Focus Not Obscured (Minimum) carry no row that would catch that (2.4.11 is in `cuts`); and `claimWording.claimText` has NOT moved — see `exceptionOutstanding2026-08-25` there. This row stays `unmetDebt: true` for those reasons and not because the three screens still fail.",
      "🟢 reMeasured2026-08-26": "THE SWEEP THIS ROW'S `guard` FIELD ASKED FOR NOW EXISTS, AND THE TWO SENTENCES ABOVE ARE OVERTAKEN IN BOTH DIRECTIONS. They are left exactly as written; this supersedes them. (1) \"14 of the 17 screen-bearing routes are UNMEASURED\" — 13 of those 14 are now swept by apps/subly/test/a11y/keyboard_sweep_test.dart. The 14th, /onboarding, is EXCLUDED BY NAME with a live reason, and the sweep FAILS if that route stops existing, so the exclusion cannot outlive its subject. The route set is derived from router.dart, never listed. (2) \"the focus ring has NO measured contrast ratio against any of the grounds it lands on\" — the `focus-visible` row below now carries four measured ratios; the closest clears SC 1.4.11's 3:1 by more than 2x. ⚠️ ONE GROUND IS STILL UNMEASURED and is named rather than glossed: the white ring on /sub/:id's AppColors.heroGradient hero has NO ratio, because focus_ring_contrast_test measures the scaffold and the card only. 🔴 AND SIX GENUINELY INOPERABLE FUNCTIONS WERE FIXED, NOT JUST MEASURED, 2026-08-26. Across the 13 swept routes, controls reachable by Tab went 28 -> 34 of 37 and dead went 9 -> 3. Per route: /sign-up 5 -> 7 of 9 (dead 4 -> 2); /reaccept-terms 2 -> 4 of 5 (dead 3 -> 1); /sub/:id 2 -> 4 of 4 (dead 2 -> 0). NO CONTROL COUNT MOVED ON ANY ROUTE — that is the shape of an inoperable-control fix rather than a deletion. The fix is the one this row's `why` predicted and it was done once more: both _LegalLink (apps/subly/lib/features/auth/legal_consent_fields.dart) and _iconButton (apps/subly/lib/features/detail/subscription_detail_screen.dart) rebuilt on FocusableTap. 🔴 THE LEVEL A DEFECT THAT CLOSED IS WORTH NAMING: until this change A KEYBOARD USER COULD NOT OPEN THE TERMS OR PRIVACY DOCUMENT THEY WERE BEING ASKED TO AGREE TO. It had never been named in this register. ⚠️ THE REMAINING 3 ARE NOT DEFECTS AND MUST NOT BE 'FIXED'. They are duplicate hit targets — consent SENTENCES that fire the tap their adjacent Checkbox already fires, which legal_consent_fields.dart documents as \"a second hit target for the box, NOT a second control\". The sweep PINS that number at 3, so making one focusable — adding a Tab stop that activates something the user already reached — turns the sweep RED. 📌 AND A CAUTION ABOUT THIS ROW'S OWN HISTORY: an earlier draft of the sweep PUBLISHED TWO FALSE DEFECTS, claiming a keyboard user could not tick consent on /sign-up and that /reaccept-terms had one keyboard exit. Both were measured false and retracted before landing. A sweep that reports a working control as dead is worse than no sweep, and this row must never carry those two claims. Measured on Flutter 3.44.9: apps/subly 785 pass / 0 fail, test/a11y/ 82, a11y_semantics 113 including the 48x48 tap-target sweep — FocusableTap's foreground ring moved no pixel.",
      "guard": "the sweep that now exists, extended from 3 screens to every declared route and moved onto a fresh stamp of tooling/bricks/app — at which point it IS the guard `aaaAdopted` -> `keyboard-no-exception` specifies, and one build discharges both limbs. Two properties of the existing file must survive that move, because both are what stop it going quietly vacuous: the per-screen counts are pinned as EXACT numbers, so it goes red when a keyboard-dead control is FIXED as loudly as when one is added; and the completeness precondition (no screen scrolls, so nothing is culled) is asserted, not assumed",
      "guardStatus": "🟨 PARTLY BUILT — apps/subly/test/keyboard_traversal_test.dart, 18 cases as of 2026-08-25 (was 16), all green, plus 10 cases on the primitive itself in packages/design_system/test/focusable_tap_test.dart. The two properties this row demands both survived the rewrite: the per-screen counts are still EXACT — 8/8, 25/27, 20/20 — so the suite goes red when a keyboard-dead control is FIXED as loudly as when one is added, and `_everythingIsLaidOut` still asserts `maxScrollExtent == 0` rather than assuming it. NEGATIVE-TESTED THE SAME DAY, on the real tree, not a fixture: reverting `FocusableTap` to the pre-fix `Semantics` + `GestureDetector` shape turns 4 of the 18 app cases red and 4 of the 10 primitive cases red; unbinding `ButtonActivateIntent` turns exactly the web-shortcut-map case red and nothing else; `ExcludeFocus` round the language `RadioGroup` turns the ArrowDown case red; `ExcludeFocus` round the language `RadioGroup` (all three tiles) ALSO turns the settings 25-of-27 pin red, because the group stops contributing its Tab stop; `ExcludeFocus` round the `ta` tile ALONE — ADDED 2026-08-25 and the one that matters — turns the arrow-walk case red and leaves EVERY OTHER CASE IN THE FILE GREEN, including the 25-of-27 pin. That last mutation is the direction proof for widening the case: run against the SAME mutant, the predecessor assertion (`reached == 1`, one ArrowDown per orbit node) stays GREEN at 18 of 18 while the widened one fails on `தமிழ்` reached 0 times and `English` 3. Strictly stronger, nothing loosened. Both mutations were applied in a scratch mirror of the workspace, never to the checkout; `ExcludeFocus` round the currency chips turns the radio-tile limb red. 🔴 THE SCOPE HAS NOT MOVED: 3 screens, on the frozen rail-prover rather than on a stamp. ⚠️ OF WHAT, THOUGH — TWO DENOMINATORS, RECONCILED 2026-08-25 AND NAMED SEPARATELY BECAUSE THEY COUNT DIFFERENT THINGS. This sentence read '3 screens of the 22 the domain names' and the 22 contradicted the '17 screen-bearing routes / 14 unmeasured' in `reMeasured2026-08-25` two fields above, added by the same edit. Neither number was wrong about its own set and NEITHER WAS THE RIGHT DENOMINATOR FOR THIS ROW AS WRITTEN. MEASURED TODAY, both with the literal command that produces them: (a) `apps/subly/lib/core/router.dart` declares 19 `GoRoute`s — `grep -c 'GoRoute(' apps/subly/lib/core/router.dart` -> 19 — of which 2 are redirect-only (`/`, `/login`) and carry no builder, so 17 BUILD A SCREEN; 3 of those 17 are swept and 14 are not. That is the denominator for THIS row, because this row's evidence is a sweep of apps/subly. (b) `tooling/screen-register.json` declares 25 SCREENS — `node -e \"console.log(JSON.parse(require('fs').readFileSync('tooling/screen-register.json','utf8')).screens.length)\"` -> 25, of which 24 are `present` and 1 `blocked`. That is the CHASSIS domain, the one the 2.1.3 guard spec below means by 'every declared route', and it is a different set from apps/subly's routes. 🔴 AND THE 22 WAS STALE FOR ITS OWN SET TOO, IN THE DIRECTION THAT FLATTERS THIS ROW: 22 was the screen register's `present` count on 2026-08-06 (`tooling/ci/assert-screen-set.mjs` quotes its own old output `22 present and anchored`; its MIN_PRESENT floor now reads 24). The domain GREW 22 -> 25 declared / 24 present, so the old figure UNDERSTATED the work outstanding and OVERSTATED 3 screens' share of it. GREEN THERE MEANS “the three swept screens are keyboard-operable”, NEVER “the criterion is met”. Do not read this row as coverage",
      "why": "the measurement points at ONE fix rather than 25: every dead control is a tap handler that owns no FocusNode, and every live one got its FocusNode from a Material primitive it happened to be built on. So the repair belongs in packages/design_system — one focusable primitive the hand-rolled controls are rebuilt on — which is chassis-cheap in exactly the way `keyboard-no-exception` claimed 2.1.3 was, PROVIDED it is done once in the shared primitive and not 50 times in stamped apps. ⚠️ Stated as the direction the evidence points. It has not been built, costed, or checked against the tap-target and semantics properties a11y_semantics_test.dart already pins on those same controls"
    },
    {
      "id": "focus-visible",
      "sc": "2.4.7",
      "level": "AA",
      "title": "Focus Visible",
      "basis": "required by the published claim — WCAG 2.2 §5.2.1: a Level AA claim requires EVERY Level A AND Level AA success criterion. 2.4.7 is LEVEL AA, so unlike `keyboard` above it is inside `claimWording.claimText` at its own level rather than one below it",
      "state": "🟨 SATISFIED WHERE IT WAS MEASURED, UNKNOWN ELSEWHERE — 2026-08-26. The focus ring `packages/design_system/lib/src/widgets/focusable_tap.dart` paints is now measured against the two grounds apps/subly actually drops that primitive on — the live scaffold and the shared `cardDecoration` card — in BOTH brightnesses, and the CLOSEST of the four clears SC 1.4.11's 3:1 by more than 2x. 🔴 THAT IS NOT 🟩 FOR THE CRITERION AND MUST NOT BE READ AS ONE. Three things are outside the sweep: the SAME 14 of 17 screen-bearing routes `keyboard` above leaves unmeasured are unmeasured here; the brand-gradient ground that `FocusableTap`'s `focusColor:` override exists for has no number; and the sweep measures ONE primitive, so any control that is focusable WITHOUT being built on `FocusableTap` (a bare Material `InkWell` or `ListTile` — `keyboard` counts nine of them on settings alone) is scored by nothing here. `unmetDebt` stays true for those three reasons and NOT because anything measured came back red",
      "unmetDebt": true,
      "🔴 whyThereWasNoRow": "BOTH OF 2.4.7's NEIGHBOURS WERE FILED AND 2.4.7 WAS NOT. `aaaAdopted` -> `focus-appearance` carries SC 2.4.13 Focus Appearance (Level AAA) with a named guard; `cuts` -> `focus-not-obscured` carries SC 2.4.11 Focus Not Obscured (Level AA) as a dated gate cut. 2.4.13 is 2.4.7's STRICT SUPERSET — it demands a visible indicator AND a >=2px-perimeter area AND 3:1 between focused and unfocused, so 2.4.13 met implies 2.4.7 met and the converse never holds. That is the SAME SHAPE this list was created for, spelled out on the `keyboard` row: an AAA superset tracked with a named guard while the mandatory limb underneath it goes unnamed, which lets ALL of the factory's focus-indicator debt read as optional beyond-AA decoration. The gap was named in this file's own words before it was filled — `keyboard` -> `reMeasured2026-08-25` lists, under what is still owed, that `SC 2.4.7 Focus Visible / SC 2.4.11 Focus Not Obscured (Minimum) carry no row that would catch that`, and the test named in `measuredBy` closes its header by recording that a register row for 2.4.7 is OWED and is not that file's to write",
      "measuredBy": "apps/subly/test/a11y/focus_ring_contrast_test.dart, 2026-08-26 — 9 cases from 6 literals (4 `testWidgets` + 2 `test`, one `testWidgets` literal generating 4 cases from a 2-brightnesses x 2-grounds loop; 4+1+2+2 = 9, which is the figure the file's own header reports and this arithmetic closes on it). Focus is driven by a REAL `Tab` key event, never `node.requestFocus()` — the ring answers `onFocusChange`, and a ring that only appears for programmatic focus is a ring no keyboard user ever sees. Neither side of the comparison is a retyped hex: the ring colour is read off the focused control's own painted `BoxDecoration.border` after the press, and the ground is resolved from that control's own BuildContext (`Theme.of(context).scaffoldBackgroundColor`, `cardDecoration(context).color`), so the natural regression — somebody moving a theme token — reddens instead of passing with both sides moved together. The ring is asserted OPAQUE before any ratio is taken, because the helper goes through `Color.computeLuminance()`, which IGNORES ALPHA and would therefore score a translucent ring FLATTERINGLY",
      "evidence": "Ring vs ground, seed #6459F5, Flutter 3.44.9 — LIGHT ring #5B5891 on scaffold #FCF8FF -> 6.16:1 · LIGHT ring #5B5891 on card #FFFFFF -> 6.46:1 · DARK ring #C4C0FF on scaffold #131318 -> 10.87:1 · DARK ring #C4C0FF on card #35343A -> 7.24:1. Floor asserted: 3.0. 🟢 THE FINDING IS THAT THE RING WAS ALREADY CONFORMANT AND NOBODY HAD COMPUTED IT — the increment adds a measurement and changes no colour, which is worth stating plainly because the honest alternative was a red. ✅ CROSS-CHECK, and it is the cheapest available evidence that the rig resolves the SHIPPED theme rather than a test-only one: three of the four grounds are numbers this corpus already wrote down independently (#FCF8FF and #131318 are the light and dark scaffolds `AppColors.muted`'s doc measures against, #35343A is the dark `surfaceContainerHighest` the same doc names), and none of the three is typed in the test file. ✅ SECOND CROSS-CHECK, RUN FOR THIS ROW ON 2026-08-26 rather than taken on trust: all four ratios were re-derived from the four hex pairs above by an independent implementation of the WCAG formula, and all four reproduce to 2dp — 6.16, 6.46, 10.87, 7.24. That verifies the ARITHMETIC only; see `guardStatus` for what was NOT re-run",
      "🔴 whichFloorIsAssertedAndWhyItIsNot2.4.7sOwn": "STATED BECAUSE THE ROW AND ITS TEST WOULD OTHERWISE LOOK MISMATCHED. SC 2.4.7 requires a visible focus indicator and PUTS NO NUMBER ON IT; the criterion that fixes a ratio for a focus indicator at Level AA is SC 1.4.11 Non-text Contrast, at 3:1. So the floor the test asserts is 3.0 and it comes from 1.4.11, not from 2.4.7. That is already this register's settled house number — `aaaAdopted` -> `contrast-enhanced-text-only` -> `carveOut` reads `TEXT ONLY. Non-text contrast stays at SC 1.4.11's Level AA 3:1` — and asserting 1.4.3's 4.5 on a border would import a TEXT floor onto a non-text pair and hold the code to a stricter bar than the claim we publish. ⚠️ THE SPLIT LEAVES A LIMB THIS ROW DOES NOT YET COVER: 2.4.7's own demand is EXISTENCE of the indicator, and the test asserts that only incidentally — `_ringOn` fails with `Tab landed on the control and it painted no ring at all` when the border is absent, on the ONE primitive it mounts. Existence across the app is the `guard` below, and it is not built. 📌 SC 1.4.11 itself still has NO ROW in this register, at any level, and that is a separate gap from this one — recorded here so it is not mistaken for closed by the presence of this row",
      "guard": "two limbs, and they are not the same guard. (a) EXISTENCE, which is 2.4.7's actual text: every node the keyboard can focus paints a visible indicator, asserted over every declared route on a fresh stamp of tooling/bricks/app rather than over one primitive in one app — the same widening `keyboard` -> `guard` already owes, and the two should be built as one sweep because they walk the identical Tab orbit. (b) RATIO, which is 1.4.11's: the existing 3:1 assertion extended to every ground a focusable control is painted on, the brand gradient included, which needs a composite-aware rig because a ring over a gradient has no single ground colour and the current helper would refuse it at the opacity assertion. 📌 Both limbs want ONE shared contrast helper: the arithmetic is now duplicated in THREE test files (`focus_ring_contrast_test.dart`'s `contrastRatio`, `a11y_semantics_test.dart`'s `_ratio`, and an inline luminance compare in packages/design_system/test/app_text_test.dart) because the design system exports no contrast utility at all. `aaaAdopted` -> `contrast-enhanced-text-only` -> `guard` already says `the first increment is the computation itself`; it now exists three times privately, which is the moment to extract it and not later",
      "guardStatus": "🟨 PARTLY BUILT — apps/subly/test/a11y/focus_ring_contrast_test.dart. It is NOT decorative and the reasons are in-file rather than in a commit message: it carries a two-case FALSIFIER group that paints the ring with the ground's OWN colour and asserts the harness scores it 1.00 and BELOW the floor, plus a near-ground mutant (`surfaceContainerHighest`, the shape of a real regression rather than a degenerate identity) asserted red with the shipped ring asserted STRICTLY further; and it pins its own helper against the two ratios WCAG fixes by definition (21:1 black-on-white, 1:1 self) so a broken helper cannot hand every case above a passing number. The file records ONE whole-file mutation test on 2026-08-26 — the ring driven to `surfaceContainerHighest` for EVERY case, not just the falsifiers — reddening 5 cases across BOTH brightnesses and all four grounds (6.16 -> 1.23 · 6.46 -> 1.29 · 10.87 -> 1.50 · 7.24 -> 1.00), which is what distinguishes a rig that measures the RING from one that pins a ground. 🔴 SCOPE, STATED SO NOBODY READS THIS AS COVERAGE: ONE primitive, TWO grounds, TWO brightnesses, in apps/subly's own test lane — NOT a stamp, NOT a route sweep, and NOT an existence check over anything the primitive does not wrap. ⚠️ WHAT WAS NOT RE-RUN WHEN THIS ROW WAS WRITTEN, 2026-08-26: `flutter test` itself. The local toolchain is the OLD 3.44.9 pin and a resolve would rewrite pubspec.lock, so the suite was deliberately not invoked from here; the rc=0 / 9-of-9 and the mutation result above are the TEST FILE'S OWN dated record, at that same 3.44.9. The numbers independently verified for this row are the four ratios and the 9-case arithmetic, both in `evidence` and `measuredBy`. Green here would mean `the ring one primitive paints is visible on two grounds`, NEVER `the criterion is met`",
      "why": "filed HERE and not in a neighbouring list, on the rule the `_readme` states — a row that fits none of the lists and is filed into one of them anyway is the same defect as the 2026-08-13 over-cut. `cuts` is for work deliberately priced out; nothing was priced out here, the measurement simply had never been taken. `aaaAdopted` is for criteria BEYOND the claimed level; 2.4.7 is AA and therefore inside it. `reclaimed` is the nearest miss and is still wrong: those four rows are dated OWNER REVERSALS of same-day cuts, each turning on a `premiseThatWasFalse`, and 2.4.7 was never cut, so there is no premise to reverse and no owner decision to record. What is left is exactly what `insideTheClaim` was created for — a criterion inside the published claim that nobody had looked at — with the polarity flipped from `keyboard`: this one came back GREEN where it was measured. That is worth its own row rather than silence, because an unmeasured pass and a measured one are indistinguishable from outside the file, and the register is the last place that should blur the two"
    }
  ],
  "claimWording": {
    "decided": "2026-08-13",
    "by": "owner 2026-08-13",
    "claimedLevel": "AA",
    "claimText": "Accessible — WCAG 2.2 Level AA",
    "🔴 exceptionOutstanding2026-08-25": "THE WORDING ABOVE IS THE OWNER'S 2026-08-13 DECISION AND IS NOT EDITED HERE. What is recorded is the qualification it currently needs, so nothing below reads as unconditional. SC 2.1.1 Keyboard (Level A) sat MEASURED AND FAILING under this claim from 2026-08-21; as of 2026-08-25 the three screens that were measured are keyboard-operable end to end (see `insideTheClaim` -> `keyboard` -> `reMeasured2026-08-25`), and 14 of apps/subly's 17 screen-bearing routes have still never been swept. So the honest sentence today is FIXED WHERE MEASURED, UNMEASURED ELSEWHERE — which is not the same thing as §5.2.1 satisfied, because §5.2.1 is a claim about ALL content, not about the part somebody happened to test. ⚠️ UNTIL THE SWEEP COVERS EVERY DECLARED ROUTE, ANY PUBLICATION OF `claimText` — on a store listing, on the website, in an accessibility statement — CARRIES [8]K-3 EXPOSURE, and an agent may not discharge that by editing this string. Two ways out and both are the owner's: extend the sweep to all declared routes and make the claim true, or qualify the published wording. The register's job is to make sure the choice is visible rather than assumed, and this field is that.",
    "aaaAttachedAs": "WCAG 2.2 §5.3.2 optional conformance-claim component — 'A list of success criteria beyond the level of conformance claimed that have been met'",
    "sanctionedBy": "WCAG 2.2 §5.2.1 Note 1 — 'authors are encouraged to report (in their claim) any progress toward meeting success criteria from all levels beyond the achieved level of conformance'",
    "thereIsNoSuchThingAs": [
      "AA+",
      "partial AAA",
      "AA with AAA extensions"
    ],
    "why": "§5.2.1 is ALL-OR-NOTHING per level: a Level AAA claim requires every Level A, AA and AAA success criterion. Twenty of thirty-one is not a level, it is a list — and the standard has a place for exactly that list. Naming the route (§5.2.1 Note 1 + §5.3.2) is what stops the next person inventing 'AA+', which is not a conformance level and would be an unverifiable claim under [8]K-3",
    "doNotUse": {
      "clause": "§5.4 Statement of Partial Conformance",
      "why": "🔴 §5.4 is for content the author does not control — third-party content injected after publication, and language-based partial conformance. It is NOT a way to say 'we met some of a higher level'. Reaching for it here would misstate a DEFINED TERM of the standard in a published legal claim, which is a worse defect than claiming nothing beyond AA at all"
    },
    "wcag2ictLimit": {
      "finding": "WCAG2ICT covers only Level A and Level AA for non-web ICT, and explicitly DECLINES to define eight terms that AAA criteria depend on: blocks of text, context-sensitive help, motion animation, perimeter, region, section, supplemental content, user inactivity",
      "consequence": "For 5 of NIKATRU's 6 platforms (iOS, Android, Windows, macOS, Linux — everything except Web) there is NO NORMATIVE VEHICLE for a Level AAA conformance claim at all. Web is the only target where 'WCAG 2.2 AAA' is even a well-formed statement",
      "why": "this is an INDEPENDENT reason the named-set approach is correct, and it is recorded because it changes what a claim can MEAN, not merely what it costs. An agent or a store listing that later writes 'AAA' against a desktop or mobile build is asserting something the applicable interpretation document refuses to define"
    }
  },
  "aaaPolicy": {
    "decided": "2026-08-13",
    "by": "owner 2026-08-13",
    "policy": "Level AA everywhere, PLUS a named set of AAA criteria that are free or chassis-cheap. FULL AAA IS REJECTED",
    "w3cBasis": "WCAG 2.2 §5.2.1 Note 2 — 'It is not recommended that Level AAA conformance be required as a general policy for entire sites because it is not possible to satisfy all Level AAA success criteria for some content.'",
    "totalAaaCriteriaInWcag22": 31,
    "adopted": 20,
    "notAdopted": 10,
    "awaitingOwner": 1,
    "everyAaaCriterionIsInExactlyOneList": true,
    "countCorrection": "🔴 the brief that carried this decision said 'the 18 named AAA criteria'. Measured by enumeration 2026-08-13: 4 already-satisfied + 5 satisfied-by-absence + 10 chassis-build + 1 with-carve-out = 20. The enumeration governs. 20 + 10 + 1 = 31, which is the AAA count in WCAG 2.2, so the set is closed — that closure is the check, not the arithmetic",
    "rungUnchanged": "no row in these lists raises [2]C-14's rung. Adopted-but-unmet criteria are carried as ⬜ debt with `unmetDebt: true`, exactly as contrast and reduce-motion were on the same day"
  },
  "aaaAdopted": [
    {
      "id": "target-size-enhanced",
      "sc": "2.5.5",
      "level": "AAA",
      "title": "Target Size (Enhanced)",
      "basis": "already-satisfied",
      "state": "SATISFIED — the house standard is 48, the criterion asks 44",
      "evidence": "48 logical px is enforced today at apps/subly/test/chassis_properties_test.dart:1209 and tooling/bricks/app/__brick__/apps/{{app_id}}/test/chassis_properties_test.dart:1250, both `greaterThanOrEqualTo(48.0)`. 48 > 44, so the enhanced criterion is cleared by the number already shipping. MASTER_PLAN §4-F's physical-size note is why 48 was chosen and it is unrelated to this: 44 is Apple's pt figure and WCAG's AAA CSS-px figure, and the house number clears both",
      "⚠️ scopeLimit": "🔴 THIS IS EXACTLY AS STRONG AS THE 48px LIMB AND NO STRONGER, AND §4-F ALREADY MEASURED THAT LIMB AS COVERING LESS THAN ITS NAME. The test is titled 'every tap target on every declared route is at least 48px' but ranges over `_iconOnlyControls` (:873) — IconButton | InkWell | GestureDetector filtered to those with NO Text descendant. Every LABELLED control is outside the domain. So 2.5.5 is adopted as SATISFIED over the same partial domain, and it becomes satisfied over the WHOLE domain on the same increment that lands meetsGuideline(androidTapTargetGuideline) across the 19 swept surfaces. Adopting a criterion does not enlarge the check that was already there",
      "costThatIsNotFree": "🔴 SC 2.5.5 HAS FEWER EXCEPTIONS THAN THE AA CRITERION IT SITS ABOVE. SC 2.5.8 (AA, new in 2.2) carries five exceptions — Spacing, Equivalent, Inline, User agent control, Essential. SC 2.5.5 (AAA, from 2.1) carries FOUR: it has NO Spacing exception. §4-F names Spacing as the exception 'dense grids and inline links depend on'. Adopting 2.5.5 therefore REMOVES Spacing from the toolbox for any surface claiming it, and THAT is the cost — carried by the Spacing and Inline shapes (dense grids, links in running text). ⚠️ CORRECTED 2026-08-13: this field previously offered 'the 20px checkbox at apps/subly/lib/features/auth/legal_consent_fields.dart:123 survives because it relies on EQUIVALENT' as the worked example. [ADR 048] RETRACTED that claim as measurably false and this file kept repeating it. There is no 20px tap target there: the consent tick is a Material Checkbox whose materialTapTargetSize resolves to 'padded' on android/fuchsia/iOS, sizing the node kMinInteractiveDimension = 48.0 — the 20px is the PAINTED square inside a 48×48 node. Measured: 19 of 19 'at least 48' cases pass. So it relies on NO exception, and citing it here inflated the apparent cost of 2.5.5 with a violation the tree does not contain. 🔴 THE REAL EXPOSURE IS THE OPPOSITE ONE AND IT IS DESKTOP: ThemeData resolves materialTapTargetSize to 'shrinkWrap' on linux/macOS/windows (theme_data.dart:405-408) → 48−8 = 40×40, FAILING — and flutter test forces TargetPlatform.android, so every one of those 19 cases is green while ~4 of 6 shipping targets take the 40px path. Needs a platform-parameterised test.",
      "guard": "the existing 48px limb, WIDENED to meetsGuideline(androidTapTargetGuideline) per §4-F. No NEW guard is owed by this adoption — it is the same assertion with a lower bar than the one already asserted. What IS owed is a REFUSAL: any future exception claimed on a 2.5.5 surface must name Equivalent, Inline, User agent control or Essential, and must NOT name Spacing",
      "guardStatus": "🟡 PARTIAL — the 48px assertion runs today over the icon-only domain; the widening to all actionable nodes is ⬜ NOT BUILT (§4-F, `meetsGuideline` measures 0 in live .dart files and assert-a11y-coverage.mjs:283-286's tap-target family has read x0 since it was written)",
      "why": "free: the number that clears it already ships and is negative-tested. The reason it is worth NAMING rather than leaving unstated is the exception delta — an adoption that quietly narrows what a future screen may justify is a cost, and an unstated cost is what this whole thread is about"
    },
    {
      "id": "concurrent-input-mechanisms",
      "sc": "2.5.6",
      "level": "AAA",
      "title": "Concurrent Input Mechanisms",
      "basis": "already-satisfied",
      "state": "SATISFIED",
      "evidence": "The criterion forbids RESTRICTING the input modalities available on a platform, except where the restriction is essential, required to ensure security, or required to respect user settings. Measured 2026-08-13 across packages/, apps/ and tooling/bricks (excluding build output): `PointerDeviceKind` -> 0, `supportedDevices` -> 0, `FocusNode(canRequestFocus: false)` -> 0. Nothing in the tree narrows a gesture recognizer to a device kind or removes keyboard focusability, so touch, mouse, stylus and keyboard all stay live wherever the platform offers them",
      "guard": "assert the three shapes above stay at ZERO in app and package source, and PRINT the count on every run so the day one appears the guard starts biting rather than continuing to pass. Same shape as the SC 3.3.8 paste guard, and for the same reason: the domain is empty and an empty domain that nobody prints is an assertion nobody notices went vacuous",
      "guardStatus": "⬜ NOT BUILT — named, not enforced. Do not read this row as coverage",
      "why": "free, and free in the strongest sense: it is satisfied by NOT doing something. The realistic way it breaks is a well-meant `supportedDevices: {PointerDeviceKind.touch}` added to stop a desktop mouse triggering a swipe — which is precisely the change the printed count would catch"
    },
    {
      "id": "three-flashes",
      "sc": "2.3.2",
      "level": "AAA",
      "title": "Three Flashes",
      "basis": "already-satisfied",
      "state": "SATISFIED",
      "evidence": "2.3.2 forbids ANY content flashing more than three times per second (2.3.1, Level A, allows it below the general and red flash thresholds; 2.3.2 removes the threshold escape). Measured 2026-08-13: no timed-media playback exists in the tree (see `vacuityGuard` below — zero video/audio playback plugins in any pubspec.yaml), and every occurrence of the word 'flash' under tooling/bricks/app/__brick__ is about a UI interstitial appearing once at launch (router.dart:279, sign_up_screen.dart:91, onboarding_screen.dart:22, providers.dart:601 and :2183), not about repeated luminance change",
      "guard": "COVERED BY `vacuityGuard` (assert-no-timed-media.mjs) FOR ITS LARGEST RISK SURFACE, and only for that. Video is where a >3Hz flash realistically arrives; a hand-written Flutter animation that flashes faster than 3Hz is possible and NOT mechanically detectable. This is stated rather than papered over — the guard reduces the surface, it does not close the criterion. ⬜ NOT BUILT AS OF 2026-08-25, AND THIS FIELD NAMES A FILE THAT HAS NEVER EXISTED: `[ -e tooling/ci/assert-no-timed-media.mjs ]` is false in this tree and `git log --diff-filter=D -- tooling/ci/assert-no-timed-media.mjs` returns nothing, so it was not deleted either — it was specified and never written. What this field records is the guard this criterion is SPECIFIED to get, not one that runs today; `vacuityGuard.guardStatus` (decided 2026-08-13) has said so all along and is the honest half. Nothing enforces this row mechanically until that file exists.",
      "guardStatus": "⬜ NOT BUILT — named, not enforced. Do not read this row as coverage",
      "why": "free today, and it is the one adopted criterion whose residual risk is NOT mechanisable. A criterion you cannot fully guard is one you may still adopt — but you may not claim the guard closes it, which is the difference between this row and Guideline 3.1's non-adoption"
    },
    {
      "id": "location",
      "sc": "2.4.8",
      "level": "AAA",
      "title": "Location",
      "basis": "already-satisfied",
      "state": "SATISFIED — the adaptive shell IS the location mechanism",
      "evidence": "2.4.8 asks for information about the user's location within a set of pages. The chassis shell supplies it structurally at every window class: packages/design_system/lib/src/widgets/app_scaffold.dart declares `required this.selectedIndex` (:123, field at :136) and passes it to the bottom NavigationBar (:206-207, compact), the NavigationRail (:231-232, medium and expanded) and the permanent NavigationDrawer (large/XL). A selected destination rendered in all five classes is the location indicator, and it is inherited by every stamp rather than built per app",
      "guard": "the existing five-window-class limb of assert-stamp-properties.mjs already asserts each class renders its own navigation affordance at its exact Material edge. The ADDITION owed is one clause: the selected destination must be programmatically determinable — i.e. `selectedIndex` reaches the rendered navigation widget in every class, not merely that a navigation widget exists. Cheap, because the domain (5 classes) is already enumerated and already non-empty",
      "guardStatus": "⬜ NOT BUILT — named, not enforced. The five-class limb IS built and verified (main @ 9007e5b); the selectedIndex clause is not",
      "why": "free: the shell was built for [2]C-14's window-class limb and this criterion falls out of it. Worth naming because the mechanism is a SHARED widget — if a stamped app hand-rolls its own bar (which apps/subly does, per DoD §4-C: 'Subly hand-rolls a fixed phone bottom-bar'), it leaves the shell and this criterion stops being inherited"
    },
    {
      "id": "sign-language",
      "sc": "1.2.6",
      "level": "AAA",
      "title": "Sign Language (Prerecorded)",
      "basis": "satisfied-by-absence",
      "state": "SATISFIED (vacuously — no prerecorded audio in synchronized media exists)",
      "conditionalOn": "vacuityGuard",
      "w3cBasis": "WCAG 2.2 Conformance: \"if there is no content to which a success criterion applies, the success criterion is satisfied\"",
      "unmetDebt": false,
      "guard": "assert-no-timed-media.mjs — see `vacuityGuard`. ⬜ NOT BUILT AS OF 2026-08-25, AND THIS FIELD NAMES A FILE THAT HAS NEVER EXISTED: `[ -e tooling/ci/assert-no-timed-media.mjs ]` is false in this tree and `git log --diff-filter=D -- tooling/ci/assert-no-timed-media.mjs` returns nothing, so it was not deleted either — it was specified and never written. What this field records is the guard this criterion is SPECIFIED to get, not one that runs today; `vacuityGuard.guardStatus` (decided 2026-08-13) has said so all along and is the honest half. Nothing enforces this row mechanically until that file exists.",
      "guardStatus": "⬜ NOT BUILT — named, not enforced. Until it is built this row is a VACUOUS CLAIM WITH NO TRIPWIRE",
      "why": "free while it is true, and false the hour someone ships an onboarding video. That is the whole reason these five carry a guard the four `reclaimed` rows did not need: SC 2.5.7's domain is non-empty and STAYS non-empty, whereas these five are true only about an absence, and an absence is exactly what a single commit reverses"
    },
    {
      "id": "extended-audio-description",
      "sc": "1.2.7",
      "level": "AAA",
      "title": "Extended Audio Description (Prerecorded)",
      "basis": "satisfied-by-absence",
      "state": "SATISFIED (vacuously — no prerecorded video in synchronized media exists)",
      "conditionalOn": "vacuityGuard",
      "unmetDebt": false,
      "guard": "assert-no-timed-media.mjs — see `vacuityGuard`. ⬜ NOT BUILT AS OF 2026-08-25, AND THIS FIELD NAMES A FILE THAT HAS NEVER EXISTED: `[ -e tooling/ci/assert-no-timed-media.mjs ]` is false in this tree and `git log --diff-filter=D -- tooling/ci/assert-no-timed-media.mjs` returns nothing, so it was not deleted either — it was specified and never written. What this field records is the guard this criterion is SPECIFIED to get, not one that runs today; `vacuityGuard.guardStatus` (decided 2026-08-13) has said so all along and is the honest half. Nothing enforces this row mechanically until that file exists.",
      "guardStatus": "⬜ NOT BUILT — named, not enforced. Until it is built this row is a VACUOUS CLAIM WITH NO TRIPWIRE",
      "why": "same absence, same tripwire. 1.2.7 is also the most expensive of the five to retrofit — extended audio description means re-cutting the video itself, not adding a track beside it"
    },
    {
      "id": "media-alternative",
      "sc": "1.2.8",
      "level": "AAA",
      "title": "Media Alternative (Prerecorded)",
      "basis": "satisfied-by-absence",
      "state": "SATISFIED (vacuously — no prerecorded synchronized media and no prerecorded video-only content exists)",
      "conditionalOn": "vacuityGuard",
      "unmetDebt": false,
      "guard": "assert-no-timed-media.mjs — see `vacuityGuard`. ⬜ NOT BUILT AS OF 2026-08-25, AND THIS FIELD NAMES A FILE THAT HAS NEVER EXISTED: `[ -e tooling/ci/assert-no-timed-media.mjs ]` is false in this tree and `git log --diff-filter=D -- tooling/ci/assert-no-timed-media.mjs` returns nothing, so it was not deleted either — it was specified and never written. What this field records is the guard this criterion is SPECIFIED to get, not one that runs today; `vacuityGuard.guardStatus` (decided 2026-08-13) has said so all along and is the honest half. Nothing enforces this row mechanically until that file exists.",
      "guardStatus": "⬜ NOT BUILT — named, not enforced. Until it is built this row is a VACUOUS CLAIM WITH NO TRIPWIRE",
      "why": "same absence, same tripwire"
    },
    {
      "id": "audio-only-live",
      "sc": "1.2.9",
      "level": "AAA",
      "title": "Audio-only (Live)",
      "basis": "satisfied-by-absence",
      "state": "SATISFIED (vacuously — no live audio-only content exists)",
      "conditionalOn": "vacuityGuard",
      "unmetDebt": false,
      "guard": "assert-no-timed-media.mjs — see `vacuityGuard`. ⚠️ THIS ONE IS ONLY PARTLY COVERED: the guard inspects BUNDLED and PACKED assets and the absence of a player, and LIVE audio arrives over the network with no asset to inspect. The player limb (limb 3) is what covers it, and it is the only limb that does. ⬜ NOT BUILT AS OF 2026-08-25, AND THIS FIELD NAMES A FILE THAT HAS NEVER EXISTED: `[ -e tooling/ci/assert-no-timed-media.mjs ]` is false in this tree and `git log --diff-filter=D -- tooling/ci/assert-no-timed-media.mjs` returns nothing, so it was not deleted either — it was specified and never written. What this field records is the guard this criterion is SPECIFIED to get, not one that runs today; `vacuityGuard.guardStatus` (decided 2026-08-13) has said so all along and is the honest half. Nothing enforces this row mechanically until that file exists.",
      "guardStatus": "⬜ NOT BUILT — named, not enforced. Do not read this row as coverage",
      "why": "same absence. Recorded with its coverage gap named, because 'the guard covers the five' would have been a claim about a guard nobody had written yet"
    },
    {
      "id": "low-or-no-background-audio",
      "sc": "1.4.7",
      "level": "AAA",
      "title": "Low or No Background Audio",
      "basis": "satisfied-by-absence",
      "state": "SATISFIED (vacuously — no prerecorded audio-only content containing speech exists)",
      "conditionalOn": "vacuityGuard",
      "unmetDebt": false,
      "guard": "assert-no-timed-media.mjs — see `vacuityGuard`. ⬜ NOT BUILT AS OF 2026-08-25, AND THIS FIELD NAMES A FILE THAT HAS NEVER EXISTED: `[ -e tooling/ci/assert-no-timed-media.mjs ]` is false in this tree and `git log --diff-filter=D -- tooling/ci/assert-no-timed-media.mjs` returns nothing, so it was not deleted either — it was specified and never written. What this field records is the guard this criterion is SPECIFIED to get, not one that runs today; `vacuityGuard.guardStatus` (decided 2026-08-13) has said so all along and is the honest half. Nothing enforces this row mechanically until that file exists.",
      "guardStatus": "⬜ NOT BUILT — named, not enforced. Until it is built this row is a VACUOUS CLAIM WITH NO TRIPWIRE",
      "why": "🔴 THE ONE OF THE FIVE MOST LIKELY TO GO FALSE, AND THE MEASUREMENT SAYS WHY. [ADR 019] locks audio/TTS to Google Cloud Chirp 3 HD, banked as content. 1.4.7 binds on prerecorded audio-only content that is primarily SPEECH — which is precisely what a TTS pipeline emits. The absence protecting this row is a schedule, not a design"
    },
    {
      "id": "animation-from-interactions",
      "sc": "2.3.3",
      "level": "AAA",
      "title": "Animation from Interactions",
      "basis": "chassis-build",
      "state": "⬜ ADOPTED AND UNMET — MEASURED UNBUILT",
      "unmetDebt": true,
      "evidence": "already adopted into [2]C-14 on 2026-08-13 as the reduce-motion limb, and already measured unbuilt on the same day: disableAnimations -> 0 files, reduceMotion -> 0, AnimationsDisabled -> 0, prefers-reduced-motion -> 0, across packages/ apps/ tooling/bricks/ (source only). The reduce-motion API surface is ABSENT ENTIRELY, not merely unasserted",
      "guard": "the [2]C-14 acceptance limb already written: on a fresh stamp, when MediaQuery.disableAnimations is true, no non-essential animation plays",
      "guardStatus": "⬜ NOT BUILT",
      "why": "this row CHANGES NOTHING except the column it is filed under. 2.3.3 was already an adopted-unbuilt acceptance limb of [2]C-14 and was already relabelled AAA on 2026-08-13; naming it here puts it in the AAA list where a reviewer will look for it, instead of only in a house-extra sentence. Its debt is unchanged and [2]C-14's rung is unchanged"
    },
    {
      "id": "re-authenticating",
      "sc": "2.2.5",
      "level": "AAA",
      "title": "Re-authenticating",
      "basis": "chassis-build",
      "state": "⬜ ADOPTED AND UNMET",
      "unmetDebt": true,
      "evidence": "the session model exists — packages/core/lib/src/auth/auth_models.dart:103,111,123 carries `expiresAt` and an expiry test, and auth_event.dart:84-103 classifies expiredOrUsed. What does NOT exist is the criterion's actual requirement: that after re-authenticating, the user can CONTINUE THE ACTIVITY WITHOUT LOSS OF DATA. No draft-preservation-across-reauth path was found",
      "guard": "on a fresh stamp, a form with unsaved input that hits a session expiry must restore that input after re-authentication. Testable at stamp time because the chassis owns both halves — the auth seam ([2]C-15) and the form screens",
      "guardStatus": "⬜ NOT BUILT",
      "why": "chassis-cheap ONCE: it is one interception point in the shared auth seam plus one draft-holding convention, and it is near-impossible to retrofit into 50 apps' forms individually. Directly coupled to DoD §4-A's account-deletion+reauth screen, which already forces a reauth path to exist"
    },
    {
      "id": "timeouts",
      "sc": "2.2.6",
      "level": "AAA",
      "title": "Timeouts",
      "basis": "chassis-build",
      "state": "⬜ ADOPTED AND UNMET",
      "unmetDebt": true,
      "evidence": "2.2.6 requires that users are WARNED of the duration of any user inactivity that could cause data loss, unless the data is preserved for more than 20 hours of inactivity. No inactivity warning and no such preservation guarantee was found in the chassis",
      "guard": "either the chassis preserves user data across >20h of inactivity (in which case the criterion is met by the storage policy and the guard asserts the policy), or the shared session layer emits a warning naming the duration. One or the other, asserted at the seam — not per screen",
      "guardStatus": "⬜ NOT BUILT",
      "why": "chassis-cheap because it is a property of ONE session layer, and it pairs with 2.2.5: the same interception point that preserves a draft across reauth is the one that knows the inactivity budget. ⚠️ WCAG2ICT explicitly DECLINES to define 'user inactivity' for non-web ICT — so on 5 of 6 platforms this criterion is adopted as a HOUSE requirement whose term has no normative non-web definition. Named, not hidden"
    },
    {
      "id": "error-prevention-all",
      "sc": "3.3.6",
      "level": "AAA",
      "title": "Error Prevention (All)",
      "basis": "chassis-build",
      "state": "⬜ ADOPTED AND UNMET",
      "unmetDebt": true,
      "evidence": "3.3.4 (AA) requires reversible/checked/confirmed submission for legal, financial and data-modifying pages; 3.3.6 (AAA) extends the SAME requirement to ALL pages requiring the user to submit information. DoD §4-B already demands `confirming(destructive->reauth)` on async actions, which is the mechanism — what is missing is that it applies to EVERY submitting form, not only destructive ones",
      "guard": "every route in the stamped screen register that submits information must offer at least one of: reversal, error-checking with correction opportunity, or a confirm step. The screen register from [2]C-13 / assert-screen-set.mjs already enumerates the screens — 25 declared / 24 `present` as MEASURED 2026-08-25, 22 when this clause was written — so the domain exists and is non-empty — this is a clause on an existing enumeration, not a new sweep",
      "guardStatus": "⬜ NOT BUILT",
      "why": "chassis-cheap because DoD §4-B's form state machine (pristine/editing/inline-error/submitting/server-error/success/offline-queued) already contains the error-checking limb; 3.3.6 is that machine applied without the 'only if legal/financial' filter. Retrofitting it across 50 apps' forms is the expensive direction"
    },
    {
      "id": "accessible-auth-enhanced",
      "sc": "3.3.9",
      "level": "AAA",
      "title": "Accessible Authentication (Enhanced)",
      "basis": "chassis-build",
      "state": "⬜ ADOPTED AND UNMET — but MUCH closer than the others, and the measurement says so",
      "unmetDebt": true,
      "evidence": "3.3.9 removes 3.3.8's Object Recognition and Personal Content exceptions: NO cognitive function test at all, unless an alternative or a Mechanism is provided. The chassis already satisfies 3.3.8 via the Mechanism exception, and that SAME mechanism carries 3.3.9 — 3.3.9 keeps the Alternative and Mechanism exceptions and drops only the other two. Measured (reclaimed row `accessible-auth-paste-otp`, 2026-08-13): AutofillHints.password/.newPassword/.email at sign_in_screen.dart:89,96, sign_up_screen.dart:142,149, reset_password_screen.dart:272,280; enableInteractiveSelection: false -> 0, oneTimeCode -> 0, maxLength: 1 -> 0",
      "guard": "the SC 3.3.8 guard named in `reclaimed`, PLUS one clause 3.3.8 does not need: no auth step may require the user to recognise an OBJECT or personal content (the two exceptions 3.3.9 removes) — i.e. no image-selection or picture-password challenge in an auth route. Zero today, and the count prints every run",
      "guardStatus": "⬜ NOT BUILT",
      "why": "chassis-cheap because the AA sibling is already satisfied by a mechanism that also satisfies the AAA one. The delta is a REFUSAL — never add an image-recognition challenge — which costs nothing to hold and is expensive to undo once a CAPTCHA-style gate is in 50 apps"
    },
    {
      "id": "focus-appearance",
      "sc": "2.4.13",
      "level": "AAA",
      "title": "Focus Appearance",
      "basis": "chassis-build",
      "state": "⬜ ADOPTED AND UNMET — MEASURED UNBUILT",
      "unmetDebt": true,
      "evidence": "2.4.13 requires the focus indicator to have an area at least the size of a 2px-thick perimeter of the unfocused component AND a contrast ratio of at least 3:1 between focused and unfocused states. Measured 2026-08-13 across packages/design_system/lib and tooling/bricks/app/__brick__: focusColor -> 0, focusedBorder -> 0, WidgetState.focused -> 0, MaterialState.focused -> 0. NOTHING in the shared theme specifies a focus indicator; every stamped app inherits Material's default, which the theme has never asserted anything about",
      "guard": "the shared theme must specify a focus indicator, and a stamp-time probe must assert its thickness and its focused/unfocused contrast ratio. Naturally paired with the 1.4.6 contrast limb, because both are computations over the SAME shared ColorScheme and the tree computes no ratio today (contrastRatio -> 0 files, computeLuminance -> 0 files)",
      "guardStatus": "⬜ NOT BUILT",
      "why": "the canonical C-14 shape: one theme value, inherited by 50 apps, near-impossible to retrofit once shipped. ⚠️ WCAG2ICT declines to define 'perimeter' for non-web ICT, so on 5 of 6 platforms the SIZE limb has no normative non-web reading and the house adopts the CSS-px reading by choice. The contrast limb is unaffected"
    },
    {
      "id": "focus-not-obscured-enhanced",
      "sc": "2.4.12",
      "level": "AAA",
      "title": "Focus Not Obscured (Enhanced)",
      "basis": "chassis-build",
      "state": "⬜ ADOPTED AND UNMET",
      "unmetDebt": true,
      "🔴 supersedes": "SC 2.4.11 Focus Not Obscured (Minimum), Level AA — STRICTLY. 2.4.11 forbids the focused component being ENTIRELY hidden; 2.4.12 forbids ANY PART of it being hidden. 2.4.12 met implies 2.4.11 met, always",
      "consequenceForTheCutList": "🔴 STATED BECAUSE TWO ROWS OF THIS REGISTER WOULD OTHERWISE CONTRADICT EACH OTHER. `cuts` -> `focus-not-obscured` cuts the CI GATE for SC 2.4.11 on the reasoning that 'no shared-theme value can establish it and no stamp-time probe can falsify it over a chassis with two screens'. Adopting the STRICTLY STRONGER 2.4.12 as a chassis build asserts that a probe CAN be written. Both cannot stand unexamined. The resolution recorded here, and it is a SEQUENCING one, not a reversal: the 2.4.11 GATE CUT STANDS TODAY and is not touched by this adoption — but the increment that builds 2.4.12's probe discharges 2.4.11's AA debt by the same work, and ON THAT DAY the 2.4.11 gate cut must be re-opened with the owner rather than left standing as an admission of a gap the code no longer has. That is the same defect as the 2026-08-13 over-cut, arriving on a timer",
      "guard": "a stamp-time probe that focuses each interactive node on each declared route and asserts no author-created content intersects its semantics rect — with the overlay stack (SnackBar, persistent bottom sheet, sticky header) deliberately populated, since an empty overlay stack makes the assertion vacuous. THIS IS THE LIMB THE 2.4.11 CUT SAID COULD NOT BE WRITTEN; adopting 2.4.12 is a commitment to write it",
      "guardStatus": "⬜ NOT BUILT",
      "why": "Flutter gives NEITHER criterion for free — neither focus-traversal auto-scroll nor Scaffold's keyboard-inset handling stops a SnackBar or a sticky header covering a focused control (recorded in the `focus-not-obscured` cut). Adopted here because the overlay stack is CHASSIS-OWNED: the shared shell decides where snackbars and sheets appear, so one policy in one place covers 50 apps"
    },
    {
      "id": "keyboard-no-exception",
      "sc": "2.1.3",
      "level": "AAA",
      "title": "Keyboard (No Exception)",
      "basis": "chassis-build",
      "state": "⬜ ADOPTED AND UNMET — MEASURED UNBUILT (2026-08-21). A keyboard sweep now exists and covers 3 screens of apps/subly; this row’s guard asks for every declared route on a FRESH STAMP, so the adoption is unchanged and the debt is unpaid",
      "unmetDebt": true,
      "evidence": "2.1.1 (A) allows an exception for functionality requiring path-dependent input; 2.1.3 removes it. The chassis's one path-dependent surface is the onboarding PageView.builder (__brick__/…/features/firstrun/onboarding_screen.dart:110) — and it already ships Next (:178) and Skip (:105), which is why SC 2.5.7 was reclaimed as SATISFIED. The same two controls are what 2.1.3 needs, PROVIDED they are keyboard-reachable. 🔴 THE LAST SENTENCE OF THIS FIELD READ ‘That has never been asserted: no keyboard-traversal test exists in the chassis suite’ AND IS NOW HALF FALSE. Corrected 2026-08-21 rather than deleted, because which half survives is the point. STILL TRUE OF THE CHASSIS: tooling/bricks/app has no keyboard-traversal test, so the onboarding PageView above is STILL unasserted and this row is STILL unmet. NO LONGER TRUE OF THE TREE: apps/subly/test/keyboard_traversal_test.dart (2026-08-21) sweeps login, settings and home with real Tab / Shift-Tab / Enter events. What it found is filed under `insideTheClaim` -> `keyboard`, because the failure it measured belongs to SC 2.1.1 (Level A, mandatory) and not to this row (Level AAA, optional): 55 controls, 30 reachable, 25 keyboard-dead — and NONE of the 25 is path-dependent input, so the exception 2.1.3 removes is not what is broken",
      "guard": "on a fresh stamp, every declared route must be fully operable by keyboard alone — traverse with Tab/Shift-Tab and Enter/Space and assert every actionable node is reachable and activatable. Non-empty domain by construction (the screen register: 25 declared, 24 `present`, MEASURED 2026-08-25; it read 22 when this clause was written), so it cannot go vacuous",
      "guardStatus": "🟨 PARTLY BUILT — apps/subly/test/keyboard_traversal_test.dart. It is the SHAPE this guard specifies (real Tab / Shift-Tab / Enter, reachability AND activation) at a THIRD of its scope in the WRONG TREE: 3 screens, not every declared route; apps/subly, the frozen rail-prover, not a fresh stamp. Its 18 cases are green — 16 UNTIL 2026-08-25, corrected here because the same file's `insideTheClaim` -> `keyboard` -> `guardStatus` has said 18 since the primitive landed and two fields of one register may not disagree about one countable thing; the count went UP, so the stale figure understated the evidence — and GREEN THERE MEANS ‘the measurement has not moved’, never ‘the criterion is met’ — the per-screen counts are pinned as exact numbers, so the file goes red when a keyboard-dead control is FIXED as loudly as when one is added. Do not read this row as coverage",
      "why": "chassis-cheap because Flutter gives most of it for free and the ONE path-dependent surface is a brick screen, not an app screen. The cost of adoption is a standing refusal: no stamped app may add a drag-only, swipe-only or gesture-only path without a keyboard equivalent — cheap to hold, expensive to retrofit across 50 apps"
    },
    {
      "id": "change-on-request",
      "sc": "3.2.5",
      "level": "AAA",
      "title": "Change on Request",
      "basis": "chassis-build",
      "state": "⬜ ADOPTED AND UNMET",
      "unmetDebt": true,
      "evidence": "3.2.5 requires that changes of context are initiated only by user request, or that a mechanism exists to turn them off. The chassis ships several AUTOMATIC context changes by design: the router's consent/terms interstitial (core/router.dart:279), the re-accept-terms redirect (features/auth/reaccept_terms_screen.dart), and the force-update wall. Each is an author-initiated context change the user did not request",
      "guard": "enumerate every automatic redirect in the shared router and require each to be on a named allowlist carrying its 3.2.5 justification — the criterion's own exceptions are narrow, and a legal-consent gate or a force-update wall is defensible where a silent navigation is not. The router is chassis-owned and the redirect set is small and enumerable, which is what makes this cheap",
      "guardStatus": "⬜ NOT BUILT",
      "why": "chassis-cheap because ALL of these live in ONE shared router; the same requirement applied per app would mean auditing 50 routers. ⚠️ Honest cost: the force-update wall and the consent interstitial are non-negotiable product requirements (DoD §4-A), so adopting 3.2.5 means committing to justify them under its exceptions rather than to remove them — and if that justification does not hold, this row becomes the first adopted criterion the factory cannot meet"
    },
    {
      "id": "images-of-text-no-exception",
      "sc": "1.4.9",
      "level": "AAA",
      "title": "Images of Text (No Exception)",
      "basis": "chassis-build",
      "state": "SATISFIED TODAY, adopted as a standing refusal",
      "unmetDebt": false,
      "evidence": "1.4.9 permits images of text ONLY where the presentation is essential or the image is customizable. Measured 2026-08-13, re-measured 2026-08-17 — every image asset in the tree: apps/subly/assets/brand/{nikatru-logo.png, nikatru-logo-dark-bg.png} and apps/subly/assets/icon/{app_icon_1024.png, app_icon_background.png/.svg, app_icon_foreground.png/.svg, app_icon_maskable.svg}. (The 2026-08-13 measurement also listed apps/subly/assets/brand/nikatru-icon.png; it was deleted 2026-08-17 as a byte-identical duplicate of app_icon_1024.png, which is still enumerated here, so the SET of distinct images this criterion ranges over is unchanged.) Every one is a LOGOTYPE or a launcher icon. WCAG names logotypes under Essential: 'text that is part of a logo or brand name' is the standard's own worked example of essential presentation. So the criterion is met, and it is met by the exception rather than by absence",
      "guard": "🔴 THE OBVIOUS GUARD IS THE WRONG ONE. 'No image asset may contain text' fails on the brand logo — an assertion that fires on conforming content, which is worse than none because it gets disabled. The correct shape: every image asset declared in a pubspec `assets:` block must be on a register naming its 1.4.9 disposition — `logotype` (Essential, the four brand/icon files today) or `no-text`. A new asset with no disposition fails the build. This reuses the same pattern as tooling/legal/asset-register.json rather than inventing a second register",
      "guardStatus": "⬜ NOT BUILT",
      "why": "chassis-cheap as a REFUSAL held from day one: text baked into a bitmap cannot be localized ([2] DoD §4-E, zero hardcoded strings) and cannot be text-scaled (the TextScaler.clamp limb), so 1.4.9 is already implied by two invariants the chassis enforces. It is worth naming because the pressure to bake text into an image is real — store screenshots and onboarding art — and 50 apps' worth of it is unretrofittable"
    },
    {
      "id": "contrast-enhanced-text-only",
      "sc": "1.4.6",
      "level": "AAA",
      "title": "Contrast (Enhanced)",
      "basis": "adopted-with-carve-out",
      "state": "⬜ ADOPTED AND UNMET",
      "unmetDebt": true,
      "carveOut": "TEXT ONLY. Non-text contrast stays at SC 1.4.11's Level AA 3:1",
      "🔴 carveOutIsAlreadyTheStandard": "MEASURED CORRECTION, 2026-08-13. SC 1.4.6 Contrast (Enhanced) IS ALREADY TEXT-ONLY IN THE STANDARD — it reads 'text and images of text', with exceptions for large text (4.5:1), incidental text, and logotypes. WCAG 2.2 has NO AAA non-text contrast criterion at all; 1.4.11 Non-text Contrast (AA, 3:1) is the only one at any level. So the carve-out as written removes nothing from the criterion. What it actually constrains is the IMPLEMENTATION: it forbids buying 1.4.6 by raising Flutter's scheme-wide `contrastLevel`, because that knob moves text and non-text roles TOGETHER. Recorded this way because 'we carved non-text out of 1.4.6' would misstate the standard, while 'we refuse the scheme-wide knob' is the decision actually being made",
      "evidence": "🔬 MEASURED INDEPENDENTLY 2026-08-13 with material_color_utilities 0.13.0 (the package Flutter's ColorScheme.fromSeed is built on), SchemeTonalSpot — fromSeed's default variant — light brightness, six far-apart brand seeds (red #D32F2F, orange #F57C00, green #388E3C, teal #00796B, blue #1976D2, purple #7B1FA2). AT contrastLevel 0.0, WHICH IS WHAT SHIPS: primary/onPrimary 6.42-6.46 · secondary/onSecondary 6.45-6.49 · surfaceVariant/onSurfaceVariant 7.21-7.26 · surface/onSurface 16.27-16.36 · outline/surface 4.27-4.28. So the SEEDED chassis path ALREADY clears 4.5:1 (the adopted AA limb) on every pair measured, ALREADY clears 1.4.6's 7:1 on surface/onSurface and surfaceVariant/onSurfaceVariant, and MISSES 7:1 on exactly two pairs — primary/onPrimary and secondary/onSecondary — by about 0.55 of a ratio point. That is the operative 1.4.6 gap, and it is much narrower than 'unmeasured'",
      "🔴 whyNotContrastLevel": "🔬 MEASURED, SAME RUN. contrastLevel 1.0: every pair clears 7:1 (primary/onPrimary 13.91-14.02, secondary/onSecondary 13.92-14.02, surfaceVariant/onSurfaceVariant 16.24-16.31, outline/surface 13.23-13.36) — and primary collapses to TONE 17.9-18.1, i.e. near-black, where hue is far harder to perceive. contrastLevel 0.5 also clears 7:1 everywhere (primary/onPrimary 12.01-12.09, outline/surface 7.37-7.45) at tone 22.6-22.8. THE BRIEF'S FIGURE IS LOCATED: '13.25-13.33:1' is the OUTLINE/SURFACE pair at contrastLevel 1.0 — measured here as 13.23-13.36 — which is a NON-TEXT pair, 1.4.11's subject, not 1.4.6's. And a sharper correction: 'flattens all brands to near-identical lightness' is TRUE AT EVERY CONTRAST LEVEL INCLUDING THE ONE WE SHIP — tonalSpot pins primary to tone 40.0-40.2 across those same six seeds at contrastLevel 0.0. Brands are differentiated by HUE (23/53/146/183/259/320 degrees), never by lightness. The real cost of raising the knob is therefore NOT lost lightness variance (there was none) but moving primary from tone 40, where hue reads, to tone 18-23, where it does not. Same conclusion, correct mechanism",
      "whyItMattersForC11": "[2]C-11 exists because a `primary: AppColors.accent` override made every seeded theme paint identically and nothing caught it for months. Store-clone detection depends on 50 apps looking DIFFERENT. A scheme-wide contrastLevel raise is the same failure with a virtuous motive: it would clear every pair and re-flatten the portfolio, and C-11's guard would be the only thing standing between it and 50 identical-looking apps",
      "guard": "compute the ACTUAL ratio for every foreground/background pair the shared theme puts on screen, and assert >=7:1 for normal text, >=4.5:1 for large text, with named exceptions for incidental text and logotypes; non-text pairs assert >=3:1 under 1.4.11 and are NOT raised. Nothing in the tree computes a ratio today (contrastRatio -> 0 files, computeLuminance -> 0 files), so the first increment is the computation itself",
      "guardStatus": "⬜ NOT BUILT",
      "residualRisk": "🔴 THE MEASUREMENT ABOVE COVERS THE SEEDED PATH ONLY, AND THE RISKY PATH IS THE OTHER ONE. buildAppTheme(seed:) calls ColorScheme.fromSeed with no role override, so its pairs are contrast-correct by construction — that is what was measured. The pinned legacy path in the same file, AppTheme.light()/AppTheme.dark(), pins primary/secondary/surface to hand-picked AppColors constants WHILE letting onPrimary/onSurface stay generated from the seed's palette. That is the shape that silently breaks a pair, it is UNMEASURED, and it is the path apps/subly runs on (MASTER_PLAN §4-D)",
      "why": "adopted because contrast lives in ONE shared theme and retrofitting it means re-deriving every shipped app's palette and re-shooting every store screenshot on six platforms. The carve-out is what keeps the adoption compatible with [2]C-11's store-clone-detection purpose"
    }
  ],
  "vacuityGuard": {
    "id": "no-timed-media",
    "guard": "tooling/ci/assert-no-timed-media.mjs",
    "guardStatus": "⬜ NOT BUILT — SPECIFIED HERE, NOT ENFORCED ANYWHERE",
    "covers": ["1.2.6", "1.2.7", "1.2.8", "1.2.9", "1.4.7"],
    "partiallyCovers": ["2.3.2"],
    "decided": "2026-08-13",
    "whyItExists": "Five adopted criteria are satisfied ONLY because no app plays video or audio. W3C: 'if there is no content to which a success criterion applies, the success criterion is satisfied.' A VACUOUS CLAIM WITH NO GUARD GOES FALSE SILENTLY — the day someone adds an onboarding video, five published conformance statements become untrue and nothing anywhere goes red. This is the same shape as the four criteria reclaimed earlier on 2026-08-13, with one difference that decides the guard: those four range over domains that are non-empty and STAY non-empty, whereas these five are true about an ABSENCE, and an absence is what a single commit reverses",
    "🔴 theBriefsScopeWasMeasuredWRONG": "The decision arrived asking for a guard that 'fails if any video/audio asset enters an APP BUNDLE'. Measured 2026-08-13, that guard would have been GREEN ON A REPO THAT ALREADY CONTAINS A MANIFEST-DECLARED MP3, and would have stayed green through the exact change it was written to catch. THE MEASUREMENTS: (1) tooling/content_pipeline/src/formats.mjs FORMAT_ALLOWLIST ALREADY CARRIES `{format:'mp3', modality:'audio'}`, locked by [ADR 017] — audio is a DELIBERATELY ALLOWED pack format, not an oversight. (2) packages/core/test/fixtures/pack/v1/assets/tone/confirm.mp3 exists and is DECLARED in that pack's manifest.json assets list with its sha256; assert-pack-inert.mjs:145-159 runs live negative tests against those very bytes. (3) [ADR 019] locks audio/TTS to Google Cloud Chirp 3 HD and banks it as content — so the pack rail is the DESIGNED delivery route for audio, and it is not an app-bundle asset at all. A bundle-scoped guard watches the door nobody is going to use",
    "limbs": [
      {
        "n": 1,
        "name": "BUNDLED ASSETS",
        "what": "walk every pubspec.yaml under apps/, packages/ and tooling/bricks/**/__brick__/**, parse the `flutter: assets:` list, resolve each entry (a trailing-slash entry ships the whole directory, so walk it), and refuse any file whose BYTES are a timed-media container",
        "bytesNotExtensions": "the repo's own rule, from assert-pack-inert.mjs:13 — 'a .mp3 holding WebP bytes and a .webp holding MP3 bytes are the same defect, a file lying about itself'. Reuse tooling/content_pipeline/src/formats.mjs's matcher shapes rather than writing a second set of magic bytes; two allowlists is how they drift",
        "measuredToday": "the only `assets:` block in the tree is apps/subly/pubspec.yaml:181-182 -> assets/brand/, holding two PNGs. Zero timed media. PRINT that count. (Was THREE until 2026-08-17, when nikatru-icon.png was deleted as a byte-identical duplicate of the launcher master apps/subly/assets/icon/app_icon_1024.png. The count is written down here precisely so that a change in it is visible rather than silent — which is also why it is PRINTED and not asserted: this limb's subject is timed media, and a brand PNG leaving is not a finding.)"
      },
      {
        "n": 2,
        "name": "THE PACK FORMAT ALLOWLIST",
        "what": "assert FORMAT_ALLOWLIST carries no entry with modality 'video', and — because it DOES carry one with modality 'audio' — assert that the audio entry stays paired with limb 3's absent player, printing both facts every run",
        "halfOfThisALREADYEXISTS": "assert-pack-inert.mjs:72 already fails the build if the allowlist gains a video modality ('Video is DORMANT — 39-CHASSIS §4 cut 8'). THE VIDEO HALF IS BUILT AND WIRED (ci.yml's `content_gate` job, step `A pack is inert data in only the locked formats` — cited by step name, not by line number, which shifts whenever anything is inserted above it). The AUDIO half is not built and CANNOT take the same shape, because audio is deliberately allowed by [ADR 017] — which is precisely why limb 3 is the load-bearing one"
      },
      {
        "n": 3,
        "name": "THE PLAYER",
        "what": "no pubspec.yaml anywhere in the repo may depend on a timed-media playback plugin — video_player, chewie, media_kit, just_audio, audioplayers, audio_session or equivalents. Measured 2026-08-13: ZERO across every pubspec.yaml in the tree. PRINT the count",
        "🔴 whyThisIsTheLOADBEARINGLIMB": "a pack MP3 with no player is inert bytes; a PLAYER is what turns bytes into 'audio content' in WCAG's sense, and therefore what makes 1.2.6-1.2.9 and 1.4.7 bind. It is also the ONLY limb that can cover SC 1.2.9 Audio-only (Live), whose content arrives over the network with no asset to inspect. A guard without this limb inspects files while the criterion turns on capability"
      }
    ],
    "printEveryRun": "🔴 ALL THREE LIMBS MUST PRINT THEIR COUNTS ON EVERY RUN, PASSING OR FAILING. Every limb quantifies over a domain that is empty or near-empty today, and this repo's recorded failure mode is not a broken check but a check that silently stopped checking. A printed zero is a zero somebody can see move; an unprinted one is indistinguishable from a scanner that stopped scanning",
    "negativeTestsRequiredBeforeTrustingIt": [
      "drop a real MP3 into apps/subly/assets/brand/ and add it to the pubspec assets list -> limb 1 must fail, naming the file and the bytes",
      "add {format:'webm', modality:'video'} to FORMAT_ALLOWLIST -> limb 2 must fail (this one is already provable today against assert-pack-inert.mjs:72)",
      "add `video_player: ^2.0.0` to any pubspec.yaml -> limb 3 must fail, naming the pubspec",
      "rename a WebP to .mp4 and declare it -> limb 1 must NOT fail (bytes govern, and a guard that fires on conforming content gets disabled)",
      "🔴 AND THE ONE THAT PROVES THE SCOPE FIX: run the guard AS THE BRIEF ORIGINALLY SCOPED IT (limb 1 alone) against the tree as it stands -> it must pass, while packages/core/test/fixtures/pack/v1/assets/tone/confirm.mp3 sits declared in a manifest. That green run IS the finding, and it is the reason limbs 2 and 3 exist"
    ],
    "conditionalityMustBeRecordedOnTheClaim": "the five criteria are adopted as CONDITIONAL: each row carries `conditionalOn: vacuityGuard`, and until the guard is built each is a vacuous claim with no tripwire. Attaching them to a published §5.3.2 optional component BEFORE the guard exists would put five statements under [8]K-3 that nothing can falsify"
  },
  "aaaNotAdopted": [
    {
      "id": "unusual-words",
      "sc": "3.1.3",
      "level": "AAA",
      "title": "Unusual Words",
      "decided": "2026-08-13",
      "by": "owner 2026-08-13",
      "why": "GUIDELINE 3.1 READABLE IS NOT ADOPTED IN FULL — all four of its AAA criteria are refused for one reason. Every one is a property of CONTENT, per app, times 50 apps, times every locale the app ships. And no CI guard is possible: whether a word is 'used in an unusual or restricted way' is a judgement about meaning in context, which no parser reaches. 📌 A CRITERION YOU CANNOT GUARD IS ONE YOU SHOULD NOT CLAIM — it decays silently on the next copy edit, and a silently-decaying claim under [8]K-3 is worse than an honest non-adoption"
    },
    {
      "id": "abbreviations",
      "sc": "3.1.4",
      "level": "AAA",
      "title": "Abbreviations",
      "decided": "2026-08-13",
      "by": "owner 2026-08-13",
      "why": "same Guideline 3.1 reasoning: per app x 50 x every locale, and unguardable. Detecting an abbreviation mechanically is possible; knowing whether its expanded form is available on FIRST use in that locale's copy is not"
    },
    {
      "id": "reading-level",
      "sc": "3.1.5",
      "level": "AAA",
      "title": "Reading Level",
      "decided": "2026-08-13",
      "by": "owner 2026-08-13",
      "why": "same Guideline 3.1 reasoning, PLUS a specific blocker worth recording on its own: 3.1.5 asks for text at a lower-secondary reading level, and THERE IS NO ACCEPTED LOWER-SECONDARY READABILITY MEASURE FOR TAMIL. The criterion cannot be evaluated, let alone guarded, in a locale the factory ships. Adopting it would mean publishing a claim about text nobody can measure"
    },
    {
      "id": "pronunciation",
      "sc": "3.1.6",
      "level": "AAA",
      "title": "Pronunciation",
      "decided": "2026-08-13",
      "by": "owner 2026-08-13",
      "why": "same Guideline 3.1 reasoning. Whether pronunciation is 'essential to the meaning' of a specific word in specific copy is a per-string editorial judgement in every locale"
    },
    {
      "id": "visual-presentation",
      "sc": "1.4.8",
      "level": "AAA",
      "title": "Visual Presentation",
      "decided": "2026-08-13",
      "by": "owner 2026-08-13",
      "why": "it requires the user to be able to SELECT foreground and background colours, set line spacing to at least 1.5, set paragraph spacing to at least 1.5x line spacing, and resize to 200% without horizontal scrolling — i.e. it ships a USER THEME EDITOR. That fights brand differentiation head-on: [2]C-11 exists so 50 apps look distinguishable for store-clone detection, and a user-repaintable theme makes the brand a suggestion. The 200% resize limb is already met by the TextScaler.clamp(1.0, 2.0) limb; the colour-selection limb is the one refused"
    },
    {
      "id": "help",
      "sc": "3.3.5",
      "level": "AAA",
      "title": "Help",
      "decided": "2026-08-13",
      "by": "owner 2026-08-13",
      "why": "context-sensitive help for every form control is a CONTENT half, per app. The chassis can ship the affordance; it cannot write 50 apps' help text. ⚠️ Note the difference from SC 3.2.6 Consistent Help (Level A), which IS satisfied and is in `reclaimed`: 3.2.6 is about the ORDER of a help mechanism that exists, 3.3.5 is about help CONTENT existing at all. Also: WCAG2ICT explicitly declines to define 'context-sensitive help' for non-web ICT"
    },
    {
      "id": "link-purpose-link-only",
      "sc": "2.4.9",
      "level": "AAA",
      "title": "Link Purpose (Link Only)",
      "decided": "2026-08-13",
      "by": "owner 2026-08-13",
      "why": "it requires every link's purpose to be identifiable from its LINK TEXT ALONE, without surrounding context — which is a property of each app's own copy, per app x 50. The AA sibling 2.4.4 (Link Purpose in Context) is inside the AA claim and is unaffected"
    },
    {
      "id": "section-headings",
      "sc": "2.4.10",
      "level": "AAA",
      "title": "Section Headings",
      "decided": "2026-08-13",
      "by": "owner 2026-08-13",
      "why": "headings organise each app's own content, which is the per-app half by definition. ⚠️ WCAG2ICT also declines to define 'section' for non-web ICT, so on 5 of 6 platforms the criterion has no normative reading even if it were adopted"
    },
    {
      "id": "identify-purpose",
      "sc": "1.3.6",
      "level": "AAA",
      "title": "Identify Purpose",
      "decided": "2026-08-13",
      "by": "owner 2026-08-13",
      "why": "MARKUP-SCOPED AND UNDEFINED FOR NATIVE. 1.3.6 asks that the purpose of UI components, icons and regions be programmatically determinable — and its sufficient techniques are ARIA/microdata vocabularies with no native equivalent. WCAG2ICT declines to define 'region' for non-web ICT. So this is not a cost decision at all: on 5 of 6 platforms the criterion has no well-formed meaning to adopt"
    },
    {
      "id": "no-timing",
      "sc": "2.2.3",
      "level": "AAA",
      "title": "No Timing",
      "decided": "2026-08-13",
      "by": "owner 2026-08-13",
      "why": "🔴 IT FORECLOSES WHOLE PRODUCT CATEGORIES. 2.2.3 permits timing ONLY where it is essential to real-time events or non-interactive media — which rules out timers, quizzes, games and drills. A 50-app factory wants those categories. This is the ONE non-adoption on this list driven by PRODUCT SCOPE rather than cost or measurability, and stating that plainly is the point: it is a deliberate refusal to constrain the portfolio, not an admission of an unbuilt thing. ⚠️ Note it does NOT weaken the AA claim: SC 2.2.1 Timing Adjustable (Level A) stays inside the AA claim, so any timer a future app ships must still be adjustable, extendable or turn-off-able"
    }
  ],
  "aaaAwaitingOwner": [
    {
      "id": "interruptions",
      "sc": "2.2.4",
      "level": "AAA",
      "title": "Interruptions",
      "state": "⬜ UNCLASSIFIED — NOT NAMED BY THE 2026-08-13 BRIEF, IN EITHER LIST",
      "surfacedBy": "an agent closing the AAA set 2026-08-13; NOT an owner decision",
      "🔴 whyItIsParkedRatherThanSorted": "WCAG 2.2 has 31 AAA success criteria. The brief named 20 to adopt and 10 to refuse. 20 + 10 = 30. SC 2.2.4 Interruptions is the thirty-first and appears in NEITHER list — which is EXACTLY the defect this entire §4-F thread was opened to fix ('a criterion in neither list is the bug'), reproduced inside the fix. Filing it into a list the owner never chose would repeat the 2026-08-13 over-cut in the same direction: a classification nobody made, read later as one somebody did. So it is recorded, measured and left for the owner",
      "measured": "the domain is NOT empty — the chassis ships several author-initiated interruptions today: packages/notifications/lib/src/local_notification_service.dart, packages/platform_storage/lib/src/in_app_review_prompter.dart + packages/core/lib/src/review/review_prompter.dart (the in-app-review prompt DoD §4-A requires), the force-update wall (referenced in __brick__/…/lib/state/providers.dart and core/app_config.dart), and dialogs/snackbars/banners across 6 brick screens (settings_screen.dart alone has 8)",
      "theQuestion": "2.2.4 requires that interruptions can be POSTPONED OR SUPPRESSED by the user, except for emergencies. Push notifications arguably inherit the platform's own notification settings as that mechanism (a User Agent control). The force-update wall and the in-app-review prompt do NOT: neither can be postponed or suppressed by the user today. So a straight adoption would land as ⬜ ADOPTED AND UNMET with a real build behind it, and a straight refusal would be the first non-adoption on this list with no cost or measurability reason behind it",
      "recommendation": "ADOPT with the review-prompt half as chassis-cheap debt (a 'don't ask again' affordance is one shared control) and record the force-update wall under 2.2.4's emergency/essential reading, exactly as SC 3.2.5's row proposes to justify the same wall. But this is a RECOMMENDATION, not a decision — the row stays in this list until the owner moves it"
    }
  ]
}
