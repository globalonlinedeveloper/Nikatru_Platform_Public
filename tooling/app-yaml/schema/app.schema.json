{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nikatru.com/tooling/app-yaml/schema/app.schema.json",
  "title": "NIKATRU app declaration",
  "description": "apps/<id>/app.yaml — the ONE place an app's identity, hosts, platforms, storefront listings and legal URLs are written. catalog/apps.json, every apps/<id>/store/<channel>/{title,short-description,category,privacy-policy-url,support-url}.txt and the Apple channels' terms-of-use-url.txt are RENDERED from it by tooling/app-yaml/render.mjs; none of them is authored. [ADR 067] decision 2.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "name", "tagline", "category", "status", "hosts", "platforms", "listings", "legal", "ai", "exportCompliance"],
  "properties": {
    "id": {
      "description": "The app id, and the catalogue `slug`. The rule is contracts/app-id (lowercase letters and digits, a letter first, 2-32 characters); tooling/ci/assert-app-id-contract.mjs fails if this property accepts an id that contract refuses. Same shape assert-catalog-contract.mjs grades and the same string the directory is named.",
      "type": "string",
      "pattern": "^[a-z][a-z0-9]*$",
      "minLength": 2,
      "maxLength": 32
    },
    "name": {
      "description": "The listing title. Rendered verbatim into every store/<channel>/title.txt and into the catalogue `name`, which is what makes them the same string by construction rather than by two spellings agreeing today. Play caps a title at 30 characters and it is the tightest of the five channels.",
      "type": "string",
      "minLength": 1,
      "maxLength": 30
    },
    "shortName": {
      "description": "The ICON LABEL — what a home screen, a task switcher and a launcher show under the app's mark. It is NOT the store title: `name` is rendered into every store/<channel>/title.txt and into the catalogue, while this string is rendered into CFBundleDisplayName (iOS + macOS), android:label, the .desktop `Name=`, msix_config.display_name (which becomes uap:VisualElements/@DisplayName) and the PWA manifest `short_name` — the six places an operating system, rather than a store, has room for. OPTIONAL in this schema so an existing declaration still parses; tooling/ci/assert-app-naming.mjs is what REQUIRES it, caps it at 15 characters, requires it to share a whole token with `name`, and requires it to be unique across apps/*. Apple QA1892 asks that the device name and the store name be similar, which is why the shared token is a rule and not a preference. 15 rather than 30: iOS truncates a home-screen label around 12-13 glyphs and Android around 11-14, so anything longer is a label no user ever reads in full.",
      "type": "string",
      "minLength": 1,
      "maxLength": 15
    },
    "tagline": {
      "description": "The one-line description. Rendered into every store/<channel>/short-description.txt and into the catalogue `tagline`. Play caps a short description at 80 characters.",
      "type": "string",
      "minLength": 1,
      "maxLength": 80
    },
    "category": {
      "description": "The store category, rendered into every store/<channel>/category.txt. One word per the vocabulary the five consoles share.",
      "type": "string",
      "minLength": 2,
      "maxLength": 40
    },
    "status": {
      "description": "The catalogue status. `preview` is what a stamp writes; `live` is a deliberate promotion. assert-catalog-contract.mjs admits exactly these two.",
      "type": "string",
      "enum": ["preview", "live"]
    },
    "hosts": {
      "description": "The app's own hosts, WITHOUT a scheme — the scheme is added by the renderer so that a catalogue row cannot carry http:// by a typo. A host here needs no HAND-MADE DNS record: a Worker route with custom_domain, or the web deployment, writes it. [ADR 006]'s proxied wildcard *.nikatru.com was the old reason and [ADR 080] section 4 deleted it on 2026-09-12.",
      "type": "object",
      "additionalProperties": false,
      "required": ["web"],
      "properties": {
        "web": {
          "description": "The app's OWN ORIGIN host, e.g. subscriptiontracker-7qg.pages.dev. This is NOT the address the app is published at. Since [ADR 075] the published address is a PATH on the apex (https://nikatru.com/<id>), because a subdomain costs a payment-provider submission per app and a path costs nothing on either provider; this host survives as the origin the apex router proxies to, and as the source of the permanent 301. It needs no hand-made DNS record \u2014 the deployment binds it. ([ADR 006]'s proxied wildcard, which used to cover it, was deleted by [ADR 080] section 4 on 2026-09-12.)",
          "type": "string",
          "format": "hostname"
        },
        "pagesOrigin": {
          "description": "The Cloudflare Pages PRODUCTION ALIAS of the app's own Direct Upload project, e.g. subscriptiontracker-7qg.pages.dev -- the host the apex router actually fetches. OPTIONAL; absent means `web`. It is declared rather than derived because it is NOT DERIVABLE: Cloudflare appended a suffix when the project name was taken, so guessing <id>.pages.dev is wrong for exactly the app this repository has. It is PREFERRED over `web` because *.pages.dev is OUTSIDE the nikatru.com zone, so the zone Redirect Rule that 301s the retired subdomain cannot match the router's own proxy subrequest -- a loop there takes the whole apex dark, marketing and the frozen legal archive with it. assert-app-address-shape.mjs checks the declaration against the deployed project.",
          "type": ["string", "null"],
          "format": "hostname"
        },
        "api": {
          "description": "The app's OWN API host, present only for an app with its own Worker. Absent or null means the app calls the shared platform Worker and the catalogue `api` is rendered as the empty string — which assert-catalog-contract.mjs requires to be PRESENT and permits to be EMPTY.",
          "type": ["string", "null"],
          "format": "hostname"
        }
      }
    },
    "platforms": {
      "description": "The platforms this app is actually built for. assert-catalog-contract.mjs cross-checks it against the channels a row claims a listing on, and assert-stamp-platforms.mjs against the build lanes.",
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "type": "string", "enum": ["web", "android", "ios", "macos", "windows", "linux"] }
    },
    "listings": {
      "description": "Storefront listing URLs, keyed by tooling/channel-register.json's `storefrontKey`. The KEY SET IS NOT DECLARED HERE: the renderer derives it from the register in register order, so adding a storefront is data rather than code ([ADR 055]). A key present here overrides; a key absent renders `null`, which is the honest answer until a store issues a real URL — inventing a plausible one publishes a link a stranger follows into a 404. `web` is derived from hosts.web and MUST NOT be written here. An empty block is written as `listings:` with nothing under it, which reads as null and means every store key renders null.",
      "type": ["object", "null"],
      "additionalProperties": { "type": ["string", "null"], "format": "https-url" },
      "properties": {}
    },
    "stores": {
      "description": "OPTIONAL. The identities a store CONSOLE issues to this app, one record per store, keyed by the tooling/channel-register.json channel id. O-SECOND-APP-SIGNS-AS-THE-FIRST limb (1): the Windows identity was one value on the windows-store channel row, and tooling/bricks/app/hooks/post_gen.dart copies that row into every app it stamps, so a second app would package and submit as the first. It is a top-level key and not `listings.windows-store.identity` because `listings` types every value as an https URL. The account-wide facts (`publisher`, `publisherDisplayName`) stay on the channel row. tooling/ci/assert-store-metadata.mjs fails two apps that declare the same non-sentinel identityName, and, while the channel row still carried its own copy, a record that disagreed with it. ⏱ 2026-09-25 (B4a-2): the channel row's copy is gone and this record is the one declaration of the app's Windows identity. tooling/app-yaml/render.mjs renders msix_config.identity_name from it, every Windows identity reader reads it through tooling/ci/read-identity.mjs windowsIdentityOf, and tooling/ci/assert-channel-register.mjs §6e recomputes its packageFamilyName from the row's publisher.",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "windows-store": {
          "description": "The MSIX Package/Identity/Name and the Package Family Name Partner Center issued to THIS app (Product → Product identity). Neither can be derived from the app id. An app with no Partner Center product yet writes the channel row's `packageIdentity.notYetConfiguredSentinel`, PARTNER-CENTER-PENDING, in both fields: an invented value would package and submit under an identity nobody owns.",
          "type": "object",
          "additionalProperties": false,
          "required": ["identityName", "packageFamilyName"],
          "properties": {
            "identityName": {
              "description": "Letters, digits, `.` and `-`: the characters the one issued identity and the sentinel both use. The pattern refuses a space or an underscore typed into the field; it cannot tell a wrong name from a right one, which is what the equality checks are for.",
              "type": "string",
              "pattern": "^[A-Za-z0-9.-]+$"
            },
            "packageFamilyName": {
              "description": "`<identityName>_<publisher id>`, where the publisher id is 13 lower-case Crockford base32 characters (tooling/ci/assert-channel-register.mjs §6e derives it from the publisher), or the sentinel.",
              "type": "string",
              "pattern": "^(PARTNER-CENTER-PENDING|[A-Za-z0-9.-]+_[0-9a-hjkmnp-tv-z]{13})$"
            }
          }
        }
      }
    },
    "legal": {
      "description": "The listing URLs. privacyPolicyUrl and supportUrl go into every channel's tree; termsUrl (added 2026-09-24, O-APPLE-LISTING-HAS-NO-EULA) goes only into the channels whose register row names terms-of-use-url.txt in its additionalFiles, today the two Apple rows. All three are compared to tooling/channel-register.json's storeMetadataContract.portfolioUrls by assert-store-metadata.mjs, so a fork here fails the build rather than pointing a store reviewer at somebody else's policy.",
      "type": "object",
      "additionalProperties": false,
      "required": ["privacyPolicyUrl", "supportUrl", "termsUrl"],
      "properties": {
        "privacyPolicyUrl": { "type": "string", "format": "https-url" },
        "supportUrl": { "type": "string", "format": "https-url" },
        "termsUrl": { "type": "string", "format": "https-url" }
      }
    },
    "markets": {
      "description": "The markets this app is offered in. Optional, and rendered into the catalogue row only when declared — the shipped row for the one live app carries none, and adding an empty field would change bytes the live site serves for no reason. [pipeline K-1/K-16].",
      "type": "array",
      "uniqueItems": true,
      "minItems": 1,
      "items": { "type": "string", "minLength": 2 }
    },
    "billing": {
      "description": "OPTIONAL. Present only for an app that sells through a STORE billing rail on mobile — [ADR 067] decision 7, [ADR 039] D2/D3/D5. Absent means the app sells through the merchant-of-record hosted checkout on the channels that permit it and sells nothing on Play or the App Store, which is [ADR 059] shape A and is true of every app today. Declaring it is an OPT-IN with consequences a guard holds: tooling/ci/assert-app-yaml.mjs limb 6 requires the app's pubspec to depend on nikatru_billing_revenuecat, its store/android-play/data-safety.json to swear Purchase history, and its store/ios-appstore/privacy-manifest.json to carry the SDK's rows — and requires the reverse, so the dependency cannot arrive without the declaration either.",
      "type": "object",
      "additionalProperties": false,
      "required": ["mobileIap"],
      "properties": {
        "mobileIap": {
          "description": "The store IAP configuration. Holds NO SECRET: the RevenueCat app ids below are public identifiers, and the SDK's public key is supplied at runtime by the app rather than written here — a key in a declaration is a key in git.",
          "type": "object",
          "additionalProperties": false,
          "required": ["provider", "entitlementId", "revenuecatAppIds"],
          "properties": {
            "provider": {
              "description": "The IAP provider. One value today, and an enum rather than a free string so a second provider is a schema change somebody reviews rather than a typo that reads as adoption.",
              "type": "string",
              "enum": ["revenuecat"]
            },
            "entitlementId": {
              "description": "The entitlement identifier configured in the provider's dashboard, e.g. 'pro'. The SAME name our own entitlement record uses, because the client only reports what the store says and the server is what grants.",
              "type": "string",
              "pattern": "^[a-z][a-z0-9_]*$",
              "minLength": 2,
              "maxLength": 40
            },
            "revenuecatAppIds": {
              "description": "The provider's own app id per platform. Both are required: an app that declares mobile IAP with only one is an app whose other store build would configure the SDK against nothing and refuse every purchase with no reason a user could act on.",
              "type": "object",
              "additionalProperties": false,
              "required": ["android", "ios"],
              "properties": {
                "android": { "type": "string", "minLength": 4, "maxLength": 64 },
                "ios": { "type": "string", "minLength": 4, "maxLength": 64 }
              }
            }
          }
        }
      }
    },
    "audience": {
      "description": "The declared audience. Optional in the same way as `markets`. ADR 068 sets the floor at 18 for every NIKATRU product, so nothing here may describe a child audience.",
      "type": "string",
      "minLength": 2
    },
    "ai": {
      "description": "REQUIRED of every app, `false` included: a claim with neither a derivation nor an owner fails, and an absent key would read as 'no' while nobody said so. O-PLAY-AI-CONTENT-REPORTING. Google Play's AI-Generated Content policy (answer/13985936, read 2026-09-18) requires an in-app report control in any app that generates content with AI. tooling/ci/assert-app-yaml.mjs limb 7 holds the consequences both ways: true requires AppConfig.generatesAiContent, the wired ReportContentDialog, and a Data safety answer for table:platform_db.content_reports; false requires the content-rating `generates-ai-content` dependency-tells claim, which fails the day a direct AI package arrives.",
      "type": "object",
      "additionalProperties": false,
      "required": ["generatesContent"],
      "properties": {
        "generatesContent": {
          "description": "Whether the app generates content with AI at runtime — text, images, voice or video a model produces for the user. Out of Play's scope: an app that only hosts AI content, summarises non-AI content, or uses AI to improve an existing productivity feature (answer/14094294).",
          "type": "boolean"
        }
      }
    },
    "exportCompliance": {
      "description": "REQUIRED of every app, `false` included, for the reason `ai` is: an optional field the renderer defaulted to false would be a legal declaration nobody made. O-APPLE-PLIST-KEYS-UNRENDERED. tooling/app-yaml/render.mjs renders `usesNonExemptEncryption` into ITSAppUsesNonExemptEncryption in both Apple Info.plists, and tooling/ci/assert-app-yaml.mjs limb 8 (export-compliance consistency) holds a `false` to the code: no shipped first-party Dart may import a cipher package or name a cipher class, and the app's pubspec may carry no direct dependency on one.",
      "type": "object",
      "additionalProperties": false,
      "required": ["usesNonExemptEncryption", "basis"],
      "properties": {
        "usesNonExemptEncryption": {
          "description": "Apple's export-compliance answer, as the build ships it. `false` means every use of cryptography in the app falls inside Apple's exemption (OS-provided HTTPS, authentication, digital signatures, hashing). The owner answers App Store Connect's export questions consistently with it.",
          "type": "boolean"
        },
        "basis": {
          "description": "The sentence a reviewer reads: what was measured and why the answer holds. A stamped app carries the brick's placeholder, marked to be re-measured before its first Apple upload.",
          "type": "string",
          "minLength": 20
        }
      }
    }
  }
}
