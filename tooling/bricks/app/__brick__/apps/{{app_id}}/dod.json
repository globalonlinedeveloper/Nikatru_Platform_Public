{
  "_readme": [
    "THIS APP'S DONE-RECORD — [pipeline N-2] Private/requirements/ (was pipeline/06-app-build.md, folded into the JSON spec 2026-08-15)",
    "",
    "Stamped with the app. It answers 'is this app done?' as a QUERY rather than a memory, and",
    "tooling/ci/assert-app-dod.mjs fails the build on it. The right-hand side of every set comparison",
    "below is tooling/dod-register.json — the DoD items, and the four human-review rows.",
    "",
    "IT IS AN OUTPUT, NEVER AN INPUT. 39-CHASSIS §4 cut 4 stands: mason vars are the stamp's only",
    "input contract. This file describes VERIFICATION STATE; it never feeds the stamp.",
    "",
    "status:",
    "  stamped        a fresh stamp. Shape + the mechanical rows are enforced; the human verdicts and",
    "                 the selection gates are recorded as pending, and their blanks do not fail.",
    "  claiming-done  everything is enforced. An app the public catalogue advertises as 'live' while",
    "                 this still says 'stamped' FAILS THE BUILD — that cross-check is what stops",
    "                 'stamped' becoming a permanent excuse.",
    "",
    "features: the six the CHASSIS itself ships, so a freshly stamped app carries six REAL rows",
    "rather than `features: []`. An app adds its own here as it builds them. Each row's `test` must",
    "resolve to a real declaration with a non-empty body, `effect` must still name a symbol that is",
    "there, and `mutation.date` EXPIRES: change the implementation and the row goes red until the",
    "mutation is re-run and the date moved. That is the difference between an event and a state.",
    "",
    "🔴 EVERY MUTATION BELOW WAS ACTUALLY RUN, on a real mason stamp, with `flutter test`, and the",
    "named test was observed RED. Two candidate rows were REJECTED at that step rather than recorded:",
    "a `themeMode` row whose mutation the suite did not notice, and an account-deletion row whose test",
    "drives the seam in packages/auth_supabase rather than anything under this app — an effect anchor",
    "outside the app cannot be re-dated against the app's own history. Account deletion is covered",
    "instead by the chassis property `account-deletion-works`, which carries six anchors of its own.",
    "A row whose mutation nobody watched is exactly the 'proven once in a terminal' claim this field",
    "exists to replace."
  ],
  "app": "{{app_id}}",
  "status": "stamped",
  "items": {
    "A": {
      "claim": "held",
      "note": "the chassis ships the screen set; assert-screen-set.mjs holds the 22-screen register with anchors"
    },
    "B": {
      "claim": "pending",
      "note": "human — the four-states verdict per data screen, recorded in humanReview"
    },
    "C": {
      "claim": "held",
      "note": "property ui-invariants-inherited pins the five Material window classes at 600/840/1200/1600 on the pure resolver AND pumps AppScaffold across the 600 boundary (rail at 768, bar at 375); responsive_width_test.dart measures the three ContentPane-capped screens at 375/768/1280(/1920)"
    },
    "D": {
      "claim": "held",
      "note": "properties theme-triplet-supplied + theme-mode-persisted, both limbs anchored (write AND read-back)"
    },
    "E": {
      "claim": "held",
      "note": "assert-no-hardcoded-strings.mjs; the chassis ships a second locale so the seam is really exercised"
    },
    "F": {
      "claim": "held",
      "note": "the mechanical WCAG limbs — text-scale clamp, 48px targets, non-empty icon labels"
    },
    "G": {
      "claim": "held",
      "note": "the stamp generates this app's OWN icons; assert-stamp-brand-assets.mjs compares against the live SDK's stock assets"
    },
    "H": {
      "claim": "held",
      "note": "the app_brick lane resolves, analyzes and tests this app on every push; the feature rows below carry the effect anchors"
    }
  },
  "features": [
    {
      "name": "sign in",
      "primaryAction": "submitting the sign-in form authenticates and moves the user off it",
      "test": "signing in through the FORM moves the user off it",
      "effect": "lib/features/auth/sign_in_screen.dart:signInWithEmail",
      "mutation": {
        "symbol": "lib/features/auth/sign_in_screen.dart:signInWithEmail",
        "observedRed": "signing in through the FORM moves the user off it",
        "note": "Commenting out `await auth.signInWithEmail(email: email, password: _password.text)` in `_signIn` leaves the form visually succeeding while no session is ever created, so the router redirect never fires: 174 -> 173 passing, and the ONE failure was the named test, `property: auth-redirect-follows-session signing in through the FORM moves the user off it`. Pre-mutation md5 F5B36F41960B6D093430DCFD41B4739E, identical after restore. Re-run 2026-09-06 on a freshly stamped probe, because [ADR 067] decision 2 (unit chassis-l10n) moved 149 of the brick's 161 arb keys into packages/design_system as ChassisLocalizations and re-pointed every `l10n.` pass in this file — an edit to the PROBED FILE expires this row exactly as a behavioural one does, and that is the guard behaving correctly. TOOLCHAIN: Flutter 3.47.2 at C:\\flutter-3.47.2, the version ci.yml pins, NOT the host default 3.44.9. Baseline before every mutation: `flutter analyze --no-fatal-infos` \"No issues found!\" and `flutter test` 174 passing. `flutter analyze` also reported \"No issues found!\" UNDER the mutation, so the red is an assertion and not a compile error. The file was restored from a pre-mutation copy and MD5-verified byte-identical on both sides, and the suite was re-observed green (174) after all six. RE-RUN 2026-09-15 on a freshly stamped probe, because [ADR 082] §5 Part B edited the PROBED FILE: `SignInView` now takes `ageSignals: ref.watch(ageSignalSourceProvider)` (the store age signal read before Sign in with Apple). The call is now the arrow body `onSignIn: (String email, String password) => auth.signInWithEmail(email: email, password: password)`; the mutation replaced that call with `Future<void>.value()`. Baseline: `flutter test` 183 passing (`flutter analyze --no-fatal-infos` reports 3 pre-existing `unnecessary_underscores` infos in lib/state/providers.dart, none in this file). Under the mutation `flutter analyze` on this file said \"No issues found!\", and `flutter test` went 183 -> 182 with the ONE failure `property: auth-redirect-follows-session signing in through the FORM moves the user off it`. Pre-mutation md5 8BACE09F59B2DA189D1D5BD322193824, identical after restore from the copy; suite re-observed green (183). RE-RUN 2026-09-25 on a freshly stamped probe, because batch b12 (Google sign-in client, behind the flag) edited the PROBED FILE: bdb674b5 added `showGoogleButton` and `onSignInWithGoogle` to the `SignInView` call, and the mutation again replaced the `onSignIn` arrow body `auth.signInWithEmail(email: email, password: password)` with `Future<void>.value()`; baseline `flutter analyze --no-fatal-infos` exited 0 with 4 pre-existing `unnecessary_underscores` infos in lib/state/providers.dart and none in this file, and `flutter test` passed 188; under the mutation `flutter analyze` on this file said \"No issues found!\" and `flutter test` went 188 -> 187 with the ONE failure `property: auth-redirect-follows-session signing in through the FORM moves the user off it`; pre-mutation md5 FF0DD29C439C89002FEEF1AB5A211F33, byte-identical to the brick source and identical after restore from the copy, and the suite was re-observed green (188); TOOLCHAIN: Flutter 3.47.2 at C:\\flutter-3.47.2, the only SDK on this host, while ci.yml pins 3.47.5, so this run is one patch release behind CI's (its `pub get` also added build and platform excludes to the probe's analysis_options.yaml, which leave lib/ and test/ analyzed), and mason_cli 0.1.3 while ci.yml activates 0.1.4.",
        "date": "2026-09-25"
      }
    },
    {
      "name": "sign out",
      "primaryAction": "ending the session puts the user back on the sign-in form",
      "test": "signing out puts the user back on the form",
      "effect": "lib/core/router.dart:refreshListenable",
      "mutation": {
        "symbol": "lib/core/router.dart:refreshListenable",
        "observedRed": "signing out puts the user back on the form",
        "date": "2026-09-06",
        "note": "Deleting `refreshListenable: ref.watch(routerRefreshProvider)` from the GoRouter leaves a router that never re-evaluates its redirect when the session ends: 174 -> 159 passing, 15 red, and among them the named test, `property: auth-redirect-follows-session signing out puts the user back on the form`. The blast radius is wide ON PURPOSE and is itself the finding — every routed property depends on the redirect being live, which is why this row and feature[0] are different claims about the same flow. Pre-mutation md5 3A74E31704EB924775928A921E38AF08, identical after restore. Re-run 2026-09-06 on a freshly stamped probe, because [ADR 067] decision 2 (unit chassis-l10n) moved 149 of the brick's 161 arb keys into packages/design_system as ChassisLocalizations and re-pointed every `l10n.` pass in this file — an edit to the PROBED FILE expires this row exactly as a behavioural one does, and that is the guard behaving correctly. TOOLCHAIN: Flutter 3.47.2 at C:\\flutter-3.47.2, the version ci.yml pins, NOT the host default 3.44.9. Baseline before every mutation: `flutter analyze --no-fatal-infos` \"No issues found!\" and `flutter test` 174 passing. `flutter analyze` also reported \"No issues found!\" UNDER the mutation, so the red is an assertion and not a compile error. The file was restored from a pre-mutation copy and MD5-verified byte-identical on both sides, and the suite was re-observed green (174) after all six."
      }
    },
    {
      "name": "finish onboarding",
      "primaryAction": "the last onboarding step records the choice and first run does not come back",
      "test": "finishing it moves the user on, and it does not come back",
      "effect": "lib/features/firstrun/onboarding_screen.dart:onboardingSeenProvider",
      "mutation": {
        "symbol": "lib/features/firstrun/onboarding_screen.dart:onboardingSeenProvider",
        "observedRed": "finishing it moves the user on, and it does not come back",
        "date": "2026-09-06",
        "note": "Deleting `await ref.read(onboardingSeenProvider.notifier).set(true)` leaves the last step navigating while recording nothing, so first run comes back on the next launch: 174 -> 173 passing, and the ONE failure was the named test, `property: onboarding-shown-once finishing it moves the user on, and it does not come back`. Pre-mutation md5 05665F307C2CA4AE9DBE5B92DF67FFAE, identical after restore. Re-run 2026-09-06 on a freshly stamped probe, because [ADR 067] decision 2 (unit chassis-l10n) moved 149 of the brick's 161 arb keys into packages/design_system as ChassisLocalizations and re-pointed every `l10n.` pass in this file — an edit to the PROBED FILE expires this row exactly as a behavioural one does, and that is the guard behaving correctly. TOOLCHAIN: Flutter 3.47.2 at C:\\flutter-3.47.2, the version ci.yml pins, NOT the host default 3.44.9. Baseline before every mutation: `flutter analyze --no-fatal-infos` \"No issues found!\" and `flutter test` 174 passing. `flutter analyze` also reported \"No issues found!\" UNDER the mutation, so the red is an assertion and not a compile error. The file was restored from a pre-mutation copy and MD5-verified byte-identical on both sides, and the suite was re-observed green (174) after all six. RE-RUN AGAIN 2026-09-06 ([ADR 067] phase 2, unit screens-money-settings), because the PROBED FILE changed a second time that day: the carousel body moved into `package:nikatru_chassis_screens/firstrun/onboarding_screen.dart` and this file became the adapter (194 -> 82 brick lines). An edit to the probed file expires this row whether or not the behaviour moved, which is the guard behaving correctly. Same mutation, same toolchain (Flutter 3.47.2 at C:\\flutter-3.47.2), freshly stamped probe: baseline `flutter analyze --no-fatal-infos` \"No issues found!\" and `flutter test` 174 passing; under the mutation analyze still said \"No issues found!\" and the suite went 174 -> 173 with the ONE red being the named test. Land-checked: the deleted string occurred exactly once before and zero times after. Pre-mutation md5 CF2A04D70979543FB4D960C6AB2B7203, identical after restore, and the suite re-observed green (174)."
      }
    },
    {
      "name": "edit profile",
      "primaryAction": "saving a display name reaches the identity seam and the UI shows the new value",
      "test": "editing it in the REAL app updates what the user sees",
      "effect": "lib/features/settings/settings_screen.dart:updateProfile",
      "mutation": {
        "symbol": "lib/features/settings/settings_screen.dart:updateProfile",
        "observedRed": "editing it in the REAL app updates what the user sees",
        "note": "Deleting the `await ref.read(authRepositoryProvider).updateProfile(displayName: displayName.trim())` call leaves the save button wired to nothing: 174 -> 173 passing, and the ONE failure was the named test, `property: profile-edit-works editing it in the REAL app updates what the user sees`. Pre-mutation md5 CF6A54C9A0F50CF0B55F76E929FC21D7, identical after restore. Re-run 2026-09-06 on a freshly stamped probe, because [ADR 067] decision 2 (unit chassis-l10n) moved 149 of the brick's 161 arb keys into packages/design_system as ChassisLocalizations and re-pointed every `l10n.` pass in this file — an edit to the PROBED FILE expires this row exactly as a behavioural one does, and that is the guard behaving correctly. TOOLCHAIN: Flutter 3.47.2 at C:\\flutter-3.47.2, the version ci.yml pins, NOT the host default 3.44.9. Baseline before every mutation: `flutter analyze --no-fatal-infos` \"No issues found!\" and `flutter test` 174 passing. `flutter analyze` also reported \"No issues found!\" UNDER the mutation, so the red is an assertion and not a compile error. The file was restored from a pre-mutation copy and MD5-verified byte-identical on both sides, and the suite was re-observed green (174) after all six. RE-RUN AGAIN 2026-09-06 ([ADR 067] phase 2, unit screens-money-settings), because the PROBED FILE changed a second time that day: the settings page body moved into `package:nikatru_chassis_screens/settings/settings_screen.dart` and this file became the adapter (968 -> 616 brick lines), with `_saveProfile` and the `updateProfile` call among the things that STAYED because they are Riverpod. Same mutation, same toolchain (Flutter 3.47.2 at C:\\flutter-3.47.2), freshly stamped probe: baseline analyze \"No issues found!\" and 174 passing; under the mutation analyze still said \"No issues found!\" and the suite went 174 -> 173 with the ONE red being the named test. Land-checked: the deleted call occurred exactly once before and zero times after. Pre-mutation md5 D5A526C6242788111ED4ED32277F6C82, identical after restore, and the suite re-observed green (174). RE-RUN 2026-09-15 ([ADR 081], PR #762), because the PROBED FILE changed again: deleteAccountFailureMessage gained the `AccountDeletionOutcome.pending` case (the 202 erasure_pending answer). Same mutation, freshly stamped probe (`mason make app -c tooling/bricks/app/_probe_vars.json`), Flutter 3.47.2 at C:\\flutter-3.47.2: baseline `flutter analyze --no-fatal-infos` 4 infos (unnecessary_underscores in lib/state/providers.dart, none an error) and `flutter test` 181 passing; the deleted `await ref.read(authRepositoryProvider).updateProfile(displayName: displayName.trim());` occurred exactly once before and zero times after; under the mutation analyze still 4 infos and the suite went 181 -> 180 with the ONE red `chassis_properties_test.dart: property: profile-edit-works editing it in the REAL app updates what the user sees`. Pre-mutation md5 F0B09CADFD88EF4618CCCD8821835647, identical after restore, and the suite re-observed 181 passing. RE-RUN 2026-09-18 (O-PLAY-AI-CONTENT-REPORTING), because the PROBED FILE changed again: the adapter gained `onReportContent:` and `showReportContentDialog`. Same mutation, freshly stamped probe, Flutter 3.47.2 at C:\\flutter-3.47.2: baseline `flutter analyze --no-fatal-infos` 4 infos (the same unnecessary_underscores) and `flutter test` 185 passing; the deleted call occurred exactly once before and zero times after; under the mutation analyze still 4 infos and the suite went 185 -> 184 with the ONE red `chassis_properties_test.dart: property: profile-edit-works editing it in the REAL app updates what the user sees`. Pre-mutation md5 E4488C56E8D5375719C09870D39B0AFD, identical after restore, and the suite re-observed 185 passing. RE-RUN 2026-09-24 (AUTH-LOGOUT-ALL), because the PROBED FILE changed again: the adapter gained `onSignOutEverywhere:` and `_signOut` took a `core.SignOutScope`. Same mutation, freshly stamped probe (`mason make app -c tooling/bricks/app/_probe_vars.json`, mason_cli 0.1.4), Flutter 3.47.5, the version tooling/versions.json pins: baseline `flutter analyze --no-fatal-infos` 4 infos (the same unnecessary_underscores) and `flutter test` 187 passing; the deleted `await ref.read(authRepositoryProvider).updateProfile(displayName: displayName.trim());` occurred exactly once before and zero times after; under the mutation analyze still 4 infos and the suite went 187 -> 186 with the ONE red `chassis_properties_test.dart: property: profile-edit-works editing it in the REAL app updates what the user sees`. Pre-mutation md5 8C1EFD897820DE2368B21589ACA379EF, identical after restore, and the suite re-observed 187 passing. RE-RUN 2026-09-25 (AUTH-LOGOUT-ALL, PR #931), because the 2026-09-24 run was taken in the drafting session and the PROBED FILE's commit landed on 2026-09-25 UTC, which assert-app-dod reads as code newer than the record. The probed file is the same code as that run; parent ruling L2 put the all-devices confirm in packages/chassis_screens and added a Cancel test to test/sign_out_everywhere_test.dart, hence 188. Same mutation, freshly stamped probe (`mason make app -c tooling/bricks/app/_probe_vars.json`), Flutter 3.47.2 at C:\\flutter-3.47.2: baseline `flutter test` 188 passing; the deleted three-line `await ref.read(authRepositoryProvider).updateProfile(displayName: displayName.trim());` statement occurred exactly once before and zero times after; under the mutation `flutter analyze --no-fatal-infos` 4 infos (the same unnecessary_underscores, none an error or warning) and the suite went 188 -> 187 with the ONE red `chassis_properties_test.dart: property: profile-edit-works editing it in the REAL app updates what the user sees`. Pre-mutation md5 CE29090AFBC4D5FCBE58E336AB0FB187, identical after restore, and the suite re-observed 188 passing. RE-RUN 2026-09-26 (O-LINK-LAUNCHER-SEAM-UNOWNED), because the PROBED FILE changed: the `url_launcher` call became the app's one launcher (`externalLinks.openUrl` in settings, `updateLinkLauncher(url).openUrl` on the force-update wall). Same mutation, freshly stamped probe (`mason make app -c tooling/bricks/app/_probe_vars.json -o . --on-conflict overwrite`), Flutter 3.47.5 at C:\\flutter-3.47.5 (the pin in tooling/versions.json), mason_cli 0.1.3: baseline `flutter analyze --no-fatal-infos` 4 infos (the same unnecessary_underscores in lib/state/providers.dart, none an error or warning) and `flutter test` 190 passing; the deleted three-line `await ref.read(authRepositoryProvider).updateProfile(displayName: displayName.trim());` statement occurred exactly once before and zero times after (`grep -c '.updateProfile(displayName: displayName.trim());' apps/probe/lib/features/settings/settings_screen.dart` 1 -> 0); under the mutation analyze reported the same 4 infos and nothing in this file, and the suite went 190 -> 189 with the ONE red `chassis_properties_test.dart: property: profile-edit-works editing it in the REAL app updates what the user sees`. Pre-mutation md5 8C9F41E8EF1AFD8779F9C0FCE8BA96C6, identical after restore, and the suite re-observed 190 passing.",
        "date": "2026-09-26"
      }
    },
    {
      "name": "consent prompt",
      "primaryAction": "the app root asks for analytics consent and Allow really opens the rail",
      "test": "the app root asks for consent, and Allow opens the rail",
      "effect": "lib/app.dart:AnalyticsGate",
      "mutation": {
        "symbol": "lib/app.dart:AnalyticsGate",
        "observedRed": "the app root asks for consent, and Allow opens the rail",
        "date": "2026-09-26",
        "note": "Replacing `AnalyticsGate` at the app root with a pass-through wrapper leaves the app running and nothing ever asking: 174 -> 164 passing, and among the ten reds is the named test, `property: analytics-on-switch-mounted the app root asks for consent, and Allow opens the rail`. The blast radius is wide ON PURPOSE and is itself the finding — the consent gate is upstream of the analytics lifecycle and of the notification-tap observation, so removing it silences three properties at once. Pre-mutation md5 873F1B9EB60731EFD8354228CF1D0873, identical after restore. Re-run 2026-09-06 on a freshly stamped probe, because [ADR 067] decision 2 (unit chassis-l10n) moved 149 of the brick's 161 arb keys into packages/design_system as ChassisLocalizations and re-pointed every `l10n.` pass in this file — an edit to the PROBED FILE expires this row exactly as a behavioural one does, and that is the guard behaving correctly. TOOLCHAIN: Flutter 3.47.2 at C:\\flutter-3.47.2, the version ci.yml pins, NOT the host default 3.44.9. Baseline before every mutation: `flutter analyze --no-fatal-infos` \"No issues found!\" and `flutter test` 174 passing. `flutter analyze` also reported \"No issues found!\" UNDER the mutation, so the red is an assertion and not a compile error. The file was restored from a pre-mutation copy and MD5-verified byte-identical on both sides, and the suite was re-observed green (174) after all six. RE-RUN AGAIN 2026-09-07 ([ADR 067] phase 2, unit app-shell), because the PROBED FILE changed a third time: `MaterialApp.router`, the clamped text scaling, the force-update gate, the consent card, the consent scrim, the offline banner and the four-state lifecycle flush moved into `package:nikatru_chassis_screens/shell/app_shell.dart` as `NikatruApp` and the four surfaces beside it (570 -> 349 brick lines), and `AnalyticsGate` became the ConsumerStatefulWidget the shell is handed rather than a widget mounted with `child:`. THE MUTATION MOVED WITH IT and is now the shell callback: `shell: (Widget routed) => AnalyticsGate(` replaced by a `Builder` pass-through, which leaves the gate DECLARED and unmounted - the [pipeline C-6] shape verbatim. Same toolchain (Flutter 3.47.2 at C:\\\\flutter-3.47.2, the version ci.yml pins, NOT the host default 3.44.9), freshly stamped probe: baseline `flutter analyze --no-fatal-infos` \"No issues found!\" and `flutter test` 174 passing; under the mutation analyze still said \"No issues found!\" - so the red is an assertion and not a compile error - and the suite went 174 -> 164 with TEN reds, among them the named test `property: analytics-on-switch-mounted the app root asks for consent, and Allow opens the rail`. The blast radius is the same three properties as before and for the same reason. Land-checked: `grep -c '=> AnalyticsGate(' apps/probe/lib/app.dart` was 1 before and 0 after. Pre-mutation md5 8A8F0AAEC50422379CBF0C709025F709, identical after restore. CORRECTION APPENDED 2026-09-07 ([ADR 067] phase 2b, unit app-shell-residues), beside the original wording and NOT over it (C-FROZEN-RECORD-SUPERSEDED, [ADR 053] rule 2): the phrase `(570 -> 349 brick lines)` above is WRONG BY TWO. Re-measured on the tree the row describes, at the app-shell merge 5c87a753: `MSYS_NO_PATHCONV=1 git show 5c87a753:tooling/bricks/app/__brick__/apps/{{app_id}}/lib/app.dart | wc -l` gives 347, and `wc -l` on the same path in a checkout at that SHA gives 347. 347 is also what `tooling/chassis-ledger.json`'s row for this file records, what the app-shell report's sections 0, 2 and 13 say, and what both second-pass reviews independently re-derived. The correct reading of this note is therefore 570 -> 347 brick lines, a callSiteDelta of -223. Nothing the mutation measured changes: the mutation, the toolchain, the freshly stamped probe, the 174 -> 164 result, the ten reds, the named test and the md5 are all as recorded above. The two-line discrepancy was in the PROSE of this note alone, and it is corrected here rather than rewritten because a dated mutation record is the artefact a later reader cannot re-derive. RE-RUN AGAIN 2026-09-16 (O-SIWA-TOKEN-NOT-REVOKED-ON-DELETE), because the PROBED FILE changed again: the app root now carries `ref.watch(appleTokenKeeperProvider);` — the Sign in with Apple refresh-token keeper, WATCHED FOR ITS EFFECT, because a Riverpod provider nobody reads is never created and the listener would silently not exist. An edit to the probed file expires this row exactly as a behavioural one does, and that is the guard behaving correctly. Same toolchain (Flutter 3.47.2 at C:\\flutter-3.47.2, the version ci.yml pins, NOT the host default), freshly stamped probe (`mason make app -c tooling/bricks/app/_probe_vars.json -o . --on-conflict overwrite`): baseline `flutter test` 185 passing, and `flutter analyze --no-fatal-infos` reports 4 pre-existing `unnecessary_underscores` infos in lib/state/providers.dart and NOTHING in this file. Same mutation as 2026-09-07: `shell: (Widget routed) => AnalyticsGate(` replaced by the pass-through `shell: (Widget routed) => _NotificationTapGate(child: _OfflineBanner(child: routed)),`, which leaves the gate DECLARED and unmounted — the [pipeline C-6] shape verbatim. Under the mutation analyze reported the SAME 4 infos and nothing in app.dart, so the red is an assertion and not a compile error, and `flutter test` went 185 -> 175 with TEN reds, among them the named test `property: analytics-on-switch-mounted the app root asks for consent, and Allow opens the rail`. The blast radius is the same three properties as before and for the same reason. Land-checked: `grep -c '=> AnalyticsGate(' apps/probe/lib/app.dart` was 1 before and 0 after. Pre-mutation md5 43A066998D857E7811A8B85472D8B6B3, identical after restore from the copy; the suite was re-observed green (185). RE-RUN 2026-09-26 (O-LINK-LAUNCHER-SEAM-UNOWNED), because the PROBED FILE changed: the `url_launcher` call became the app's one launcher (`externalLinks.openUrl` in settings, `updateLinkLauncher(url).openUrl` on the force-update wall). Same toolchain rule (Flutter 3.47.5 at C:\\flutter-3.47.5 (the pin in tooling/versions.json), mason_cli 0.1.3), freshly stamped probe (`mason make app -c tooling/bricks/app/_probe_vars.json -o . --on-conflict overwrite`): baseline `flutter test` 190 passing, and `flutter analyze --no-fatal-infos` reports 4 pre-existing `unnecessary_underscores` infos in lib/state/providers.dart and NOTHING in this file. Same mutation: `shell: (Widget routed) => AnalyticsGate(...),` replaced by the pass-through `shell: (Widget routed) => _NotificationTapGate(child: _OfflineBanner(child: routed)),`. Under the mutation analyze reported the SAME 4 infos and nothing in app.dart, so the red is an assertion and not a compile error, and `flutter test` went 190 -> 180 with TEN reds, among them the named test `property: analytics-on-switch-mounted the app root asks for consent, and Allow opens the rail`; the other nine are analytics-lifecycle-complete RUN 1 and RUN 2, notification-tap-observed and review-prompt-gated in chassis_properties_test.dart, and five in consent_prompt_real_surface_test.dart. Land-checked: `grep -c '=> AnalyticsGate(' apps/probe/lib/app.dart` was 1 before and 0 after. Pre-mutation md5 052E332AE239A629AECBB568F6C29BB7, identical after restore from the copy; the suite was re-observed green (190)."
      }
    },
    {
      "name": "dark mode",
      "primaryAction": "the dark theme is a genuinely dark scheme, not the light one under another name",
      "test": "light and dark are genuinely different schemes",
      "effect": "lib/app.dart:darkTheme",
      "mutation": {
        "symbol": "lib/app.dart:brightness: Brightness.dark",
        "observedRed": "light and dark are genuinely different schemes",
        "date": "2026-09-26",
        "note": "Flipping `brightness: Brightness.dark` to `Brightness.light` in the dark theme leaves a perfectly valid app with two IDENTICAL schemes: 174 -> 173 passing, and the ONE red is the named test, `property: theme-triplet-supplied light and dark are genuinely different schemes`. A single, precise red is what makes this row a different claim from feature[4]'s — the same file, two independent properties. Pre-mutation md5 873F1B9EB60731EFD8354228CF1D0873, identical after restore. Re-run 2026-09-06 on a freshly stamped probe, because [ADR 067] decision 2 (unit chassis-l10n) moved 149 of the brick's 161 arb keys into packages/design_system as ChassisLocalizations and re-pointed every `l10n.` pass in this file — an edit to the PROBED FILE expires this row exactly as a behavioural one does, and that is the guard behaving correctly. TOOLCHAIN: Flutter 3.47.2 at C:\\flutter-3.47.2, the version ci.yml pins, NOT the host default 3.44.9. Baseline before every mutation: `flutter analyze --no-fatal-infos` \"No issues found!\" and `flutter test` 174 passing. `flutter analyze` also reported \"No issues found!\" UNDER the mutation, so the red is an assertion and not a compile error. The file was restored from a pre-mutation copy and MD5-verified byte-identical on both sides, and the suite was re-observed green (174) after all six. RE-RUN AGAIN 2026-09-07 ([ADR 067] phase 2, unit app-shell), because the PROBED FILE changed a third time - see the consent-prompt row for what moved. `theme:` and `darkTheme:` STAYED in the brick: they carry the stamped `const Color(0xFF<seed_hex>)`, which is mustache and cannot leave, and `assert-stamp-properties.mjs` anchors both reads here. Same mutation, same toolchain (Flutter 3.47.2 at C:\\\\flutter-3.47.2), freshly stamped probe: baseline analyze \"No issues found!\" and 174 passing; under the mutation analyze still said \"No issues found!\" and the suite went 174 -> 173 with the ONE red being the named test `property: theme-triplet-supplied light and dark are genuinely different schemes`. A single, precise red is still what makes this row a different claim from the consent-prompt one - the same file, two independent properties. Land-checked: `grep -c 'brightness: Brightness.dark' apps/probe/lib/app.dart` was 1 before and 0 after. Pre-mutation md5 8A8F0AAEC50422379CBF0C709025F709, identical after restore. RE-RUN AGAIN 2026-09-16 (O-SIWA-TOKEN-NOT-REVOKED-ON-DELETE), because the PROBED FILE changed again — see the consent-prompt row for what arrived (`ref.watch(appleTokenKeeperProvider);` at the app root). `theme:` and `darkTheme:` are untouched by that edit and still carry the stamped `const Color(0xFF<seed_hex>)`, which is mustache and cannot leave the brick. Same mutation, same toolchain (Flutter 3.47.2 at C:\\flutter-3.47.2), freshly stamped probe: baseline `flutter test` 185 passing, analyze 4 pre-existing `unnecessary_underscores` infos in lib/state/providers.dart and none in this file; under the mutation analyze reported the same 4 and nothing in app.dart, and the suite went 185 -> 184 with the ONE red being the named test `property: theme-triplet-supplied light and dark are genuinely different schemes`. A single, precise red is still what makes this row a different claim from the consent-prompt one — the same file, two independent properties. Land-checked: `grep -c 'brightness: Brightness.dark' apps/probe/lib/app.dart` was 1 before and 0 after. Pre-mutation md5 43A066998D857E7811A8B85472D8B6B3, identical after restore; the suite was re-observed green (185). RE-RUN 2026-09-26 (O-LINK-LAUNCHER-SEAM-UNOWNED), because the PROBED FILE changed: the `url_launcher` call became the app's one launcher (`externalLinks.openUrl` in settings, `updateLinkLauncher(url).openUrl` on the force-update wall). `theme:` and `darkTheme:` are untouched by that edit. Same mutation, Flutter 3.47.5 at C:\\flutter-3.47.5 (the pin in tooling/versions.json), mason_cli 0.1.3, freshly stamped probe (`mason make app -c tooling/bricks/app/_probe_vars.json -o . --on-conflict overwrite`): baseline `flutter test` 190 passing, analyze 4 pre-existing `unnecessary_underscores` infos in lib/state/providers.dart and none in this file; under the mutation analyze reported the same 4 and nothing in app.dart, and the suite went 190 -> 189 with the ONE red being the named test `property: theme-triplet-supplied light and dark are genuinely different schemes`. Land-checked: `grep -c 'brightness: Brightness.dark' apps/probe/lib/app.dart` was 1 before and 0 after. Pre-mutation md5 052E332AE239A629AECBB568F6C29BB7, identical after restore; the suite was re-observed green (190)."
      }
    }
  ],
  "humanReview": [
    {
      "id": "four-states",
      "verdict": "",
      "reviewer": "",
      "date": "",
      "artifact": "",
      "asks": "loading (skeleton), empty (illustration+CTA), error (message+Retry) and offline are FOUR visually distinct outcomes on every data screen"
    },
    {
      "id": "depth-beyond-viewing",
      "verdict": "",
      "reviewer": "",
      "date": "",
      "artifact": "",
      "asks": "at least one interactive feature beyond viewing content. UNVERIFIED: the applicable Apple clause is carried as 4.2.2 from 00-STAGE-3-14-RESEARCH.md, not read from developer.apple.com"
    },
    {
      "id": "first-value-and-paywall",
      "verdict": "",
      "reviewer": "",
      "date": "",
      "artifact": "",
      "asks": "first value arrives fast and the paywall sits AFTER the value moment. A judgment, never a threshold a guard may assert — the '60 seconds' in master-requirements §2.4 is a target with no measurement method"
    },
    {
      "id": "polish",
      "verdict": "",
      "reviewer": "",
      "date": "",
      "artifact": "",
      "asks": "brand icon and splash applied, motion tokens, skeletons not spinners, friendly empty/error states"
    }
  ],
  "selection": {
    "record": "",
    "sha256": "",
    "decided": "",
    "decidedBy": "",
    "gates": {
      "g1a": "",
      "g1b": "",
      "g1c": "",
      "g2": "",
      "g3": ""
    }
  }
}
