{
  "_readme": [
    "THE PUBLISHED-CLAIMS REGISTER — [pipeline K-3] Private/requirements/ (was pipeline/08-compliance-legal.md, folded into the JSON spec 2026-08-15)",
    "",
    "One row for every EMPHASISED claim in the visible text of the three published legal pages, paired to",
    "whatever in this tree makes it true — or, where nothing does, to the owner item that will.",
    "",
    "🔴 WHAT MAKES THIS UNFAKEABLE, AND IT IS THE ONLY REASON THE FILE IS WORTH HAVING.",
    "The domain is not this file. The domain is the set of <b>/<strong> spans in the visible text of",
    "sites/nikatru/{privacy,terms,refund}.html, extracted by tooling/ci/text-reductions.mjs. The guard requires",
    "the two sets to be EQUAL, both directions:",
    "  · a span with no row FAILS — you cannot publish a new emphasised promise without recording it;",
    "  · a row with no span FAILS — you cannot keep a row for a sentence the site no longer makes.",
    "Neither side can be shrunk without editing a published document, which is a visible change a human",
    "reviews. A register that listed only its own rows would be a list somebody trims when it is",
    "inconvenient; this one cannot be.",
    "",
    "⚠️ HONEST LIMITATION, AND THE GUARD'S HEADER SAYS THE SAME THING. Only rows typed `code` carry a",
    "mechanical assertion. A `descriptive` row proves that somebody looked at the sentence and wrote it",
    "down — nothing more. THIS FILE DOES NOT PROVE THE PAGES ARE TRUE. It proves that every emphasised",
    "claim is accounted for, and that the specific claims we CAN check against code still hold.",
    "",
    "Row types:",
    "  code        an assertion below runs against real files and can fail. This is the load-bearing kind.",
    "  provider    the claim names a third party; it must resolve to a row in provider-register.json.",
    "  descriptive the claim is a statement of intent, a defined term, or a commitment with no code",
    "              consequence (a refund window, a heading, the trading name). Paired, not proven.",
    "  known-gap   the claim is WRONG or contradicted today, and fixing it means editing published legal",
    "              copy — owner work. Carries an ownerItem and PRINTS on every run.",
    "",
    "Enforced by tooling/ci/assert-policy-claims.mjs. COVERAGE LOST if zero spans are extracted from any",
    "page: an emphasis-stripping rewrite must be loud, not silently satisfying.",
    "",
    "⚠️ `claim` IS THE NORMALISED SPAN TEXT (entities decoded, whitespace collapsed, curly quotes and",
    "dashes folded onto ASCII — see html-text.mjs normaliseForMatch). It is compared to the page, not",
    "typed from memory; if a row here does not match, the page is what is right."
  ],

  "pages": ["privacy.html", "terms.html", "refund.html", "delete-account.html"],
  "siteRoot": "sites/nikatru",

  "claims": [
    {
      "page": "delete-account.html",
      "claim": "Delete account",
      "type": "descriptive",
      "why": "The name of the in-app control, in both trees that render one: tooling/bricks/app/__brick__/apps/{{app_id}}/lib/features/settings/settings_screen.dart builds a tile from l10n.deleteAccount, and apps/subly/lib/features/settings/settings_screen.dart ships the same control ([ADR 027]). The page used to hedge — \"where an app shows a Delete account control\" — precisely because apps/subly had none; the hedge is gone because the control landed. What keeps the sentence true is tooling/ci/assert-deletion-control.mjs, which quantifies over the brick AND every apps/* workspace member with NO exemptions and fails the build for any app that offers accounts without one; the `account-deletion-works` key in assert-stamp-properties.mjs covers the stamped half."
    },
    {
      "page": "delete-account.html",
      "claim": "Settings",
      "type": "descriptive",
      "why": "Where that control lives. Same screen file as above; the tile is built inside the settings screen and nowhere else."
    },
    {
      "page": "delete-account.html",
      "claim": "the same email address and password no longer sign in anywhere",
      "type": "descriptive",
      "why": "The identity limb of services/platform/src/routes/account.ts: the Supabase admin user record is deleted LAST, and the route answers 502 rather than ok:true when that call fails. services/platform/test/auth.test.ts asserts on the request the route MADE, which is the one limb row counts cannot fake. The route answers 501 while SUPABASE_SERVICE_ROLE_KEY is unset (an owner action) — which is why section 1 keeps the email route as a first-class path rather than a fallback, and why the in-app control reports that refusal in the words \"Nothing was deleted\" instead of a generic failure ([ADR 027], packages/core/lib/src/auth/account_deletion.dart)."
    },
    {
      "page": "delete-account.html",
      "claim": "Usage statistics and consent records.",
      "type": "descriptive",
      "why": "The two tables tooling/legal/data-inventory.json declares with erasure kind `pseudonymous`. The claim that follows is checkable and is checked there: neither table carries a user-shaped column, and the guard fails if one is ever added."
    },
    {
      "page": "delete-account.html",
      "claim": "never against your account",
      "type": "descriptive",
      "why": "[ADR 020]: no anon_id -> user_id map may ever be written. services/platform/migrations/0002_analytics.sql keys both tables on anon_id, and tooling/ci/assert-data-inventory.mjs fails the `pseudonymous` declaration the day either table grows a `user_id` or *_user_id column."
    },
    {
      "page": "delete-account.html",
      "claim": "Records of a payment we could not match to any account.",
      "type": "descriptive",
      "why": "`unclaimed_payments` (services/platform/migrations/0004_money_rail.sql section D). The sentence that follows it — the account link is removed, the record stays — is the `unlink` erasure kind, proven by services/platform/test/erasure-reach.test.ts, which asserts the row survives and the column is NULL."
    },
    {
      "page": "delete-account.html",
      "claim": "What you created inside an app.",
      "type": "descriptive",
      "why": "The four `subly_db` tables tooling/legal/data-inventory.json declares with erasure kind `no-route`: services/platform's DELETE /v1/account reads PLATFORM_DB only, and services/subly-api has no account route. assert-data-inventory.mjs PRINTS that gap on every run. The paragraph exists because [ADR 027] shipped the in-app control WITHOUT closing that gap, and a section headed 'What is kept, and why' that omitted the user's own app content would be the omission the section exists to prevent. It comes down with the row, when a route reaches those tables."
    },
    {
      "page": "delete-account.html",
      "claim": "Anything the payment provider holds.",
      "type": "descriptive",
      "why": "Descriptive, and deliberately so: what a store does with its own records is outside this tree and nothing here can assert it. It is on the page because a reader who deletes an account and still sees a charge needs to know where to look."
    },
    {
      "page": "delete-account.html",
      "claim": "Deleting an account is not reversible.",
      "type": "descriptive",
      "why": "True of the route by construction: the purges are DELETEs and the identity call is a DELETE, with no soft-delete column anywhere in services/platform/migrations/. There is nothing to restore from."
    },
    {
      "page": "privacy.html",
      "claim": "Nikatru",
      "type": "descriptive",
      "why": "The trading name, defined here for the rest of the document. check-site-integrity.mjs separately asserts the LEGAL person behind it is named in visible text on this page."
    },
    {
      "page": "privacy.html",
      "claim": "Information you give us",
      "type": "descriptive",
      "why": "A section heading introducing the category. The stores that hold it are enumerated in tooling/legal/data-inventory.json, which is where the checkable claim lives."
    },
    {
      "page": "privacy.html",
      "claim": "Information collected automatically",
      "type": "descriptive",
      "why": "A section heading. Same as above — the inventory carries the store-level facts."
    },
    {
      "page": "privacy.html",
      "claim": "Usage statistics, only if you allow them",
      "type": "code",
      "assert": {
        "kind": "present",
        "files": ["tooling/bricks/app/__brick__/apps/{{app_id}}/lib/state/providers.dart"],
        "pattern": "if \\(!ref\\.watch\\(analyticsEnabledProvider\\)\\)",
        "why": "'only if you allow them' is a claim about a gate, and this is the gate: the analytics transport and the analytics sink both resolve to a no-op while consent is not granted. Deleting the check makes the page a lie, and this row goes red with it."
      }
    },
    {
      "page": "privacy.html",
      "claim": "pseudonymous, not anonymous",
      "type": "code",
      "assert": {
        "kind": "present",
        "files": ["services/platform/src/routes/events.ts"],
        "pattern": "anon_id",
        "why": "The page describes a stable per-install identifier and says plainly that records carrying it are pseudonymous rather than anonymous. `anon_id` is that identifier, and it is the column the events table is keyed on. If the server stopped carrying it the page would be over-disclosing rather than under-disclosing, which is still a page that does not describe the system."
      }
    },
    {
      "page": "privacy.html",
      "claim": "We do not collect or store your IP address",
      "type": "code",
      "assert": {
        "kind": "absent",
        "files": ["services/platform/src/routes/events.ts"],
        "pattern": "(?i)cf-connecting-ip",
        "why": "The strongest available reading of 'we do not collect or store your IP address' on the analytics path: the route that writes the events table never reads the header that carries it. The geo fields it does write come from `request.cf`, which Cloudflare derives at the edge. ⚠️ This asserts the header is not READ by this route — it is not a dataflow proof, and it says so rather than implying more reach than it has."
      }
    },
    {
      "page": "privacy.html",
      "claim": "Launch-notification signups",
      "type": "descriptive",
      "why": "A section heading. The store it describes (nikatru-signups) has a row in tooling/legal/data-inventory.json, and that row carries the retention answer this heading's paragraph used to only raise as a question: retention `ttl`, ~~365 days~~ **400 days since 2026-08-13** (owner-declared at 365 on 2026-08-09; raised by an agent to clear the DPDP Rule 8(3) one-year floor, which 365 misses by one day across a leap year — see [ADR 045] §9), read out of sites/nikatru/functions/api/subscribe.js by tooling/ci/assert-retention-coverage.mjs. ⚠️ The published paragraph still says only 'as long as necessary' — that is WIDER than what the code now does, which is the safe direction for a notice to be wrong in, but naming the period on the page is an owner-signed copy change and is not made here."
    },
    {
      "page": "privacy.html",
      "claim": "email address and the time you signed up",
      "type": "code",
      "assert": {
        "kind": "present",
        "files": ["sites/nikatru/functions/api/subscribe.js"],
        "pattern": "const record = \\{ email, ts:",
        "why": "'and nothing else' is the claim, and this is the whole record. The moment somebody adds a third field to that object literal this row goes red, which is exactly when the page stops being true. This is the most precise pairing in the file: one published sentence, one line of code."
      }
    },
    {
      "page": "privacy.html",
      "claim": "salted, one-way fingerprint",
      "type": "code",
      "assert": {
        "kind": "present",
        "files": ["sites/nikatru/functions/api/subscribe.js"],
        "pattern": "crypto\\.subtle\\.sign",
        "why": "A KEYED construction, not a digest: an unsalted sha256 of an IPv4 address is reversible by exhausting 2^32 values, so it would be the address wearing a hash and the page would be wrong. check-site-integrity.mjs enforces the same property from the code side; this row is what ties it to the published sentence."
      }
    },
    {
      "page": "privacy.html",
      "claim": "Purchase information",
      "type": "descriptive",
      "why": "A section heading. The providers in the purchase path are enumerated in tooling/legal/provider-register.json."
    },
    {
      "page": "privacy.html",
      "claim": "do not collect or store your full card or bank details.",
      "type": "code",
      "assert": {
        "kind": "absent",
        "walk": { "root": "services", "filenameRe": "\\.sql$", "minFiles": 5 },
        "pattern": "(?i)\\b(card_number|cardnumber|cvv|cvc|pan_number|iban|account_number|routing_number|sort_code)\\b",
        "why": "No schema in any Worker may declare a column that holds a payment instrument. Walked rather than listed so a NEW migration file is covered the day it lands, and floored at the 5 migration files that exist today so a walk that stops finding them is COVERAGE LOST rather than a pass. ⚠️ A column-name blocklist is a tripwire, not a proof: it catches the obvious spelling of a card column, not a card number stuffed into a `notes` field."
      }
    },
    {
      "page": "privacy.html",
      "claim": "not",
      "type": "descriptive",
      "why": "Two separate one-word emphases — 'We do NOT sell your personal information' (§1) and 'likewise NOT sent to a third-party analytics product' (§3). Both collapse to the same span text, so they share one row; the substantive claims they belong to are carried by the surrounding rows."
    },
    {
      "page": "privacy.html",
      "claim": "Apple App Store, Google Play and Microsoft Store",
      "type": "provider",
      "providers": ["apple-app-store", "google-play", "microsoft-store"]
    },
    {
      "page": "privacy.html",
      "claim": "Cloudflare",
      "type": "provider",
      "providers": ["cloudflare"],
      "note": "Two spans on this page carry this text and BOTH are now true. They were not on version 2026-08-01: the second sentence said usage statistics AND crash reports both run on Cloudflare, and crash reports run on Oracle Cloud. The 2026-08-10 signature split that sentence so each system names its own host, which is what retired the `crash-reports-infrastructure` gap. The note is kept rather than deleted because the defect it records is the reason a per-span row cannot distinguish two identical spans — if a future edit re-merges the sentence, this row goes green on a page that is wrong again."
    },
    {
      "page": "privacy.html",
      "claim": "merchant of record",
      "type": "provider",
      "providers": ["paddle"],
      "note": "The ROLE, now stated beside the provider that fills it rather than instead of one. [ADR 004] locked Paddle and [ADR 039] D1 scoped it to every channel an app store does not bill itself; the `Paddle` span in the same sentence carries the name. The `merchant-of-record-unnamed` gap was retired by the 2026-08-10 signature."
    },
    {
      "page": "privacy.html",
      "claim": "Paddle",
      "type": "provider",
      "providers": ["paddle"],
      "note": "The seller named, closing `merchant-of-record-unnamed`. terms.html and refund.html named Paddle on 2026-08-10 without a version bump because they carry no policy version; privacy.html could not, which is why this span waited for the signature and arrived with it. The register's `namedIn` for paddle now lists all three pages, so assert-policy-claims re-checks the disclosure against the page text on every run."
    },
    {
      "page": "privacy.html",
      "claim": "RevenueCat",
      "type": "provider",
      "providers": ["revenuecat"],
      "note": "The IAP aggregator ([ADR 039] D5), disclosed as a PROCESSOR and never as a seller — the sentence says it 'processes that on our behalf and is never the seller', because a buyer contracts with the store or with Paddle, never with RevenueCat. Closes `iap-aggregator-unnamed`. It holds no live data today (every kind:store row in tooling/channel-register.json is served:false), which is exactly why the disclosure had to land before the first store purchase rather than after it."
    },
    {
      "page": "privacy.html",
      "claim": "Supabase",
      "type": "provider",
      "providers": ["supabase"],
      "note": "The identity provider, wired by the cut-1 auth reversal (#280) and disclosed here. Closes `identity-provider-unnamed`. Nothing is stored under it today because no shipped app offers accounts — this is a disclosure that had to precede the first account, not one that was wrong."
    },
    {
      "page": "privacy.html",
      "claim": "Resend",
      "type": "provider",
      "providers": ["resend"],
      "note": "🔴 THE FIFTH CORRECTION, AND THE ONLY ONE WITH NO GAP ROW BEHIND IT. The other four privacy.html omissions signed on 2026-08-10 were each carried as a `disclosureGaps` row that printed on every CI run; Resend was carried by nothing, because it was absent from tooling/legal/provider-register.json entirely — and every guard here quantifies over that register. It has delivered Supabase auth mail (and therefore held every user's email address) since auth mail went live under [ADR 029]. Found by inspection during the signature, not by a check. TRANSACTIONAL ONLY: the sentence names sign-in and account-verification messages and asserts no marketing channel, because ConsentPurpose.marketingEmail is collection-only with no sender and no list ([ADR 040] decision 5)."
    },
    {
      "page": "privacy.html",
      "claim": "Oracle Cloud",
      "type": "provider",
      "providers": ["oracle-cloud"],
      "note": "Where crash reports actually land. Closes `crash-reports-infrastructure`: the sentence used to cover usage statistics and crash reports with one host and was wrong about the second. Split at the 2026-08-10 signature so each system names its own. tooling/legal/provider-register.json already declared `crash_report` against the oracle-cloud sink, so the page was the only half of the pair that disagreed with the register."
    },
    {
      "page": "privacy.html",
      "claim": "We do not use advertising networks or advertising SDKs, we do not sell or share data with data brokers or advertisers, and no Nikatru app carries third-party advertising.",
      "type": "code",
      "assert": {
        "kind": "absent",
        "walk": { "root": ".", "filenameRe": "^pubspec\\.yaml$", "minFiles": 10 },
        "pattern": "(?im)^\\s*(google_mobile_ads|firebase_admob|admob_flutter|applovin_max|unity_ads_plugin|facebook_audience_network|ironsource_mediation|appodeal_flutter|adjust_sdk|appsflyer_sdk|facebook_app_events)\\s*:",
        "why": "A package manifest can refute the THIRD-PARTY half of this sentence outright: an advertising or attribution SDK cannot enter this workspace without appearing as a dependency. Walked over every pubspec.yaml in the tree (floored at 10) so a new package is covered the day it is created. 🔴 THE WORD `third-party` IS LOAD-BEARING AND WAS ADDED DELIBERATELY ON 2026-08-10. The sentence used to read 'no Nikatru app carries advertising', and this walk could never have refuted THAT: Google Play's own ads declaration counts a house ad — 'a small ad banner, interstitial ad, ad wall, and/or widget' promoting our own apps — as advertising, keyed on FORMAT rather than on ownership or SDK presence (support.google.com/googleplay/android-developer/answer/9859455). A first-party promo surface adds no dependency, so the old claim would have gone false while this row stayed green — the exact silent-failure class this register exists to catch. The claim is now scoped to what the walk can actually see, and the first-party half is disclosed in §3 and asserted by the `first-party promotions`, `on your device` and `Offers and promotions` rows. ⚠️ A NAMED LIST IS STILL NOT EXHAUSTIVE; the standing trigger 'the day an ad SDK enters a pubspec' is the `global-privacy-control` row in tooling/legal/duty-matrix.json, and the trigger for the FORMAT question is `first-party-promotion-legal-basis` in the same file. The sentence, this row and apps/subly/store/android-play/ads-declaration.json remain pinned by exact string equality, so rewording any one without the others is a build failure."
      }
    },
    {
      "page": "privacy.html",
      "claim": "first-party promotions",
      "type": "code",
      "assert": {
        "kind": "absent",
        "walk": { "root": ".", "filenameRe": "^pubspec\\.yaml$", "minFiles": 10 },
        "pattern": "(?im)^\\s*(google_mobile_ads|firebase_admob|admob_flutter|applovin_max|unity_ads_plugin|facebook_audience_network|ironsource_mediation|appodeal_flutter|adjust_sdk|appsflyer_sdk|facebook_app_events)\\s*:",
        "why": "'we show them ourselves, and no advertising network, ad exchange or advertising SDK is involved' is the half of §3 a dependency walk CAN refute, and it is the same walk the advertising row uses. Kept as its own row rather than folded into that one because the two sentences are in different sections and either could be edited without the other."
      }
    },
    {
      "page": "privacy.html",
      "claim": "The legal basis we rely on.",
      "type": "descriptive",
      "why": "A lead-in, and a required disclosure: GDPR Art 13(1)(c) wants the purpose AND the basis. Descriptive because nothing in this tree can prove a legal basis is correctly chosen — that is a lawyer's judgement, and this register does not pretend otherwise (see its preamble)."
    },
    {
      "page": "privacy.html",
      "claim": "legitimate interest",
      "type": "descriptive",
      "why": "The basis itself, GDPR Art 6(1)(f), with the SPECIFIC interest named in the same paragraph as Art 13(1)(d) requires. [research/44] V4 records why this is the basis rather than consent: Recital 47 treats direct marketing as capable of being a legitimate interest, and adding a consent gate would be legally unnecessary friction that wrongly implies the processing becomes unlawful when refused. The code agrees rather than merely not disagreeing: `ConsentPurpose.promo` in packages/core/lib/src/analytics/consent.dart carries `basis: ConsentBasis.legitimateInterest`, so `unknown` permits and only `denied` forbids. ⚠️ THE ROW IS `descriptive` AND THAT IS NOT A DODGE — the balancing test is the substance and no guard can perform it. What IS mechanical is the objection control, and it is asserted by the `Offers and promotions` row below."
    },
    {
      "page": "privacy.html",
      "claim": "What we use to decide what to show you.",
      "type": "descriptive",
      "why": "A lead-in to the targeting-inputs disclosure. The substantive claim under it — entitlement state only, decided on the device — is carried by the `on your device` row."
    },
    {
      "page": "privacy.html",
      "claim": "on your device",
      "type": "code",
      "assert": {
        "kind": "absent",
        "files": ["packages/core/lib/src/promo/promo_gate.dart"],
        "pattern": "(?im)^\\s*import\\s|https?:|HttpClient|\\bfetch\\(|\\bDio\\(|analytics|\\brecord\\(",
        "why": "'the decision is made on your device' is the one sentence in §3 that a file can refute. [research/44] §4.3 specifies PromoGate as a PURE function over persisted counters — no UI, no config read, no I/O — so the decision must reach no network and emit no event. 🔴 THE FIRST LIMB IS THE SHARP ONE AND IT IS WHY THE PATTERN IS NOT JUST A LIST OF HTTP SPELLINGS: promo_gate.dart declares ZERO imports today (`library;` and nothing else), so `^\\s*import\\s` fires on ANY dependency arriving — a network client, an analytics rail, a storage rail, anything. A purity claim defended only by a blocklist of client names is refuted by the client whose name is not on the list; a purity claim defended by 'this file imports nothing' cannot be. The remaining limbs catch an impurity written inline without an import. Comments are stripped before matching, so the doc comment explaining that the gate touches no network does not trip it. NEGATIVE-TESTED 2026-08-10 by adding `import 'dart:io';` + `HttpClient().close();` to PromoGate.decide: assert-policy-claims went red with `dart analyze` clean, and restoring the file returned it to green."
      }
    },
    {
      "page": "privacy.html",
      "claim": "Your right to object, and how to use it.",
      "type": "descriptive",
      "why": "GDPR Art 21(4): the objection right must be 'explicitly brought to the attention of the data subject and presented clearly and separately from any other information', at the latest at the time of the first communication. This lead-in plus the in-card control ([research/44] §7 rung 4, `PromoObjectionControl` inside `PromoSurface`) are the two halves of that; a Settings-only control would satisfy neither the 'separately' limb nor 'at the time of the first communication'."
    },
    {
      "page": "privacy.html",
      "claim": "Offers and promotions",
      "type": "code",
      "assert": {
        "kind": "present",
        "files": ["apps/subly/lib/features/settings/settings_screen.dart"],
        "pattern": "recordPromoObjection\\(",
        "why": "The page names a control and tells the reader where it is; this row is what makes that a promise rather than an intention. Modelled exactly on the 'Usage statistics can be withdrawn without contacting us.' row, which asserts `recordAnalyticsConsent\\(` in the same screen file — delete the row from Settings and the claim goes red. 🔴 THE STRING IS ALSO THE SCREEN'S LABEL, AND THAT WAS NOT FREE: r4 shipped the l10n value as 'Promotional offers' while this signed sentence says 'Offers and promotions'. The page is the promise and the app string is not owner-gated, so the APP was moved to the page's words (apps/subly and the brick, en and ta) rather than the reverse — the page and the app must use one name for one thing, or a reader following the instruction hunts for a heading that is not there. NEGATIVE-TESTED 2026-08-10 by deleting the `recordPromoObjection` call from settings_screen.dart: assert-policy-claims went red with `dart analyze` clean, and restoring it returned it to green."
      }
    },
    {
      "page": "privacy.html",
      "claim": "Nikatru apps only",
      "type": "descriptive",
      "why": "The scope limit, and the sentence a store reviewer reads to decide whether the surface is an ad network. Descriptive today because v1 is SAME-APP ONLY ([ADR 040] decision 2) and there is no second app to promote — so nothing in the tree can yet be pointed at that would refute it. 🔴 IT BECOMES ASSERTABLE THE DAY A CROSS-APP SURFACE SHIPS ([research/44] §7 rung 6): the payload's app ids must all resolve to rows in sites/_shared/_data/apps.json, and that is the row to re-point this at rather than a new one."
    },
    {
      "page": "privacy.html",
      "claim": "Usage statistics can be withdrawn without contacting us.",
      "type": "code",
      "assert": {
        "kind": "present",
        "files": ["apps/subly/lib/features/settings/settings_screen.dart"],
        "pattern": "recordAnalyticsConsent\\(",
        "why": "The withdrawal path in the one shipping app, in the screen the page tells the user to open. It is the same call the first-run prompt makes with `granted` flipped, so withdrawal appends a new consent artifact rather than editing the old one. Delete the row from Settings and this claim goes red — which is the point: the sentence promises a control, not an intention."
      }
    },
    {
      "page": "privacy.html",
      "claim": "Settings",
      "type": "descriptive",
      "why": "Names the screen the user is told to open. The control on it is the subject of the withdrawal row above; this span is the signpost."
    },
    {
      "page": "privacy.html",
      "claim": "Usage statistics",
      "type": "descriptive",
      "why": "The label of the control, quoted so the page and the app use one name for one thing. The brick's own no-hardcoded-strings guard owns whether that label is localised."
    },

    {
      "page": "terms.html",
      "claim": "Nikatru",
      "type": "descriptive",
      "why": "The trading name. terms.html §preamble also names the sole proprietor, which check-site-integrity.mjs asserts."
    },
    {
      "page": "terms.html",
      "claim": "Paddle",
      "type": "code",
      "providers": ["paddle"],
      "assert": {
        "kind": "present",
        "files": ["services/platform/src/lib/mor/registry.ts"],
        "pattern": "MOR_VERIFIERS[^=]*=\\s*\\[[^\\]]*paddleVerifier",
        "why": "REPLACED THE `Razorpay` known-gap ROW. §4 previously told a buyer their website purchase was 'processed securely by Razorpay' — a company no code in this repository reaches, for a purchase the same site said does not happen, in a legal relationship a gateway is not ([ADR 004] locked a merchant of record; [ADR 039] D6 then confirmed Paddle serves Indian buyers too and that Razorpay is NOT the app rail). The page now names Paddle, so the pairing can be a real one: `MOR_VERIFIERS` is the merchant-of-record webhook registry, it is DATA rather than a URL shape (see the `_money` note in provider-register.json), and `paddleVerifier` is the only rail in it. Remove Paddle from that array and the page names a seller no money route can verify a webhook from — which is exactly when this row should go red. ⚠️ THIS ASSERTS THE RAIL IS REGISTERED, NOT THAT A SELLER ACCOUNT EXISTS: account state is private-SSoT material (OWNER_QUEUE A-1) and no in-tree guard can or should observe it."
      }
    },
    {
      "page": "terms.html",
      "claim": "merchant of record",
      "type": "provider",
      "providers": ["paddle"],
      "note": "The ROLE, published beside the name for the first time. It is the operative disclosure rather than decoration: a merchant of record is the legal SELLER, so §4 now tells a buyer that Paddle — not Nikatru — is their counterparty, takes the payment and remits the sales tax. `tooling/legal/provider-register.json` roles.merchant_of_record is the same sentence in the register. Deliberately NOT a second `code` row: the terms.html `Paddle` row above already asserts the registry, and two rows asserting one fact inflates the apparent strength of this file (the honest-limitation rule in the preamble)."
    },
    {
      "page": "terms.html",
      "claim": "\"as is\" and \"as available\"",
      "type": "descriptive",
      "why": "A warranty disclaimer. Its scope and enforceability are a question for a lawyer, not for a guard, and this register does not pretend otherwise."
    },

    {
      "page": "refund.html",
      "claim": "Nikatru",
      "type": "descriptive",
      "why": "The trading name."
    },
    {
      "page": "refund.html",
      "claim": "Apple App Store, Google Play",
      "type": "provider",
      "providers": ["apple-app-store", "google-play"]
    },
    {
      "page": "refund.html",
      "claim": "Microsoft Store",
      "type": "provider",
      "providers": ["microsoft-store"],
      "note": "⚠️ THE SPAN MOVED SECTIONS AND THAT IS THE SUBSTANCE OF THE CHANGE, not a reformat. §1 used to send a Microsoft Store buyer to account.microsoft.com for a refund. [ADR 039] D1 puts the Microsoft Store on the Paddle rail (Store Policies v7.19 §10.8.1/§10.8.6 permit a third-party purchase API for non-game PC apps; `packages/purchases/lib/src/purchase_capabilities.dart` carries the same citation on `windowsStore.channelPermitted: true`), so Microsoft never takes the payment and has no order to refund. The store is still named — it is still how the app is distributed — but it is named in §2, beside the seller that actually took the money."
    },
    {
      "page": "refund.html",
      "claim": "Paddle",
      "type": "provider",
      "providers": ["paddle"],
      "note": "§2 previously read 'Purchases on our website (via Razorpay)' and §4 issued refunds through that company. Both now name Paddle, and §2 says plainly that Paddle rather than Nikatru is the legal seller and the one that issues the refund. Typed `provider` and not `code`: the terms.html `Paddle` row carries the single registry assertion, and a duplicate here would report two proofs of one fact."
    },
    {
      "page": "refund.html",
      "claim": "merchant of record",
      "type": "provider",
      "providers": ["paddle"],
      "note": "Same role disclosure as the terms.html row. It is on BOTH pages by design and by rule: assert-policy-claims.mjs §3(c) requires a merchant-of-record row to be named in terms.html AND refund.html, because those are the two documents a buyer reads about the same purchase, and a buyer told one seller in one and another in the other has been told the wrong counterparty."
    },
    {
      "page": "refund.html",
      "claim": "Digital delivery:",
      "type": "descriptive",
      "why": "A commitment about digital goods with no code consequence. Whether it is enforceable where a buyer lives is a legal question this register does not answer."
    },
    {
      "page": "refund.html",
      "claim": "We will provide a refund if:",
      "type": "descriptive",
      "why": "The refund conditions. No code path issues refunds today — every kind:store channel in tooling/channel-register.json is served:false and no payment provider is wired — so there is nothing to assert against."
    },
    {
      "page": "refund.html",
      "claim": "Subscriptions:",
      "type": "descriptive",
      "why": "Cancellation behaviour. Store-side today, and no website subscription exists to cancel."
    },
    {
      "page": "refund.html",
      "claim": "7 days",
      "type": "descriptive",
      "why": "The window for requesting a refund. A COMMITMENT WE CHOSE, not a statutory period — it is deliberately not described as one, and no guard here invents a legal minimum to compare it to."
    },
    {
      "page": "refund.html",
      "claim": "5-7 business days",
      "type": "descriptive",
      "why": "How long a refund takes to appear. Depends on the buyer's bank and the provider; not a promise any file in this tree can keep or break."
    }
  ]
}
