{
  "_readme": [
    "THE PERSONAL-DATA INVENTORY — [pipeline K-8] company/pipeline/08-compliance-legal.md",
    "",
    "One row for every place this factory can put data: every table any migration creates, every D1",
    "database, KV namespace and R2 bucket any wrangler config binds, and every KV binding a Cloudflare",
    "Pages Function reaches. What each holds, how long it is kept, what code writes it, and — where it",
    "holds anything about a person — the SENTENCE ON THE PUBLISHED PAGE that discloses it.",
    "",
    "🔴 THE ROW SET MUST EQUAL THE DERIVED STORE SET, BOTH DIRECTIONS, and the second direction is the",
    "one that matters. `a store with no row` catches data arriving undisclosed. `a row with no store`",
    "is what stops this file becoming a wish-list: without it, a table could be renamed, the row would",
    "keep describing the old name, and the inventory would go on looking complete while describing a",
    "schema that no longer exists.",
    "",
    "DERIVED, NOT LISTED. The right-hand side is computed from the tree on every run:",
    "  · every CREATE TABLE in services/*/migrations/*.sql, with COMMENTS AND STRING LITERALS STRIPPED",
    "    first — a table name inside a comment is not a table, and this repo has already shipped a guard",
    "    that matched the comment explaining why a binding did NOT exist;",
    "  · every d1_databases / kv_namespaces / r2_buckets entry in EVERY wrangler config in the repo,",
    "    PARSED as JSONC rather than grepped. ⚠️ Repo-wide on purpose: assert-clone-contract.mjs reads",
    "    only services/<app>-api/wrangler.jsonc, so services/subly-api is invisible to it. Borrowing",
    "    that walk would have inherited the blind spot and reported full coverage of a subset;",
    "  · every KV binding a Pages Function calls (`env.SIGNUPS.put(...)`). These are bound in the",
    "    Cloudflare dashboard and appear in NO wrangler config, so a wrangler-only walk would miss the",
    "    one store in this repository that holds a plain email address.",
    "",
    "⚠️ WHAT IS PUBLIC HERE. This is a public repository and this file describes SHAPE, never CONTENT: a",
    "store exists, it holds this CATEGORY of data, it is written by this file, it is kept for this long.",
    "No row contains a person's data, an identifier, a key, or a database id. See tooling/legal/README.md.",
    "",
    "🔴 AND EVERY TABLE CARRIES AN ERASURE STORY, because a delete route that silently misses rows is",
    "worse than no delete route: the user is told their data is gone and stops asking. The erasure route",
    "(services/platform/src/routes/account.ts) DERIVES the tables it empties from the schema — every table",
    "with a `user_id` column — which is right, and has one consequence nobody had written down: a table",
    "holding personal data WITHOUT such a column is invisible to it, and the route still answers ok:true.",
    "`provider_notifications` was exactly that table until migration 0006. So `erasure` is now required on",
    "every d1-table row and every row that holds personal data, its `kind` is CHECKED AGAINST THE PARSED",
    "SCHEMA (a `purge` row whose table has no `user_id` fails; a `pseudonymous` row whose table GROWS one",
    "fails, because the declaration has become false), and a store no route can reach PRINTS every run",
    "with the thing that blocks it named. The behaviour behind the declaration is proven separately, by",
    "services/platform/test/erasure-reach.test.ts, which plants a row in every table this file names,",
    "runs the REAL route against the REAL migrations, and asserts the erased user's id survives in no",
    "column of any table.",
    "",
    "RETENTION IS A DECLARATION, AND `undecided` IS AN HONEST ONE. Every row carries `retention`.",
    "`ttl` states the bound and the code that sets it. `keep` requires a written reason. `undecided`",
    "requires an ownerItem and PRINTS on every run — because choosing how long to keep a real person's",
    "data is a legal and business decision, not something an agent should quietly pick a number for. The",
    "guard fails a row with NO retention field at all; it never invents one.",
    "",
    "Enforced by tooling/ci/assert-data-inventory.mjs. COVERAGE LOST when any of the three walks",
    "under-reaches the floors in `derivation` below."
  ],
  "derivation": {
    "_why": [
      "THE FLOORS ARE A RELATIONSHIP, NOT A GUESS. Each is the count measured on the tree the day this",
      "file landed, and a walk that finds FEWER has broken rather than shrunk — a migration directory",
      "that stops being found makes every 'is this table declared?' question vacuously true.",
      "They are also the mutation targets: deleting one migration file is COVERAGE LOST, not a pass."
    ],
    "migrationRoots": [
      "services"
    ],
    "minMigrationFiles": 6,
    "wranglerRoots": [
      "services",
      "tooling/bricks"
    ],
    "minWranglerConfigs": 3,
    "pagesFunctionRoots": [
      "sites/nikatru/functions"
    ],
    "minPagesFunctionFiles": 1,
    "minPagesKvBindings": 1,
    "_whyErasureFloor": [
      "How many rows must actually have their erasure story CHECKED AGAINST THE SCHEMA. Measured on the",
      "tree the day the limb landed (13 d1-table rows + the one Pages KV namespace that holds a plain",
      "email address). A run that checks fewer has stopped reaching rows, and 'every table has an erasure",
      "story' would range over a subset while printing ok — the exact shape check-migrations.mjs failed in",
      "silently when it dropped from 5 files to 4."
    ],
    "minErasureRowsChecked": 14
  },
  "retentionKinds": {
    "_why": "The vocabulary rows are validated against, declared in the register rather than in the guard so that renaming a kind out from under the rows using it fails.",
    "ttl": "the store expires the record itself, and the row names the code that sets the expiry.",
    "swept": "a DECLARED period that a job of ours enforces, because the store has no expiry of its own. Requires a `periodDays` and a `sweptBy` naming the code that deletes. ⚠️ DISTINCT FROM `ttl` ON PURPOSE: `ttl` says the STORE forgets, which is a property of the substrate and survives our code being wrong or absent; `swept` says WE forget, which holds only while a scheduled job keeps running. D1 has no TTL, so every D1 period is this kind — and calling it `ttl` would claim a durability guarantee the substrate does not give. Added 2026-08-12 with the events rollup: `events` and `provider_notifications` had been sitting at `undecided` since before [ADR 045] locked 400 and 730, so this file was still printing RETENTION UNDECIDED and asking the owner for numbers they had already given.",
    "keep": "kept until deliberately deleted. Requires a written reason — 'we might want it' is not one.",
    "undecided": "nobody has decided yet. Requires an ownerItem, and PRINTS every run so it cannot become permanent by being invisible.",
    "derived": "holds no record of its own; its lifetime is the lifetime of the rows inside it (a database, not a table)."
  },
  "erasureKinds": {
    "_why": [
      "The vocabulary lives here, not in the guard, for the same reason `retentionKinds` does: renaming a",
      "kind out from under the rows using it must FAIL rather than quietly mean nothing.",
      "",
      "🔴 EVERY KIND IS CHECKED AGAINST THE PARSED SCHEMA, not taken on trust. That is what makes this a",
      "RELATIONSHIP rather than a second wish-list: `purge` asserts the column the route derives from is",
      "really there, and `pseudonymous` / `no-personal-data` assert that no user-shaped column is — so a",
      "migration that adds `user_id` to `events` tomorrow turns the declaration false and the build red,",
      "instead of leaving a row that says 'unreachable' about a table that is now reachable."
    ],
    "purge": "the table carries a column literally called `user_id`, so the schema-derived sweep in the named `route` DELETEs this person's rows. Checked: the column must exist in the migrations.",
    "unlink": "the row is NOT this person's, but a `*_user_id` column REFERENCES them; the sweep NULLs that column so no dangling identifier of an erased person survives. Requires `column`. Checked: the column must exist and must end in `_user_id`.",
    "pseudonymous": "nothing in the table resolves to an account, so an erasure request cannot address it and 'zero rows for this user' is vacuously true. Requires a written reason. Checked: the table must carry NO user-shaped column — the day one is added this declaration is false and the guard says so.",
    "no-personal-data": "the table holds nothing about a person, so there is nothing to erase. Requires personalData:false. Checked the same way as `pseudonymous`.",
    "no-route": "🔴 A STORE HOLDING PERSONAL DATA THAT NO DEPLOYED ERASURE ROUTE REACHES. Requires `blockedBy` naming what stops it. PRINTS on every run rather than failing the build — the fix is an architecture or owner decision, and a guard that reddens main on work nobody can do today is a guard somebody switches off."
  },
  "stores": [
    {
      "id": "d1:platform_db",
      "kind": "d1-database",
      "name": "platform_db",
      "personalData": false,
      "holds": "The shared portfolio database. Holds no rows of its own; its tables do.",
      "retention": {
        "kind": "derived",
        "reason": "A database is a container. Every table in it carries its own row below."
      },
      "writtenBy": [
        "services/platform/wrangler.jsonc"
      ],
      "note": "🔴 ONE DATABASE FOR THE WHOLE PORTFOLIO. services/platform is its sole migrations applier, which is why check-migrations.mjs treats a destructive statement here as a portfolio-wide outage rather than one app's problem."
    },
    {
      "id": "d1:subly_db",
      "kind": "d1-database",
      "name": "subly_db",
      "personalData": false,
      "holds": "Subly's own database. Holds no rows of its own; its tables do.",
      "retention": {
        "kind": "derived",
        "reason": "A container. See the table rows below."
      },
      "writtenBy": [
        "services/subly-api/wrangler.jsonc"
      ]
    },
    {
      "id": "d1:{{app_id}}_db",
      "kind": "d1-database",
      "name": "{{app_id}}_db",
      "personalData": false,
      "holds": "The per-app database a BACKEND stamp creates. A template, not a live store — the name is a mustache placeholder and resolves per stamped app.",
      "retention": {
        "kind": "derived",
        "reason": "A container, and a template of one. Each stamped app inherits the table rows its own migrations create."
      },
      "writtenBy": [
        "tooling/bricks/app/__brick__/{{#needs_backend}}services{{/needs_backend}}/{{app_id}}-api/wrangler.jsonc"
      ],
      "note": "Included deliberately rather than filtered out as 'just a template'. It is the binding every future app will have, and an inventory that skips the brick is an inventory that is wrong the day app number two ships."
    },
    {
      "id": "table:platform_db.entitlements",
      "kind": "d1-table",
      "name": "entitlements",
      "personalData": true,
      "holds": "One row per (user, app, entitlement): what a person has paid for. Keyed on a store/aggregator user id, not on a name or an email.",
      "retention": {
        "kind": "keep",
        "reason": "An entitlement is the record of what somebody bought and is entitled to keep using. Deleting it deletes their purchase. It is retained for as long as the account exists, and erasure is [pipeline K-7]'s route — DELETE /v1/account, which landed with [4]B-5 and reaches this table through its `user_id` column."
      },
      "erasure": {
        "kind": "purge",
        "route": "services/platform/src/routes/account.ts",
        "reason": "Keyed (user_id, app_id, entitlement). The account is gone, so the entitlement it granted is gone with it — and master §0.1 G2 names this table specifically."
      },
      "writtenBy": [
        "services/platform/migrations/0001_entitlements.sql",
        "services/subly-api/src/routes/webhooks.ts"
      ],
      "disclosure": {
        "page": "privacy.html",
        "quote": "if you buy something, the transaction is processed by the relevant app store or by our payment processor"
      }
    },
    {
      "id": "table:platform_db.provider_notifications",
      "kind": "d1-table",
      "name": "provider_notifications",
      "personalData": true,
      "holds": "🔴 THE ONE TABLE IN platform_db THAT IS NOT PSEUDONYMOUS, and it is named as such rather than discovered. Every merchant-of-record notification, stored VERBATIM — byte for byte, exactly as it arrived and exactly as it was signed. Whatever the provider put in it is therefore in it, which for a subscription event includes the buyer's name, email address and billing country. That is inherent in 'recorded verbatim' ([pipeline 5]M-2): a re-serialised or redacted copy is a different byte string and can never be re-verified against the signature that came with it, so a redacted store is a store that cannot prove what it received. It carries NO anon_id, so no code path can pair a payer with a pseudonymous install ([ADR 020]:21).",
      "retention": {
        "kind": "swept",
        "periodDays": 730,
        "sweptBy": "services/platform/src/scheduled.ts — the `provider_notifications` limb of `retentionSweep`, PROVIDER_NOTIFICATIONS_RETENTION_DAYS. 🔴 IT DELETES ONLY WHERE `derived_at IS NOT NULL AND derive_error IS NULL`, and that predicate is a LEGAL CONDITION, not an optimisation: if derivation never resolved, no derived record exists, the raw payload IS the book of account by function, and it inherits CGST s.36 (~2,698 days). The sweep's legality is a property of that predicate, not of the period.",
        "reason": "🔒 LOCKED 730 DAYS (2 years) by [ADR 045]. ⚠️ THE OBVIOUS JUSTIFICATION IS FALSE AND IS NOT USED: 'we need it to fight chargebacks' does not survive Paddle's own documentation — Paddle is merchant of record, the dispute is raised against Paddle, its help centre says seller evidence is 'not required or accepted', and it retains the transaction for 5 years. The real reasons are that Paddle will NOT replay a notification older than 90 days, so from day 91 ours is the sole surviving copy; and that Visa's outer dispute window is 540 days, which 730 clears with margin. 📌 THIS ROW READ `undecided` / O-3 until 2026-08-12 — see the note on `table:platform_db.events`; both were locked by ADR 045 on 2026-08-12 and neither row was updated, so this file printed two RETENTION UNDECIDED lines for decisions that had already been made."
      },
      "erasure": {
        "kind": "purge",
        "route": "services/platform/src/routes/account.ts",
        "reason": "🔴 THE GAP THIS FILE WAS EXTENDED TO CLOSE. Until services/platform/migrations/0006_erasure_reach.sql this table carried NO `user_id`, so the schema-derived sweep could not see it — a person could ask to be erased, be told they were, and have their name and email address still sitting in the verbatim payload. 0006 adds the column and services/platform/src/lib/mor/store.ts stamps it at DERIVE time (the row is written before it is interpreted, so that is the first moment the account is known), which puts the table inside the sweep with no edit to the route.",
        "unverified": {
          "question": "Whether a merchant-of-record payment notification carries a STATUTORY RETENTION BASIS (tax, accounting, or consumer-law) that would override erasure for this row, and for how long.",
          "ownerItem": "O-3",
          "status": "UNVERIFIED — no primary source for such an obligation could be read from this environment, and none is invented here. The default shipped is to PURGE, because honouring the published promise needs no justification while RETAINING a person's data does. Nothing writes to this table yet (OWNER_QUEUE A-1), so the decision is still free; it stops being free at the first payment."
        }
      },
      "writtenBy": [
        "services/platform/migrations/0004_money_rail.sql",
        "services/platform/migrations/0006_erasure_reach.sql",
        "services/platform/src/lib/mor/store.ts"
      ],
      "disclosure": {
        "page": "privacy.html",
        "quote": "if you buy something, the transaction is processed by the relevant app store or by our payment processor"
      },
      "note": "⚠️ THE PUBLISHED SENTENCE QUOTED ABOVE IS TRUE AND INCOMPLETE. It tells a reader the transaction is processed by a payment processor; it does not tell them we KEEP A COPY of what that processor sent us about them. That is a wording correction and it belongs in the same O-3 version bump as the merchant-of-record naming — recorded here rather than fixed by editing a published page from a code change."
    },
    {
      "id": "table:platform_db.provider_accounts",
      "kind": "d1-table",
      "name": "provider_accounts",
      "personalData": true,
      "holds": "The link between a provider's subscription id and one of our account ids, written once, on the first notification that provably carries both ([pipeline 5]M-7). Four columns and two timestamps; no name, no email, no payment detail. It exists because whether checkout metadata propagates onto RENEWAL transactions is a vendor fact this repo could not establish from a primary source, and a renewal that cannot be attributed is a paying customer who silently loses access.",
      "retention": {
        "kind": "keep",
        "reason": "It is the answer to 'why do we believe this subscription belongs to this account', and it is needed for as long as the subscription can renew, lapse or be refunded — i.e. for the life of the account. It is ERASED WITH THE ACCOUNT and needs no special handling to be: the shared erasure route derives its table set from the schema (every table carrying a `user_id` column), so this table joined that set the moment it was created, with no edit to the route."
      },
      "erasure": {
        "kind": "purge",
        "route": "services/platform/src/routes/account.ts",
        "reason": "The row IS the link between a person and a subscription. When the person is erased the link has nothing left to join, and leaving it would keep their account id in the database after the account is gone."
      },
      "writtenBy": [
        "services/platform/migrations/0004_money_rail.sql",
        "services/platform/src/lib/mor/store.ts"
      ],
      "disclosure": {
        "page": "privacy.html",
        "quote": "if you buy something, the transaction is processed by the relevant app store or by our payment processor"
      }
    },
    {
      "id": "table:platform_db.unclaimed_payments",
      "kind": "d1-table",
      "name": "unclaimed_payments",
      "personalData": true,
      "holds": "Money that arrived and could not be attributed to an account ([pipeline 5]M-7) — the alternative to the legacy route's acknowledge-and-discard, which left the payer's only record with the provider. The buyer's email is stored as a SHA-256 HEX DIGEST, never as an address: the claim-on-first-signin lookup is an exact match, which a hash serves exactly as well, so the queryable column is not a list of customers' email addresses. Also the provider's own ids, for a support lookup in their dashboard.",
      "retention": {
        "kind": "undecided",
        "ownerItem": "O-3",
        "reason": "A row here is the evidence that somebody paid and we could not find them; deleting it destroys the only thing that could ever resolve their support ticket, and keeping it forever means holding a pseudonymised identifier for a person who may never appear. The ROW is not erased; the LINK to the erased person is — see `erasure` below."
      },
      "erasure": {
        "kind": "unlink",
        "column": "claimed_user_id",
        "route": "services/platform/src/routes/account.ts",
        "reason": "🔴 THE QUESTION 0004 DELIBERATELY LEFT OPEN, ANSWERED — AND ANSWERED THE WAY THAT DESTROYS NOTHING. The row is the record of money that arrived and could not be attributed: deleting it removes the only thing that could ever resolve that payer's support ticket, so it is kept. But `claimed_user_id` is the ERASED PERSON'S ACCOUNT ID, and a row still addressable by the identifier of someone who asked to be forgotten is not erased. So the route NULLs the column and keeps the row. The rule is derived from the schema, not from a list: `user_id` means the row IS theirs (delete it), `*_user_id` means the row REFERENCES them (null it), so a future `linked_user_id` is covered the day it is created. ⚠️ `claimed_at` is deliberately LEFT SET — 'this payment was claimed' stays true and is what stops it being claimed twice; what is erased is WHO. ⚠️ HONEST RESIDUE: `customer_email_hash` is a SHA-256 of an email address and is NOT anonymous — it is pseudonymised, and the same person hashing to the same value is the whole point of the lookup. Unlinking does not remove it, and no claim is made here that it does; whether an erasure should also clear the hash on an unclaimed row is part of the same undecided retention question above."
      },
      "writtenBy": [
        "services/platform/migrations/0004_money_rail.sql",
        "services/platform/src/lib/mor/store.ts"
      ],
      "disclosure": {
        "page": "privacy.html",
        "quote": "if you buy something, the transaction is processed by the relevant app store or by our payment processor"
      }
    },
    {
      "id": "table:platform_db.cancellation_requests",
      "kind": "d1-table",
      "name": "cancellation_requests",
      "personalData": true,
      "holds": "One row per time a user pressed Cancel ([pipeline 5]M-9, ROSCA). It holds WHO asked (the Supabase `sub`), for which app, in which money world, and which provider subscription it was about — copied at request time rather than joined, because the entitlement row can be revoked and rewritten afterwards and this record has to keep saying what the user was cancelling. It holds no name, no email and no payment detail. 🔴 IT EXISTS BECAUSE OF A GAP THE USER WOULD OTHERWISE FALL INTO: only the merchant of record can actually stop a subscription, so between 'I pressed cancel' and 'the rail acted' the ONLY evidence that they asked is what we wrote down. Without it, a cancellation the provider dropped is indistinguishable from one nobody requested, and the person still being billed is the one who has to prove otherwise.",
      "retention": {
        "kind": "keep",
        "reason": "KEPT until the account is erased, and the reason it is kept is the whole point of the table: it is the only evidence the user asked. It joins the erasure set BY CONSTRUCTION: the column is spelled `user_id`, and the shared erasure route (services/platform/src/routes/account.ts) derives the tables it empties from the schema, so this is emptied with no edit to the route. That was verified rather than assumed — adding the table turned services/platform/test/auth.test.ts red on its 'an empty derivation is a refusal' fixture, which is the derivation working. (Contrast `unclaimed_payments.claimed_user_id`, which is UNLINKED rather than deleted for the reason recorded above.)"
      },
      "erasure": {
        "kind": "purge",
        "route": "services/platform/src/routes/account.ts",
        "reason": "A cancellation request IS a record about a person and goes when they do. The evidentiary value it exists for — 'the user asked three times and nothing happened' — is value to THAT user; once the account is erased there is nobody it could be produced for."
      },
      "writtenBy": [
        "services/platform/migrations/0005_cancellation_requests.sql",
        "services/platform/src/routes/cancellation.ts"
      ],
      "disclosure": {
        "page": "privacy.html",
        "quote": "if you buy something, the transaction is processed by the relevant app store or by our payment processor"
      }
    },
    {
      "id": "table:platform_db.revocation_reasons",
      "kind": "d1-table",
      "name": "revocation_reasons",
      "personalData": false,
      "holds": "NOTHING ABOUT A PERSON. Eight seeded rows naming why access can end or be restored — refund_approved, chargeback, chargeback_reversed, subscription_expired, trial_expired, payment_failed_final, cancelled_at_period_end, subscription_paused — each with a flag for whether it RESTORES access and an English sentence saying what it means. It is a lookup table, and it is a table rather than a CHECK constraint or a comment precisely so that the set is machine-readable and can be extended additively ([pipeline 5]M-3).",
      "retention": {
        "kind": "keep",
        "reason": "Reference data seeded by the migration itself. Deleting it would leave every `revocation_reason` value in `entitlements` unresolvable — the rows would still say why access ended, and nothing would be able to say what those words mean."
      },
      "erasure": {
        "kind": "no-personal-data",
        "reason": "Eight seeded vocabulary rows. There is nothing here to erase, and an erasure sweep that DID empty it would break every entitlement row's `revocation_reason` for everyone else — which is why the erasure test asserts this table is still full after a deletion, rather than assuming a sweep would leave it alone."
      },
      "writtenBy": [
        "services/platform/migrations/0004_money_rail.sql"
      ]
    },
    {
      "id": "table:platform_db.events",
      "kind": "d1-table",
      "name": "events",
      "personalData": true,
      "holds": "Usage-statistics events, collected only after consent. Each row carries the client's install id (anon_id), the app, platform, version and a coarse geo derived at the network edge.",
      "retention": {
        "kind": "swept",
        "periodDays": 400,
        "sweptBy": "services/platform/src/scheduled.ts — the `events` limb of `retentionSweep`, EVENTS_RETENTION_DAYS. ⚠️ Its cutoff is bounded by the rollup watermark (`rollupBoundedCutoff`), not by age alone, so a stalled rollup makes this limb INERT rather than destructive.",
        "reason": "🔒 LOCKED 400 DAYS by [ADR 045] (owner-delegated 2026-08-12). Floor 365 — DPDP Rules 2025 require a minimum of one year, phased in ~14 May 2027, which is the trap: a 90-day period feels privacy-forward today and becomes non-compliant then, with the rows already unrecoverable. Ceiling 425 — the GA4 standard maximum. 📌 THIS ROW READ `undecided` / ownerItem O-3 UNTIL 2026-08-12, THREE DAYS AFTER THE NUMBER WAS LOCKED. It was therefore PRINTING `RETENTION UNDECIDED (O-3)` on every run, asking the owner for a decision they had already made — the exact 'a rule left unamended while already false is worse than no rule' failure [ADR 034] names. Corrected in the increment that added the rollup, because that increment is what made anyone read this row again."
      },
      "erasure": {
        "kind": "pseudonymous",
        "reason": "Keyed on `anon_id`, an install id, and there is NO `anon_id -> user_id` map anywhere — [ADR 020] forbids one ever being written, because that map is the thing that would turn pseudonymous analytics into erasure-subject personal data. So an account deletion cannot address these rows, and 'zero rows for this user' is vacuously true rather than achieved. ⚠️ THE PUBLISHED PROMISE IS WIDER THAN THAT: privacy.html promises deletion of 'information associated with you' without qualification. Either the promise narrows or the client sends its anon_id on erasure — a wording decision that belongs in the same O-3 version bump as the other corrections, not in a code change. This declaration is CHECKED: the guard fails if this table ever grows a user-shaped column, because on that day the sentence above stops being true."
      },
      "writtenBy": [
        "services/platform/migrations/0002_analytics.sql",
        "services/platform/src/routes/events.ts"
      ],
      "disclosure": {
        "page": "privacy.html",
        "quote": "if you agree when asked, we record which features of an app are used and when"
      },
      "note": "🔴 KEYED ON anon_id, NOT ON A USER. That is deliberate (nothing writes an anon_id → user_id mapping, and the route's own header says nothing ever may) and it has a consequence stage 8 records rather than hides: 'delete my account' provably cannot purge these rows, because there is nothing in them that identifies the account. Either the erasure promise narrows or the client sends its anon_id on erasure — a wording decision, and it belongs in the same version bump as the other O-3 corrections."
    },
    {
      "id": "table:platform_db.events_daily",
      "kind": "d1-table",
      "name": "events_daily",
      "personalData": true,
      "holds": "🔴 A ROLLUP, AND STILL PERSONAL DATA — the two are not in tension and this row exists to say so plainly. One aggregate per (day, app, install, event, feature) with a row count. It DROPS nine of the raw table's columns — session id, platform, app version, client clock, country, region, city, consent id, and the raw params beyond the feature name — but it KEEPS `anon_id`, the install identifier, because every one of the five decision numbers is an install-level DISTINCT count and three are per-install joins across different event types. Marginal counts by (day, app, event) cannot reconstruct any of them, which was measured rather than assumed. So this is a SMALLER personal-data surface than `events`, not an anonymous one, and it must never be described as anonymised.",
      "retention": {
        "kind": "swept",
        "periodDays": 1100,
        "sweptBy": "services/platform/src/scheduled.ts — the `events_daily` limb of `retentionSweep`, EVENTS_DAILY_RETENTION_DAYS. Deletes on AGE ALONE, unlike the `events` limb: nothing downstream consumes this table, so there is no watermark for its cutoff to be bounded by. It is the end of the chain.",
        "reason": "1100 days (~3 years) = 1095 + 5 days' margin, the same shape as events' 400 = 365 + 35. The floor is 1095 — three calendar years, the minimum for TWO year-over-year comparisons — and that is the first thing the raw table's 400 days cannot buy, which is the entire reason this table exists. No statutory ceiling applies: GA4's 425-day cap governs raw, event-level, device-and-geo-bearing data, and this table has none of that. DPDP s.6(1) purpose limitation still binds, and 'portfolio seasonality across three years' is a stated, bounded purpose. ⚠️ THIS IS A POLICY NUMBER, NOT A MEASUREMENT — it is the one value in the rollup increment an owner may reasonably want to move, and it is locked rather than deferred only because tooling/ops/register.json caps undeclared periods at `_maxUndeclared: 0`."
      },
      "erasure": {
        "kind": "pseudonymous",
        "reason": "Keyed on `anon_id` and carrying no `user_id`, exactly as `events` is, and for the same reason: [ADR 020] forbids an anon_id → user_id map ever being written, because that map is what would turn pseudonymous analytics into erasure-subject personal data. So an account deletion cannot address these rows either. ✅ BUT A CONSENT WITHDRAWAL CAN, and that is the reach this table does have: `consent_artifacts` carries `anon_id`, so a withdrawal resolves to an install id and reaches this table by `DELETE … WHERE anon_id = ?` directly. That is why the rollup deliberately does NOT carry `consent_id` — it would be a second identifier buying no erasure reach the first does not already give. The same widening gap `events` records against privacy.html applies here unchanged."
      },
      "writtenBy": [
        "services/platform/migrations/0007_events_rollup.sql",
        "services/platform/src/scheduled.ts"
      ],
      "disclosure": {
        "page": "privacy.html",
        "quote": "if you agree when asked, we record which features of an app are used and when"
      },
      "note": "Derived entirely from `events` and disclosed by the same sentence — it holds no category of information the raw table did not already hold, and strictly fewer. The reason it is listed separately rather than folded into the events row is that its LIFETIME differs: 1100 days against 400, so a reader asking 'what is still held about me after two years' gets the right answer only if this row exists."
    },
    {
      "id": "table:platform_db.rollup_state",
      "kind": "d1-table",
      "name": "rollup_state",
      "personalData": false,
      "holds": "One row per rollup naming the last complete UTC day fully consumed into `events_daily`. Three columns: the rollup's name, a 'YYYY-MM-DD' watermark, and a timestamp. No identifier of any person, device or install.",
      "retention": {
        "kind": "keep",
        "reason": "One row, no personal data, and it is the FAIL-CLOSED INTERLOCK the events sweep reads. Deleting it frees nothing measurable and makes the events sweep INERT — which is the safe direction, and precisely why it must never be swept on a schedule that could race the job that writes it. A retention period on this table would be a period on a control, not on data."
      },
      "erasure": {
        "kind": "no-personal-data",
        "reason": "A date and a table name. There is nothing here to erase and no subject to erase it for."
      },
      "writtenBy": [
        "services/platform/migrations/0007_events_rollup.sql",
        "services/platform/src/scheduled.ts"
      ],
      "note": "Listed because this file's own rule is that EVERY table in platform_db carries a row — including the ones with nothing personal in them. A table absent from the inventory is indistinguishable from a table nobody checked, which is the state the inventory exists to make impossible."
    },
    {
      "id": "table:platform_db.consent_artifacts",
      "kind": "d1-table",
      "name": "consent_artifacts",
      "personalData": true,
      "holds": "Append-only record that a consent decision was made: which install, when, granted or withdrawn, and WHICH POLICY VERSION was shown. No IP address, no device fingerprint.",
      "retention": {
        "kind": "keep",
        "reason": "The artifact IS the evidence that the choice was the user's. Deleting it destroys the only proof that consent was obtained, which is the opposite of what a retention limit is for. Append-only: a withdrawal is a NEW row, never an edit of the old one."
      },
      "erasure": {
        "kind": "pseudonymous",
        "reason": "Keyed on `anon_id` for the same [ADR 020] reason as `events`, so an account deletion cannot address it either. Unlike `events` there is also a positive reason not to want it addressable: the artifact is the proof that a consent decision was the user's, and a store that can be emptied by asking is not evidence. Checked the same way — a user-shaped column appearing here turns this row's claim false and the build red."
      },
      "writtenBy": [
        "services/platform/migrations/0002_analytics.sql",
        "services/platform/src/routes/events.ts"
      ],
      "disclosure": {
        "page": "privacy.html",
        "quote": "We keep a record of each consent decision"
      },
      "note": "The policy version in this table is what tooling/ci/assert-policy-archive.mjs exists to keep resolvable: a record citing 2026-07-26 is worth exactly our ability to still serve the 2026-07-26 bytes."
    },
    {
      "id": "table:platform_db.cron_heartbeat",
      "kind": "d1-table",
      "name": "cron_heartbeat",
      "personalData": false,
      "holds": "One row per scheduled run: proof the nightly cron actually fired. No user data of any kind.",
      "retention": {
        "kind": "keep",
        "reason": "Operational evidence, and tiny. It is how a silently-dead scheduler is noticed at all."
      },
      "erasure": {
        "kind": "no-personal-data",
        "reason": "One row per scheduled run: a timestamp and an outcome. No user id, no install id, nothing that is about anybody."
      },
      "writtenBy": [
        "services/platform/migrations/0003_cron_heartbeat.sql"
      ]
    },
    {
      "id": "table:subly_db.subscriptions",
      "kind": "d1-table",
      "name": "subscriptions",
      "personalData": true,
      "holds": "The subscriptions a Subly user tracks — service name, cost, renewal date. USER-ENTERED CONTENT about the person's own finances, keyed on their user id.",
      "retention": {
        "kind": "keep",
        "reason": "This is the user's own data and the entire point of the app; it exists until they delete it or delete their account. The in-app deletion control exists in BOTH trees and is asserted — the `account-deletion-works` coverage key on the brick, and tooling/ci/assert-deletion-control.mjs over apps/subly with no exemption ([ADR 027]) — and since 2026-08-04 that control REACHES THESE ROWS. See `erasure` below."
      },
      "erasure": {
        "kind": "purge",
        "route": "services/subly-api/src/routes/account.ts",
        "reason": "🔴 THE GAP THIS ROW USED TO DECLARE, CLOSED 2026-08-04. Until then these four rows read `no-route`: DELETE /v1/account lived only on the shared platform Worker, which sweeps PLATFORM_DB, so a Subly user could delete their account, lose their login, and leave every subscription, budget and payment they had ever entered in a database no login could ever reach again.\n\nTHE OBJECTION THAT HELD IT UP WAS REAL AND IS ANSWERED RATHER THAN WAIVED. services/subly-api/src/middleware/auth.ts accepts a token signed with the shared HS256 secret when the asymmetric path fails, and an irreversible route must not sit behind a symmetric secret — one leaked environment variable would mint a token for any user of any app. So the route was NOT put behind that middleware. `erasureAuth` (same file) verifies ES256 against the PUBLIC JWKS and is not even given the environment, so the secret is out of scope by the shape of the function signature; src/index.ts mounts DELETE /v1/account behind it; and the route ADDITIONALLY refuses any request whose `tokenAssurance` is not 'asymmetric'. Two independent limbs, because a mounting is one line somebody can move in a tidy-up and a refusal inside the handler is not. Both are recorded failing inputs in services/subly-api/test/erasure.test.ts, which drives the REAL Worker and proves the SAME HS256 token is ACCEPTED by GET /v1/subscriptions and REFUSED here — so the refusal is about the boundary, not about an invalid token.\n\nThe table set is DERIVED from the schema (every table carrying a `user_id`), so a fifth user-owned table is covered by its own migration with no edit to the route, and an EMPTY derivation is a 503 refusal rather than a fast path.\n\n⚠️ THIS ROUTE ERASES NOTHING BUT subly_db. The identity record and platform_db stay the shared Worker’s: services/platform/src/routes/account.ts relays the caller’s own bearer token to this route AFTER its service-role precondition and BEFORE it deletes the identity, so a failure here stops the erasure while the user still has a login to retry with. Ordering, not duplication — exactly one piece of code destroys the identity."
      },
      "writtenBy": [
        "services/subly-api/migrations/0001_init.sql",
        "services/subly-api/src/routes/subscriptions.ts"
      ],
      "disclosure": {
        "page": "privacy.html",
        "quote": "in apps that offer accounts) create an account"
      }
    },
    {
      "id": "table:subly_db.budgets",
      "kind": "d1-table",
      "name": "budgets",
      "personalData": true,
      "holds": "A user's monthly budget figure. User-entered, keyed on their user id.",
      "retention": {
        "kind": "keep",
        "reason": "The user's own data, kept until they delete it. Same erasure position as subscriptions."
      },
      "erasure": {
        "kind": "purge",
        "route": "services/subly-api/src/routes/account.ts",
        "reason": "Same as table:subly_db.subscriptions, and by the same mechanism rather than by a second decision: the column is spelled `user_id`, and the erasure route derives its table set from the schema, so `budgets` is in the sweep without being named anywhere in the code. tooling/ci/assert-erasure-reach.mjs is what refuses to let it drop back out."
      },
      "writtenBy": [
        "services/subly-api/migrations/0001_init.sql",
        "services/subly-api/src/routes/budget.ts"
      ],
      "disclosure": {
        "page": "privacy.html",
        "quote": "in apps that offer accounts) create an account"
      }
    },
    {
      "id": "table:subly_db.budget_categories",
      "kind": "d1-table",
      "name": "budget_categories",
      "personalData": true,
      "holds": "Per-category budget allocations. User-entered, keyed on their user id.",
      "retention": {
        "kind": "keep",
        "reason": "The user's own data, kept until they delete it."
      },
      "erasure": {
        "kind": "purge",
        "route": "services/subly-api/src/routes/account.ts",
        "reason": "Same as table:subly_db.subscriptions, and by the same mechanism rather than by a second decision: the column is spelled `user_id`, and the erasure route derives its table set from the schema, so `budget_categories` is in the sweep without being named anywhere in the code. tooling/ci/assert-erasure-reach.mjs is what refuses to let it drop back out."
      },
      "writtenBy": [
        "services/subly-api/migrations/0001_init.sql",
        "services/subly-api/src/routes/budget.ts"
      ],
      "disclosure": {
        "page": "privacy.html",
        "quote": "in apps that offer accounts) create an account"
      }
    },
    {
      "id": "table:subly_db.payment_history",
      "kind": "d1-table",
      "name": "payment_history",
      "personalData": true,
      "holds": "Dates and amounts a user recorded against their own subscriptions. ⚠️ A record OF payments, not a payment instrument: no card, bank or account number is stored, which is the claim tooling/legal/policy-claims.json asserts against every migration in the tree.",
      "retention": {
        "kind": "keep",
        "reason": "The user's own history, kept until they delete it."
      },
      "erasure": {
        "kind": "purge",
        "route": "services/subly-api/src/routes/account.ts",
        "reason": "Same as table:subly_db.subscriptions, and by the same mechanism rather than by a second decision: the column is spelled `user_id`, and the erasure route derives its table set from the schema, so `payment_history` is in the sweep without being named anywhere in the code. tooling/ci/assert-erasure-reach.mjs is what refuses to let it drop back out."
      },
      "writtenBy": [
        "services/subly-api/migrations/0001_init.sql",
        "services/subly-api/src/routes/subscriptions.ts"
      ],
      "disclosure": {
        "page": "privacy.html",
        "quote": "do not collect"
      }
    },
    {
      "id": "kv:33e4e58abb644d0e917a159227c6c134",
      "kind": "kv-namespace",
      "name": "CONFIG_KV",
      "personalData": false,
      "holds": "Per-app configuration overrides, keyed `config:<app>`. DATA and flags, never anything about a person.",
      "retention": {
        "kind": "keep",
        "reason": "Configuration. It is the current value or it is nothing."
      },
      "writtenBy": [
        "services/platform/wrangler.jsonc"
      ]
    },
    {
      "id": "kv:bcfcb1618d1a4bfd8d02f87d3d21010d",
      "kind": "kv-namespace",
      "name": "JWKS_CACHE",
      "personalData": false,
      "holds": "A cache of the identity provider's public signing keys. Public key material, nothing user-specific.",
      "retention": {
        "kind": "keep",
        "reason": "A cache of public keys, refreshed on rotation. Nothing here identifies anyone."
      },
      "writtenBy": [
        "services/subly-api/wrangler.jsonc"
      ],
      "note": "The brick's backend variant binds the SAME namespace id, so this row covers both. Deduplicated by id, not by binding name — two Workers sharing a cache is one store."
    },
    {
      "id": "kv:nikatru-signups",
      "kind": "kv-namespace-pages",
      "name": "SIGNUPS",
      "personalData": true,
      "holds": "Launch-notification signups: `sub:<email>` → { email, ts }. AND, under a separate `rl:` prefix, a short-lived per-fingerprint rate-limit counter. The email address is stored in plain text and is the only directly-identifying value anywhere in this repository.",
      "retention": {
        "kind": "ttl",
        "reason": "DECIDED BY THE OWNER 2026-08-09: 365 days. sites/nikatru/functions/api/subscribe.js sets it in one place — `const SIGNUP_RETENTION_DAYS = 365;` — and the signup put now carries `expirationTtl` exactly as the rate-limit key beside it always did. This was THE SHARPEST GAP IN THIS FILE and it was deliberately left open until then: setting a TTL on this namespace silently deletes the owner's real launch list after whatever number an agent picks, and no number was derivable from anything in this tree, so what was needed was a decision and not a guess. tooling/ops/register.json → retention.kv.nikatru-signups.signup carries the machine-checked half (`rule: ttl` + a `ttlSource` that assert-retention-coverage.mjs must find verbatim in the code), and tooling/ci/test/signup-retention.test.mjs asserts the put really carries the TTL. ⚠️ KEYS WRITTEN BEFORE 2026-08-09 KEEP NO EXPIRY — Workers KV fixes expiry at write time, so ageing those out is an operator action against the namespace, not something this code does."
      },
      "erasure": {
        "kind": "no-route",
        "blockedBy": "No code in this repository deletes a signup key. The store is a Cloudflare Pages KV namespace bound in the dashboard, keyed `sub:<email>`, and the only writer (sites/nikatru/functions/api/subscribe.js) is a PUT — there is no DELETE handler and no authenticated surface on this site that could own one. An erasure request for this store is served by the OPERATOR deleting the key, which is a real answer and not an automated one, and sites/nikatru/delete-account.html says so in those words rather than implying the website erases it. Building a public delete endpoint here would need an ownership proof (a confirmation link to the address) that nothing on this site can send today — no transactional mail path exists."
      },
      "writtenBy": [
        "sites/nikatru/functions/api/subscribe.js"
      ],
      "disclosure": {
        "page": "privacy.html",
        "quote": "email address and the time you signed up"
      },
      "note": "Bound in the Cloudflare Pages dashboard and declared in NO wrangler config, which is exactly why the Pages-Function walk exists: a wrangler-only inventory would report full coverage while missing the one store holding a plain email address."
    }
  ]
}
