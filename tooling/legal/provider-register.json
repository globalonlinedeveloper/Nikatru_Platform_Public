{
  "_readme": [
    "THE PROVIDER REGISTER — [pipeline K-5] Private/requirements/ (was pipeline/08-compliance-legal.md, folded into the JSON spec 2026-08-15)",
    "",
    "Every third party that touches a payment, a user's data, or the delivery of the Services: what role",
    "it plays, whether the published pages name it, and — the field the whole file hangs off — whether it",
    "is REACHABLE IN CODE today.",
    "",
    "WHY IT EXISTS. K-5 says 'the commercial surface names the actual seller and payment path'. Nothing",
    "compared the two. At the moment this register landed the three published pages and the code disagreed",
    "in four independent places, and no build anywhere could go red over any of them:",
    "",
    "  · services/subscriptiontracker-api/src/routes/webhooks.ts declares POST /revenuecat. RevenueCat is named in no",
    "    published page and in no ADR. ⏱ 2026-09-16: that route is RETIRED; RevenueCat is reached through",
    "    services/platform's POST /v1/money/:provider (its revenuecatVerifier), and privacy.html names it.",
    "  · [ADR 004] LOCKED Paddle as the merchant of record on 2026-07-23. Paddle is named in none of",
    "    privacy.html, terms.html or refund.html. ✅ HALF FIXED — terms.html §4 and refund.html §2/§4 now",
    "    name Paddle and describe the merchant-of-record role ([ADR 039]). privacy.html still says only",
    "    'a merchant of record' and is the remaining half, because that page is inside the policy-VERSION",
    "    surface: correcting it is a version bump plus an archive snapshot (assert-policy-archive.mjs),",
    "    which is one owner signature covering every privacy.html correction at once, not three.",
    "  · terms.html and refund.html say website purchases are processed by Razorpay, while privacy.html",
    "    says 'We do not sell anything directly from this website today'. The pages contradict each other.",
    "    ✅ FIXED — the `website-seller-contradiction` gap was retired when the wording landed. All three",
    "    pages now agree that nothing is sold directly from the website yet; the two that are not version-",
    "    gated additionally name the seller that will take the payment.",
    "  · privacy.html says usage statistics AND CRASH REPORTS run on infrastructure provided by",
    "    Cloudflare. Crash reports run on Oracle Cloud (Private/runbooks/glitchtip.md). ✅ FIXED at the",
    "    2026-08-10 signature, which split the sentence so each system names its own host. ⏱ AND THE HOST",
    "    ITSELF THEN MOVED: GlitchTip left Box A for Box B on 2026-09-02, so from that date the split",
    "    sentence named the right SHAPE and the wrong MACHINE. Corrected in the 2026-09-05 mint — the sink",
    "    row is now `hostinger` and the page names Hostinger. See that row's notes.",
    "",
    "🔴 EVERY ONE OF THOSE IS FIXED BY EDITING A PUBLISHED LEGAL PAGE, WHICH IS OWNER WORK (OWNER_QUEUE",
    "O-3). They are therefore carried as `disclosureGap` rows that PRINT on every run — never silently",
    "exempted, and never made a build failure, because failing CI on copy only the owner can write blocks",
    "every other lane on this repository. tooling/ci/assert-policy-claims.mjs FAILS the moment a gap row's",
    "stated reason stops being true, so an exemption cannot outlive what it was granted for.",
    "",
    "⚠️ WHAT IS PUBLIC HERE AND WHAT IS NOT. This is a public repository. A row records the provider's",
    "NAME, its ROLE, where the code reaches it and whether a page discloses it. It records nothing about",
    "the merchant account itself — no merchant ids, no KYC state, no bank or payout details, no contract",
    "terms. Those live in the private SSoT and a row points at them by NAME only, in `privateRecord`,",
    "the same split tooling/channel-register.json uses for signing custody. See tooling/legal/README.md.",
    "",
    "Enforced by tooling/ci/assert-policy-claims.mjs:",
    "  · every claims row typed `provider` resolves to a row here                        [K-5]",
    "  · every row with `reachableAt` names a route path that really exists in services/  [K-5]",
    "  · every route path segment in services/** is either a provider row's tell or is",
    "    named in `nonProviderRouteSegments` below — so a NEW payment webhook cannot be",
    "    added without somebody writing its name down                                     [K-5]",
    "  · a `merchant_of_record` row with status `live` MUST be named in BOTH terms.html",
    "    and refund.html; one that is not yet live PRINTS with its owner item             [K-5]",
    "  · every `disclosureGap` row carries an ownerItem and a `stillTrue` probe, and the",
    "    guard FAILS when the probe stops matching — the gap is resolved, retire the row   [C-6 split]",
    "  · COVERAGE LOST on an empty register or zero route paths extracted                 [F-10]",
    "",
    "And by tooling/ci/assert-sink-disclosure.mjs — THE SINK-DISCLOSURE LIMB:",
    "  · every `infrastructure` row with status `live` DECLARES the personal-data",
    "    categories it receives and retains (`receives`), with a stated `receivesBasis`   [K-5]",
    "  · every published \"we do not collect / do not store X\" denial is SCOPED against",
    "    the union of those declarations, and FAILS when a live sink declares what a",
    "    page denies                                                                      [K-5]",
    "  · a denial that resolves to NO declared category FAILS unless it is classified",
    "    in `denialsOutOfScope` — a new denial cannot be published unscoped               [K-5]",
    "  · COVERAGE LOST on zero live infrastructure rows, an empty declared union, or",
    "    zero denials extracted from the pages                                            [F-10]"
  ],
  "nonProviderRouteSegments": {
    "_why": [
      "🔴 THIS IS THE HALF THAT MAKES THE ROUTE LIMB UNFAKEABLE, and it is data rather than a list",
      "inside the guard on purpose. The guard extracts every literal path segment registered in",
      "services/**/src/**/*.ts. A segment is allowed to exist only if it is either a provider row's",
      "tell (RevenueCat's `/revenuecat`) or is written down HERE as a resource of ours.",
      "",
      "So adding `app.post('/paddle', …)` — or `/stripe`, or `/lemonsqueezy` — turns the build RED",
      "until somebody either adds the provider row or claims the segment as ours. There is no third",
      "option and no way to pad the allowlist except by naming the thing you are adding.",
      "",
      "⚠️ `:param` segments are dropped before the comparison, not listed here: `/:id` and `/:app` are",
      "placeholders, and listing them would be listing the syntax rather than a resource."
    ],
    "segments": [
      "v1",
      "health",
      "config",
      "webhooks",
      "events",
      "consent",
      "subscriptions",
      "budget",
      "categories",
      "entitlements",
      "renewals",
      "account",
      "money",
      "plan",
      "cancel",
      "checkout",
      "subject",
      "receipts",
      "apple-token",
      "report"
    ],
    "_subject": [
      "🟢 `subject` ADDED 2026-09-09, AND THIS LIMB CAUGHT IT AGAIN. GET /v1/entitlements/subject is the",
      "\"what do I own\" read the per-app route cannot answer — [ADR 057] §5, the bundle union. It is a",
      "RESOURCE OF OURS and not a provider tell: nothing posts to it, it takes no body, and it is behind",
      "platformAuth like the route it sits under.",
      "",
      "⚠️ IT IS LISTED BECAUSE THE GUARD IS RIGHT TO ASK. A new segment under a Worker that already",
      "carries the money rail is exactly where a payment webhook would land unnoticed, and the answer",
      "has to be written down by the person adding it rather than inferred from the name."
    ],
    "_receipts": [
      "🟢 `receipts` ADDED 2026-09-09, AND THIS LIMB CAUGHT IT AGAIN. POST /v1/receipts/:store is a",
      "RESOURCE OF OURS and NOT a provider tell, and the distinction is the whole design: a provider",
      "tell is a segment the PROVIDER posts to (RevenueCat's `/revenuecat`), whereas nothing external",
      "posts here at all. Our own authenticated client posts an opaque store token, and the SERVER then",
      "calls the store's API itself — the push is never the evidence ([ADR 057]; the bundle entitlement",
      "design §3.2). Google's RTDN carries no proof in its body and Microsoft has no push whatsoever.",
      "",
      "⚠️ THIS IS EXACTLY THE SEGMENT THE LIMB EXISTS FOR. A route named `receipts` under the Worker that",
      "already carries the money rail is where a payment webhook would land unnoticed, and the answer has",
      "to be written down by the person adding it rather than inferred from the name. If a store rail",
      "ever DOES push here, this entry becomes false and a provider row is owed instead."
    ],
    "_checkout": [
      "🟢 `checkout` ADDED 2026-08-12, AND THIS LIMB CAUGHT IT AGAIN — the third time. The route",
      "services/platform/src/routes/checkout.ts mounts POST /v1/checkout (the Paddle",
      "create-transaction server half, [ADR 044]) and it passed typecheck, its own tests and every",
      "other guard while being invisible to this register.",
      "",
      "`checkout` is OURS: it is the door, not a vendor. The same distinction `_money` records one",
      "entry down, and the route's own author reached it independently — services/platform/src/index.ts",
      "notes it is mounted at `/v1/checkout` RATHER than under a segment named for a merchant of record.",
      "",
      "⚠️ THE TELL THAT WOULD MAKE THIS WRONG: if the segment were ever `/paddle`, this entry would be",
      "the wrong answer and a provider row would be the right one. The vendor behind the door is Paddle;",
      "the door is ours. Naming the door after the vendor is what turns a resource into a tell."
    ],
    "_money": [
      "🟢 `money` WAS ADDED BECAUSE THIS LIMB CAUGHT IT — the second time in two days, on a change that",
      "had already passed every other guard. services/platform/src/routes/money.ts mounts",
      "POST /v1/money/:provider ([pipeline 5]M-1, the merchant-of-record webhook) and CI went red naming",
      "a segment nobody had classified. `money` is OURS: it is the door, not a vendor.",
      "",
      "⚠️ AND THE PROVIDER IS A `:param`, WHICH THIS LIMB DROPS BY DESIGN. So adding Lemon Squeezy's",
      "adapter tomorrow adds NO new literal segment, and the K-5 property this limb exists for — 'a new",
      "payment webhook cannot land without somebody writing its name down' — would not fire here. That",
      "is not a hole, and it is not left to a comment either: the provider set is DATA in",
      "`MOR_VERIFIERS` (services/platform/src/lib/mor/registry.ts), and tooling/ci/assert-mor-adapters.mjs",
      "now FAILS when a registered rail has no row in THIS FILE. The obligation moved to the register",
      "that actually knows the provider set, rather than being satisfied by a URL shape.",
      "",
      "Paddle's row keeps `reachableAt: null` deliberately: the reverse check requires the last segment",
      "of `reachableAt` to appear as a LITERAL route segment somewhere in services/**, and `paddle` never",
      "does — it is resolved from the registry at request time. Claiming `/v1/money/paddle` would make",
      "this file assert something no route registration says."
    ],
    "_account": [
      "🟢 `account` WAS ADDED BECAUSE THIS LIMB CAUGHT IT, which is the whole point and worth recording.",
      "services/platform/src/routes/account.ts (DELETE /v1/account, [pipeline B-5]) landed on main WHILE",
      "this register was being written. The guard went red on the next run naming the new segment: a route",
      "nobody had classified as ours or as a provider's. It is ours — the erasure route — so it is claimed",
      "here rather than given a provider row. That is the limb working on a real, unplanned change on its",
      "first day, rather than on a fixture."
    ],
    "_plan": [
      "🟢 `plan` AND `cancel` WERE ADDED BECAUSE THIS LIMB CAUGHT THEM — the third time in three days,",
      "on a change that had already passed every other guard. services/platform/src/routes/cancellation.ts",
      "mounts POST /v1/plan/cancel ([pipeline 5]M-9, the ROSCA cancel path) and CI went red naming two",
      "segments nobody had classified.",
      "",
      "Both are OURS and neither is a vendor tell. `plan` is the thing the user bought, in the chassis's",
      "own vocabulary — deliberately not \"subscription\", which [pipeline C-10] bans from shared code as",
      "apps/subscriptiontracker's domain noun. `cancel` is the verb. No merchant of record is named in either, and none",
      "can be: the route resolves the subscription from the verified session, never from the request."
    ],
    "_appleToken": "⏱ 2026-09-16 · O-SIWA-TOKEN-NOT-REVOKED-ON-DELETE. `apple-token` is OURS, not a provider's tell: PUT /v1/account/apple-token is the app handing us the user's Apple refresh token so the account deletion can revoke it at Apple. Nothing calls IN on it — it is not a webhook, takes no provider signature, and is behind platformAuth like the rest of /v1/account. The call OUT to Apple is in services/platform/src/lib/apple-revoke.ts and reaches appleid.apple.com, never this Worker."
  },
  "roles": {
    "_why": "The vocabulary rows are validated against, declared once HERE rather than inside the guard — the same reason keyKinds lives in tooling/channel-register.json. Renaming a role out from under the rows still using it fails.",
    "store_billing": "an app store that sells to the user and pays us. It is the seller of record for that sale and we never see the card.",
    "merchant_of_record": "the legal seller for anything sold outside an app store. It collects and remits sales tax worldwide and is who the buyer contracted with.",
    "payment_gateway": "processes a card payment on our behalf. NOT the seller — we remain the seller, and the tax duty stays with us.",
    "iap_aggregator": "normalises store purchase events into one entitlement feed. Holds purchase state, not payment instruments.",
    "infrastructure": "runs or hosts the Services. Sees traffic and whatever the Services store, and is disclosed as a processor rather than as a seller.",
    "identity": "authenticates users and holds account credentials.",
    "email_delivery": "sends the transactional email the Services generate — sign-in links, verification. Holds the recipient address in order to deliver to it, and is disclosed as a processor. NOT a marketing channel: a mailing list is a separate purpose with its own consent (ConsentPurpose.marketingEmail) and would be its own disclosure.",
    "content_delivery": "serves a file the Services load into the visitor's browser at run time — a script, a font, a WebAssembly runtime. It sees that request (the visitor's IP address, user agent and referrer) and nothing the Services store. Disclosed as a party the browser contacts, not as a processor of stored data, and never a seller."
  },
  "dataCategories": {
    "_why": [
      "🔴 THE SINK VOCABULARY. Declared HERE and not inside the guard, for the same reason `roles` above is:",
      "renaming a category out from under the rows still using it must FAIL rather than quietly mean nothing.",
      "",
      "WHY THIS EXISTS AT ALL, and it is worth stating in full because the defect it answers was PUBLISHED.",
      "privacy.html said 'We do not collect or store your IP address'. The self-hosted GlitchTip on the Oracle",
      "box was storing `user.ip_address`, re-observed on two independent events. NO GUARD COULD SEE IT:",
      "  · assert-policy-claims.mjs compares published TEXT to repo TEXT — it can prove the site says what",
      "    the repo says and nothing more, and both false statements passed it;",
      "  · the one claim row that IS typed `code` asserts `absent(cf-connecting-ip)` over ONE file, and its",
      "    own `why` concedes it 'is not a dataflow proof';",
      "  · tooling/legal/data-inventory.json declares 19 stores and EVERY ONE is Cloudflare D1 or KV. The",
      "    strings `glitchtip`, `oracle`, `sentry` and `ip_address` appear nowhere in it. The only",
      "    personal-data store outside Cloudflare was invisible BY CONSTRUCTION.",
      "This register is the one artifact that already knew the crash rail is a live infrastructure sink of",
      "its own — the row was `oracle-cloud` when this paragraph was written and is `hostinger` since the",
      "2026-09-02 move. So the declaration is attached to the thing that knows the sink exists.",
      "",
      "`tells` are how a category is recognised in a PUBLISHED DENIAL — the words a reader would see, not the",
      "column names. They are matched against the text AFTER the denial verb, so 'We receive confirmation of",
      "the purchase but do not collect or store your full card or bank details' is scoped to the object of",
      "the denial and not to the whole sentence. A tell that is too narrow makes a future denial UNSCOPEABLE",
      "(which FAILS); a tell that is too broad makes it INTERSECT (which FAILS). Neither direction passes",
      "quietly, which is why erring is safe here and guessing is not."
    ],
    "ip_address": {
      "what": "The network address a request came from, in a form that still identifies the connection. Personal data under GDPR and India's DPDP. ⚠️ GlitchTip's `scrub_ip_addresses` flag ANONYMISES (IPv4 /24, IPv6 /64) and then STORES — an anonymised prefix is still this category, which is exactly why the flag reading `True` hid the breach for weeks.",
      "tells": [
        "ip address",
        "ip addresses"
      ]
    },
    "ip_fingerprint": {
      "what": "A salted, one-way fingerprint of a network address, used as an abuse counter key and discarded within the hour. A DIFFERENT category from ip_address on purpose, and the published page draws the same line ('The fingerprint is not reversible to an IP address'). The construction is keyed (crypto.subtle.sign), not a digest — an unsalted sha256 of an IPv4 address is reversible by exhausting 2^32 values and would BE the address wearing a hash.",
      "tells": [
        "one-way fingerprint"
      ]
    },
    "crash_report": {
      "what": "An exception, its stack trace and the app/platform/version context around it. Received by GlitchTip, which we run ourselves.",
      "tells": [
        "crash report",
        "crash reports",
        "stack trace"
      ]
    },
    "usage_event": {
      "what": "One record that a feature of an app was used, written only after consent.",
      "tells": [
        "usage statistic",
        "usage statistics"
      ]
    },
    "install_identifier": {
      "what": "The random per-install identifier (`anon_id`) that makes usage records pseudonymous rather than anonymous. Not a name, not an email, not an advertising ID — but stable, so records from one installation group together.",
      "tells": [
        "install identifier",
        "random identifier created on your device"
      ]
    },
    "approximate_location": {
      "what": "Country, region and city, derived at the network edge from an address that is then discarded. Coarse by construction; it is not a coordinate.",
      "tells": [
        "approximate location",
        "coarse location"
      ]
    },
    "consent_record": {
      "what": "The append-only record that a consent decision was made: which install, when, granted or withdrawn, and which policy version was shown.",
      "tells": [
        "consent record",
        "consent records"
      ]
    },
    "email_address": {
      "what": "A plain email address, as typed. Held by the launch-notification signup list, and present verbatim inside a merchant-of-record notification.",
      "tells": [
        "email address",
        "email addresses"
      ]
    },
    "buyer_billing_details": {
      "what": "The buyer's name and billing country, as they arrive inside a merchant-of-record notification stored VERBATIM ([pipeline 5]M-2 — a re-serialised or redacted copy can never be re-verified against the signature it came with).",
      "tells": [
        "billing address",
        "billing country",
        "buyer name"
      ]
    },
    "entitlement_record": {
      "what": "What a person has paid for: one row per (user, app, entitlement).",
      "tells": [
        "what you have paid for",
        "entitlement",
        "entitlements"
      ]
    },
    "account_identifier": {
      "what": "The account id a row is keyed on (a Supabase `sub`, or a store/aggregator user id). Not a credential.",
      "tells": [
        "account identifier",
        "account id"
      ]
    },
    "user_content": {
      "what": "What a person created inside an app — for apps/subscriptiontracker, the subscriptions they track and their budget figures. User-entered content about their own finances.",
      "tells": [
        "what you created inside an app",
        "subscriptions you track"
      ]
    },
    "payment_instrument": {
      "what": "A card or bank number, CVV, IBAN or equivalent — the instrument itself. NOTHING in this factory receives this category, and no migration may declare a column for one (asserted separately by the `full card or bank details` claim row in policy-claims.json).",
      "tells": [
        "card or bank details",
        "card details",
        "bank details",
        "card number"
      ]
    }
  },
  "denialsOutOfScope": {
    "_why": [
      "🔴 THE HALF THAT MAKES THE DENIAL LIMB UNFAKEABLE, and it is DATA rather than a list inside the guard",
      "for exactly the reason `nonProviderRouteSegments` is. The guard extracts every \"we do not collect /",
      "do not store X\" clause from the visible text of the published pages. A denial is allowed to exist",
      "only if it resolves to a category in `dataCategories` above — or is written down HERE with a reason.",
      "",
      "So publishing 'we do not store your precise location' turns the build RED until somebody either adds",
      "the category (and therefore has to say whether a live sink receives it) or classifies the sentence as",
      "not being about a data category at all. There is no third option, and no way to pad this list except",
      "by naming the sentence you are adding.",
      "",
      "⚠️ A row here is checked in BOTH directions: one whose denial no longer appears on its page FAILS, so",
      "an exemption cannot outlive the sentence it was granted for — the same rule `disclosureGaps` carry",
      "through `stillTrue`. And a row that DOES resolve to a category FAILS as inert: an exemption for",
      "something that is not there reports judgement over nothing."
    ],
    "rows": [
      {
        "page": "privacy.html",
        "denial": "do not knowingly collect personal information from anyone under 18",
        "why": "§8, the age requirement. This denies collecting anything at all FROM A CLASS OF PERSON (anyone under 18 - the single floor set by [ADR 068] on the DPDP Act 2023 threshold, replacing the earlier under-13 wording on 2026-09-05) — it is scoped by WHO, not by WHAT. Scoping it against a sink's category list would be a category error in the literal sense: every category would intersect it, and the guard would go red on a sentence that is true. The duty it belongs to is age-assurance, which is carried as a row in tooling/legal/duty-matrix.json where the trigger can be stated."
      }
    ]
  },
  "firstPartyDomains": {
    "_why": [
      "Read by the EGRESS limb of tooling/ci/assert-policy-claims.mjs (2026-09-11), which takes every host a shipped Content-Security-Policy",
      "(apps/*/web/_headers, sites/*/_headers) lets a browser reach and requires it to be ours or to carry a provider row's tell.",
      "A host on one of these domains, or any subdomain of one, is ours. The vendor BEHIND it — Cloudflare for subscriptiontracker-api.nikatru.com, Hostinger",
      "for glitchtip.nikatru.com — is already its own row and is disclosed as that vendor, not as a hostname.",
      "Declared here, not inside the guard, for the reason `roles` is: a list inside a guard is a list nobody reviews with the register."
    ],
    "domains": [
      "nikatru.com",
      "rajasekarselvam.com"
    ]
  },
  "providers": [
    {
      "id": "apple-app-store",
      "name": "Apple App Store",
      "role": "store_billing",
      "status": "deferred",
      "reachableAt": null,
      "tells": [
        "apple app store"
      ],
      "namedIn": [
        "privacy.html",
        "refund.html"
      ],
      "privateRecord": "OWNER_QUEUE A-4 — the Apple Developer account. CLOSED 2026-08-31: the account EXISTS and the membership is ACTIVE, verified by an authenticated App Store Connect call on 2026-09-05 and again on 2026-09-08 (HTTP 200, ACCOUNT_HOLDER record). It holds zero certificates.",
      "notes": [
        "Named by its full store name in privacy.html §3 and refund.html §1. ⚠️ NOT in terms.html: §4 says",
        "'that store's terms (Apple, Google, Microsoft)' — a parenthetical reference to the companies, not a",
        "naming of the stores as sellers. The distinction was found by this register's own disclosure check",
        "and the row was corrected to match the page rather than the page assumed to match the row.",
        "Carried as a row because the pages name it, and a register that omits a named provider cannot be",
        "the answer to 'who is in the payment path'."
      ]
    },
    {
      "id": "google-play",
      "name": "Google Play",
      "role": "store_billing",
      "status": "deferred",
      "reachableAt": null,
      "tells": [
        "google play"
      ],
      "namedIn": [
        "privacy.html",
        "refund.html"
      ],
      "privateRecord": "OWNER_QUEUE A-3 — the Play publisher account.",
      "notes": [
        "Named in privacy.html and refund.html; terms.html says only 'Google' in a parenthetical.",
        "tooling/channel-register.json android-play is served:false.",
        "⚠️ CORRECTED 2026-08-05 — this note said 'No account yet'. The Play ORGANIZATION account has been",
        "verified since 2026-08-04; see nikatru/vendors/google.md. `status` stays \"deferred\", which is a",
        "statement about REVENUE and is still true: zero apps published, so nothing has ever been sold",
        "through this provider. The account and the revenue are two different facts, and only one of them",
        "was wrong. Compare microsoft-store below, which makes no account claim and so never went stale."
      ]
    },
    {
      "id": "microsoft-store",
      "name": "Microsoft Store",
      "role": "store_billing",
      "status": "deferred",
      "reachableAt": null,
      "tells": [
        "microsoft store"
      ],
      "namedIn": [
        "privacy.html",
        "refund.html"
      ],
      "privateRecord": "OWNER_QUEUE A-2 — the Partner Center publisher account.",
      "notes": [
        "Named in privacy.html and refund.html; terms.html says only 'Microsoft' in a parenthetical.",
        "tooling/channel-register.json windows-store is served:false."
      ]
    },
    {
      "id": "cloudflare",
      "name": "Cloudflare",
      "role": "infrastructure",
      "status": "live",
      "reachableAt": null,
      "tells": [
        "cloudflare"
      ],
      "namedIn": [
        "privacy.html",
        "subscriptiontracker/privacy.html"
      ],
      "privateRecord": null,
      "receives": [
        "email_address",
        "ip_fingerprint",
        "usage_event",
        "install_identifier",
        "approximate_location",
        "consent_record",
        "entitlement_record",
        "buyer_billing_details",
        "account_identifier",
        "user_content"
      ],
      "receivesBasis": "Derived from tooling/legal/data-inventory.json, which enumerates every D1 table, KV namespace and Pages-Function binding in this factory and marks each `personalData` true or false — all 19 of them are Cloudflare. `crash_report` is deliberately NOT in this list: crash reports land on the `hostinger` sink (the row was `oracle-cloud` until the 2026-09-02 move), which is what the `crash-reports-infrastructure` gap was about. `payment_instrument` is deliberately not here either, and no migration may declare a column for one.",
      "transits": {
        "ip_address": "Cloudflare IS the network edge — it terminates TLS, so the client address is inherent in the connection and cannot not be seen. It is not retained for us: the events path derives country/region/city from `request.cf` and discards the address (privacy.html discloses exactly this), and the signup path keeps a keyed one-way fingerprint rather than the address. ⚠️ THIS IS THE ESCAPE ROUTE FROM THE DENIAL LIMB AND IT IS NAMED AS ONE — moving a category from `receives` to `transits` silences the intersection check. It is a visible edit in a diff and it PRINTS on every run, but it is a declaration about a sink no repo-resident guard can observe."
      },
      "notes": [
        "The only LIVE provider in this register. Hosts both static sites (Git integration), both Workers,",
        "D1 and KV. privacy.html discloses it as the hosting provider and as the network edge — both true.",
        "⚠️ The SECOND sentence naming Cloudflare in privacy.html is not true, and it is carried as the",
        "`crash-reports-infrastructure` disclosureGap below rather than being fixed here: fixing it means",
        "editing a published page."
      ]
    },
    {
      "id": "paddle",
      "name": "Paddle",
      "role": "merchant_of_record",
      "status": "live",
      "ownerItem": "A-1",
      "reachableAt": null,
      "tells": [
        "paddle"
      ],
      "namedIn": [
        "terms.html",
        "refund.html",
        "privacy.html",
        "subscriptiontracker/privacy.html"
      ],
      "privateRecord": "OWNER_QUEUE A-1 — the Paddle seller application, KYC and payout account. [ADR 004] holds the decision itself; [ADR 039] holds which channels it is the rail for.",
      "notes": [
        "🔒 LOCKED as the primary merchant of record by [ADR 004] on 2026-07-23, and SCOPED by [ADR 039] on",
        "2026-08-09: Paddle is the rail for every channel with no store tax — the web, the Microsoft Store",
        "(Store Policies v7.19 §10.8.1/§10.8.6 permit a third-party purchase API for non-game PC apps at no",
        "Store Fee), Windows/macOS/Linux direct downloads, Snap and APK sideload. Apple and Google Play use",
        "their own billing, so Paddle is NOT the seller there. `packages/purchases/lib/src/",
        "purchase_capabilities.dart` carries the same split with the same citations, per channel.",
        "",
        "[ADR 039] D6 also settles INDIA, which is why the pages can say 'in every country we sell to,",
        "including India' rather than hedging: Paddle Billing ships India UPI AutoPay (renders on country=IN",
        "+ INR checkouts; ₹15k recurring cap, our SKUs well under) and Paddle registers and remits Indian",
        "OIDAR GST itself. ⚠️ THE UPI DETAIL IS RECORDED HERE AND DELIBERATELY NOT PUBLISHED: the ADR's own",
        "rider is 'live-verify UPI renders for THIS account when checkout opens', so which payment METHODS a",
        "buyer sees is an unverified vendor fact, while WHO THE SELLER IS is a decided one. The pages carry",
        "the second and not the first.",
        "",
        "⛔ THE INDIA PARAGRAPH ABOVE IS SUPERSEDED 2026-09-09 BY [ADR 076] AND IS KEPT VERBATIM AS THE",
        "DATED RECORD IT IS. Paddle is NO LONGER the India rail: the owner moved India to Razorpay on",
        "2026-09-09 on a like-for-like re-measurement of D6's own comparison (see the `razorpay` row below",
        "for the arithmetic). Paddle remains the rail for every other channel, and [ADR 004]'s",
        "merchant-of-record lock is untouched — only D6's India limb moved.",
        "🔴 WHAT THIS MAKES WRONG ON A PUBLISHED PAGE, and it is why the correction sits in this row and",
        "not only in the ADR: terms.html and refund.html name Paddle as the seller 'in every country we sell",
        "to, including India'. That sentence was true when it was written and is not true under [ADR 076].",
        "Nothing is sold from the website today, so nobody is being misled right now — but the pages must",
        "be corrected BEFORE the first India sale, and editing them is owner work. Carried here rather than",
        "as a `disclosureGap` row because a gap row needs a `stillTrue` probe over the CURRENT page text,",
        "and the current text is not yet wrong about anything sold.",
        "",
        "NAMED IN terms.html §4 AND refund.html §2/§4 since this change; both describe the role (legal",
        "seller, takes the payment, remits the tax) and say the receipt names the contracting Paddle entity.",
        "privacy.html still says only 'a merchant of record' — that is the narrowed `merchant-of-record-",
        "unnamed` gap below, and it stays open because privacy.html is version-gated.",
        "",
        "⛔ SUPERSEDED 2026-08-28. THE PARAGRAPH BELOW IS KEPT VERBATIM AS THE DATED RECORD IT IS — it was",
        "written while `status` was `not-yet-applied` and it argued for keeping it there. The correction",
        "follows it, and the paragraph named its own trigger, so read them as one:",
        "",
        "  ⚠️ `status` STAYS `not-yet-applied`, AND NAMING PADDLE DID NOT CHANGE IT. The status is about the",
        "  SELLER ACCOUNT (OWNER_QUEUE A-1, private-SSoT material this file may not record), not about the",
        "  disclosure. The pages are written in the future tense for exactly that reason — 'we do not sell",
        "  anything directly from this website today; this is the arrangement that will apply when we do' —",
        "  so nothing published here asserts an account that does not exist. The day the row goes `live` the",
        "  both-pages rule in assert-policy-claims.mjs §3(c) is already satisfied instead of newly owed.",
        "",
        "🟢 CORRECTION 2026-08-28 · `status` IS NOW `live`. The evidence is [ADR 044], LOCKED 2026-08-11,",
        "which records a LIVE PRODUCTION seller credential — so OWNER_QUEUE A-1, the thing the paragraph",
        "above said the status was about, is DONE for Paddle:",
        "  · `PADDLE_API_KEY_LIVE`, prefix `pdl_live_apikey_`, authenticating against `api.paddle.com`;",
        "  · 403 from the SANDBOX host with that same key — which is what proves it is a PRODUCTION key and",
        "    not a sandbox one wearing a live name;",
        "  · a live `POST /transactions` returning 201 with `txn_01kzs3qcvryq785t7shpq5d7wj`.",
        "The both-pages rule played out exactly as predicted: terms.html §4 and refund.html §2/§4 already",
        "named Paddle before the flip, privacy.html §4 does too, `namedIn` lists all three, and",
        "assert-policy-claims.mjs §3(c) now runs its LIVE branch over them instead of printing a gap.",
        "",
        "⚠️ WHAT `live` DOES NOT ASSERT, so the row is not read for more than it says: the DESTINATION",
        "SECRET. `PADDLE_NOTIFICATION_SECRET` is generated per notification-destination in the seller",
        "console and is a Worker secret no in-tree guard can observe. The seller ACCOUNT is live; whether",
        "the webhook destination is wired is a separate, still owner-gated fact, and",
        "tooling/ci/assert-mor-adapters.mjs goes on printing that one gap on every run.",
        "",
        "⛔ AND `live` IS STILL PER CHANNEL — the one thing a reader must not flatten. [ADR 039] D1 (LOCKED",
        "2026-08-09) is untouched by this correction: Apple and Google Play take the merchant-of-record",
        "role on their own in-app purchases and Paddle is NOT the seller there. See the first note above,",
        "and packages/purchases/lib/src/purchase_capabilities.dart, which carries the same split per channel."
      ]
    },
    {
      "id": "lemon-squeezy",
      "name": "Lemon Squeezy",
      "role": "merchant_of_record",
      "status": "deferred",
      "reachableAt": null,
      "tells": [
        "lemon squeezy",
        "lemonsqueezy"
      ],
      "namedIn": [],
      "privateRecord": null,
      "notes": [
        "🔴 THE PARALLEL BET IS RETIRED. [ADR 039] D4 (2026-08-09): the 'apply to Lemon Squeezy in parallel'",
        "clause of [ADR 004] is withdrawn — it was never evidenced, and the product is strategically",
        "sunsetting under the Stripe acquisition. `privateRecord` dropped its A-1 pointer with it: there is",
        "no application to point at.",
        "",
        "⚠️ THE ROW IS KEPT AND `status` STAYS `deferred`, WHICH IS A DECISION ABOUT THIS GUARD AND NOT A",
        "HEDGE. assert-policy-claims.mjs §3(c) treats `deferred` as deliberately silent and every other",
        "non-live status as 'being pursued, so the missing disclosure is a real gap that prints'. A retired",
        "bet is the first kind, not the second: printing 'the backup merchant of record is not named in our",
        "terms' on every CI run would be printing the absence of a decision nobody is taking. The row itself",
        "survives for the reason tooling/channel-register.json keeps rows for channels we do not serve — the",
        "researched facts are recorded once instead of rediscovered under time pressure, and a deletion",
        "would leave the retirement invisible.",
        "",
        "Paddle-concentration risk is real and [ADR 039] D4 grades it medium-high (termination / fund-hold",
        "class). The `MoRWebhookVerifier` seam makes a fallback a config change, but WHICH successor",
        "(FastSpring / Polar / Creem / direct Stripe + a tax engine) is a queued owner decision and is",
        "deliberately not answered by putting a name in this row."
      ]
    },
    {
      "id": "razorpay",
      "name": "Razorpay",
      "role": "payment_gateway",
      "status": "wired-not-live",
      "reachableAt": null,
      "tells": [
        "razorpay"
      ],
      "namedIn": [
        "terms.html",
        "refund.html"
      ],
      "privateRecord": null,
      "notes": [
        "WAS NAMED IN TWO PUBLISHED PAGES AND REACHABLE FROM NO CODE; the naming has been removed and the",
        "code was never there. Until this change terms.html §4 said website purchases 'are processed",
        "securely by Razorpay' and refund.html §2/§4 routed website refunds through it, while privacy.html",
        "said we do not sell anything directly from this website at all. There is no Razorpay integration",
        "anywhere in this repository — no route, no key, no webhook — so the pages named a payment path",
        "that did not exist, for a purchase the same site said does not happen.",
        "",
        "A gateway is also the WRONG SHAPE for what [ADR 004] decided: a gateway leaves US the seller and",
        "leaves the worldwide sales-tax duty with us, which is the exact burden a merchant of record was",
        "chosen to remove.",
        "",
        "🔒 AND [ADR 039] D6 (2026-08-09) SETTLED IT ON MEASUREMENT RATHER THAN PREFERENCE: as a payment",
        "aggregator Razorpay makes NIKATRU the domestic seller in India — net ~82% of sticker after our own",
        "18% GST, against ~92.5% via Paddle on the annual SKU, plus a recurring B2C GST filing surface. That",
        "is why the India clause on the pages names Paddle and not a domestic rail. RAZORPAY IS NOT THE APP",
        "RAIL. Its recorded scope is unchanged by that lock and is narrow: a nikatru.com website-domestic",
        "gateway, dashboard-hosted, with ZERO code in this repository.",
        "",
        "⚠️ THE ROW IS KEPT RATHER THAN DELETED so the removal is a recorded fact instead of an absence. If",
        "the revisit trigger in [ADR 039] D6 ever fires (web-India above ~100 sales/month, or measurably",
        "poor Paddle UPI acceptance) and a domestic rail is offered alongside the merchant of record, THIS",
        "is the row that goes back to `namedIn` — and note that the both-pages rule in",
        "assert-policy-claims.mjs applies to the merchant of record and NOT to a gateway, so nothing would",
        "catch that silently. The re-disclosure is a human duty and this paragraph is where it is written.",
        "",
        "⚠️ WHETHER A RAZORPAY MERCHANT ACCOUNT EXISTS IS NOT RECORDED HERE and must not be: account state",
        "is private-SSoT material (see the ⚠️ note at the top of this file). `not-named-not-wired` is a",
        "statement about this repository and the published pages, and about nothing else.",
        "",
        "🟢 CORRECTED 2026-09-09 BY [ADR 076], WHICH AMENDS [ADR 039] D6. EVERYTHING ABOVE IS LEFT",
        "VERBATIM AS THE DATED RECORD IT IS — it was written while D6 stood, and it argued from D6's",
        "numbers. The correction follows it; read them as one.",
        "",
        "  ⛔ THE PARAGRAPH BEGINNING '🔒 AND [ADR 039] D6 (2026-08-09) SETTLED IT ON MEASUREMENT' IS",
        "  SUPERSEDED. Its arithmetic — 'net ~82% of sticker via Razorpay against ~92.5% via Paddle' —",
        "  compared two DIFFERENT prices: the 82% took GST out of ₹999 while the 92.5% added GST on top,",
        "  and the 92.5% omitted Paddle's FX margin entirely (MSA cl. 7.1 charges 3% on INR; the clause was",
        "  first read 2026-08-31, three weeks AFTER D6 locked, and it is absent from Paddle's pricing page).",
        "  Held at ONE customer-facing price the model gives Razorpay ₹795 against Paddle ₹702 — Razorpay",
        "  ahead by ₹93 per payer per year, and ahead at ₹999+GST too. Razorpay's real cost is 2% platform",
        "  plus a 0.5% subscriptions add-on, with the 18% GST on the fee returning as input tax credit.",
        "",
        "🔒 SO INDIA IS RAZORPAY BY OWNER DECISION, 2026-09-09. International stays Paddle, unchanged.",
        "Paddle cannot serve an India MONTHLY SKU at all — the $0.50 flat fee on ₹149 is ~42% effective —",
        "and the owner separately recalls Paddle transaction failures in India, which is D6's own revisit",
        "trigger ('measured-poor Paddle UPI acceptance') firing on his own experience.",
        "",
        "🔴 AND `status` STAYS `not-named-not-wired`, WHICH IS THE POINT OF THIS PARAGRAPH RATHER THAN AN",
        "OVERSIGHT. This field is a statement about THIS REPOSITORY and the published pages, and both halves",
        "of it remain true on 2026-09-09: there is still no Razorpay route, key or webhook anywhere in this",
        "tree, and no published page names Razorpay. What changed is the DECISION, not the code — so the row",
        "now records a divergence between the two, deliberately, instead of a status that would claim a rail",
        "that does not exist. `services/platform/src/routes/checkout.ts` carries `PADDLE_PRICE_IDS` and no",
        "Razorpay equivalent; `services/platform/src/app-config-data.json` declares USD offerings and no INR",
        "ones, and its own README says why.",
        "",
        "⬜ WHAT CLOSES THE DIVERGENCE, and none of it is agent work: a Razorpay merchant account and",
        "integration, INR SKUs on a config that first needs a region dimension, and RE-DISCLOSURE on the",
        "published pages. That last one is OWNER work twice over — terms.html and refund.html name Paddle as",
        "the seller 'in every country we sell to, including India', which this decision makes wrong for",
        "India, and privacy.html is inside the policy-VERSION surface, so correcting it is a version bump",
        "plus an archive snapshot (assert-policy-archive.mjs) plus a signature. ⚠️ NOTE THE GAP THE",
        "PARAGRAPH ABOVE ALREADY NAMED: the both-pages rule in assert-policy-claims.mjs §3(c) applies to the",
        "merchant of record and NOT to a gateway, so nothing here would catch the re-disclosure silently",
        "going undone. It is a human duty and this is still where it is written.",
        "",
        "⏱ 2026-09-22 — `status` MOVES TO `wired-not-live` AND `namedIn` STOPS BEING EMPTY. EVERYTHING",
        "ABOVE IS LEFT VERBATIM. The 🔴 paragraph of 2026-09-09 held `not-named-not-wired` on two measured",
        "halves, and BOTH halves have since stopped being true. This paragraph is the measurement, not a",
        "re-argument:",
        "",
        "  · NAMED. `sites/nikatru/terms.html` §4 says, in visible text, that for purchases made in India",
        "    payments are taken by Razorpay acting as our payment gateway and that Nikatru remains the",
        "    seller for those purchases; `sites/nikatru/refund.html` §3 is HEADED 'India — purchases taken",
        "    through Razorpay' and names it five more times. Both pages shipped before this edit, which is",
        "    why `tooling/legal/policy-claims.json` already carries a Razorpay claim row for each of them",
        "    saying the register was behind the page. `privacy.html` names it NOWHERE, and that is the",
        "    remaining disclosure gap — it is inside the policy-VERSION surface, so correcting it is a",
        "    version bump plus an archive snapshot plus the owner's signature, exactly as the ⬜ paragraph",
        "    above says.",
        "",
        "  · WIRED, IN THE HALF A WEBHOOK IS. `services/platform/src/lib/mor/razorpay.ts` exists and",
        "    exports `razorpayVerifier`, which declares `provider: 'razorpay'` and",
        "    `secretEnvVar: 'RAZORPAY_WEBHOOK_SECRET'` and verifies an HMAC-SHA256 hex signature over the",
        "    raw body from the `X-Razorpay-Signature` header;",
        "    `services/platform/src/lib/mor/registry.ts` lists it in `MOR_VERIFIERS`;",
        "    `services/platform/test/razorpay-verifier.test.ts` exercises it. So the sentence 'no route, no",
        "    key, no webhook' is now wrong about the webhook and right about the other two.",
        "",
        "🔴 `wired-not-live` IS THE PRECISE WORD, AND THE PRECEDENT IS THE `revenuecat` ROW BELOW. Nikatru",
        "cannot take one rupee through this gateway today: there is no checkout route (",
        "`services/platform/src/routes/checkout.ts` carries `PADDLE_PRICE_IDS` and no Razorpay equivalent),",
        "no INR offering in `services/platform/src/app-config-data.json`, and no `RAZORPAY_WEBHOOK_SECRET`",
        "in any secret register — so the verifier refuses every real event for want of a secret, which is",
        "the same shape as RevenueCat's. `live` is the word that would be false.",
        "",
        "⬜ WHAT MOVES THIS ROW TO `live`, and the first three are OWNER work: a Razorpay merchant account",
        "and settlement account; the API keys; the dashboard webhook plus its secret pasted into the vault.",
        "Then, as agent work: the checkout route at the IN book amounts, INR offerings, and one real",
        "test-mode event recorded in the store to prove the parser — ⚠️ which today has nowhere to land,",
        "because `services/platform/wrangler.jsonc` deploys with `MONEY_ENVIRONMENT: \"live\"` and no sandbox",
        "environment exists. `platform-state/open.json` O-RAZORPAY-CHECKOUT-ADAPTER is the row.",
        "",
        "⚠️ [ADR 076] §6 STILL CARRIES THE RIDER THAT CASHFREE WAS NEVER EVALUATED (0% platform fee to ₹20",
        "lakh until 2027-03-31). Two published pages now name Razorpay, so switching processor costs a",
        "re-disclosure on both. That is a cost to state before the checkout lands, not after.",
        "",
        "⏱ AND THE SCOPE SENTENCE ABOVE — 'a nikatru.com website-domestic gateway' — IS NOW TOO NARROW.",
        "[ADR 094] (LOCKED 2026-09-22) adds the apps.gov.in build and any buyer IN INDIA of the three",
        "browser extensions. `tooling/channel-register.json` records the extension half today as",
        "`purchaseRail.regionRails` on `chrome-webstore`, `edge-addons` and `amo`; `apps-gov-in` keeps",
        "`rail: none` until a build carries the checkout ([ADR 094] §4 item 5)."
      ]
    },
    {
      "id": "revenuecat",
      "name": "RevenueCat",
      "role": "iap_aggregator",
      "status": "wired-not-live",
      "reachableAt": null,
      "tells": [
        "revenuecat",
        "revenue cat"
      ],
      "namedIn": [
        "privacy.html"
      ],
      "privateRecord": null,
      "notes": [
        "⏱ 2026-09-16 — THE ROUTE MOVED, AND `reachableAt` WENT TO null WITH IT. RevenueCat now arrives at",
        "services/platform's `POST /v1/money/:provider` as `revenuecat` (revenuecatVerifier in",
        "services/platform/src/lib/mor/revenuecat.ts, an HMAC over the body), exactly as Paddle does — and",
        "Paddle's row carries `reachableAt: null` for the same reason: a `:provider` segment is not a literal",
        "route, so the K-5 route limb cannot see it, and assert-mor-adapters.mjs limb 2b holds every",
        "registered rail to a row here instead. The legacy bearer-gated route it replaced",
        "(services/subscriptiontracker-api's POST /v1/webhooks/revenuecat) is retired (O-REVENUECAT-VERIFIER",
        "leg b). The platform verifier refuses every real event until an app declares its RevenueCat app id",
        "(O-REVENUECAT-ACCOUNT), so the status stays `wired-not-live`. The paragraphs below are kept as written.",
        "",
        "🔴 THE ONLY PROVIDER IN THIS REGISTER THAT REAL CODE REACHES, AND IT IS NAMED IN NO PAGE.",
        "services/subscriptiontracker-api/src/routes/webhooks.ts mounts POST /v1/webhooks/revenuecat, authenticated by a",
        "shared secret and fail-closed when the secret is unset. It upserts rows into platform_db",
        "`entitlements` keyed on a RevenueCat app_user_id.",
        "",
        "It is `wired-not-live` rather than `live` because no store account exists to send it an event:",
        "every kind:store row in tooling/channel-register.json is served:false. The route works; nothing",
        "calls it. That is precisely why it is easy to leave undisclosed until the day it matters.",
        "",
        "🔒 [ADR 039] D5 LOCKED RevenueCat as the mobile-store IAP glue (2026-08-09) — one SDK for both",
        "stores, hosted receipt and lifecycle handling, native Paddle Billing integration so every rail",
        "resolves to ONE entitlement answer; $0 to $2,500 MTR, then 1% of tracked revenue. It enters the",
        "codebase properly at the Play-Billing increment and at A-4; until then it is a dependency-in-",
        "waiting with a built, dormant, bearer-verified, fail-closed webhook.",
        "",
        "🔴 `role` STAYS `iap_aggregator` AND THAT IS THE LOAD-BEARING PART OF THIS ROW. RevenueCat is NOT a",
        "merchant of record and must never be recorded as one: the store is the seller for a store purchase",
        "and Paddle is the seller everywhere else, while RevenueCat only normalises the resulting purchase",
        "events into one entitlement feed. The register's own vocabulary draws the line — `iap_aggregator`",
        "is 'holds purchase state, not payment instruments', `merchant_of_record` is 'the legal seller'. If",
        "this row's role were ever flipped, assert-policy-claims.mjs §3(c) would immediately start demanding",
        "that terms.html and refund.html name RevenueCat as a seller, which would be a false disclosure the",
        "guard had asked for. Locked here in prose because no guard can hold a definition for us.",
        "",
        "It holds an `account_identifier` (the RevenueCat app_user_id) and `entitlement_record` data, so the",
        "disclosure duty is privacy.html's third-party list, not the buyer-facing seller pages — carried as",
        "the `iap-aggregator-unnamed` gap below."
      ]
    },
    {
      "id": "supabase",
      "name": "Supabase",
      "role": "identity",
      "status": "wired-not-live",
      "reachableAt": null,
      "tells": [
        "supabase"
      ],
      "namedIn": [
        "privacy.html"
      ],
      "privateRecord": null,
      "notes": [
        "The auth vendor. services/platform/wrangler.jsonc declares SUPABASE_URL and the brick's",
        "initNikatruAuth wires it; [ADR 005] holds the persistence decision and [ADR 024] the app's",
        "auth-init exception. No shipped app creates accounts today — the brick's default is",
        "needs_backend:false — so nothing is stored under it yet.",
        "",
        "Named in no page. privacy.html's third-party section lists stores, Cloudflare and 'a merchant of",
        "record' and does not mention an identity provider. Carried as the `identity-provider-unnamed`",
        "gap: it becomes a disclosure duty the day an app actually offers accounts, and that day is a",
        "declared trigger in tooling/legal/duty-matrix.json rather than a memory."
      ]
    },
    {
      "id": "hostinger",
      "name": "Hostinger",
      "role": "infrastructure",
      "status": "live",
      "reachableAt": null,
      "tells": [
        "hostinger"
      ],
      "namedIn": [
        "privacy.html",
        "subscriptiontracker/privacy.html"
      ],
      "privateRecord": "Private/runbooks/glitchtip.md — the self-hosted GlitchTip instance and its operations.",
      "receives": [
        "crash_report"
      ],
      "receivesBasis": "🔴 THIS ROW IS WHY THE SINK-DISCLOSURE LIMB EXISTS. Until 2026-08-06 this sink ALSO received `ip_address`, while privacy.html published 'We do not collect or store your IP address' — and nothing in this repository could see it, because the only personal-data store outside Cloudflare appears in no inventory here. Fixed at the sink and verified BY OBSERVATION, not by reading a setting (Private/pre-minimal-2026-09-08:archive/2026-09-05/session-notes.md §17 — the old `knowledge/` prefix merged into `Private/` in the 2026-08-15 flatten; §17 is unmoved): two layers, because either alone is a false fix. (1) The sink refuses — 12 IP keys added to GlitchTip's `sensitive_keys`, which is what catches a CLIENT-SUPPLIED address; tested with 203.0.113.45. (2) Nothing is collected — nginx blanks the 11 client-IP headers in python_ipware's precedence tuple, so `get_client_ip` falls through to the Docker gateway and returns None. Probed after: no `user` object at all on a fresh event; the negative control against the pre-change config returned the address unmodified, so the assertion can fail. The 18 back-catalogue events holding a real address were redacted in place, non-destructively.",
      "transits": {
        "ip_address": "An ingest request necessarily arrives from the device's address; that is TCP, not a choice. Nothing writes it — see `receivesBasis`. ⚠️ AND THIS IS EXACTLY THE DECLARATION A REPO-RESIDENT GUARD CANNOT CHECK. If somebody re-enables IP storage on the live GlitchTip without editing this line, every guard in this repository stays green. Only a probe against the live GlitchTip API can close that. ⏱ CORRECTED 2026-09-16 — this sentence said no such probe exists; one does since PR #778 (O-CRASH-EVENT-IP-DROP): `node tooling/ci/assert-glitchtip-no-ip.mjs --live` posts a subject event (no user, spoofed client-IP headers, plus the real CF-Connecting-IP the edge adds) and a negative control through the public glitchtip.nikatru.com edge, then reads both back through the GlitchTip API. It is an ATTENDED, on-demand laptop duty (duty.laptop.glitchtip-no-ip-live in tooling/ops/register.json), never a CI gate, because the compose file lives at /opt/glitchtip on the box and is in no tree CI can read — so nothing runs the live leg automatically. tooling/ci/assert-sink-disclosure.mjs names that probe for this row and exits 2 if the probe, its --live leg or the duty row disappears; it checks the probe's shape on every push and cannot run it."
      },
      "notes": [
        "Crash reports go to a GlitchTip instance we run ourselves on a Hostinger VPS (Box B).",
        "",
        "🔴 THIS ROW WAS `oracle-cloud · Oracle Cloud` UNTIL THE 2026-09-05 POLICY MINT, AND THE MOVE IT",
        "MISSED HAPPENED ON 2026-09-02. GlitchTip left the Oracle always-free VM (Box A) for Box B that",
        "day. Everything operational followed it — tooling/ops/register.json `_substrateHosts",
        ".glitchtip-heartbeat` resolves to `hostinger`, the recovery row's substrate is",
        "`glitchtip-box-docker`, the backup chain monitor was renamed — and the two things a READER sees",
        "did not: this register and the published policy page both went on naming a machine the service",
        "had left. Nothing in this repository could go red over it, because every guard here compares the",
        "page against the register and the register agreed with the page. That is the shape of this whole",
        "file's blind spot, already written down in the `resend` row: the guards check the list against",
        "the pages, never the list against reality.",
        "",
        "The row was RENAMED rather than relabelled. A provider row is an identity — `receives`,",
        "`transits` and every disclosure duty hang off it — and the identity really did change: a",
        "different company, a different contract, a different jurisdiction for the same crash payloads.",
        "Keeping the id `oracle-cloud` on a Hostinger machine would have preserved a key at the cost of",
        "the thing the key names. Oracle Cloud is still a provider — Box A runs the self-hosted identity",
        "stack — and it keeps its own row below, which is what that id should always have meant.",
        "",
        "The rest of the paragraph on privacy.html (that crash reports are NOT sent to a third-party",
        "analytics product, and are received by software we run ourselves) is true and stays true under",
        "either host — which is exactly why the wrong host survived a version signature.",
        "",
        "⏱ 2026-09-11 — NAMED IN THE PER-APP NOTICE TOO (owner decision \"Name Hostinger\"). apps/subscriptiontracker/privacy.yaml",
        "listed `oracle-cloud` as the infrastructure vendor of the identity machine and omitted this row, the LIVE crash sink,",
        "under a self-hosted rule applied to one vendor only (HANDOFF-stores REVIEW #12). The declaration now lists both, the",
        "notice was re-rendered, and `namedIn` carries subscriptiontracker/privacy.html so assert-policy-claims limb (b) re-reads",
        "that page for `hostinger` on every run."
      ]
    },
    {
      "id": "oracle-cloud",
      "name": "Oracle Cloud",
      "role": "infrastructure",
      "status": "wired-not-live",
      "reachableAt": null,
      "tells": [
        "oracle"
      ],
      "namedIn": [
        "subscriptiontracker/privacy.html"
      ],
      "privateRecord": "Private/runbooks/boxes/oracle.md — Box A, the always-free VM (wiped of the platform 2026-09-24 and kept for non-platform use; until then it ran the self-hosted Supabase stack), and the OCI tenancy whose Object Storage holds the encrypted third backup copy.",
      "notes": [
        "🔴 THIS ID EXISTED BEFORE 2026-09-05 AND MEANT SOMETHING ELSE. It carried the CRASH SINK — the",
        "GlitchTip instance — because that was the only thing on the Oracle box anybody had written a row",
        "for. GlitchTip moved to Box B on 2026-09-02 and the sink row moved with it, to `hostinger`. What",
        "is left is what the id says on its face and what apps/subscriptiontracker/privacy.yaml was already pointing at:",
        "the vendor whose machine runs the identity stack. So this is not a re-created row, it is the row",
        "the id should have been all along, and it is written down rather than left implicit because the",
        "conflation is what let a wrong host survive a policy signature.",
        "",
        "STATUS TRACKS THE `supabase` ROW IT HOSTS, deliberately. The self-hosted Supabase is",
        "`wired-not-live`: the code reaches it and no shipped app creates accounts, so `auth.users` is 0",
        "and this box retains no personal data today. `wired-not-live` therefore keeps it OUT of",
        "assert-sink-disclosure's live-sink quantification — correct today, and wrong the moment an app",
        "offers accounts, at which point this row must go `live` and declare `receives` and a",
        "`receivesBasis` for what the auth database holds.",
        "",
        "⚠️ AND NOTHING HERE FIRES ON THAT TRANSITION, which is stated rather than glossed. The",
        "`identity-provider-unnamed` gap is RETIRED — it was about naming Supabase on the page, and",
        "privacy.html names it — so it is not a waiting trigger, and no row in",
        "tooling/legal/duty-matrix.json is keyed on the first account either. The `supabase` row carries",
        "exactly the same exposure for exactly the same reason, and this row is deliberately given the",
        "same status so that the two move together when somebody does move them. A person comparing the",
        "register to reality is still the only thing that closes it, as the `resend` row already says.",
        "",
        "NAMED IN NO PAGE. privacy.html named `Oracle Cloud` only as the crash-report host, and that",
        "sentence now names Hostinger; nothing published says where the identity stack runs. The identity",
        "PROVIDER is disclosed (the `supabase` row, §4), which is the duty; the machine it runs on is not",
        "a separate disclosure and is not claimed to be one here.",
        "",
        "⏱ 2026-09-11 CORRECTION, appended rather than replacing ([ADR 053] rule 2): \"NAMED IN NO PAGE\" above was false for",
        "the generated per-app notice. sites/nikatru/subscriptiontracker/privacy.html has listed `oracle-cloud` as a processor",
        "since it was first rendered on 2026-09-06, and `namedIn` said nothing. It now records that page, so the naming is",
        "checked against the page text like every other disclosure. The portfolio privacy.html still does not name Oracle.",
        "",
        "⏱ 2026-09-24 — THE IDENTITY STACK MOVED TO HOSTINGER (Box C) AND BOX A WAS WIPED OF THE PLATFORM; appended rather",
        "than replacing ([ADR 053] rule 2). Oracle now runs none of the services: the VM is kept for non-platform use and",
        "holds nothing of the platform. What Oracle STILL holds is the encrypted third backup copy: /srv/restic from the",
        "services box, copied daily into the OCI Object Storage bucket `nikatru-backup-thirdcopy` through the `ocienc:`",
        "crypt remote (duty.restic-third-copy in tooling/ops/register.json). So the row STAYS, as a party that stores",
        "ciphertext, and apps/subscriptiontracker/privacy.yaml keeps it as a processor with that purpose; dropping a party",
        "that still holds data would under-disclose. The paragraphs above that call it the identity stack's host, and the",
        "tie of its status to the `supabase` row, describe it until this date. `status` stays `wired-not-live`: the only",
        "status rule this register's `_readme` states is that \"every `infrastructure` row with status `live` DECLARES the",
        "personal-data categories it receives and retains\", and nothing in it makes `wired-not-live` false for a party",
        "that only stores an encrypted copy."
      ]
    },
    {
      "id": "resend",
      "name": "Resend",
      "role": "email_delivery",
      "status": "live",
      "reachableAt": null,
      "tells": [
        "resend"
      ],
      "namedIn": [
        "privacy.html",
        "subscriptiontracker/privacy.html"
      ],
      "privateRecord": "nikatru/vendors/resend.md — the account, the API key and the sending domain. The SMTP wiring itself is recorded in nikatru/vendors/supabase.md ([ADR 029]).",
      "notes": [
        "🔴 THIS ROW IS A FOUND DISCLOSURE GAP, AND THE FINDING IS THAT THERE WAS NO GAP ROW. Resend has",
        "delivered every Supabase auth email since auth mail went live — smtp.resend.com, sending as",
        "auth@mail.nikatru.com ([ADR 029]) — so it has held every user's email address that whole time. It",
        "appeared in NEITHER this register NOR sites/nikatru/privacy.html until 2026-08-10. The other four",
        "privacy.html corrections signed that day each had a `disclosureGaps` row printing on every CI run;",
        "this one had nothing, because a provider absent from the register is absent from everything the",
        "register drives. That is the difference between a gap that is TRACKED and a gap that is INVISIBLE,",
        "and it is recorded here rather than quietly closed: nothing in this repository would have raised",
        "it, and nothing in this repository will raise the next one either. The guards check the list",
        "against the pages, never the list against reality — adding a vendor without adding its row is",
        "still a silent hole, and only a person comparing the two closes it.",
        "",
        "It is deliberately NOT in retiredDisclosureGaps: no gap row ever existed, and inventing one would",
        "fabricate a history in which the omission was known and scheduled. It was not.",
        "",
        "⚠️ SCOPE BOUNDARY, STATED SO IT IS NOT MISTAKEN FOR COVERAGE: assert-sink-disclosure.mjs quantifies",
        "over `infrastructure` rows with status `live`, and this row is `email_delivery`. The categories",
        "Resend holds are therefore NOT checked against the published denials by that guard. The role is",
        "the right one — Resend hosts nothing — and borrowing `infrastructure` to pick up the limb would be",
        "putting a false claim in the register to satisfy a check. What DOES bind is assert-policy-claims",
        ".mjs: `namedIn` forces this row's tells to appear in privacy.html's visible text on every run, so",
        "the disclosure cannot silently disappear once made.",
        "",
        "TRANSACTIONAL ONLY. The privacy sentence names sign-in and account-verification messages and claims",
        "no marketing channel, because there is none: ConsentPurpose.marketingEmail is collection-only, with",
        "no sender and no list ([ADR 040] decision 5). Describing a channel we do not operate would be a",
        "promise to keep true for nothing."
      ]
    },
    {
      "id": "sentry-cdn",
      "name": "Sentry (Functional Software, Inc.) — browser SDK CDN",
      "role": "content_delivery",
      "status": "not-named-not-wired",
      "reachableAt": null,
      "tells": [
        "sentry"
      ],
      "namedIn": [],
      "privateRecord": null,
      "notes": [
        "REGISTERED 2026-09-11 (REVIEW-stores-2026-09-10 #11). The WEB build of an app makes the visitor's browser load the Sentry browser SDK",
        "from Sentry's CDN; apps/subscriptiontracker/web/_headers allows it as `script-src https://browser.sentry-cdn.com`.",
        "MEASURED, not inferred: `curl -sS https://nikatru.com/subscriptiontracker/main.dart.js` (200, 4,445,101 B) contains",
        "`browser.sentry-cdn.com/10.38.0/bundle.tracing.min.js`, 2026-09-11.",
        "WHAT IT RECEIVES: the request for a script — IP address, user agent, referrer — on a web visit. Crash REPORTS are not sent to Sentry:",
        "they land on the self-hosted GlitchTip (`hostinger` row). So this is a CDN row, not a crash sink.",
        "NAMED IN NO PAGE — disclosure gap `web-boot-third-parties-unnamed` prints on every run until the owner names it or the egress is",
        "closed (serve the SDK from the app origin).",
        "⏱ 2026-09-12 — EGRESS CLOSED (W5), so this row is `not-named-not-wired`: named in no page, and no shipped Content-Security-Policy",
        "lets a browser reach browser.sentry-cdn.com any more. sentry_flutter 9.26.0-9.30.0 hard-codes that URL as a `const`",
        "(lib/src/web/sentry_js_bundle.dart), so it cannot be re-pointed at the app origin. packages/telemetry/lib/src/telemetry_bootstrap.dart",
        "`useHttpTransportOnWeb` turns `autoInitializeNativeSdk` off on web (no injection) AND installs the SDK's own `HttpTransport`,",
        "without which the already-installed JavascriptTransport would drop every event silently. Crash reports still go to the",
        "self-hosted GlitchTip (`hostinger` row); only the CDN script request is gone. Kept as a row so a future SDK or app that",
        "re-adds the script re-meets a named provider."
      ]
    },
    {
      "id": "google-gstatic",
      "name": "Google (gstatic.com content delivery)",
      "role": "content_delivery",
      "status": "not-named-not-wired",
      "reachableAt": null,
      "tells": [
        "gstatic"
      ],
      "namedIn": [],
      "privateRecord": null,
      "notes": [
        "REGISTERED 2026-09-11 (REVIEW-stores-2026-09-10 #11). A Flutter WEB build fetches its CanvasKit renderer from www.gstatic.com and",
        "fallback fonts from fonts.gstatic.com unless built with --no-web-resources-cdn; apps/subscriptiontracker/web/_headers allows both.",
        "MEASURED 2026-09-11 in the live https://nikatru.com/subscriptiontracker/main.dart.js: `www.gstatic.com/flutter-canvaskit/a804b261…`",
        "and `fonts.gstatic.com/s/`.",
        "WHAT IT RECEIVES: the requests for those files — IP address, user agent, referrer — on a web visit.",
        "⚠️ THE TELL IS `gstatic`, NOT `google`, ON PURPOSE: privacy.html already names \"Google Play\" (the `google-play` row), and a `google`",
        "tell would read that sentence as this disclosure and close the gap below on a page that says nothing about a CDN.",
        "NAMED IN NO PAGE — see gap `web-boot-third-parties-unnamed`.",
        "⏱ 2026-09-12 — EGRESS CLOSED (W5), so this row is `not-named-not-wired`: named in no page, and no shipped Content-Security-Policy",
        "lets a browser reach gstatic any more. deploy-web.yml builds with `--no-web-resources-cdn` (CanvasKit from the bundle's /canvaskit/),",
        "apps/subscriptiontracker/web/flutter_bootstrap.js passes `fontFallbackBaseUrl: \"fallback-fonts/\"`, and",
        "tooling/web/self-host-fallback-fonts.mjs copies the engine's 725-file fallback set (21,803,256 B for Flutter 3.47.2) into",
        "build/web/fallback-fonts/, pinned by tooling/web/fallback-fonts.lock.json, and refuses to deploy a bundle missing either setting.",
        "Google still serves those files to the DEPLOY RUNNER at build time; no visitor request reaches it. Kept as a row, not deleted,",
        "so the next Flutter web app that loses the flag re-meets a named provider instead of an unregistered host.",
        "MEASURED LIVE 2026-09-12 on the deployed build 1.0.361 (#676, merge 3e012242, deploy-web run 34638710419), headless Chrome loading",
        "https://nikatru.com/subscriptiontracker/ and #/sign-in to first frame: requests to www.gstatic.com 0 and fonts.gstatic.com 0 (1.0.360, one",
        "build earlier, same probe: 2 and 2). CanvasKit from /subscriptiontracker/canvaskit/chromium/, the on-demand Noto fallback from",
        "/subscriptiontracker/fallback-fonts/ (200 font/woff2, byte-identical to the lock). What the bytes still carry, stated so a grep is not",
        "misread: main.dart.js keeps the engine's runtime default string https://fonts.gstatic.com/s/ (overridden by the bootstrap config) and",
        "flutter_bootstrap.js keeps Flutter's loader branch naming www.gstatic.com (not taken when useLocalCanvasKit is true). Neither is a request."
      ]
    }
  ],
  "disclosureGaps": [],
  "retiredDisclosureGaps": [
    {
      "_note": "🔴 RETIRED, NOT DELETED — 2026-08-10, by the owner signature that bumped sites/nikatru/privacy.html to version 2026-08-10. All four were live rows in `disclosureGaps` until that commit, and each one is a sentence that was WRONG or MISSING on a published legal page for weeks; deleting the record would delete the only evidence of how long. assert-policy-claims.mjs reads `disclosureGaps` ONLY, so nothing in this array is checked — which is exactly why each row carries `closedBy`, naming the span that now says the true thing and the tooling/legal/policy-claims.json row that pairs it. If a future edit drops one of those spans, THAT row fails; this list is a record, not a guard, and does not pretend to be one."
    },
    {
      "id": "crash-reports-infrastructure",
      "provider": "oracle-cloud",
      "page": "privacy.html",
      "was": "One sentence said usage statistics AND crash reports were 'received and stored by software we run ourselves, on infrastructure provided by Cloudflare as our hosting provider'. Usage statistics do run on Cloudflare. Crash reports run on an Oracle Cloud VM, so the sentence covered two systems on two providers and was wrong about the second.",
      "closedBy": "§4 now splits it: usage statistics name Cloudflare, crash reports name Oracle Cloud. Paired by the `Oracle Cloud` provider row in tooling/legal/policy-claims.json, and oracle-cloud.namedIn now lists privacy.html so the disclosure is re-checked against the page text on every run.",
      "closedIn": "2026-08-10",
      "laterCorrection": "⏱ 2026-09-05 mint, appended beside the 2026-08-10 wording rather than replacing it ([ADR 053] rule 2): the sentence `closedBy` describes was correct on the day it was signed and stopped being correct on 2026-09-02, when the GlitchTip instance moved from Box A (Oracle Cloud) to Box B (Hostinger). The provider row was renamed `oracle-cloud` -> `hostinger` and the page now names Hostinger, so every `Oracle Cloud` above is the history and not the current state. The row is left here rather than reopened in `disclosureGaps`: the gap this row records — one sentence covering two systems on two hosts — really was closed and did not recur; what recurred is a different defect, a correct sentence naming a machine the service had left, and it has no gap row because no guard here compares the register to reality."
    },
    {
      "id": "merchant-of-record-unnamed",
      "provider": "paddle",
      "page": "privacy.html",
      "was": "§3 described the merchant-of-record ROLE and committed to naming the provider before selling started, while terms.html and refund.html already named Paddle. One page generic where two were specific.",
      "closedBy": "§4 names Paddle beside the role. Paired by the `Paddle` provider row in tooling/legal/policy-claims.json; paddle.namedIn now lists all three pages.",
      "closedIn": "2026-08-10"
    },
    {
      "id": "iap-aggregator-unnamed",
      "provider": "revenuecat",
      "page": "privacy.html",
      "was": "§3 named no in-app-purchase aggregator, though [ADR 039] D5 locked RevenueCat on 2026-08-09 and it holds an account identifier and entitlement records.",
      "closedBy": "§4 names RevenueCat explicitly as a PROCESSOR acting on our behalf and explicitly not as the seller. Paired by the `RevenueCat` provider row in tooling/legal/policy-claims.json. ⚠️ The duty row `iap-aggregator-disclosed-before-a-store-purchase` in tooling/legal/duty-matrix.json KEEPS its trigger: the disclosure landing is not the same fact as the first real store purchase arriving.",
      "closedIn": "2026-08-10"
    },
    {
      "id": "identity-provider-unnamed",
      "provider": "supabase",
      "page": "privacy.html",
      "was": "§3 named no identity provider although Supabase is wired as one. Nothing was stored under it because no shipped app offers accounts — a disclosure that had to land BEFORE the first account, not one that was already wrong.",
      "closedBy": "§4 names Supabase as the store of account credentials and sign-in sessions. Paired by the `Supabase` provider row in tooling/legal/policy-claims.json.",
      "closedIn": "2026-08-10"
    },
    {
      "id": "web-boot-third-parties-unnamed",
      "providers": [
        "google-gstatic",
        "sentry-cdn"
      ],
      "page": "privacy.html",
      "was": "The web app at nikatru.com/subscriptiontracker makes every visitor's browser fetch the Sentry browser SDK from browser.sentry-cdn.com (measured in the live main.dart.js, 2026-09-11). §4 \"Third-party services\" does not name Sentry as a party the browser contacts. ⏱ 2026-09-12: the Google half (CanvasKit, fonts) was closed rather than named — see the google-gstatic row.",
      "closedBy": "The egress was CLOSED rather than disclosed: no visitor request reaches Google or Sentry any more (W5 PRs, 2026-09-12). CanvasKit and the font fallback are served from the app origin (google-gstatic row) and the web build sends events with the Dart SDK's HTTP transport instead of loading Sentry's browser SDK (sentry-cdn row). The page did not change, and needed not to.",
      "closedIn": "2026-09-12"
    },
    {
      "id": "web-boot-third-parties-unnamed-per-app-notice",
      "providers": [
        "google-gstatic",
        "sentry-cdn"
      ],
      "page": "subscriptiontracker/privacy.html",
      "was": "The generated per-app notice (apps/subscriptiontracker/privacy.yaml `processors`, rendered by tooling/app-yaml/render-privacy.mjs) lists five service providers (four until hostinger was added on 2026-09-11) and not Sentry's CDN, which the same app's web build contacts on every visit. ⏱ 2026-09-12: Google's gstatic CDN was closed rather than named — see the google-gstatic row.",
      "closedBy": "As web-boot-third-parties-unnamed: the two CDNs are no longer contacted, so the processor count in the per-app notice is correct as it stands.",
      "closedIn": "2026-09-12"
    }
  ]
}
